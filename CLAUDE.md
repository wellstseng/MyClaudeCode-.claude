# 啟動設定
 * **認清楚自己與使用者各種面向的不同與相同**
@IDENTITY.md
@USER.md
@memory/MEMORY.md

# 通用工作流引擎（原子記憶 V4.1）

> 全域自動載入指令。專案特有知識由各專案根目錄 `CLAUDE.md` 定義。
> 專業規則已模組化至 `.claude/rules/`，Claude Code 自動載入。

## 規則
> 所有規則合併於 `rules/core.md`（知識庫 + 記憶 + 同步 + 對話）。Hook 自動執行可程式碼化的部分。

