# 變更記錄

> 保留最近 ~8 筆（PostToolUse hook 自動滾動到 `_CHANGELOG_ARCHIVE.md`）。
> 每條僅留「標題 + 一句摘要」。實作細節見 `DevHistory/`（各主題演進檔）。

---

## 2026-04-02 V3.1 Token Diet — 原子記憶精簡
- Phase 1 直刪：移除 31 條 Claude 不使用的自動化描述條目
- Phase 2 信號自描述化：5 個 Guardian 信號加入行動指令，移除冗餘 atom 條目
- Phase 3 JIT 按需注入：記憶系統開發知識移到 `_reference/internal-pipeline.md`，複合條件觸發
- Phase 4 MCP atom_write/promote tools：程式化 atom 寫入，rules/memory-system.md 精簡
- **成果**：decisions+arch+memory-system 從 1,841→631 tok（**-65.7%, -1,210 tok**），超越計畫目標 36%

| 日期 | 變更 | 涉及檔案 |
|------|------|---------|
| 2026-08-07 | **fix(memory): atom 定位機制索引 SoT 化——多專案分區佈局六症狀根治** — 起於 D:\AI-PLAY\AI-gen-projs（memory/projects/<專案名>/ 分區）實案，全部**先重現再修**。①定位：`locate_existing_atom` 索引命中防護由「須落 search_roots 內」細化為**段層級**（skip 段/_archive*），shared 搜尋根放寬到整個 memory root——已歸位到 projects/ 兄弟子夾的 atom，append/replace 一步命中（原四步 workaround 消滅）；`_LOCATE_SKIP_DIRS` 加 roles，personal/roles 保護不失守；create 反向用同一定位防撞名叉檔。②scope：真兇＝`write_index` 硬編 `scope="global"` 每次 create/replace 蹍索引——改 create 傳 scope_label、replace/edit_meta 沿用既有值；atom-move 一律沿用（含 cross-root）、新增 `--scope` 明確覆寫、scope_changed 誠實。③subdir：atom_write 新參數（相對 memory root、僅 shared、僅 create 落點、逐段沙盒＋保護段拒絕），分區佈局一次寫到位。④衝突偵測 block 資格閘：CONTRADICT 需**二次獨立判定一致**才 block（翻面→UNSTABLE warn）；高相似 LLM ERROR 由保守 contradict 改 fail-open warn；`--subdir` 分區感知——跨 projects/<X> 分區相似 atom 不 block；降級一律入 warnings 浮出。⑤待審出路接通：/conflict 新 pending/approve/reject 子命令接既有 `conflict-review.py`（原 block 訊息指向不存在的 /conflict-review）。⑥trigger >30 寫入當下即拒（py+js 雙入口；append 不受 legacy 超長牽連）；atom_edit_meta 支援專案層（index root 上溯 `find_index_dir`，單一實作供 atom-move 共用）。相容：global/shared 扁平佈局行為不變。驗證：`lib/verify` **144 綠**（新 verify_atom_locate_scope 17 + verify_conflict_write_gate 7）；stdio 直驅新 spawn server.js 端到端 7 情境全過；全域 validate_index/scope_drift 零。**js 改動需重啟 MCP server 生效**。 | `lib/{atom_io,atom_locations,atom_index_json,atom_io_cli}.py`, `tools/{atom-move,memory-conflict-detector}.py`, `tools/workflow-guardian-mcp/lib/{atom-tools,mcp,realm,funnel}.js`, `skills/conflict/SKILL.md`, `lib/verify/{verify_atom_locate_scope,verify_conflict_write_gate}.py`(新), `lib/verify/verify_atom_io_edit_metadata.py` |
| 2026-08-06 | **fix(install): settings.json 直譯器路徑可攜化——`tools/fix-hook-python.py` 一鍵校正** — 承前條：settings.json 進 git 但 21 處指令（statusLine + 20 hook）寫死原作者絕對路徑，別台機器沿用該檔＝全部 hook 一起起不來。**否決「改成裸 python」**：實測本機 PATH 首位 `python` 是某 venv 的 3.11、而 hook 實際跑 3.14 launcher——無聲換直譯器不是修好，是換一種踩法。改走明示校正：掃 statusLine + 各 event hooks 的指令開頭直譯器 → 實跑候選 `-c` 驗版本（<3.9 拒寫）→ 只換開頭那段、`-c "..."` 主體一字不動 → `pythonw` 維持 w 版（不彈 console）→ 備份 `settings.json.bak`。預設 dry-run；本機現況全 `[OK ]` 零改動。hook 全域相依實掃＝純標準函式庫，CPython 3.9+ 皆可。Install-forAI.md 補必做檢查段＋兩條檢查表（含無 codex 環境的備援裁判確認）。verify 新 8 綠，`run_verify` **1349 綠**。 | `tools/fix-hook-python.py`(新), `tools/verify/verify_fix_hook_python.py`(新), `Install-forAI.md` |
| 2026-08-06 | **feat(codex): 裁判後端鏈——codex 缺席／未開通時以 Sonnet 備援（可攜性 + 反靜默降級）** — 起於使用者提問「別人 git 裝了這套但沒有 Codex 權限會怎樣」。查證：三道 fail-open 保險都在（裁判缺席永不擋收尾），但有**靜默缺口**——`codex_binary` 寫死本機絕對路徑且 config 進 git，別台機器即使裝了 codex 也被 `which()` 判定不存在而整組關閉，且**無任何訊號**（違可觀測性鐵律）。落地：新模組 `judge_backend.py`（後端選擇規則唯一來源）——①binary 解析改「config 路徑 → PATH → 裸 codex」三段；②codex 不可用 → headless `claude -p --model sonnet` 備援（使用者裁定用 Sonnet 而非 Opus：主力是 Opus，同模型自審＝自評換包裝；且 enforce 是同步閘，Opus 易撞 timeout 使閘門形同虛設）；③**備援預設無 block 權**（同廠盲點相關 → advisory-only，`fallback.allow_block` 才升硬閘）；④codex 授權/額度失敗（未登入/401/429）當輪即切備援出判定 + 落抑制標記 `reprobe_hours` 內不重試、成功即清；⑤備援子 session 帶 env `CLAUDE_COMPANION_JUDGE=1`，五支自家 hook 全數早退（防裁判觸發裁判的遞迴、防狀態汙染），工具面只留唯讀；⑥完全無後端 → SessionStart **揭露一次**；⑦判定來源 `judge_backend`/`model` 入 acceptance-audit.jsonl。**實跑驗證**：headless Sonnet 判假案卷 14.1s / exit 0 / JSON 可解析，且正確抓出「型別標註≠行為改變」與「宣稱補測試但無測試」兩項；子 session 零 state 殘留。verify 新 27 綠，`run_verify` **1341 綠**。 | `tools/codex-companion/judge_backend.py`(新), `tools/codex-companion/assessor.py`, `hooks/codex_companion.py`, `hooks/{dispatcher,acceptance_spec,lang_guard,version_guard}.py`, `tools/codex-companion/verify/verify_judge_backend.py`(新), `workflow/config.json`, `.gitignore`, `TECH.md` |
| 2026-08-06 | **chore(guardian): 跨 session 衝突預警加 ⚠️ 前綴（警告標記依類型分流）** — 承 PAN 的 ⛔，使用者裁決跨 session 衝突預警用 ⚠️ 區隔，**不是所有警告共用同一標記**。`wg_coordination.py` 三處文字（`format_conflict_warning` / `format_late_collision` / `check_bash_git_finalize`）加前綴 + verify 釘住。順手修正記憶污染：自動萃取誤記的 atom「所有系統警告文字必須固定加上 ⛔」（過度概括、與實際分流規則矛盾）已刪除，改寫正確 atom `guardian-警告訊息辨識度-emoji-前綴分流`（含「字級顏色無 API 可控」的根因與兩處 verify 錨點）；index/mirror/catalog 同步、`sync-atom-index --check` 無 drift。另修 `pan-hermes…` atom Status 殘留「翻 deny 待正樣本」（判讀已終局）。`run_verify` **1314 綠**。 | `hooks/wg_coordination.py`, `hooks/verify/verify_session_coordination.py`, `_AIDocs/_atoms/MemDev/guardian-警告訊息辨識度-emoji-前綴分流.md`(新), `memory/所有系統警告文字必須固定加上-前綴-以提高可見度.md`(刪), `memory/_atom_index.json` |
| 2026-08-06 | **docs(tech): PAN 預告閘門 + 驗收裁判（AI 審查 AI）補進 TECH.md** — 兩套新機制此前只記在 Architecture.md/atom/DevHistory，TECH.md 深度文件缺章。補 §5.14（PAN：檢核對象/白名單分類器/mode 終局 warn 判讀結論/armed 快路徑與豁免/Hermes 不移植三部件）+ §5.15（驗收裁判四段閉環表：規格工件→影子裁判→enforce Stop 閘→迴歸提示，含回測轉正數據/殺閘/配額分桶/證據管道鐵律）+ §13 總表兩列 + §5.3 第五類審計指標。順修 drift：目錄樹補 version_guard/acceptance_spec 兩 standalone hook、MCP tool 數 4→5（背景服務行 + §13 行補 anti_evasion_report，§5.5 補現況段）。 | `TECH.md` |
| 2026-08-06 | **chore(guardian): PAN 警告訊息固定 ⛔ 前綴（辨識度）** — 使用者反映閘門警告淹沒在淡色小字裡不易察覺。查證：警告走 hook `systemMessage` 通道，**字級/顏色由 harness 決定、無 API 可控**，唯一能動的是訊息內容本身；候選加工手段中使用者選定「只加 ⛔」（emoji 為純 unicode，任何渲染器都吃，不依賴 markdown 支援）。實模板（`config.deny_template`，warn 與 deny 共用）與程式內 fallback 模板同步加前綴，新增 verify 釘住兩者（防日後改文案時漏掉）。`run_verify` **1313 綠**。 | `workflow/config.json`, `hooks/handlers/pre_tool_use.py`, `hooks/verify/verify_pre_action_notice.py` |
| 2026-08-06 | **decision(guardian): PAN 預告閘門 warn→deny 終局判讀＝不翻（mode 永久 warn）** — warn 期滿、自動監控探針達 READY 後執行判讀。截點 `14:29:22`，窗口 21 筆裁決相關 entry / 5 sid。門檻 1 過（`b816ea82`/`d897bf55` 兩個非探針 sid 各有 pass）、門檻 2 過（force_release 4/21＝19.0%，貼線）、門檻 3 過（`exempt_continuation` 0 筆）；**門檻 4 失敗**——6 個 warn/force_release 回合全查，漏偵率 14.3%（嚴格）〜33.3%（規格字面），皆遠超 ≤5%。**決定性反證**：`e25fb7ea` t4 於 13:33:15 輸出 `pan_validate_notice`=True 的合格預告，4 秒後 13:33:19 的 Edit 仍讀到 `text_blocks:0` → transcript 快路徑是**寫入延遲結構性存在**，非機率問題；另 9 筆 `fail_open_no_transcript` 來自 3 個查無 transcript 檔的 sid（推定 subagent），deny 模式下只能零防護或全誤攔。**無程式碼變更**（config `mode` 本就是 `warn`）；§4 fallback 觸發，替代資料源（`prompt_id` 對齊 / PostToolUse 側錄）待使用者裁決方向，先不動手。 | `_AIDocs/DevHistory/pan-deny-judgement-2026-08-06.md`(新), `_AIDocs/Architecture.md`, `_AIDocs/_atoms/MemDev/pan-hermes不移植部件與vscode-text-block不落盤實測.md`, `memory/_staging/next-phase-pan-deny.md`(刪), `memory/_staging/pan_deny_ready_check.py`(刪) |
| 2026-08-06 | **feat(guardian): 迴歸累積提示 + 驗收裁判計畫終結（Phase 4 收官）** — 裁判抓到的漏原本修完就過去、同型錯下次照犯。落地（Q7 採計畫建議值 hook 提示，不 skill 化）：`handlers/stop.py::_acceptance_regression_hint`——本 session `acceptance-audit.jsonl` 有 **fail/high（enforce 級，回測 4/4 零誤擋的那一級）** 判定 → 收尾 block 訊息 **piggyback**（同 token 預警模式，零獨立打斷）建議 (a) 專案補測試案例 (b) 模式類落 atom；每 session 一次、不建佇列不建表、fail-open。**enforce 上線後真實 audit 回顧**：3 筆判定＝1 uncertain（安全出口）+ 2 fail 皆真命中（宣稱完成時回測仍在背景跑，裁判引工具軌跡抓到）→ 標註 true_hit（precision 2/2），**零誤擋、無退影子必要**。verify 新 11 綠；hooks/verify 全套 **1004 綠 0 紅**；真實資料 smoke 過。計畫終結收攤：知識落 atom「專案工作驗收裁判的分級啟動與殺閘設計」、刪計畫檔、清 _staging 殘留；保留 verify/done/ 規格檔與 audit/backtest jsonl。 | `hooks/handlers/stop.py`, `hooks/verify/verify_acceptance_regression_hint.py`(新), `workflow/config.json`, `_AIDocs/Architecture.md`, `memory/_staging/next-phase-project-work-verification.md`(刪) |
