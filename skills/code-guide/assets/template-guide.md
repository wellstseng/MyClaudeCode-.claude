# 導讀地圖：<目標名稱>

> 範圍：<本次導讀涵蓋的檔案清單，含行數>
> 知識庫引用：<Step 0 引用的 _AIDocs / atom 來源；無則寫「無」>

---

## 1. 一句話心智模型

<這個檔/系統在大局中扮演什麼角色（誰依賴它、它依賴誰），以及複雜度主要來自哪裡。
2~4 句，第一句必須讓讀者能轉述給別人聽。>

## 2. 資料模型先行

<核心欄位/狀態表格。先建狀態心智，後面看任何方法時腦中已有「它在改哪幾個狀態」的掛載點。
這張表是讀者 trace 時的隨身查表，不是要求先背完。>

| 欄位 | 型別 | 角色（一句話） | 宣告處 |
|------|------|---------------|--------|
| <欄位名> | <型別> | <它代表什麼狀態> | [<檔名:行號>](<相對路徑>#L<行號>) |

<表格外補充：欄位之間的關係、生命週期、誰是 source of truth。>

## 3. 入口與主流程（按動詞分組）

<對外行為動詞化（如 開/關/插隊）。每個動詞組依序給三件事 + 呼叫鏈：>

### 動詞：<開 / 關 / ...>

- **進入點**：[<方法簽名>](<相對路徑>#L<行號>)
- **觸發來源**：<誰呼叫、何時觸發 — UI 事件 / 生命週期 / 網路封包 / 熱更 bootstrap / 幀驅動>
- **核心參數**：

| 參數 | 型別 | 用途（一句話） |
|------|------|---------------|
| <參數名> | <型別> | <用途> |

- **呼叫鏈**：<A() → B() → C()，每站附行號錨點>

## 4. 核心演算法：不變量

<不逐行講。每個核心演算法一句話描述「它維持什麼性質」（invariant），讀者之後逐行讀是驗證、不是理解。>

- [<方法名:行號>](<相對路徑>#L<行號>)：<維持的不變量一句話>

## 5. 輔助層 / 支線

<快取、防禦、優化等支線。重點標出設計決策的分界線：誰必須用新鮮資料、誰可容忍 stale。
第一遍閱讀可跳過的段落在此明說。>

| 支線 | 位置 | 一句話 | 第一遍可跳過？ |
|------|------|--------|---------------|
| <快取/防禦/優化> | [<檔名:行號>](<相對路徑>#L<行號>) | <做什麼> | <是/否> |

## 6. 閱讀順序清單

<把前五區變成可執行路線。標精讀/掃過 + 預估時間，讓讀者能排程。>

| 順序 | 目標 | 優先級 | 預估 |
|------|------|--------|------|
| 1 | [<檔名:行號>](<相對路徑>#L<行號>) <段落描述> | 精讀 / 掃過 | <分鐘> |

## 7. 實機觀察入口

<可邊跑邊看的驗證手段：Inspector 哪個物件、什麼 log 關鍵字、哪個 debug 工具 / MCP 工具。
靜態讀碼建的是假設，掛上實機觀察才變確信。>

## 8. 文件時效警告

<Step 0 查到的相關文件中，哪些與原始碼有出入、哪些已過時。明確點名「以原始碼為準」的段落。
無出入則寫「本次未發現知識庫與原始碼矛盾」。>
