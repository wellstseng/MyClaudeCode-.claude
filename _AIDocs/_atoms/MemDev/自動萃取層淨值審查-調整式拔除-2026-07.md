# 自動萃取層淨值審查-調整式拔除-2026-07

- Scope: global
- Author: holylight
- Confidence: [臨]
- Trigger: auto-capture, DedupStage, 淨值審查, 調整式拔除, 草稿, _drafts, 注入縮量, usefulness, turn-global, Wilson, write-only, 記憶效益
- Created-at: 2026-07-01
- Related: confirmations-已退役-phase2-usefulness-接管晉升, memory-pipeline-silent-failure-2026-05, cognitive-patterns

## 知識

- [臨] 2026-07-01 對「自動萃取→草稿→晉升→注入→DedupStage」鏈做 8-agent 對抗審查+codex 第二意見+逐檔實證，結論=**調整式拔除**（非全拔）：拔掉自我指涉的自動機器、保留人工策展語料+靜態注入。
- [臨] 決定性遙測：auto-capture 34.3 草稿/天、7% 存活、**0 下游消費（write-only 死路）**；draft 池 81% 近重複；DedupStage 實跑截斷 0/16、子字串去重 0/16、真近重複 0/3、restore_from_trash 可執行碼 0 呼叫點（救回接虛空）；晉升成功率 prior-subtracted 0.373、全系統 max Wilson 下界 0.264 < promote_lb 0.6（實務永不晉升）。
- [臨] 已拔：`workflow/config.json` response_capture.per_turn.enabled + session_end_flush.enabled → false（保留 failure_extraction+episodic）；settings.json 移除 quick-extract Stop hook；DedupStage 全套（lib/dedup_stage.py + verify_dedup_stage.py + verify_session_end_dedup_wiring.py + session_end.py::_dedup_sweep_core def/call）。縮量(A)：wg_core TURN_BUDGET_LIMIT 800→500、compute_token_budget 1500/3000/5000→1000/2000/3000。
- [臨] **審查主張被實證駁回（勿再據以動手）**：(1)「37 顆假 episodic 種子污染」＝Beta(1,1) Laplace prior，atom_access 已 succ=α−1 減 prior、n=0 排除於晉升/降級——是審查 agent 聚合口徑誤（把 raw α 當數據），非系統 bug。(2)「α/β 噴到全部共注入 atom」高估——stop.py _attribute_usefulness 只記 detect_atom_use 判定真被用者；真缺陷是 outcome 為 turn-global → 高頻規則 atom 因無因果的失敗輪累積 β，Wilson 被關聯性壓低（頻率偏差、非因果）。(3)「死的確認碼」＝2026-06-12 蓄意退役的休眠 OR 軌，非疏於維護，不強拆。
- [臨] 保留（真資產）：62 人工 index atom + failure atom（memory/*.md、_AIDocs/Failures/*.md，git-tracked、人工糾正）、手動 /extract + atom_write、/refile、failure→_AIDocs/Failures、episodic 生成、read_hits 純曝光計數。

## 行動

- 判斷記憶價值時：勿以 atom 數/修改檔數為 proxy；用 useful_hits/used_fail(prior-subtracted succ=α−1) + read_hits 增長 + _meta/atom_io_audit.jsonl 時戳，且降級候選清單須人讀（turn-global 頻率偏差、非因果）。
- 回滾：全 git-tracked，git revert 相關 commit 即復原；恢復 auto-capture 改 config 兩 flag 回 true。
- 未來若要救 draft 效益：走人工 /extract + atom_write（human-in-loop），勿重啟每輪自動 auto-capture（0 消費、自食迴圈）。paraphrase 語意去重、causal-級 usefulness 歸因＝可選增強，未實作。
