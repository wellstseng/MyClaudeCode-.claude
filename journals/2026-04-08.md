# 工作日誌：2026-04-08

## Sessions (2)

| 時間 | 專案 | Prompts | 改檔 | 主要意圖 |
|------|------|---------|------|---------|
| 15:00–… | TSLG.Develop | 10 | 26 | general(5), debug(4), design(1) |
| 15:35–… | TSLG.Develop | 5 | 7 | general(4), design(1) |

## 知識摘要

- Form 路徑解析依賴 workingDirectory。FormModule.cs 設為 ./Form，因 launchSettings workingDirectory 為 ../env，實際解析為 Develop/Server/env/Form。
- 決定同步修改 FormModule.cs、GenCaseSetting/Program.cs 及 Merge/Program.cs 三處 Form 路徑，確保工具與 Server 路徑一致，避免工具執行失敗。
- FormFolderPath 改為 ../res/design (解析為 Develop/Server/res/design)。Config 路徑從 env/Config 改為 ../res/config，Shared/Map/ 下 6 處引用需修正。
- GenCaseSetting 與 Merge 功能暫不處理，後續移除時統一處理。
- SGISettingSet.cs 移除 #if DEBUG 分支，統一指向 ../res/set.xml。launchSettings.json (27 個) 的 workingDirectory 由 ../env 改為 ../res。
- node.json 與 set.xml 變體移至 res/，env/ 僅保留 BattleLog/ 與 schema_mysql/。
- 專案 27 個 launchSettings.json workingDirectory 從 ../env 改為 ../ (Develop/Server/)。StressBot/Merge 為 ../../../env -> ../../../。程式碼路徑基準從 env/ 改為 Server/，Fo
- Userjoy.CoreModule 3.0.1 的 NodeConfigFilePath 為 public static，無法改源碼，故在 SGIAppBase 加 static constructor 於建構前覆寫為 res/node.json。
- 專案硬編碼 "/Config/" 改為 "/res/config/"，修正 NavigateService.cs, FirebaseManager.cs, MapEditor.cs, MapData.cs(V2) 等 8 處。
- FormModule.cs:184 快取於 %TEMP%\Form\{serverId}\，若 FileStream 未 Dispose 會鎖檔，重啟後 Windows 釋放 handle 才讀取成功。
- 決策：採用方案 D (SessionEnd 自動追記 + `/journal` 精修)。日誌路徑 `journals/daily/YYYY-MM-DD.md`。
- 資料來源：`wg_episodic.py` + `state-*.json`。聚合腳本約 80 行 Python。
- 日誌格式含 Sessions, Main Work, Knowledge, Redmine Links。支持 TSLG, Orbit, CatClaw 多專案。
