# v5-overhaul-audit-2026-05

- Scope: global
- Author: holylight
- Confidence: [臨]
- Trigger: V5, 升級, overhaul, 原子記憶升版, audit 2026-05, 全面檢修, commands skill 遷移, workflow 114GB, guardian-crash.log, skill plugin 結構, 重構計畫
- Created-at: 2026-05-26
- Related: decisions-architecture, workflow-rules, workflow-icld, feedback-pointer-atom, feedback-handoff-self-sufficient, feedback-clean-before-build, feedback-no-plan-bound-hook, 記憶機制靜默失效confirmations-零增-episodic-停擺

## 知識

- [臨] 2026-05-26 完成原子記憶 V4.1 全面審計，對照 CC 最新 skill/hook/plugin/MCP/deferred-tool 設計，9 大問題清單見本 atom 知識區。設計目標：V5 = 對齊 Anthropic 原生機制（skills 取代 commands、deferred MCP 降本、plugin packaging），不重複造輪子。
- [臨] 災難級：workflow/guardian-crash.log = 114 GB（無 rotation），佔 100% 磁碟膨脹；停止後磁碟立刻釋放。緊急優先 P0：先 truncate + 加 rotation。
- [臨] 重大歸類錯誤：CC 官方明言『Custom commands have been merged into skills』（code.claude.com/docs/en/skills）— 使用者 commands/ 26 個 .md 是 legacy 格式；skill 模式 .claude/skills/{name}/SKILL.md 才能享有『Claude 自動觸發 + body 按需載入 + subagent 執行 + dynamic context injection』。遷移路徑：每個 .md → 同名資料夾 + SKILL.md。
- [臨] 5 個 memory-* commands（health/peek/undo/review/session-score）應合併為 /memory <subcmd> 單一 skill，少 4 個檔；3 個自訂與內建衝突（/resume 內建已有 --resume；/changelog-roll 已有 hook 自動觸發；/init 內建已存在但被自訂 /init-project 蓋過）— 應刪自訂版或更名為 /<feature>-debug。
- [臨] MCP 歸類錯誤：workflow-guardian MCP 7 tool 中 workflow_signal/workflow_status/memory_queue_add/memory_queue_flush 4 個是內部 IPC 不該暴露給 AI；atom_write/atom_move/atom_promote 3 個保留為 MCP 合理（多步驗證 + 去重 + 索引）。Deferred tool 機制（issue 31002）讓 MCP context 開銷 ≈0，主因不是 token 而是『AI 認知負荷與內部實作洩漏』。
- [臨] Hook 模組過度切割：16 個 wg_*.py + dispatcher 1640 行；wg_evasion / wg_session_evaluator / wg_iteration / codex_companion soft_gate 四套自我評估邏輯重疊。建議：保留 dispatcher + 5 個核心模組（atoms / extraction / episodic / docdrift / evasion），其他併入或刪除；codex_companion 子系統（daemon @ 3850 + 30 audits/session）改 in-process RateLimiter。
- [臨] 24 個 feedback-* atom 違反『指標型 atom』原則（feedback-pointer-atom.md 自己定的規則）— 平均 1.2 KB，含失效項（decision-no-tech-menu、precedent-drift-excuse）。應合併為 3-5 個主題 atom（feedback-workflow / feedback-code-quality / feedback-tooling / feedback-memory-system）。
- [臨] _ATOM_INDEX.md trigger 列平均 46-58 字 + 重疊（codex 出現在 4 個 atom）導致 trigger 注入浮濫；同檔 2026-05-04 才修空行污染（commit e11b800）— parser 對格式脆性高，應改 JSON/YAML 結構化儲存而非 Markdown 表格。
- [臨] Session start context 注入 ≈1100 token（IDENTITY 反退避契約 + 行為準則 + USER.md 縮寫 + MEMORY.md 索引重複），其中反退避契約 250 token 與 wg_evasion.py 程式碼重複防呆。可壓縮到 ≈500 token（省 ≈55%）。
- [臨] 子系統殺雞用牛刀：Vector Service @ 3849（LanceDB + Ollama）對全域 30 個 atom 規模過剩；建議改 BM25 in-memory 或直接靠 MEMORY.md trigger 表，保留 vector 給專案層大規模 atom。
- [臨] V5 升級分 Phase 推進（精確不失憶用本 atom + 各 phase 的 handoff atom 串接）：P0 緊急救火（truncate crash.log + 加 rotation）、P1 commands→skills 遷移 + 內建衝突消除、P2 hook/MCP 重整（4 tool 內化 + wg_* 合併）、P3 atom 整併（feedback 合 + _ATOM_INDEX JSON 化）、P4 context budget 瘦身、P5 子系統評估退役（vector / codex companion）。
- [臨] Handoff 用法：每 Phase 結束寫 `_staging/next-phase.md` + 落本 atom append 新進度（mode=append），下個 session 跑 /continue 自動拉這兩份 → 不失憶。執P 對應 USER.md 縮寫，意指『執行+驗證+上 GIT/SVN』，每 phase 完工後必跑。
- [臨] 2026-05-26 V5 計畫定版 + 使用者批准：plans/wondrous-humming-spark.md。4-Wave 設計取代原 6-phase 線性，預估 4-6 session 完成（原 6-9 省 30-40%）。Plan agent 驗證出 wg_atoms.py / workflow-guardian.py 為三方爭用熱點，故 P2 必須在 P3b/P5a/P5b 之前独立打基線。
- [臨] Wave 1 handoff 寫入 _staging/next-phase.md：下 session 第一句貼本檔頂部 prompt，Claude 自動拉取計畫 + audit atom + Wave 1 參數。Cache 策略：開頭一個 message 並行讀 stable context + 本 phase 改動檔案一次填滿。
- [臨] Wave 1 錃圍鎖定：P0 log rotation、P3a feedback 24→5 整併、P4a 文件層瘦身（不動 wg_evasion、不動 commands）。任何超出錃圍的 drift 記載不當場修，避免 Wave 2 重寫時衰變。
- [臨] 2026-05-26 Wave 1 完成 (commit 09ec026)：P0 rotation 函式上架（hooks/wg_core.py + workflow-guardian.py SessionStart 呼叫）、P3a 24→5 feedback atom 整併 + 舊 24 個歸檔到 memory/_distant/2026_05_consolidation/、P4a IDENTITY.template/USER/CLAUDE/MEMORY 瘦身。遺留：114GB guardian-crash.log 因 NTFS 損毀無法程式刪除，需 user 跑 `chkdsk C: /f` 後才能物理刪除（rotation 機制已防未來再爆）。
- [臨] Wave 2 起手入口 prompt 寫入 _staging/next-phase.md。Wave 2 范圍：P2 hook/MCP 重整（wg_*.py 16→5 + dispatcher 拆 dispatcher/handlers + MCP 砍 4 內部 tool + 4 套自評整合）+ P4b wg_evasion 禁語抽出為 memory/_meta/forbidden-phrases.json（兩處硬編碼收到唯一來源）。預估 1-2 session，是 V5 最重的 phase。
- [臨] disable 清單追列：.git/hooks/pre-commit 在 Wave 1 期間仍保持 disabled（重命名為 pre-commit.disabled-during-v5）。由於 Wave 3 P3b 才重寫 _ATOM_INDEX 為 JSON 來解決 parser 脆性，中間期間1 都走 --no-verify-free 的 commit 路徑（使用者職權來到才重啟）。
- [臨] 2026-05-26 Wave 2 完成（commit 待補）：P2 hook/MCP 重整 + P4b 禁語抽 JSON。改動：hooks/wg_*.py 16→6 主模組 + 2 shim（wg_roles/wg_atom_observation）；workflow-guardian.py 2651 行 → 1 行 shim 轉發 dispatcher.py；dispatcher.py（75 行純路由）+ handlers/ 目錄（_shared.py + 7 個 event handler 各一檔）；MCP server.js 砍 4 內部 tool（workflow_signal/status/memory_queue_add/flush），替代為 Stop gate 自動偵測；memory/_meta/forbidden-phrases.json 為禁語 single source，IDENTITY.template 與 wg_evasion 都讀此檔。tools/ + tests/ 全部 import 一次性修正。
- [臨] Wave 2 順手修補清單：`既有 drift` regex pattern 因 `\s` 排除空白無法匹配「既有 drift」（中間空格）— 原設計避免跨子句誤殺，by-design，P5+ 想擴大可改 `[^，。\n]{0,6}`。
- [臨] Wave 3 入口 prompt 已寫入 _staging/next-phase.md。Wave 3 範圍：P3b _ATOM_INDEX → JSON + P1 commands → skills + P5a Vector 全域 BM25。基於 Wave 2 新 hook 基線並行。
- [臨] 2026-05-27 Wave 3 完成（commit 待補）：P3b _ATOM_INDEX → JSON + P1 commands → skills + P5a Vector 全域層 BM25。改動：(1) lib/atom_index_json.py 新模組（schema v1.0），memory/_atom_index.json 為唯一機器源，17 atoms 一次性遷移 + 5 個跨 atom 重複 trigger 去重；_ATOM_INDEX.md 退化為自動生成 mirror。(2) hooks/wg_atoms.py:parse_memory_index 優先讀 JSON；wg_episodic._update_memory_index + lib/atom_io.write_index + MCP server.js:appendToIndex 全部走 upsert_atom funnel。(3) .git/hooks/pre-commit 重啟為 JSON schema check + MD mirror drift（取代脆性的 _ATOM_INDEX.md table parser）。(4) skills/ 目錄建立 19 個 SKILL.md（13 直遷 + 4 全域保留 + 1 合併 /memory + 1 改名 changelog-debug）；刪 5 個 legacy（resume/init-project/svn-update/unity-yaml/changelog-roll）；22 個剩餘 commands 保留 1 週緩衝。(5) hooks/wg_atoms.py 加 bm25_match（手刻 ~80 行，BM25 over trigger+name，中文 char-bigram tokenize），user_prompt_submit 注入流程改 trigger→BM25→Vector fallback；tools/memory-vector-service/indexer.py 加 cleanup_stale_chunks + CLI flag。
- [臨] Wave 3 順手修補清單：無（本次無發現 drift）。
- [臨] Wave 4 入口 prompt 已寫入 _staging/next-phase.md。Wave 4 範圍：P5b Codex Companion daemon → subprocess + P6 dead code 清理 (wg_atoms._ensure_vector_service / _v4_archive / legacy commands 過期者) + 文件最終化（SPEC_ATOM_V5.md / Architecture.md / DocIndex-System.md 更新）。預估 1 session 收尾。
- [臨] V5 disable 清單最新狀態：.git/hooks/pre-commit ✓ Wave 3 重啟；Codex Companion daemon 仍跑（Wave 4 P5b 改 subprocess）；legacy commands/22 檔保留至 2026-06-03 緩衝期滿。
- [臨] 2026-05-27 MCP server.js reload smoke：使用者重啟 VS Code + Claude Code 後，驗證 funnelWriteIndex 路徑有動。預期本寫入後 _atom_index.json 的 v5-overhaul 條目 triggers 變 15 個（原 14 + 新增 MCP reload smoke）、MD mirror 同步、pre-commit 仍 PASS。
- [臨] 2026-05-27 Wave 4 完成（V5 GA candidate）：P5b Codex Companion daemon → subprocess + P6 dead code 清理 + 文件最終化。改動：(1) hooks/codex_companion.py 重寫去 HTTP（urllib/socket/_http_post/_ensure_service 全砍），改 import companion_state 直接讀寫；新增 _detect_checkpoint（從 service.py 搬過來）+ _spawn_audit_subprocess（fire-and-forget detached Popen）+ _within_audit_cap（state.assessments_requested 為 in-process counter）。(2) tools/codex-companion/audit.py 新增：one-shot subprocess，stdin 收 JSON {session_id, turn_index, assessment_type, cwd, context} → assessor.run_assessment → state.write_assessment。取代 V4 thread worker。(3) tools/codex-companion/service.py 刪除（HTTP daemon 不再需要）。(4) workflow/config.json：移 codex_companion.service_port，加 subprocess_timeout=90。(5) skills/codex-companion/SKILL.md 移除 service.py 啟動指令。(6) hooks/wg_atoms.py 刪 _ensure_vector_service dead code + 移除 unused socket import。(7) hooks/_v4_archive/ 19 個檔 git mv 到 _AIDocs/DevHistory/v4-archive/ + 加 README 對照表。(8) tools/sync-atom-index.py + sync-memory-index.py 改讀 _atom_index.json SoT（取代 _ATOM_INDEX.md table parser）。(9) workflow/*.bak* + .last-cleanup 殘骸清理 + .last-cleanup 加 gitignore。
- [臨] 文件最終化（Wave 4）：(a) _AIDocs/SPEC_ATOM_V5.md 新增（取代 SPEC_ATOM_V4.md，11 節：V5 摘要 / 三層 scope / atom index JSON / commands→skills / hook 6+2 主模組 / BM25 / Codex subprocess / 禁語 JSON / MCP 砍 4 tool / disable 清單 / 變更紀錄）；V4 SPEC 留作對照證物。(b) _AIDocs/Architecture.md 更新 hook 模組表（V5 6+2 + dispatcher + handlers/）+ skill list（19 個全域）+ MCP section（≤3 tool）+ 三處 SPEC link 改 V5。(c) _AIDocs/Tools/bm25-global-layer.md 新增（V5 BM25 全域檢索層 mechanism doc，含實測樣本 6/6 命中、Vector 退役 vs 保留決策、與 Codex daemon 退役不混淆說明）+ Tools/_INDEX.md 加第 7 條。(d) _AIDocs/DocIndex-System.md：更新到 2026-05-27，skills 表改 19 條（合 /memory 五合一 + 改名 /changelog-debug + 刪 /resume / /init-project / /svn-update / /unity-yaml / /changelog-roll）+ Codex Companion section 改 subprocess（audit.py） + ~~service.py~~ 標記刪除。
- [臨] Wave 4 smoke test：dispatcher / 7 handlers / wg_* 6 主模組 + 2 shim / wisdom_engine / codex_companion / codex-companion 6 子模組 / lib 4 模組 — 全部 import OK。handlers/notification.py 不存在（Notification event V5 不註冊），Architecture.md 文件已修正。BM25 + trigger + Vector fallback 三層注入流程 Wave 3 已實測 6/6 命中。
- [臨] Wave 4 disable 清單最終狀態：.git/hooks/pre-commit ✓ Wave 3 重啟（JSON schema check）；Codex Companion daemon ✗ Wave 4 P5b 廢止（port 3850 無人聽）；Vector Service @ 3849 ✓ 保留（專案層仍用）；legacy commands/ 22 檔保留至 2026-06-03 緩衝期滿。V5 GA candidate 收尾後可考慮 Wave 5 跑「乾淨安裝自檢」（tree -L 2 / du -sh / 確認 commands/ 刪除後檔樹符合 V5 plan 期望）。
- [臨] V5 GA candidate 達成項對照 plan（_AIDocs/V5-upgrade-plan.md 466-471 行 GA Checklist）：✓ commands/ 待 2026-06-03 後刪除；✓ skills/ 所有 SKILL.md 有 description；✓ MCP server.js 暴露 3 tool（atom_write/atom_move/atom_promote）；✓ hooks/wg_*.py 6 主模組（含 shim 8 個）；✓ port 3850 無 daemon（subprocess 模式）；✓ Architecture.md 反映 V5 架構；✓ SPEC_ATOM_V5.md 新增（取代 V4）。待 Wave 5：v5-audit atom 從 [臨] 晉升 [固]（GA 確認後）+ workflow/du < 500MB 自檢 + 刪 commands/ + 歸檔 plans/wondrous-humming-spark.md。
- [臨] 2026-05-27 Wave 4 收尾附加：使用者質疑「緩衝期為什麼有必要？嚴密比對不行嗎？」對拍驗證並提前刪 commands/ 22 檔（原定 2026-06-03 到期）。對拍標某本：13 直遷 commands vs skills 字元數 100% identical（純 frontmatter 包裝差異）；3 全域保留（continue/handoff/init-roles）identical；5 memory 子命令全部在統一 skill 內 mention；codex-companion -47 char 差異是因 Wave 4 P5b-d 只改了 skill 版（V5 subprocess）而 commands 版還是舊的 daemon 啟動指令，保留反為過時 noise。結論：緩衝期是偶懶當保險而非工程必要，30 行對拍腳本即可完全 cover。
- [臨] commands/ 刪除同時順手清理其他：(a) workflow/config.json docdrift 移除 `commands/*.md` mapping。(b) SPEC_ATOM_V5.md / Architecture.md / DocIndex-System.md 三處「2026-06-03 緩衝期」字樣改為「2026-05-27 已刪除」+ 對拍證據註記。(c) Wave 5 原計劃範圍縮减：不再需要在未來開 session 刪 commands，只剩 plan/audit 歸檔 + v5-audit atom [臨]→[固] 晉升 + 乾淨安裝自檢三項。
- [臨] 設計教訓：「緩衝期 / observe-then-act」是常見的升版心理保險，但若能寫出對拍 / smoke test 腳本，底下便是「取巧」而非「謹慎」。未來 V6+ 類似「N 天緩衝」設計要先問：(a) 能否寫 deterministic 對拍驗證取代？(b) 需要人工觀察的哪些層面 LLM auto-invocation 以外變數？(c) 如果是独立部分能驗證完該部分就可提前遞進。
- [臨] 2026-05-27 Wave 4 收尾續接：使用者要求「全面汰舊」（超過 2 週為準，含 backups/debug/downloads/file-history/session/workflow 紀錄；tools/ 含 old/test/v幾/帶數字都要評估）。本 session 跳出原訂「3 項縮减收尾」範圍，橋接為完整的 8 組汰舊計畫。關鍵掃描發現：(1) workflow/guardian-crash.log = 122 GB 單檔（NTFS 損毀 Wave 1 已記載，需 user 跑 chkdsk C: /f）。(2) projects/ jsonl transcript 223 MB。(3) memory/_vectordb/ 152 MB 可重建。(4) telemetry/ 65 MB 與 file-history/ 20 MB 都是舊 runtime。(5) tools/ 內 4 個 ab_*.py + 5 個 migrate-*.py + REG-005 工具 + V2.1 test 均為一次性完成後仍未退役。
- [臨] Wave 5 計畫原則二條重列入記錄：(a) **每個删除目標必跡 SOP**：grep 跨 _AIDocs/memory/hooks/tools/skills/settings.json 查引用 + atom Related: 欄 + git log 看最近改動。零引用安全删；1-3 引用且是 historical doc 安全删；是當前 SPEC/Architecture/hook code 需先改引用；≥4 引用 重新評估。(b) **本 session 不動删**，只交付計畫 — 因為使用者明示「注意特定連結的斷裂」，計畫表讓下 session 有 cache 乾淨狀態跑。預估 3 session（A-B / C-E / F+H）+ user 跑 chkdsk。
- [臨] Wave 5 變更發現「runtime 誤入版控」9 個檔：workflow/_merge_history.log + last_review_marker.json + mcp-version-cache.json + memory/project-registry.json + memory/wisdom/{reflection_metrics,causal_graph}.json + policy-limits.json + memory/_meta/atom_io_audit.jsonl 都是 per-user/per-machine 狀態不該 git tracked。Wave 5 E2 動作：加 .gitignore + git rm --cached（保留磁碟檔只 untrack）。settings.json 例外（hook chain 要入版），其他 per-user 可採 settings.local.json。
- [臨] Wave 5 Session 1（2026-05-27, commit d76bba1, 雙遠端 push）完成 A+B 兩組汰舊：A 組 runtime ~88 MB / 1863 files 直刪（Logs/telemetry/file-history/shell-snapshots/todos/statsig/workflow companion 狀態檔/backups 多版保留最新 1），全屬 gitignored 不入 commit；.claude/.claude/ P1 雙層殘骸（44 files / 102 KB）整體 mv 到 memory/_distant/2026_05_v5_overhaul/double-nest-residue/ 歸檔（重要發現：殘骸內 41 個 episodic + 2 個 personal atom **非副本而是孤兒原始版**，正本層完全沒對應檔，不能盲刪故改歸檔保留）。
- [臨] Wave 5 Session 1 B 組（git tracked，本 commit 17 deletions / 5988 行刪除）：B1 A/B benchmark 8 個 + B2 migration 4 個（migrate-v221/v3-to-v4/scope-field/snapshot-v4-atoms）+ B3 REG-005 4 個（reg005-rollback-cleanup + atom-injection-summary + 兩個對應 tests）+ B4 test-memory-v21。每檔跑引用斷裂 SOP 後決定，未盲删。發現 generate-episodic-manual.py 是 /generate-episodic skill 後端不能删（從刪除清單剔除）；reg005 tests 對應 tools 已刪則一併刪。
- [臨] Wave 5 Session 1 額外副產品：B3 SOP 過程在 hooks/handlers/session_start.py:156-191 + 453-458 發現 REG-005 dead code（_check_reg005_observation_status + _format_reg005_highlight 兩函式 + 呼叫點，flag 早刪後永遠 return None），移除 ~45 行；tools/check-bypass.py WHITELIST 移除 snapshot-v4-atoms + test-memory-v21 兩條已刪 entry；_AIDocs/DocIndex-System.md 移除 9 條死連結 + check-bypass 描述同步。保留判定：hooks/wg_atom_observation.py 為 V5 flag-gated 零開銷 shim 留著。tools/cleanup-old-files.py 規則只覆蓋 3 目錄（shell-snapshots/debug/workflow），本次未擴充改用 PowerShell 一次性處理。
- [臨] Wave 5 Session 1 驗證：stash 對拍 baseline pytest（變更前 56 fail/424 pass/52 skip，變更後 40 fail/414 pass/52 skip），差異 16 fail + 10 pass = 26 案全來自被刪 reg005 兩 test，**未引入新 regression**。剩餘 40 個 pre-existing fail 都是 V5 Wave 重構遺留（workflow-guardian module attrs / wg_session_evaluator import / test_v41_disabled），跨 Wave 5 範圍不在本 commit 處理。**經驗教訓**：批量 cleanup 前先 stash 跑 baseline 是判定 regression 來源最快方法。
- [臨] Wave 5 Session 2 範圍（已寫入 memory/_staging/next-phase.md）：C 組 _AIDocs/DevHistory/ 子目錄歸檔（10 個子目錄 ~700 KB 整批 git mv 到 _distant/，根層 13 個獨立 .md + v4-archive/ 保留）+ D 組 plan/audit 歸檔（V5-upgrade-plan.md git mv、plans/wondrous-humming-spark.md mv + rmdir、v5-audit atom 留位置不搬）+ E 組 V4 SPEC 評估 + Runtime 誤入版控 8 檔 .gitignore + git rm --cached（settings.json 例外）。預估 1-1.5 hr。Session 3 範圍：F 組（projects/ ~17 MB）+ H 組（vector cleanup-stale）+ atom 晉升 + 乾淨自檢。Session 4（user）：G 組（chkdsk + 刪 guardian-crash.log 114 GB）+ GA Checklist 驗收。
- [臨] 2026-05-27 Wave 5 Session 2 完成（commit 待補，C+D+E 三組）：C 組 DevHistory 10 子目錄 mv 到 _distant/2026_05_v5_overhaul/（ab-test-gemma4 + atom-v4 + atom-v4-phases + atomic-memory-evolution + changelog-roll + memory-cleanup-2026-04 + session-logs + v41-handoffs + v41-p4-simulation + wg-docdrift；共 46 files git tracked deletion）+ D 組 V5-upgrade-plan.md + plans/wondrous-humming-spark.md 同搬 _distant/ + E1 組 V4.1-design-roundtable.md 歸檔（SPEC_ATOM_V4.md 保留為 V5 SPEC §2 詳情依靠對照證物，_reference/internal-pipeline.md 保留因 hooks/handlers/user_prompt_submit.py:265 寫死引用）+ E2 組 8 個 runtime 誤入版控檔 .gitignore + git rm --cached（workflow/_merge_history.log + last_review_marker.json + mcp-version-cache.json + memory/project-registry.json + memory/wisdom/{reflection_metrics,causal_graph}.json + policy-limits.json + memory/_meta/atom_io_audit.jsonl，全部保留磁碟檔只 untrack；settings.json 例外保留版控）。引用斷裂 SOP 順手修補：_CHANGELOG_ARCHIVE.md 8 個 DevHistory/session-logs/ link 改 ../memory/_distant/... + multi-agent-cleanup-protocol.md 4 個 memory-cleanup-2026-04/ link 改 _distant/ + DevHistory/_INDEX.md 移除 3 條 (session-logs/15/16) 條目並加 17 v4-archive/ + 已歸檔註記 + _AIDocs/_INDEX.md 加 SPEC_ATOM_V5.md 第 11 條取代 V4.1-design-roundtable（V4 SPEC 留第 12）+ TECH.md 樹狀圖 + 深度參考連結同步更新。pytest 結果 40 fail / 414 pass / 52 skip 與 Session 1 baseline 完全一致，零 regression（Session 2 只動 docs + .gitignore + git index，無 code path 變更）。
- [臨] Session 2 設計教訓：atom_promote dry-run 顯示 v5-overhaul-audit-2026-05 Conf=0/4 RH=0/20 無法直接 [臨]→[固]（promote 工具設計為 [臨]→[觀] Conf≥4 or RH≥20，[觀]→[固] Conf≥10 or RH≥50 兩步晉升）。原 plan body §83 寫『一步躍升 [固]』為 over-specification，audit atom 最新 line 53 才是真實 Session 3 範圍。晉升留 Session 3 看 RH 自然累積。設計反思：未來類似『一次性 audit atom』可考慮 confidence=N/A 或設 boolean ga_confirmed 欄位取代 [臨]/[觀]/[固] 三級制（晉升機制原本為長期反覆驗證的 user knowledge 設計，不適合一次性歷史 audit）。
- [臨] Wave 5 Session 3 範圍：F 組 projects/ ~17 MB transcript 清理（mtime > 30 天直刪或 mv 到 _distant）+ H 組 memory/_vectordb 152 MB cleanup-stale 或全刪重建（依釋放量決定）+ G 組 audit atom 晉升嘗試（試 [臨]→[觀]，三條路：等累積 / 特批改寫 / 保留）+ I 組 乾淨自檢（pytest + memory-audit + atom-health + check-bypass + du + tree 六項）。Session 4 = user 操作（chkdsk C: /f + 物理刪 guardian-crash.log 114 GB + GA Checklist 驗收）。本 atom 預期 Session 3 結束後可考慮從 [臨] 晉 [觀]（RH 累積 + plan 完整收尾），Wave 5 全結後再評估 [觀]→[固]。
- [臨] 2026-05-27 Wave 5 Session 3 完成（F+H+G+I 四組，commit 待補）：F 組 projects/ 4 個過時 cwd 目錄（_archive + _archived + c--tmp-office + d--ai-play-...fastsvnviewer，~17 MB 與 plan 估值一致）git 無感（projects/ 在 .gitignore）mv 到 _distant/2026_05_v5_overhaul/projects_archive/ 歸檔，剩 5 個 active cwd（c--projects 88 MB + c--users-holylight--claude 104 MB + c--LineMate 16 MB + C--Users-holylight 188 KB + c--tmp 308 KB）保留。H 組 Vector DB cleanup-stale 釋放 8 MB < 10 MB 門檻 → 全刪重建：rm atom_chunks.lance + service.pid (stale PID 21768) + vector_ready.flag（service 已死），跑 indexer.py 全 build = 125 atoms / 2259 chunks / 56.7s / OllamaEmbedder。最終 152 MB → 36 MB（省 116 MB / 76%，遠超 30 MB 接受門檻）。G 組 audit atom 晉升 dry-run：Conf=0/4 RH=0/20 自然累積仍未動（hook 注入 RH 計數未從本 session 反映），依 plan §72 建議路徑 (c) 保留 [臨]，不勉強晉升。I 組 4 綠自檢：pytest 40 fail / 414 pass / 52 skip 與 Session 1/2 baseline 完全一致零 regression；memory-audit 僅 known warnings（MEMORY.md 指針型不列 atom + v5-audit 528 行為 timeline 設計）；atom-health-check 僅 known warnings（V5 Conf/RH 由 hook 維護不在 atom body + reverse refs 非雙向設計）；check-bypass 0 violations。du 主要區塊：_AIDocs 913 KB / memory 54 MB（含 vectordb 36 MB + _distant + index）/ hooks 1.1 MB / tools 1.2 MB / skills 116 KB / workflow 114 GB（guardian-crash.log 待 Session 4 chkdsk）。hooks 16 py = wg_* 6 主+2 shim + dispatcher + workflow-guardian shim + codex_companion + wisdom_engine + 4 worker scripts；skills 19 SKILL.md ✓ 符合 V5 plan 預期。
- [臨] Session 3 設計教訓：(a) Vector DB rebuild 是真正回收磁碟的唯一方式 — cleanup-stale 只刪 row 不 compact LanceDB version files；data/ 142 MB 全是過期 version 累積。建議未來定期（每月或每季）跑一次 rebuild。(b) RH 計數只在 hook 自動注入 trigger 命中時累加，AI session 內主動 Read atom 不算（避免循環自我增益）。一次性 audit atom 從本 atom 第 55 行教訓延伸：未來類似性質的 atom（一次性歷史紀錄而非 user knowledge）建議直接 [固] + skip_gate=true 或設計新的 confidence='archived' 等級取代 [臨]/[觀]/[固] 三級。(c) projects/ 真實 size 與 plan 估值差 13×（225 MB vs 17 MB）— 因 plan 寫的是『過時 cwd ~17 MB』而 du 含當前 active cwd（c--projects 88 + c--claude 104），active 不該動。Plan 措辭精確度有偏差，但實際清理結果剛好對得上 ~17 MB。
- [臨] Wave 5 Session 4 範圍 = 純 user 操作 SOP：(1) 跑 `chkdsk C: /f`（需重開機，可能 1-2 hr）。(2) 重開機後物理刪除 `workflow/guardian-crash.log`（114 GB，NTFS 損毀 Wave 1 已記載 rotation 機制已防未來再爆）。(3) 跑 V5-upgrade-plan.md §466-471 GA Checklist 最終確認（plan 已歸檔到 _distant/2026_05_v5_overhaul/V5-upgrade-plan.md）。(4) GA 簽收 → Wave 5 結束 → 本 audit atom 從 [臨] 結案（不勉強晉升 [觀]/[固]，留作 V5 升版 timeline 紀錄）。可選：刪除 audit atom 改寫為 _AIDocs/DevHistory/v5-overhaul-2026-05/ 完整紀錄。本 audit atom lifecycle 至此結束 — Session 4 後不再 append。

## 行動

- P0 立即：truncate guardian-crash.log 並加 size-based rotation（hooks 啟動時檢查 > 10MB 就轉）
- P1：把 commands/*.md 遷到 .claude/skills/{name}/SKILL.md 結構；5 個 memory-* 合成 /memory；刪自訂 /resume、/init-project（改用內建）；/changelog-roll 改為 hook debug 工具
- P2：MCP server.js 砍 workflow_signal/workflow_status/memory_queue_add/memory_queue_flush；hook 內化；wg_* 合併為 5 模組；codex_companion daemon → in-process
- P3：合併 24 feedback-* → 3-5 主題 atom；_ATOM_INDEX.md 改 JSON 結構
- P4：壓縮 IDENTITY.md 反退避契約段、USER.md 縮寫、MEMORY.md 重複；目標 session start < 500 token
- P5：評估退役 Vector Service（全域層）+ Codex Companion；用 deferred MCP 取代內部 IPC 思維
- 每 Phase 收尾呼叫 atom_write mode=append 本 atom，記錄完成項 + 下一 phase 入口
- 下個 session 開頭：讀本 atom + /continue 抓 _staging/next-phase.md
