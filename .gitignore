# ─── Credentials & Auth (NEVER commit) ───────────────────
.credentials.json
mcp-needs-auth-cache.json
workflow/.rdchat_password
workflow/.rdchat_token.json

# ─── Runtime state (session-specific, auto-generated) ────
backups/
cache/
debug/
downloads/
file-history/
ide/
plans/
plugins/
projects/
session-env/
shell-snapshots/
statsig/
telemetry/
todos/

# ─── Runtime cache files ─────────────────────────────────
stats-cache.json
.last-cleanup
.last-update-result.json

# ─── Workflow Guardian runtime state ─────────────────────
# config.json IS tracked; state files are not
workflow/state-*.json
workflow/state-*.json.bak-*
workflow/guardian-crash.log
workflow/extract-worker.log
workflow/hot_cache.json
workflow/vector_ready.flag
workflow/.backend_long_die.json
workflow/_merge_history.log
workflow/last_review_marker.json
workflow/mcp-version-cache.json

# ─── Per-user / per-machine state (Wave 5 untracked) ─────
memory/project-registry.json
memory/wisdom/reflection_metrics.json
memory/wisdom/causal_graph.json
memory/_meta/atom_io_audit.jsonl
policy-limits.json

# ─── Codex Companion runtime state ────────────────────────
workflow/companion-state-*.json
workflow/companion-assessment-*.json
workflow/companion-metrics-*.json
workflow/companion.pid

# ─── Runtime logs (atom-debug, hook output) ───────────────
Logs/

# ─── Memory Vector Service runtime ────────────────────────
memory/_vectordb/*.lance
memory/_vectordb/*.lance/**
memory/_vectordb/service.pid
memory/_vectordb/service.log

# ─── ACT-R access logs (runtime, auto-generated) ──────────
memory/*.access.json
memory/**/*.access.json
**/memory/*.access.json
**/memory/**/*.access.json
_AIDocs/Failures/*.access.json

# ─── Staging area (temp files, session prompts) ──────────
memory/_staging/

# ─── Episodic atoms (TTL 24d, auto-generated) ─────────────
memory/episodic/

# ─── Distant memory archive (expired atoms) ───────────────
memory/_distant/

# ─── User-specific config (per-user, auto-generated) ──────
# IDENTITY.md / USER.md 為個人實例（從 IDENTITY.template / USER.template 拷出）；模板 tracked, 實例 ignored
USER.md
USER-*.md
!prompts/user-decision-*.md
IDENTITY.md
IDENTITY-*.md

# ─── User-specific atoms (hardware, local env) ────────────
memory/hardware.md

# ─── Session transcripts (huge, not needed in repo) ───────
projects/*/*.jsonl
projects/*/*/

# ─── Nested .claude (auto-generated by Claude Code) ───────
.claude/

# ─── Node.js runtimes (portable, not tracked) ────────────
tools/node22/

# ─── Vector DB eval/audit logs (local diagnostics) ──────
memory/_vectordb/audit.log
memory/_vectordb/eval-results-*.json

# ─── Playwright MCP (browser automation cache) ──────────
.playwright-mcp/

# ─── Dashboard screenshots (local artifacts) ────────────
dashboard-*.png

# ─── Python cache ────────────────────────────────────────
__pycache__/
*.pyc

# ─── OS / Editor ─────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
memory/mail-sorting.md
sessions/

# ─── Runtime / session artifacts ─────────────────────────
channels/
history.jsonl
paste-cache/
tasks/
.last-cleanup
