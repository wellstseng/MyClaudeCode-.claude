# 2026-04-26 工作日誌

## 總結

修改了 journal-aggregate.py 檔案，並為 UjToolsMenu.cs 中的三個 ILRuntime 函式名正名為 Hotfix。移除了 BtnEventCatcher.cs 中的 `Contains("ILR_CLRBind_")` 運算子，因該邏輯在 HybridCLR 下已失效。重命名了 SVN 目錄，將 `BRM_ILR_Hotfix/` 改為 `BRM_Hybrid_Hotfix/`，並將 `4_ILRuntime/` 改為 `4_HybridCLR/`。

1 專案 · 1 sessions · 4 prompts · 1 檔 · 0 commits

- **TSLG.Develop**: 改 1 檔（未 commit）

| Sessions | Prompts | 改檔 | Commits |
|:--:|:--:|:--:|:--:|
| 1 | 4 | 1 | 0 |

---

## TSLG.Develop
`c:\Projects\TSLG\Develop` · svn

**Sessions (1)** · 4 prompts · 1 改檔 · general(3) build(1)
- `22:40–…`

**修改檔案 (1)**
- `C:/Users/wellstseng/.claude/tools/journal-aggregate.py`

**知識 (6)**
- 在 UjToolsMenu.cs 中，有 3 處 ILRuntime 函式名（如 CompileILRErrorOutputDataReceived）需要正名為 Hotfix 對應。
- 在 BtnEventCatcher.cs 中，確認 `Contains("ILR_CLRBind_")` 邏輯在 HybridCLR 下已失效（不再產生 wrapper class），故已移除該邏輯。
- SVN 目錄已進行重命名：`BRM_ILR_Hotfix/` -> `BRM_Hybrid_Hotfix/`；`4_ILRuntime/` -> `4_HybridCLR/`。
- 日誌生成邏輯 (has_records) 已修正：不再僅依賴 episodic atoms 或 workflow state，現將 VCS commits 納入「實際工作證據」的判定範圍，實現 VCS-only 模式。
- SVN commit 日期處理：由於 SVN XML 使用 UTC 時間，為確保日誌歸檔的準確性，必須抓取較寬的日期範圍，再依據本地時區進行重新歸日判斷。
- SVN log 解碼修正：為避免中文編碼失敗，已將 `cp950` 解碼改為更穩健的 `bytes + UTF-8 decode (errors=replace)` 處理方式。
