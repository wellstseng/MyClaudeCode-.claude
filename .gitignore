# ─── Credentials & Auth (NEVER commit) ───────────────────
.credentials.json
mcp-needs-auth-cache.json
workflow/.rdchat_password
workflow/.rdchat_token.json

# ─── Runtime state (session-specific, auto-generated) ────
backups/
cache/
debug/
downloads/
file-history/
ide/
plans/
plugins/
projects/
session-env/
shell-snapshots/
statsig/
telemetry/
todos/

# ─── Runtime cache files ─────────────────────────────────
stats-cache.json
.last-cleanup

# ─── Workflow Guardian runtime state ─────────────────────
# config.json IS tracked; state files are not
workflow/state-*.json
workflow/state-*.json.bak-*
workflow/state-*.json.corrupt
workflow/coord-warn-cache-*.json
workflow/world-dev.json
workflow/_*.log
workflow/guardian-crash.log
workflow/extract-worker.log
workflow/hot_cache.json
workflow/vector_ready.flag
workflow/vector_rekick.marker
workflow/vector_starting.lock
workflow/.backend_long_die.json
workflow/_merge_history.log
workflow/last_review_marker.json
workflow/realm_automove_pending.json
workflow/mcp-version-cache.json
workflow/aec-report/
workflow/aec-decision/
workflow/ups-sentinel/
workflow/dpm-done/
workflow/outcome_stats.jsonl
workflow/outcome-unknown-advisory.json
workflow/health-reports/
workflow/health-last-run.json
workflow/pan-pass/
workflow/pan-deny/
workflow/acceptance-spec/
workflow/acceptance-audit.jsonl*
workflow/acceptance-backtest.jsonl*

# ─── Per-user / per-machine state (Wave 5 untracked) ─────
memory/project-registry.json
memory/wisdom/reflection_metrics.json
memory/wisdom/causal_graph.json
memory/_meta/atom_io_audit.jsonl*
memory/_promotion_audit.jsonl
policy-limits.json

# ─── Codex Companion runtime state ────────────────────────
workflow/companion-state-*.json
workflow/companion-assessment-*.json
workflow/companion-metrics-*.json
workflow/companion.pid
workflow/companion-backend.json

# ─── Runtime logs (atom-debug, hook output) ───────────────
Logs/

# ─── Memory Vector Service runtime ────────────────────────
memory/_vectordb/*.lance
memory/_vectordb/*.lance/**
memory/_vectordb/service.pid
memory/_vectordb/service.log

# ─── ACT-R access logs (runtime, auto-generated) ──────────
# 政策：所有 atom 遙測 sidecar（read_hits/confirmations/usefulness）一律不進版控——
# per-machine 執行期資料，提交會 churn + 跨機衝突。下方 blanket 收斂全部位置
# （含 _AIDocs/_atoms/ local realm、_AIDocs/Tools/；2026-06 補 S1/S2 realm 遷移破口）。
**/*.access.json
memory/*.access.json
memory/**/*.access.json
**/memory/*.access.json
**/memory/**/*.access.json
_AIDocs/Failures/*.access.json

# ─── Staging area (temp files, session prompts) ──────────
memory/_staging/

# ─── Auto-capture drafts (隔離區，未確認 [臨]，不入版控/索引/注入) ─────────
memory/_drafts/
**/memory/shared/_drafts/

# ─── Episodic atoms (TTL 24d, auto-generated) ─────────────
memory/episodic/

# ─── Distant memory archive (expired atoms) ───────────────
memory/_distant/

# ─── User-specific config (per-user, auto-generated) ──────
# IDENTITY.md / USER.md 為個人實例（從 IDENTITY.template / USER.template 拷出）；模板 tracked, 實例 ignored
USER.md
USER-*.md
!prompts/user-decision-*.md
IDENTITY.md
IDENTITY-*.md

# ─── User-specific atoms (hardware, local env) ────────────
memory/hardware.md

# ─── Session transcripts (huge, not needed in repo) ───────
projects/*/*.jsonl
projects/*/*/

# ─── Nested .claude (auto-generated by Claude Code) ───────
.claude/

# ─── Node.js runtimes (portable, not tracked) ────────────
tools/node22/

# ─── Vector DB eval/audit logs (local diagnostics) ──────
memory/_vectordb/audit.log
memory/_vectordb/eval-results-*.json

# ─── Playwright MCP (browser automation cache) ──────────
.playwright-mcp/

# ─── Dashboard screenshots (local artifacts) ────────────
dashboard-*.png
world-*.png

# ─── Python cache ────────────────────────────────────────
__pycache__/
*.pyc

# ─── OS / Editor ─────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
*~
memory/mail-sorting.md
sessions/

# --- Session/Task/Codex runtime state (wellstseng local) ---
history.jsonl
tasks/
.codex/

# codex-bridge runtime state
workflow/codex-bridge/

# ─── merge union (mac line) ───
memory/_meta/atom_io_audit.jsonl
.last-update-result.json
channels/
paste-cache/

# ─── merge union (upstream) ───
workflow/cross-project-index-cache.json
memory/personal/
memory/_meta/atom_io_audit.jsonl.*
workflow/companion-state-*.tmp
workflow/aec-tempfiles/
chrome/
journals/

# vendor deps（各機 npm install，不進版控）
node_modules/
