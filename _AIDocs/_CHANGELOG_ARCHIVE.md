# 變更記錄 — 封存

> 從 `_CHANGELOG.md` 滾動淘汰的歷史記錄。

---

| 日期 | 變更 | 涉及檔案 |
|------|------|---------|
| 2026-08-07 | **fix(memory): atom 定位機制索引 SoT 化——多專案分區佈局六症狀根治** — 起於 D:\AI-PLAY\AI-gen-projs（memory/projects/<專案名>/ 分區）實案，全部**先重現再修**。①定位：`locate_existing_atom` 索引命中防護由「須落 search_roots 內」細化為**段層級**（skip 段/_archive*），shared 搜尋根放寬到整個 memory root——已歸位到 projects/ 兄弟子夾的 atom，append/replace 一步命中（原四步 workaround 消滅）；`_LOCATE_SKIP_DIRS` 加 roles，personal/roles 保護不失守；create 反向用同一定位防撞名叉檔。②scope：真兇＝`write_index` 硬編 `scope="global"` 每次 create/replace 蹍索引——改 create 傳 scope_label、replace/edit_meta 沿用既有值；atom-move 一律沿用（含 cross-root）、新增 `--scope` 明確覆寫、scope_changed 誠實。③subdir：atom_write 新參數（相對 memory root、僅 shared、僅 create 落點、逐段沙盒＋保護段拒絕），分區佈局一次寫到位。④衝突偵測 block 資格閘：CONTRADICT 需**二次獨立判定一致**才 block（翻面→UNSTABLE warn）；高相似 LLM ERROR 由保守 contradict 改 fail-open warn；`--subdir` 分區感知——跨 projects/<X> 分區相似 atom 不 block；降級一律入 warnings 浮出。⑤待審出路接通：/conflict 新 pending/approve/reject 子命令接既有 `conflict-review.py`（原 block 訊息指向不存在的 /conflict-review）。⑥trigger >30 寫入當下即拒（py+js 雙入口；append 不受 legacy 超長牽連）；atom_edit_meta 支援專案層（index root 上溯 `find_index_dir`，單一實作供 atom-move 共用）。相容：global/shared 扁平佈局行為不變。驗證：`lib/verify` **144 綠**（新 verify_atom_locate_scope 17 + verify_conflict_write_gate 7）；stdio 直驅新 spawn server.js 端到端 7 情境全過；全域 validate_index/scope_drift 零。**js 改動需重啟 MCP server 生效**。 | `lib/{atom_io,atom_locations,atom_index_json,atom_io_cli}.py`, `tools/{atom-move,memory-conflict-detector}.py`, `tools/workflow-guardian-mcp/lib/{atom-tools,mcp,realm,funnel}.js`, `skills/conflict/SKILL.md`, `lib/verify/{verify_atom_locate_scope,verify_conflict_write_gate}.py`(新), `lib/verify/verify_atom_io_edit_metadata.py` |
| 2026-08-06 | **fix(install): settings.json 直譯器路徑可攜化——`tools/fix-hook-python.py` 一鍵校正** — 承前條：settings.json 進 git 但 21 處指令（statusLine + 20 hook）寫死原作者絕對路徑，別台機器沿用該檔＝全部 hook 一起起不來。**否決「改成裸 python」**：實測本機 PATH 首位 `python` 是某 venv 的 3.11、而 hook 實際跑 3.14 launcher——無聲換直譯器不是修好，是換一種踩法。改走明示校正：掃 statusLine + 各 event hooks 的指令開頭直譯器 → 實跑候選 `-c` 驗版本（<3.9 拒寫）→ 只換開頭那段、`-c "..."` 主體一字不動 → `pythonw` 維持 w 版（不彈 console）→ 備份 `settings.json.bak`。預設 dry-run；本機現況全 `[OK ]` 零改動。hook 全域相依實掃＝純標準函式庫，CPython 3.9+ 皆可。Install-forAI.md 補必做檢查段＋兩條檢查表（含無 codex 環境的備援裁判確認）。verify 新 8 綠，`run_verify` **1349 綠**。 | `tools/fix-hook-python.py`(新), `tools/verify/verify_fix_hook_python.py`(新), `Install-forAI.md` |
| 2026-08-06 | **feat(codex): 裁判後端鏈——codex 缺席／未開通時以 Sonnet 備援（可攜性 + 反靜默降級）** — 起於使用者提問「別人 git 裝了這套但沒有 Codex 權限會怎樣」。查證：三道 fail-open 保險都在（裁判缺席永不擋收尾），但有**靜默缺口**——`codex_binary` 寫死本機絕對路徑且 config 進 git，別台機器即使裝了 codex 也被 `which()` 判定不存在而整組關閉，且**無任何訊號**（違可觀測性鐵律）。落地：新模組 `judge_backend.py`（後端選擇規則唯一來源）——①binary 解析改「config 路徑 → PATH → 裸 codex」三段；②codex 不可用 → headless `claude -p --model sonnet` 備援（使用者裁定用 Sonnet 而非 Opus：主力是 Opus，同模型自審＝自評換包裝；且 enforce 是同步閘，Opus 易撞 timeout 使閘門形同虛設）；③**備援預設無 block 權**（同廠盲點相關 → advisory-only，`fallback.allow_block` 才升硬閘）；④codex 授權/額度失敗（未登入/401/429）當輪即切備援出判定 + 落抑制標記 `reprobe_hours` 內不重試、成功即清；⑤備援子 session 帶 env `CLAUDE_COMPANION_JUDGE=1`，五支自家 hook 全數早退（防裁判觸發裁判的遞迴、防狀態汙染），工具面只留唯讀；⑥完全無後端 → SessionStart **揭露一次**；⑦判定來源 `judge_backend`/`model` 入 acceptance-audit.jsonl。**實跑驗證**：headless Sonnet 判假案卷 14.1s / exit 0 / JSON 可解析，且正確抓出「型別標註≠行為改變」與「宣稱補測試但無測試」兩項；子 session 零 state 殘留。verify 新 27 綠，`run_verify` **1341 綠**。 | `tools/codex-companion/judge_backend.py`(新), `tools/codex-companion/assessor.py`, `hooks/codex_companion.py`, `hooks/{dispatcher,acceptance_spec,lang_guard,version_guard}.py`, `tools/codex-companion/verify/verify_judge_backend.py`(新), `workflow/config.json`, `.gitignore`, `TECH.md` |
| 2026-08-06 | **chore(guardian): 跨 session 衝突預警加 ⚠️ 前綴（警告標記依類型分流）** — 承 PAN 的 ⛔，使用者裁決跨 session 衝突預警用 ⚠️ 區隔，**不是所有警告共用同一標記**。`wg_coordination.py` 三處文字（`format_conflict_warning` / `format_late_collision` / `check_bash_git_finalize`）加前綴 + verify 釘住。順手修正記憶污染：自動萃取誤記的 atom「所有系統警告文字必須固定加上 ⛔」（過度概括、與實際分流規則矛盾）已刪除，改寫正確 atom `guardian-警告訊息辨識度-emoji-前綴分流`（含「字級顏色無 API 可控」的根因與兩處 verify 錨點）；index/mirror/catalog 同步、`sync-atom-index --check` 無 drift。另修 `pan-hermes…` atom Status 殘留「翻 deny 待正樣本」（判讀已終局）。`run_verify` **1314 綠**。 | `hooks/wg_coordination.py`, `hooks/verify/verify_session_coordination.py`, `_AIDocs/_atoms/MemDev/guardian-警告訊息辨識度-emoji-前綴分流.md`(新), `memory/所有系統警告文字必須固定加上-前綴-以提高可見度.md`(刪), `memory/_atom_index.json` |
| 2026-08-06 | **docs(tech): PAN 預告閘門 + 驗收裁判（AI 審查 AI）補進 TECH.md** — 兩套新機制此前只記在 Architecture.md/atom/DevHistory，TECH.md 深度文件缺章。補 §5.14（PAN：檢核對象/白名單分類器/mode 終局 warn 判讀結論/armed 快路徑與豁免/Hermes 不移植三部件）+ §5.15（驗收裁判四段閉環表：規格工件→影子裁判→enforce Stop 閘→迴歸提示，含回測轉正數據/殺閘/配額分桶/證據管道鐵律）+ §13 總表兩列 + §5.3 第五類審計指標。順修 drift：目錄樹補 version_guard/acceptance_spec 兩 standalone hook、MCP tool 數 4→5（背景服務行 + §13 行補 anti_evasion_report，§5.5 補現況段）。 | `TECH.md` |
| 2026-08-06 | **chore(guardian): PAN 警告訊息固定 ⛔ 前綴（辨識度）** — 使用者反映閘門警告淹沒在淡色小字裡不易察覺。查證：警告走 hook `systemMessage` 通道，**字級/顏色由 harness 決定、無 API 可控**，唯一能動的是訊息內容本身；候選加工手段中使用者選定「只加 ⛔」（emoji 為純 unicode，任何渲染器都吃，不依賴 markdown 支援）。實模板（`config.deny_template`，warn 與 deny 共用）與程式內 fallback 模板同步加前綴，新增 verify 釘住兩者（防日後改文案時漏掉）。`run_verify` **1313 綠**。 | `workflow/config.json`, `hooks/handlers/pre_tool_use.py`, `hooks/verify/verify_pre_action_notice.py` |
| 2026-08-06 | **decision(guardian): PAN 預告閘門 warn→deny 終局判讀＝不翻（mode 永久 warn）** — warn 期滿、自動監控探針達 READY 後執行判讀。截點 `14:29:22`，窗口 21 筆裁決相關 entry / 5 sid。門檻 1 過（`b816ea82`/`d897bf55` 兩個非探針 sid 各有 pass）、門檻 2 過（force_release 4/21＝19.0%，貼線）、門檻 3 過（`exempt_continuation` 0 筆）；**門檻 4 失敗**——6 個 warn/force_release 回合全查，漏偵率 14.3%（嚴格）〜33.3%（規格字面），皆遠超 ≤5%。**決定性反證**：`e25fb7ea` t4 於 13:33:15 輸出 `pan_validate_notice`=True 的合格預告，4 秒後 13:33:19 的 Edit 仍讀到 `text_blocks:0` → transcript 快路徑是**寫入延遲結構性存在**，非機率問題；另 9 筆 `fail_open_no_transcript` 來自 3 個查無 transcript 檔的 sid（推定 subagent），deny 模式下只能零防護或全誤攔。**無程式碼變更**（config `mode` 本就是 `warn`）；§4 fallback 觸發，替代資料源（`prompt_id` 對齊 / PostToolUse 側錄）待使用者裁決方向，先不動手。 | `_AIDocs/DevHistory/pan-deny-judgement-2026-08-06.md`(新), `_AIDocs/Architecture.md`, `_AIDocs/_atoms/MemDev/pan-hermes不移植部件與vscode-text-block不落盤實測.md`, `memory/_staging/next-phase-pan-deny.md`(刪), `memory/_staging/pan_deny_ready_check.py`(刪) |
| 2026-08-06 | **feat(guardian): 迴歸累積提示 + 驗收裁判計畫終結（Phase 4 收官）** — 裁判抓到的漏原本修完就過去、同型錯下次照犯。落地（Q7 採計畫建議值 hook 提示，不 skill 化）：`handlers/stop.py::_acceptance_regression_hint`——本 session `acceptance-audit.jsonl` 有 **fail/high（enforce 級，回測 4/4 零誤擋的那一級）** 判定 → 收尾 block 訊息 **piggyback**（同 token 預警模式，零獨立打斷）建議 (a) 專案補測試案例 (b) 模式類落 atom；每 session 一次、不建佇列不建表、fail-open。**enforce 上線後真實 audit 回顧**：3 筆判定＝1 uncertain（安全出口）+ 2 fail 皆真命中（宣稱完成時回測仍在背景跑，裁判引工具軌跡抓到）→ 標註 true_hit（precision 2/2），**零誤擋、無退影子必要**。verify 新 11 綠；hooks/verify 全套 **1004 綠 0 紅**；真實資料 smoke 過。計畫終結收攤：知識落 atom「專案工作驗收裁判的分級啟動與殺閘設計」、刪計畫檔、清 _staging 殘留；保留 verify/done/ 規格檔與 audit/backtest jsonl。 | `hooks/handlers/stop.py`, `hooks/verify/verify_acceptance_regression_hint.py`(新), `workflow/config.json`, `_AIDocs/Architecture.md`, `memory/_staging/next-phase-project-work-verification.md`(刪) |
| 2026-08-06 | **feat(codex): 驗收裁判回測達標 + Stop 閘轉正（Phase 2.5+3——「說做完＝真做完」閉環上線）** — user 拍板以**歷史回放+種缺陷回測**代替 1-2 週影子等待（`backtest_acceptance.py`：A 完好回放 10/B 種缺陷 7/C 截斷紀律 3，真值由構造保證，真 codex 管道）。兩輪 40 審：第一輪未過→根因＝證據管道太瘦，**加碼**（diff 逐檔 2000+600、總 9000、stat 6000、案卷 22000）+模板補「完整清單反證」規則（檔案級缺席可證，內容級仍回無法判定）；第二輪 C 紀律兩輪 6/6 滿分、B 抓取 5/7（「宣稱全綠零證據」3/3 全中）、A 3 誤報中 2 個經查是回測構造錯（裁判抓對我的 harness bug），排除後精確率 83%、**high 級兩輪 4/4 全真命中零誤擋**。user 判讀視同 Q5 達標+Q6 拍板只擋 high。**Phase 3 enforce 閘**：Stop 同步審（`_enforce_acceptance_gate`，settings.json codex Stop timeout 10→150）——fail+high→block 附逐條證據與剩餘次數；沿用 `stop_gate_max_blocks=2` 第 3 次強制放行+揭露 advisory；裁判逾時→uncertain 放行+degraded metric；unbound 經 `map_acceptance_verdict` 永不 block；enforce 判定照寫 jsonl 雙軌。**實彈演練過**：漏做+假宣稱→BLOCK（檔案級反證+驗證缺口雙證據）→補做+真 pytest→RELEASED。verify 40 綠（enforce 新 6）；一鍵退影子=`enforce:false`。 | `hooks/codex_companion.py`, `tools/codex-companion/{acceptance.py,assessor.py,prompts.py,state.py,backtest_acceptance.py(新)}`, `hooks/verify/verify_acceptance_review.py`, `workflow/config.json`, `settings.json`, `.gitignore`, `memory/_staging/next-phase-project-work-verification.md` |
| 2026-08-06 | **feat(codex): 影子驗收裁判（專案工作驗收裁判 Phase 2——案卷組裝 + acceptance_review）** — 裁判首次拿著「這個任務的驗收標準」審完成宣稱，而非通用直覺。**①案卷/綁定/稽核**：新模組 `tools/codex-companion/acceptance.py`——任務↔規格檔**四分流綁定**（bound=本 session 唯一 open 規格才發審計；ambiguous/other_session/none 一律記 uncertain 不發、不猜「最新一份」，INV-CASE-BINDING-OR-UNCERTAIN）+ **diff 案卷採樣**（--stat 全給 + 逐檔頭尾採樣 + 超預算檔列「未採樣清單」帶警語 + untracked 檔頭；一切截斷 in-band 標記，INV-EVIDENCE-PIPE-HONESTY）+ 稽核落盤 `workflow/acceptance-audit.jsonl`（gitignored、2MB 輪替、`human_label` 預留人工標註）。**②裁判**：`ACCEPTANCE_REVIEW` 模板（verdict/score/problems[]/uncertain_reason，判定步驟明寫「證據不足回 uncertain、採樣截斷不得扣分」）；`map_acceptance_verdict`＝紅線程式化執行點（binding≠bound 強制 uncertain、fail 無證據 problem 降 uncertain、裁判逾時→uncertain 揭露不靜默通過）。**③觸發**：Stop 完成宣稱+有改動+bound，或規格檔 status 改 done（權威訊號）；同規格每 session 重審上限 2。**④影子模式**：verdict 只寫 jsonl + advisory（fail 注入門檻降 medium、文案明示不阻斷），零 block 路徑（窮舉測試背書）。**⑤Q8 配額分桶**：共用 30 之上 acceptance 上限 8/保底 6，互不餓死；撞閘記 metric。**⑥Q5 轉正門檻（user 拍板）**：N≥20 ∧ precision≥60% ∧ uncertain≤30% 轉正；<50% 殺閘；`promotion_stats()` 程式化。**實彈 smoke**：tmp repo 故意漏做一項+假宣稱全綠 → codex fail、兩 problem 均引案卷證據。verify 新 31 綠；hooks/verify 全套 **984 綠 0 紅**；companion 64 綠。 | `tools/codex-companion/{acceptance.py(新),assessor.py,prompts.py,state.py,audit.py}`, `hooks/codex_companion.py`, `hooks/verify/verify_acceptance_review.py`(新), `workflow/config.json`, `.gitignore`, `memory/_staging/next-phase-project-work-verification.md` |
| 2026-08-06 | **fix(codex): 審計輸入組成系統性根治——plan_review 100% 收不到計畫正文 + 組裝收斂單一模組** — 使用者要求系統性檢視「協作 LLM 收到的輸入」而非逐案補丁。**①根因**：`plan_content` key 全庫從無寫入端（`assessor.py` 恆 fallback 成 tool trace 摘要）、Write content 在 hook 入口整包丟棄、ExitPlanMode plan 被 `[:200]` 靜默截 → 歷史 105 次 plan_review **100% 只收到動作紀錄**，codex 回「未提供計畫正文」是忠實行為（與 08-05 handoff `[:6000]` 案同型：餵給裁判的輸入≠磁碟 artifact）。**②統一原則落地**（規則唯一來源 `artifact_io.py` docstring + `assessor.build_prompt()` 純函式）：artifact 必附實體內容（超長頭尾採樣+in-band 標記：plan 8000+2000、handoff 4500+1500）、動作紀錄不得替代本體（解析不到 → skip + `audits_skipped_no_artifact` metric，不空審）、集合截斷附 `showing last N of M` 計數標頭（state 腰斬計入 `trace_dropped`）、prompt 總量軟預算 16000 超額縮 trace 重組一次。**③hook 瘦身**：只傳觸發事實；EnterPlanMode 不再觸發（必空審）；plan artifact 由 trace 反掃 `plans/*.md`；剪 dead payload（`user_goal_hint`/`trigger_*`/`verification_signals`——模板從無 placeholder 組了全丟）；補 `user_goal`（UserPromptSubmit 首 prompt write-once，三模板共用，補 brief「背景」要件）。**④模板誠實化**：不再宣稱 Files Examined 含 Read/Glob/Grep/Agent（matcher 收不到，防「未讀檔」誤報）。`run_verify` **1259 綠**（新 verify_prompt_input_integrity 12 案 + verify_artifact_sampling 6 案取代舊 4 案）；smoke_plan_review 以原事故檔（17KB atomic-singing-charm.md）真打 codex 過。 | `hooks/codex_companion.py`, `tools/codex-companion/{artifact_io.py(新),assessor.py,prompts.py,state.py,audit.py}`, `tools/codex-companion/verify/{verify_artifact_sampling.py(新),verify_prompt_input_integrity.py(新),smoke_plan_review.py(新),verify_handoff_content_sampling.py(刪)}`, `_AIDocs/Architecture.md` |
| 2026-08-06 | **feat(guardian): 驗收規格工件 hook（專案工作驗收裁判 · 規格工件段）** — 任務「做完的定義」原只活在對話/plan 文字、session 結束即蒸發，裁判與收尾自核無標準可對。落地：standalone PostToolUse hook `hooks/acceptance_spec.py` **分級啟動**（輕量極簡鐵則）——ExitPlanMode 獲同意 → 注入指示從 plan 落 `<專案根>/.claude/verify/acceptance-<slug>.md`（frontmatter task_slug/session_id/created_at/source/status 供後續案卷綁定 + 必須發生/禁止發生/驗證指令三段極簡列表，收尾逐項自核、全過移 done/）；無 plan 但同 session 修改 ≥3 檔（讀 guardian state `modified_files`，排除 memory/_staging 等非任務寫入）→ 一次性建議；規格檔落盤 → sidecar 抑制重複提醒；advisory-only 不阻斷、小任務零打擾、每 session 每型至多一次。**本 session 實彈自證**：hook 上線當場對本任務觸發多檔提醒 → 依指示落規格檔 → sidecar 正確記錄抑制。verify 新 14 支全綠；`hooks/verify` 全套 **953 綠 0 紅**（3 既存紅燈已由併發 session 修復）。 | `hooks/acceptance_spec.py`(新), `hooks/verify/verify_acceptance_spec.py`(新), `workflow/config.json`, `settings.json`, `.gitignore`, `_AIDocs/Architecture.md` |
| 2026-08-06 | **fix(verify): run_verify 輸出順序修復 + `--json` 結構化輸出（memory-llm-eval Phase 0，user 單獨授權）** — 「吞輸出」實跑重現後確認是假象：真根因＝paths print 在 stdout=pipe 時 block-buffered、程序結束才 flush，路徑清單排到 pytest 輸出之後，配合工具截斷呈現「只剩路徑清單」；修 flush 順序。新增 `--json`：in-process pytest + 收集 plugin，stdout 純 JSON（頂層 summary + 每案 id/file/markers/phase/outcome/duration_s/message），供任何 agent（含 Codex 裁判）程式化讀取驗證結果。三路驗證：預設模式順序正確、`--json` 1231 案全收錄可解析、失敗路徑（臨時失敗測試對拍）message/exit code 正確。順帶更正計畫檔過期錨點（1174/3→1231/0；`skills/*/verify` 已不存在）。 | `run_verify.py`, `memory/_staging/next-phase-memory-llm-eval.md` |
| 2026-08-06 | **feat(guardian): PowerShell 納入 PAN 預告閘門（使用者裁決）** — Windows 環境寫入動作（Remove-Item/Set-Content 等）原可繞過閘門；使用者裁決程式邏輯管起來。落地：`_PAN_GATED_TOOLS` + PowerShell、與 Bash 共用白名單前綴分類器（config `bash_readonly_prefixes` 補唯讀 cmdlet：get-*/select-object/sort-object/measure-object/format-*/out-string/write-output 等 + `git ls-remote`）、settings.json PreToolUse matcher 加 PowerShell；未命中白名單/賦值段/here-string 保守 gated。閘門定位＝保險絲，行為責任（未預告不呼叫）仍在 IDENTITY 契約；使用慣例 Bash 優先、PowerShell 留 Windows 特有操作。`run_verify` **1231 綠**（基線 1221 + 新 10）。 | `hooks/handlers/pre_tool_use.py`, `workflow/config.json`, `settings.json`, `hooks/verify/verify_pre_action_notice.py`, `_AIDocs/Architecture.md` |
| 2026-08-06 | **feat(guardian): PAN Phase 1.5 判讀 + Phase 2 落地（warn 軟著陸，不直上 deny）** — **①Phase 1.5 判讀**：log 樣本遠低於門檻（僅 1 session 3 筆真實評估 vs 要求 ≥30 跨 ≥3 session），且「有寫預告的回合」零正樣本——本 session（VSCode）live 再證發現 3 更嚴重：**整回合輸出多段可見文字（含合格預告）而 `text_blocks:0`**，同回合快路徑在此環境近乎全盲 → 判讀結論：不直上 deny，走 warn 軟著陸 + `lenient_first_miss` 雙緩衝；sidechain 豁免維持三層縱深（isSidechain 擷取端跳過 + state 缺 fail-open 保底〔04929b55 連環 fail_open 實證保底動作〕+ deny 文案 self-healing），不新增訊號。**②Phase 2 落地**：config `mode:"warn"` + `lenient_first_miss:true`（deny 模式首 miss 降 warn、第 2 次起 deny）；compaction continuation 豁免（probe `first_user_head` 命中 harness 續接敘述 → 整回合放行 + log `exempt_continuation`）；deny_template 消費/壞模板 fallback、lenient、continuation 各補 verify（原計畫 +8 案中 5 案 Phase 1 已涵蓋）。live 實測 warn 模式 force_release 正常動作。**③翻 deny 條件**（接續檔 `_staging/next-phase-pan-deny.md`）：warn 期收集到「獨立短訊息預告可靠落盤」正樣本後再翻。`run_verify` **1221 綠**（基線 1218 + 新 3）。 | `hooks/handlers/pre_tool_use.py`, `workflow/config.json`, `hooks/verify/verify_pre_action_notice.py`, `_AIDocs/Architecture.md` |
| 2026-08-05 | **feat(guardian): 實作前預告閘門 PAN Phase 1（Hermes 技轉，observe 上線）** — 技轉來源 `~/Downloads/H0XmxIalAEQ/Hermes_實作前預告閘門_ClaudeCode技轉文件.md`；計畫 `plans/atomic-singing-charm.md`。每回合首次動手工具（Write/Edit/NotebookEdit/非唯讀 Bash）前檢查本 turn 可見預告（「執行目標」+「預估/概估」+ 實質內容）。**①落地**：`pan_validate_notice` 純驗證器（Hermes R1-R8 移植 + 佔位符 `<…>` span 剝除防模板複誦）+ `get_current_turn_visible_text`（只收 text block 防 tool_input 冒充、isSidechain 跳過、超長 turn 頭尾各半保留）+ mode 三態 observe/warn/deny（deny 上限 2 次第 3 次強制放行，guardian 慣例）+ pass marker armed 快路徑 + fail-open log 節流 + TTL prune + IDENTITY.md 常駐指示一行。**不移植**：Hermes 兩階段狀態機/歷史清除/scaffolding 隔離——CC deny→retry 迴圈與 tool pairing 原生涵蓋。**②首日實測三發現**（Phase 1.5 決策材料）：(a) transcript 行有 `isSidechain` 欄位、payload 有 `permission_mode`/`prompt_id`/`tool_use_id`；(b) `cur_tool_flushed=true`＝PreToolUse 當下 transcript 已含本次 tool_use（無 flush lag，單樣本）；(c) ⚠**VSCode 擴充環境「長文字+工具」訊息的 text block 常不落 transcript、短文字+工具會落**（五訊息探針實證、延遲 flush 排除）→ 同回合快速路徑不可靠，翻 deny 前必須決策資料源或 lenient。**③順修 3 既有紅**：episodic purge `_distant` 桶月份改跟隨 `today` 基準（8 月翻頁測試必紅根因）；effect_report 測試補 distinct-session 去重口徑（adfe94e 改實作未同步測試）。`run_verify` **1218 綠**（基線 1181 + PAN 37 + 順修回歸）。 | `hooks/handlers/pre_tool_use.py`, `hooks/wg_evasion.py`, `hooks/handlers/session_start.py`, `workflow/config.json`, `IDENTITY.md`, `hooks/wg_episodic.py`, `hooks/verify/verify_pre_action_notice.py`(新), `hooks/verify/verify_effect_report.py`, `_AIDocs/Architecture.md` |
| 2026-08-05 | **fix(hooks): codex handoff 自檢輸入靜默截斷 + staging 檔名閘慣例修正；plan(memory): AI 裁判子系統計畫定稿** — **① codex 截斷根治**：`_read_handoff_content` 原 `[:6000]` 靜默切斷（12778 字計畫檔第 6000 字＝codex 報的截斷點，逐字吻合）→ codex 連 4 輪 severity=high「文件截斷/缺 §9」全是忠實審查被污染的輸入、非幻覺；改頭尾採樣（4500+1500）+ 明確中段省略標記（文末授權段自此可見）；verify 新 4 支（`verify_handoff_content_sampling.py`），codex-companion 套件 50 綠。教訓落 atom `codex-handoff自檢誤報文件截斷-…`（MemDev local）。**② staging 檔名閘**：`post_tool_use.py` 原要求唯一 `next-phase.md`，與 `wg_handoff` glob／`codex_companion._NEXT_PHASE_RE` 的 `next-phase*` 慣例矛盾（現存 4 份計畫並存）→ 改 prefix 判定。**③ 計畫定稿**：`_staging/next-phase-ai-judge-subsystem.md`（Opus 初擬、Fable 校驗修整：Phase 2 裁判只收無標準答案題型、Phase 3 定位為閉環落點）；六欄白話骨架固化 `memory/templates/next-phase-plan-template.md`（[臨] 起）。 | `hooks/codex_companion.py`, `hooks/handlers/post_tool_use.py`, `tools/codex-companion/verify/verify_handoff_content_sampling.py`(新), `memory/_staging/next-phase-ai-judge-subsystem.md`(新), `memory/templates/next-phase-plan-template.md`(新), `_AIDocs/_atoms/MemDev/codex-handoff自檢誤報文件截斷-….md`(新) |
| 2026-08-05 | **fix(memory): 健檢兩誤報根治 + enforce 索引同步 + 核心層 8降5升** — 專案層 /memory health 轉來的核心層批次（Guardian CrossRealmWriteBlock 正確攔截後由 ~/.claude session 執行）。**① 誤報根治**：`validate_index` 補 personal/ 存在性口徑（`SKIP_DIRS` 含 personal → tree_stems 拿不到 → 每次固定 11~12 筆假 error「索引指向不存在的檔案」；只補 index→file 口徑、不動 SKIP_DIRS 不進掃描報表），專案層驗收 Errors 13→1；`SKIP_DIRS` 補 `_rejected`/`_drafts`（掃描器吃到歸檔草稿報 24 筆假 never-tracked；atom_locations/wg_core 那兩份是可寫白名單、語意不同不合併），驗收全消。**② enforce 根治**：`--enforce` 移 `_distant/` 原本只搬檔不清 `_atom_index.json` → dangling entry 下次健檢變真 error；補搬檔成功即 `index_delete_atom`（同 --delete 5b），合成樹功能測試 PASS。**③ 生命週期批次**：8 顆 [臨]>30d 降 `_distant/2026_08/`（腦內世界 4 顆經 git 活動裁決不豁免——7/1 後無實質推進）；5 顆 [觀]→[固]（usefulness lb 0.63~0.79，非同批連跳）；廢除已過時誤報 atom；9 顆來源 atom Related 清 15 筆 dangling refs（成因確定走 atom_edit_meta，不走 atom-heal LLM）；索引全鏈同步，核心層健檢 Errors 0/Warnings 0。 | `lib/atom_spec.py`, `tools/memory-audit.py`, `memory/`+`_AIDocs/_atoms/` 索引與 atom 批次, `_AIDocs/_INDEX.md`, `TECH.md` |
| 2026-07-31 | **feat(guardian): 跨 session 衝突預警（多大師計畫 Stage 0+1）** — 需求「多 session 併行有無溝通管道」：查證 CC 原生無跨 session 管道（Agent Teams 實驗性限單 team）。七席共議（CC 架構師/考古/風險/效益 + Codex 極簡/保守/激進）+ 雙稽核 + 紅隊，使用者裁決方案 A。落地：`wg_coordination.py` 純檔案方案（不依賴 3848 daemon）——PreToolUse 掃其他活 session state 的 `modified_files`（entry 級 session_id 歸屬）同檔 warn；Bash `git add -A`/`reset --hard` 且同 cwd 有他人未收改動 → 警告（正中 pain atom 實證痛點）；PostToolUse write_state 後 60s late-collision 補償。關鍵定案：協調資料不進 state 檔（Node/Python 雙寫入通道 + 三條 GC 互不相認）；advisory 禁帶 `permissionDecision:"allow"`（自動核准繞權限，CC 稽核抓出）；probe 實測 PreToolUse additionalContext=after-write 下一輪可見（非寫前攔截）。per-session NDJSON log `Logs/session-coordination/`；config `coordination.*` 一鍵關 + 4 週零命中日落條款；Stage 2 收件匣/Phase 3 認領制 defer 待數據。e2e smoke 5.5ms。Codex 紅隊 8 缺陷全處置：null config 崩潰隔離、引號**解包**（同修 `git add "-A"` 繞過與 `echo "git add -A"` 誤報）、dry-run/`clean -n` 排除、Win32 `\\?\` 前綴正規化、掃描上限 20+`scan_overflow` log（截斷盲區可稽核）、warn-cache 改發警時記錄（deny 回合不污染、修正重試照警）+24h 修剪、Bash 措辭誠實化（state 不追 VCS 狀態，提示查證）；oversize skip 為既定設計（log 可稽核）。`run_verify` **1176 綠**（基線 1154 + 新 22：warn/deny 互斥矩陣、60s 窗邊界、紅隊回歸全組）。計畫全紀錄 `DevHistory/session-coordination-bus.md`。 | `hooks/wg_coordination.py`(新), `hooks/handlers/{pre_tool_use,post_tool_use,_shared}.py`, `workflow/config.json`, `.gitignore`, `hooks/verify/verify_session_coordination.py`(新), `_AIDocs/{Architecture,DevHistory/session-coordination-bus}.md` |
| 2026-07-30 | **fix(memory): 一行路標跨 realm 斷鏈 — 路徑一律絕對化** — 症狀（使用者在專案層實證）：Stop `[Guardian:AtomAudit]` 催讀 `Read memory/shared/ProjectWorkflow/pitfalls.md`，該檔實體在 `C:\Projects\.claude\` 下，模型以 cwd 解析 → 找不到＝路標斷鏈。根因：`rel_path` 只相對 atom 自己的 realm root（`~/.claude` 或各專案 `.claude`），但所有一行路標（cold / budget-skip / related-skip / 壓縮截斷 / AtomAudit）都裸印 rel_path，跨 realm 必失效，最壞是解析到另一 realm 的同名檔（靜默讀錯）。修法：`wg_atoms.pointer_path()` 單一渲染入口——有實檔 Path 就輸出絕對路徑（正斜線），無則退 rel（legacy caller 相容）；`format_cold_inject_line` 加選填 `atom_path`；`load_atoms_within_budget`／`ups_inject` 兩處 skip 行／壓縮截斷 `_display_path`（原 `relative_to(CLAUDE_DIR)` 同病）／Stop AtomAudit（改取 record 既有絕對 `path`，rel 僅 fallback）全數改走。`run_verify` **866 綠**（基線 861 + 新 5：pointer_path 絕對/退回、cold 行、專案 realm assemble E2E、AtomAudit 訊息）。 | `hooks/wg_atoms.py`, `hooks/handlers/{ups_inject,stop}.py`, `hooks/verify/verify_atom_consumption_audit.py` |
| 2026-07-29 | **feat(memory): 取用端閉環稽核（AtomAudit）+ atom 瘦身規範** — 實證根因（c:\Projects session：trigger 命中的 10.4KB atom 被 per-turn 預算降為一行路標、AI 全程未展開漏掉獨有教訓；肥→降級→沒人讀的因果鏈）。**A 取用端**：`ups_inject` 落 `state["injection_log"]`（name/path/source/form/turn_seq，cap 100）；Stop 新 **AtomAudit Gate**——source=trigger（＝任務域吻合的等價訊號；trigger 詞面全管線本就不留存，不動匹配演算法）且 form∈{skip,cold} 且非本 turn 注入、`accessed_files`（既有 Stop 尾段回收線，不加 per-Read hook）無對應 Read → 三選一表態 (a)他源等價 (b)無關理由 (c)補讀；per-atom 每 session 一次、沿用 `stop_gate_max_blocks` 第 3 次強制放行、fail-open stderr、config `atom_audit.enabled`。**B 瘦身**：`KNOWLEDGE_BUDGET_BYTES=3KB` 硬拒（機制實查：hot/cold 判定與大小無關——降級真兇是 TURN_BUDGET_LIMIT=500 tok 三態，>~1KB 知識段注入必截斷，管線無單顆 byte 門檻可反推 → 採規格 fallback 3KB、依據註明於常數）——write-gate 排最前（explicit_user/pitfall 不豁免）＋落檔 floor（build/create_atom/append，append 以拼接後總量計＝肥大化實際路徑，skip_gate 繞不過）；write_raw（episodic/failures）豁免、validate 不檢＝存量讀取不受影響；樣式軟警（逐筆表格/路徑清單）附掛 create 成功訊息；atom 選填 `- Status:` 現況行（build `status` 參數 + `_FRONTMATTER_KEEP_RE`）→ cold/budget-skip 一行注入自帶最低現況。⚠ js 改動（mcp.js/atom-tools.js）需**重啟 MCP server** 生效。`run_verify` **1149 綠**（基線 1123 + 新 26）+ handle_stop E2E smoke 4 案。 | `hooks/handlers/{stop,ups_inject}.py`, `hooks/wg_atoms.py`, `lib/{atom_spec,atom_io,atom_io_cli}.py`, `tools/memory-write-gate.py`, `tools/workflow-guardian-mcp/lib/{mcp,atom-tools}.js`, `hooks/verify/verify_atom_consumption_audit.py`(新), `lib/verify/verify_knowledge_budget.py`(新), `_AIDocs/Architecture.md` |
| 2026-07-28 | **feat(guardian): SessionStart 未推送 commit 提醒** — 補上晉升自動提交的可觀測性缺口：那條 push 走背景 detached（SessionEnd 30s 預算內不等網路），push 掛掉時 commit 只留本地、當下無人知。新增 `_unpushed_advisory`（`git rev-list --count @{u}..HEAD` > 0 → 浮一行含筆數 + 指向 `Logs/auto-commit.log`），接在週健檢死人開關之後。唯讀不寫；無 upstream / detached HEAD / 非 git 目錄一律靜默（那些是正常狀態不是異常）；自身出錯回 `[]` 不阻斷 SessionStart；同步時零 context。`run_verify` **1123 綠**（基線 1116 + 新 7）。 | `hooks/handlers/session_start.py`, `hooks/verify/verify_unpushed_advisory.py`(新), `_AIDocs/Architecture.md` |
| 2026-07-28 | **feat(memory): 晉升 sweep 改完的 atom 當場自動提交** — SessionEnd 的 `[臨]→[觀]` auto-promote 是程式改檔、改動面完全可預測（只有 `- Confidence:` 行與知識行 `[臨]` 前綴，內容不動），過去卻只改檔不提交 → 留在工作樹，而使用者常多 session 共用同一 git 工作樹，這些改動會被別的 session 收尾時誤夾帶進不相干的 commit。新增 `_auto_commit_promotions`：用 **`git commit -- <paths>` pathspec 形式**提交（**不下 `git add`**——pathspec 只提交指定路徑的工作樹內容、完全不碰 index，別的 session 已 stage 的東西原封不動，這是共用工作樹下唯一安全的提交形式）；push 走背景 detached（SessionEnd 只有 30s 預算、網路不可控），輸出落 `Logs/auto-commit.log`。index.lock 競態短重試 3 次；無實際改動不產生空 commit；樹外/不存在路徑濾掉；任何失敗一律 fail-open + stderr 出聲、改動留工作樹（可觀測性鐵律）。開關 `self_iteration.auto_commit_promotions` / `auto_push_promotions`（預設開）。配套 `wg_atoms._self_iterate_atoms` 的 promoted 條目補 `path` 欄（原只有 stem，atom 散在 `memory/` 與 `_AIDocs/` 多根、無從定位）。`run_verify` **1116 綠**（基線 1106 + 新 10）。 | `hooks/handlers/session_end.py`, `hooks/wg_atoms.py`, `workflow/config.json`, `hooks/verify/verify_auto_commit_promotions.py`(新), `_AIDocs/Architecture.md` |
| 2026-07-28 | **fix(memory): `atom_write` append/replace 認不得子夾 atom — 落點與定位分離** — 症狀：對專案 shared atom 下 `mode=append` 恆回 `Atom not found`，而 AtomFunnelBlock 又擋直接 Write/Edit → 唯一合法入口打不到檔的死結。根因：`_resolve_target` 只有 `realm=local` / `feedback-` 會做子夾路由，`scope=shared/role/personal` 一律回平層目錄，`file_path.exists()` 直接判不存在；實測 C:/Projects `shared/` 平層 .md **0 個**、91 顆 atom 全在 classifier sweep 歸位後的主題子夾 → 該專案 append/replace **100% 失效**（`realm=local` 的 global atom 在 domain 給錯/沒給時同病）。修法：**create 落點維持平層**（主題分層本就是事後 `project_hooks` sweep 的職責，非 write 端猜測），**append/replace 改走新的 `locate_existing_atom`**（`_atom_index.json` path 優先 → rglob scope 子樹 → 撞名列出全部候選明確報錯、不靜默取第一個；跨 scope 保護 + 草稿牢籠 `_drafts`/`personal/auto/` 排除），索引回寫 path 由定位到的實體路徑推導。MCP 實際入口是 js（`toolAtomWrite` 解析路徑 → spawn py 落檔），js **不補第二套定位**（原 `findAtomFileRecursive` fallback 只覆蓋 scope=global 正是雙份漂移的產物）→ 平層 miss 時 spawn `atom_io_cli` 新增的 `locate` action，規則 py 單一來源。EOL byte-stable 未破（append 3 行 = 3 insertions、CRLF 53/53 無翻）。⚠ js 改動需**重啟 MCP server** 生效。`run_verify` **1106 綠**（基線 1092 + 新 `verify_atom_subdir_locate.py` 14）。 | `lib/atom_locations.py`(`locate_existing_atom`), `lib/atom_io.py`(`_resolve_target` search_roots / `write_atom` / `locate_atom`), `lib/atom_io_cli.py`(`locate` action), `tools/workflow-guardian-mcp/lib/atom-tools.js`, `lib/verify/verify_atom_subdir_locate.py`(新), `_AIDocs/SPEC_ATOM_V5.md`(§2.3), `_AIDocs/_atoms/MemDev/scope-shared-無主題子夾路由-*.md` |
| 2026-07-25 | **feat/fix(memory): V5.1 Ultra-Optimization — 檢索精準化 + 記憶完備性 + 全鏈修復（多 agent 併行波次）** — 對照 CC 原生機制 + SOTA 演算法 + 佛法心識模型的全系統極限優化（計畫與稽核彙整見 `memory/_staging/ultra-opt-2026-07/`）。**① 檢索**：RRF 三路融合（trigger/BM25/vector rank 融合 k=60 × activation 調節 gain=0.25；config `vector_search.fusion`，legacy 可回退）× ACT-R 個別化 decay（d=clamp(0.5−γ·wilson_lb,0.3,0.5)，γ=`stability_gamma`=0.3）；新 atom activation −10→中性 0；`bm25_min_score` 3.5→7.0（回歸集調參：負例誤注入 21.4%→0%、R@3 -1.5pt）；enrichment gating（trigger 命中 <3 才打專案層 vector）。**實測 Recall@1 34→53.6%、MRR 0.584→0.709（誤注入不變）；UPS 主路徑 90.1→16.0ms（-82%，`_kw_match` 熱點修）**。**② 新工具 `tools/memory-eval/`**：223 條合成查詢回歸集（LLM 離線生成）+ Recall@1/@3/MRR/誤注入 + baseline 比對——終結盲調參，本波所有檢索參數以此定值。**③ 效用校準**：`wilson_z` 1.96→1.28（3 連勝 lb=0.6468 過升門；audit 晉升候選 0→7）、demote 增 `demote_min_n=5`、decay λ=0.97 加每日護欄（`last_decay_date`）。**④ 記憶完備性（佛法三缺口工程化）**：失念偵測 recall-miss（`wg_recall_miss.py`：SessionEnd 比對失敗證據×未注入 atom trigger → `Logs/recall-miss.jsonl` + 效果報表 D 節 + 週健檢黃燈）；壞滅緣（atom optional `- Depends:` + `check_stale_deps`）；證據等級（optional `- Evidence: 實證\|引述\|推測` + 衝突裁決優先序 + fast-refute）。**⑤ 修復（稽核 A/B/C 級擇要）**：vector `/reindex` 404（MCP 寫後重索引恢復 `/index/incremental`+stale 清理）、indexer 改讀 access sidecar（25% ranking 權重解凍）、local realm atoms 入向量索引（全量重建 304 atoms/3749 chunks）、embed contextual prefix、fallback state 重建+advisory（TTL 清 state 後注入全滅根治）、token 口徑統一 `_estimate_tokens`（CJK 假 budget 根治）、failing_tests turn 邊界、post_tool_use 單次寫 state、DPM 檔案側 marker（state 競態二連發根治）、StagingName 子資料夾豁免、audit 索引 parser 認原生清單格式、write-gate pitfall 不再跳過 dedup、audit delete/restore 同步 index SoT、sidecar 隨搬、MCP spawn timeout、execWriteGate stdin 化、ThreadingHTTPServer+starter PID 驗證、reranker import json、oscillation 偵測恢復、episodic 死碼刪除。**⑥ 裁決不做**（防重議，詳 `10-assessment-matrix.md`）：SPLADE/ColBERT/HippoRAG/FSRS 整套/jieba/RAPTOR；Mem0 四操作+A-MEM 延後至獨立 next-phase。`run_verify` **1092 綠**（基線 924）。 | `hooks/{wg_atoms,wg_core,wg_episodic,wg_recall_miss(新),wisdom_engine}.py`, `hooks/handlers/`(×10), `lib/{atom_spec,atom_access,atom_io,atom_index_json,ollama_extract_core}.py`, `tools/memory-eval/`(新), `tools/memory-vector-service/{service,indexer,reranker,starter}.py`, `tools/{memory-audit,memory-conflict-detector,memory-write-gate,atom-health-check,memory-effect-report,health-weekly}.py`, `tools/workflow-guardian-mcp/lib/{funnel,atom-tools,atom-access}.js`, `workflow/config.json`, `settings.json`, 新 verify ×22, `TECH.md`(§5.11/5.12+校準), `_AIDocs/{SPEC_ATOM_V5(§13/14),Architecture,DocIndex-System,context-memory-governance(§4.5),_INDEX}.md`, `version.json`(5.1) |
| 2026-07-08 | **feat/fix(memory): 程式化強化 E 組 — 召回根治 + 效果實證（4+1 項）** — ① **vector 就緒空窗根治**：先實證診斷——no_flag 主因非冷啟動時序（7 月冷啟動窗 0 件），而是**服務起不來的整段故障窗**（07-01 連續 4.4h、9 次 SessionStart 啟動全失敗；6 月 76% 為已修的歷史靜默失效期）＋ stderr 進 DEVNULL 零證據。對症：內嵌啟動碼抽出為 `starter.py`（SessionStart/UPS 共用）——service stdout/stderr 落 `Logs/vector-service.log`（>5MB 輪替、embedder 載入計時）、hang 死自癒（health timeout+port 被占 → kill service.pid 重啟）、等待窗 15s→120s（bge-m3 fallback 冷載遠超 15s）、spawn lock 防多 session 重複載；UPS 端 `_ensure_vector_ready` re-kick（cooldown 120s + ≤300ms 短等，flag 遺失類毫秒級恢復）——服務中途死改下一 prompt 自癒。E2E 實測 kill 後 **4s 復活**。② **救援日誌**：`wg_rescue.py` 注入時確定性抽高特異 token（路徑/inline-code/ALL_CAPS/snake≥8；泛詞黑名單+子字串抑制+跨 atom 歸因模糊剔除，寧缺勿濫），工具呼叫命中 → `Logs/rescue-log.jsonl` {atom,token,evidence,turn_seq}；Agent prompt 欄/寫 memory .md 不掃防自我命中。③ **效果報表**：`memory-effect-report.py` 彙總 access.json（曝光+α/β Wilson）+ rescue-log → 三清單（top 有用/高曝光零使用 token 稅附 trigger 收斂建議/零曝光死重候選）+30 天週趨勢；接入 `/memory health` step4 + 週健檢 5b（token 稅→黃、零效用證據→黃）。④ **專案層 enrichment 放行**：審計證實 ups_search 註解寫 enrichment 但未實作（命中>0 即跳過 vector）→ 放行為命中>0 且有專案層 atom 才跑、結果只取專案層命中；預算由 assemble 端 TURN_BUDGET 硬頂結構性保證。⑤ **原生記憶橋接**：`native-memory-bridge.py` 核心 13 atom 指標行鏡像進 `projects/<slug>/memory/`（harness 清單格式；絕不放 atom marker，`verify_native_bridge` 組合驗證掃描不誤納；`--create` 顯式建目錄）——已對本環境 slug 落地。裁決記錄（不做）：trigger 同義詞擴充/重複勞動偵測/兩級注入重構（詳 TECH §5.10）。`run_verify` **924 綠**（889 基線 + 35 新：starter 12 + rescue 10 + effect 5 + enrichment 3 + bridge 5）。 | `tools/memory-vector-service/{starter.py(新),service.py}`, `hooks/handlers/{session_start,ups_search,ups_inject,post_tool_use}.py`, `hooks/{wg_atoms,wg_rescue(新)}.py`, `tools/{memory-effect-report,native-memory-bridge}.py`(新), `tools/health-weekly.py`(5b), `skills/memory/SKILL.md`(step4), `hooks/verify/verify_{vector_starter,rescue_log,effect_report,project_enrichment,native_bridge}.py`(新), `TECH.md`(§5.10), `_AIDocs/{Architecture,DocIndex-System}.md` |
| 2026-07-08 | **feat(guardian): 程式化強化 C 組 — Q4 新能力（statusline / skill 硬化 / 週健檢 / OTEL 評估）** — ① **statusline 常駐可觀測層**：`tools/statusline.py`（stdin 吃 CC status JSON，純 stdlib 讀 `state-<sid>.json`＋`vector_ready.flag`＋`aec-report/<sid>-t*.json` 最大 turn → 一行 ANSI：模型 · ctx% · 改N 讀M 佇K · vec✓ · AEC:sev；fail-open 必告知：state 壞→紅字 `WG:?`、最外層兜底任何錯誤仍印一行）+ settings.json `statusLine`（refreshInterval 10）；chat 注入退役：UPS 週期性 `[Guardian] Reminder` 整段刪除（config/DEFAULTS 鍵 `remind_after_turns`/`max_reminders`＋state `remind_count` 一併清），Stop `[Guardian:SyncReminder]` 保留 enforcement 但訊息瘦身（不列檔案清單，模型自行 git status）。② **skill invocation 硬化**：21 skills 逐一評估（官方語意先查證：`disable-model-invocation: true`＝模型硬封鎖含自然語言請求＋description 移出 context 省 token；user `/slash` 不受影響），9 個重炮/儀式/debug 型套用（atom-debug/changelog-debug/codex-companion/continue/extract/fix-escalation/generate-episodic/heal-review/upgrade；codex-companion 兼反逃避——模型不得自關監督器）；**consciousness-stream 偏離原提案保留可呼叫**：rules/core.md「用識流…」映射由模型代打，封鎖即斷該入口；handoff 同理（wg_handoff 注入「建議主動 /handoff」）。③ **週健檢 routine**：`tools/health-weekly.py` 唯讀聚合 memory-audit/atom-health-check/雙索引 --check/vector/**管線鮮度**（近 14 天有 session 但 promotion audit 或 episodic 零新增 → 紅＝靜默停擺）→ `workflow/health-reports/`（輪替 12）+ `health-last-run.json`；Task Scheduler `Claude-Memory-WeeklyHealth`（週一 09:00＋StartWhenAvailable，Start-ScheduledTask 實測 Result=0）；SessionStart `_health_advisory` 死人開關（缺檔/逾 10 天/red>0 → advisory）——排程器本身死了也浮出，「靜默死 27 天」最後防線；踩坑實證：pythonw 下 `sys.stdout=None`、開頭 reconfigure 秒死（首次排程 Result=1 抓到）→ 入口防護＋OS atom。④ **OTEL 評估結論＝不實作**：官方 export 無 per-hook latency、api_request 無法歸因注入 token 稅到個別來源、需常駐 collector 違反單機輕量原則 → 兩目標指標皆測不到；未來真需 hook 延遲分布走 dispatcher 自計時 JSONL（MemDev atom 存結論防重評）。⑤ `.gitignore`：`memory/_promotion_audit.jsonl` untrack（per-machine 審計尾巴，比照 atom_io_audit）＋ health-reports/＋health-last-run.json。`run_verify` **889 綠**（873 基線 + 16 新：statusline 8 + health-weekly 8）。 | `tools/{statusline,health-weekly}.py`(新), `settings.json`(statusLine), `hooks/handlers/{user_prompt_submit,stop,session_start}.py`, `hooks/wg_core.py`(鍵清理), `workflow/config.json`(-remind 鍵), `skills/*/SKILL.md`(×9 frontmatter), `hooks/verify/{verify_statusline,verify_health_weekly}.py`(新)+`verify_sync_reminder_session_filter.py`(斷言更新), `.gitignore`, `_AIDocs/_atoms/{MemDev/otel-*,OS/pythonw-*}.md`(atom×2), `TECH.md`(§5.9/8.1.1), `_AIDocs/{Architecture,DocIndex-System}.md` |
| 2026-07-08 | **perf/fix(guardian): 程式化強化 B 組 — 性能與結構修復（Q3-B）** — ① **Stop transcript 三讀合一**：新 `wg_evasion.read_transcript_tail`（2MB 尾窗、捨首不完整行），`get_last_assistant_text`/`get_current_turn_text`/`estimate_context_usage`/`token_warn_payload` 全收 `text=` 共用尾段（原各自全檔讀 ×3）；handle_stop 頂部一次 tail-read + 一次 last_text，砍全部 lazy 重讀；once-per-turn 歸因/`_bump_outcome_stats`/evasion_events 語義不動 → settings.json Stop guardian timeout 20→10。② **`_detect_uncommitted_files` batch 化**：`_find_vcs_root` 純檔案系統 walk-up 定根分組（零 per-file subprocess），git 每根一次 `status --porcelain -z --`（NUL 安全）、svn 每根一次多目標 status；路徑比對 realpath+casefold（Win 不分大小寫）。③ **PostToolUse matcher 移除 Read**：accessed_files 改 Stop `_harvest_accessed_files` 從共用尾段一次回收（去重、冪等）；省每次讀檔一個 pythonw 行程；已接受邊界＝超出尾窗的極早期讀取會漏、`at` 為回收時刻。④ **episodic 生成品質**：新 `wg_core.sanitize_harness_noise` 剔 `<ide_opened_file>`/`<system-reminder>`/`<command-*>` 標籤（含未閉合）與 `[Guardian:*]` 等殘渣行——topic tracker 記錄端（`first_prompt_summary`/keyword）+ episodic 摘要端（舊 state 防禦）雙保險；知識段只收 LLM 萃取項+覆轍信號（工作區域/修改數歸摘要行，閱讀/版控統計本就有閱讀軌跡段，去重複）；A/B 實測：摘要 IDE 標籤消失、知識段 9→3 行；刪死碼 `_llm_extract_knowledge`+`_EXTRACT_PROMPT_TEMPLATE`（全 repo 零呼叫）。⑤ **實證不成立即跳過**：:3848「啟動才搶埠、無 runtime 重試」假說被隔離埠 38482 E2E 推翻（heartbeat 自 2026-03 da1ff4c 存在；持埠者 SIGKILL 後存活實例 ~15s 重綁；live whoami mtime==檔 mtime、AEC 路由 200）→ server.js 不動；跨專案 alias/trigger 掃描實測 9 專案 ~11ms/prompt → SessionStart 快取化不做。`run_verify` **873 綠**（855 基線 + 18 新：tailread/batch-vcs 10 + episodic noise 8）。 | `hooks/wg_evasion.py`(read_transcript_tail+text=), `hooks/wg_handoff.py`(text=), `hooks/handlers/stop.py`(tail-read 合一/_harvest_accessed_files/batch VCS), `hooks/handlers/post_tool_use.py`(-Read 分支), `hooks/wg_core.py`(sanitize_harness_noise), `hooks/wg_atoms.py`(_update_topic_tracker), `hooks/wg_episodic.py`(摘要/知識段/-死碼), `settings.json`(matcher/-Read+Stop timeout 10), `hooks/verify/{verify_stop_tailread_batchvcs,verify_episodic_noise_filter}.py`(新), `TECH.md`, `_AIDocs/{Architecture,DocIndex-System}.md` |
| 2026-07-08 | **feat(guardian): 程式化強化 A 組 — 可觀測性與誠實度程式化 5 項（Q3 核心）** — ① **AEC (b) 欄 cross-check**：Stop 偵測退避即存 `state["evasion_events"]` 證據（不受 UPS 清旗影響）；emit 時 hook 證據（窗口＝上次 emit 之後，`>=` 涵蓋同 turn blocked 再 emit）非空而 (b)=「無」→ Python one-writer 升 severity=real-evasion + report 附 `hook_evidence`、Stop fallback 連證據一起大聲（不信模型自評；Node chip 純內容判定屬既知不對稱，以 report 檔為準）。② **護欄觸發 JSONL**：`wg_core.append_guard_log` → `Logs/guard-{evasion,docdrift}.jsonl`＋lang_guard standalone 自含版 `guard-lang.jsonl`（每護欄獨檔＝並行 Stop hook 無競寫；5MB 輪轉），補齊「誤攔率無法量測」缺口。③ **outcome unknown 比率遙測**：Stop `_bump_outcome_stats` 隨 α/β 歸因 once-per-turn 三值計數 → SessionEnd `flush_outcome_stats` 落 `workflow/outcome_stats.jsonl`（滾動 50）→ 連續 3 session >70%（config `usefulness.unknown_watch`）→ marker → 下個 SessionStart 注入 advisory（防換 Opus 後完成語 regex 失配 → α/β 晉升軌靜默停滯）。④ **HUD 刪除決策後驗**：`_drain_aec_decisions` 加 Phase 1——已注入 delete 項下輪 `exists()` 實查，仍在→重注入一次（`reinjected`）、再仍在→告警結案（`verified`，不無限 nag）；prose 項保守跳過。⑤ **UPS 被 kill 哨兵**：UPS 開頭 arm `workflow/ups-sentinel/<sid>.json`、正常結尾 clear；見殘留＝上輪注入被 harness timeout 砍 → 浮一行告警（過去完全靜默）。`run_verify` **855 綠**（819 基線 + 36 新：crosscheck 9/guard-log 5/outcome 11/sentinel 6/drain 後驗 5）。 | `hooks/wg_core.py`(append_guard_log), `hooks/wg_evasion.py`(crosscheck_aec_severity/flush_outcome_stats), `hooks/handlers/{stop,post_tool_use,user_prompt_submit,session_end,session_start}.py`, `hooks/wg_docdrift.py`, `hooks/lang_guard.py`, `workflow/config.json`(unknown_watch), `hooks/verify/{verify_aec_crosscheck,verify_guard_trigger_log,verify_outcome_watch,verify_ups_sentinel}.py`(新)+`verify_aec_decision_drain.py`(擴), `TECH.md`, `_AIDocs/{Architecture,DocIndex-System}.md` |
| 2026-07-08 | **fix/refactor(identity+memory): IDENTITY 覆寫根因根治 + Opus 契約定版 + 啟動鏈瘦身 + atom 併合（審計裁決後續批）** — ① **根因破案**：IDENTITY.md 兩度被覆寫成空 stub 的元兇是 `user-init.sh` 雙檔 pipeline（每 SessionStart 無條件 cp `IDENTITY-{user}.md`→`IDENTITY.md`；個人槽改空置後成定時炸彈）→ 改為僅目標缺失時從 template 災復還原；事故鐵律入 atom（MemDev/啟動鏈自動覆寫陷阱）。② **IDENTITY 定版**：使用者裁決採用 Opus 瘦身版（5,702→3,373B，砍 CC 原生已涵蓋/矯 Fable 條款、保留強化自動推進/單一建議/不 hedge），實例+template 同步；SessionStart 哨兵條件校準（「自主行為契約」AND size≥2000）。③ **templates/ 搬遷**：IDENTITY/USER 兩 template 移 `templates/`，7 處引用同步（user-init/哨兵/Install-forAI/README/TECH/DocIndex/upgrade skill）。④ **必載檔 meta 敘述掃除**：CLAUDE.md 註記行、IDENTITY/USER「由 @import 載入」行全清（固定流程非 AI 認知項不佔 token），維護指引移 README「啟動檔維護規則」段 + Install-forAI 啟動檔角色註。⑤ **rules/core.md 降載**：版本治理/realm 細節/並行段縮成 atom 指針（3,562→2,477B）。⑥ **atom 併合 4 組**（72→68）：品質完整性→cognitive-patterns、post-mortem-write-raw→write-raw、記憶汙染→跨session資訊失真、feedback-memory-structure→doc-sync；12 顆 Related repoint、wg_core:925 死指向修正。合計 always-load 每 session 再省 ~1,100 tok。`run_verify` **819 綠**。 | `hooks/user-init.sh`, `hooks/handlers/session_start.py`, `hooks/wg_core.py`, `IDENTITY.md`, `templates/{IDENTITY,USER}.template.md`(搬遷), `CLAUDE.md`, `USER.md`/`USER-holylight.md`, `rules/core.md`, `README.md`, `Install-forAI.md`, `TECH.md`, `_AIDocs/DocIndex-System.md`, `skills/upgrade/SKILL.md`, `memory/`+`_AIDocs/`(atom 併合×4+新 atom), `IDENTITY.opus-draft.md`(刪，已定版) |
| 2026-07-08 | **refactor(realm): 分類詞庫單一來源化（審計裁決後續）** — realm 分類器的詞庫（`LOCAL_REALM_LEXICON` 15 詞）/核心保護清單（prefixes 6＋exact 7）/權重（10/1）原為 py（`lib/atom_locations.py`）與 js（`lib/realm.js`）**兩份手抄長常數**（全系統最脆 parity 面，新增詞要改兩處），仿 forbidden-phrases.json 先例抽出至 **`memory/_meta/realm-lexicon.json`** 單一來源，兩端模組載入時讀同檔（快取）；缺失/損毀 fail-open 至內建最小保護清單（詞庫停用、全判 core 安全預設）＋ stderr 浮訊號（可觀測性鐵律）。抽出前逐項 diff：**兩端零 drift**。守法升級：test_17 由 eval 原始碼塊改 require 實跑對拍（同時驗演算法鏡像＋兩端讀同 JSON）、新增 test_14b（JSON schema 完整性＋py 載入==JSON＋sentinel 詞守「改回手抄」倒退）；test_22（cleanRealmSegment eval）不變。學習詞庫 realm-lexicon-learned.json 原即獨立、不動。`run_verify` **819 綠**（基線 818＋test_14b）；py/js 三代表詞（MemDev/protected/unknown）實測輸出 byte-一致。⚠ realm.js 載入時讀 JSON，改詞庫需重啟 MCP 生效。 | `memory/_meta/realm-lexicon.json`(新 SoT), `lib/atom_locations.py`, `tools/workflow-guardian-mcp/lib/realm.js`, `lib/verify/verify_atom_io_equivalence.py`(test_14b 新/test_17 改), `_AIDocs/{SPEC_ATOM_V5,Architecture}.md`, `tools/workflow-guardian-mcp/lib/_MAP.md` |
| 2026-07-07 | **audit(memory): 全系統 6 切面深挖審計 + Opus 轉換適配（一夜批）** — ① **事故修復**：IDENTITY.md 被並發操作覆寫成 316B 空槽 stub（gitignored、git 救不回），從啟動快照原文救回；SessionStart 加完整性哨兵（缺契約段/過短→告警）。② **Bug 修**：user-extract L2 在 ~/.claude session 因 realm 閘 scope=personal 100% 拒寫→偵測 cwd 改 global；寫入失敗仍宣告「將記為 atom」假成功→三態宣告（wrote/deduped/failed）+ 失敗告警；`_aec_blank`/`aecBlank`（py↔js 同步）接受「無（附說明）」修 real-evasion ~83% 誤升級；atom 注入 token 估算 len//4 對中文低估 4-6 倍→改 CJK-aware（TURN_BUDGET 500 名目=真實）；`_RANKED_FLOOR=0.55` 鉗死 config search_min_score→移除。③ **死碼清理**：quick-extract.py 孤兒刪除、hot cache 通道整條摘除（寫入端已停產、json 凍結 6 天）、stop.py per-turn no-op 呼叫×6 + 死 import、log_injection 殘體、Blind-Spot no-op、⚡晉升 chat 提示行（38 hint 僅 4 跟進）、config 死鍵×2、mcp.js 死 tool 函數×4、funnel/realm.js 死出口、skills 空 verify 佔位目錄×15。④ **性能**：wg_atoms urllib 惰性 import（進程稅 ~1.5s→0.06s）、pre_tool_use 僅 Agent/Task 載 injection blob、Stop timeout 5→20s。⑤ **語料整頓**：刪死機制 atom×1、殭屍 Trigger 行×4 檔、死 Related 修復、realm 遷移 core→MemDev×3、trigger 收斂×7（workflow-svn 砍「衝突/conflict」等泛詞）、過時 claim 更新×5（confirmations 軌正式除役同步 decisions/TECH）。⑥ **Opus 適配**：settings.json model→opus[1m]、embed_timeout 1.5→3.0、IDENTITY 瘦身草案另存 `IDENTITY.opus-draft.md` 待裁決。`run_verify` **818 綠**。 | `hooks/`(user-extract-worker/wg_evasion/wg_atoms/wg_extraction/extract-worker/handlers×8), `lib/`, `settings.json`, `workflow/config.json`, `tools/workflow-guardian-mcp/lib/{mcp,funnel,realm,anti-evasion}.js`, `skills/{conflict,changelog-debug}/SKILL.md`, `skills/*/verify/`(刪×15), `memory/`(decisions/trigger 收斂/遷移), `_AIDocs/Failures/`, `_AIDocs/_atoms/MemDev/`, `TECH.md`, `IDENTITY.md`(救回), `IDENTITY.opus-draft.md`(新) |
| 2026-07-07 | **docs(identity): IDENTITY.template.md ↔ IDENTITY.md 雙向同步取真** — template 補「高階自主行為契約」5 節 + 人文/決策支援條目；IDENTITY.md 實例補回 template 較新的收尾檢核段（門檻觸發 + AEC MCP emit + (e) 版本脈絡掃除，7/2 已上線的實際系統行為，實例先前停在舊 prose 版）。兩檔除頂部說明（模板用途 vs 個人擴充指引）外內容一致（diff 驗證）。 | `IDENTITY.template.md`(+契約段), `IDENTITY.md`(收尾檢核段更新，gitignore 不入 repo), `_AIDocs/_CHANGELOG.md` |
| 2026-07-07 | **refactor(identity): Fable 行為契約改併入 IDENTITY.md，個人擴充槽恢復空置** — 使用者裁決（單人環境）：契約 5 節移入 `IDENTITY.md`「高階自主行為契約」段（團隊共用必載層），CLAUDE.md 移除 `@IDENTITY-holylight.md`、該檔恢復空置待未來個人擴充。順手修補 IDENTITY.md 行為準則段重複的「決策支援」條目（併為一條）。atom 同步改指 IDENTITY.md。 | `IDENTITY.md`(+契約段/-重複條目), `CLAUDE.md`(-import), `IDENTITY-holylight.md`(空置 stub), `_AIDocs/_atoms/MemDev/模型行為移植-fable行為契約必載檔.md`(replace), `_AIDocs/_CHANGELOG.md` |
| 2026-07-07 | **feat(identity): IDENTITY-holylight.md 個人擴充槽啟用 — Fable 行為契約必載化（模型行為移植）** — 修長期無效擴充槽：DocIndex 宣稱 `IDENTITY-{user}.md` 為 @import 個人擴充，實際 CLAUDE.md 從未 import 且檔案內容只是 IDENTITY.md 複本。改寫為真擴充內容（Fable 5 行為協議蒸餾 5 節：意圖優先/自動推進/自主觀察背景執行/收尾完整性/表達校準，模型無關）+ CLAUDE.md 補 `@IDENTITY-holylight.md`，供切換 Opus 等模型時重現「抓重點、自動推進、自主觀察」工作風格。設計原則入 atom：每-session 行為規範必放必載檔、trigger atom 只承載場景觸發式細節。 | `CLAUDE.md`(+import), `IDENTITY-holylight.md`(複本→真擴充), `_AIDocs/_atoms/MemDev/模型行為移植-fable行為契約必載檔.md`(新), `_AIDocs/_CHANGELOG.md` |
| 2026-07-06 | **fix/refactor(guardian): Anti-Evasion HUD 後續修正 3 項** — ① **blank-detection 放寬**：`_aec_blank`/`aecBlank`（py↔js MIRROR）strip 尾標點後比對「無」，修 routine 報告因寫成「無。」被誤判 real-evasion 洗回 chat 的 bug（live dogfood 現場揪出）。② **per-turn 報告檔改子夾** `workflow/aec-report/<sid>-t<turn>.json`（write/read/test 三處 + `.gitignore workflow/aec-report/` 同步；HUD read glob 從扁平改子夾）。③ **HUD autospawn 分級**：`_maybe_spawn_hud` 加 `sev in (notable,real-evasion)` 條件 → 只真發現彈窗、routine 靜默入 disk（配合使用者「看完就關」workflow）。WORKFLOW_DIR 全域（`~/.claude/workflow`，非 per-project）故專案 session 報告亦集中此。`run_verify` 綠（含 blank 三態回歸）。 | `hooks/wg_evasion.py`(_aec_blank), `hooks/handlers/post_tool_use.py`(落檔子夾+spawn 分級), `tools/workflow-guardian-mcp/lib/anti-evasion.js`(aecBlank+REPORT_DIR), `hooks/verify/verify_aec_emission_gate.py`, `.gitignore`, `workflow/config.json`(hud_autospawn) |
| 2026-07-02 | **feat(guardian): Anti-Evasion HUD — 收尾檢核 (a)(b)(c)(d) 移出 chat 洗版，改 severity-gated 獨立窗 + Stop 硬閘保證 emit** — 消滅 chat 最擾民的「收尾檢核」洗版：收尾改以 MCP 結構化 tool `anti_evasion_report(a,b,c,d)` 送出，chat 只留折疊 chip、內容走 dark HUD。**one-writer spine**（解 MCP 進程無 session 身份 + Node writeState 無鎖 race）：MCP tool 全程**不碰 state**、只回 chip；Python `post_tool_use` 新分支（帶原始 `session_id`+`turn_seq`）獨佔寫 `state["anti_evasion_report"]` + 落 per-turn `workflow/aec-report-<sid>-t<turn>.json`（atomic）+ 判 HUD fallback。**Stop 硬閘**：ScanReport 閘滿足方式從 prose 標記（`detect_missing_scan_report`）換成本回合 emit（`detect_missing_aec_emission`，**turn_seq+session_id 雙鍵**＝sibling 隔離：共用工作樹下隔壁 session 的 emit 不誤放行本 session）；門檻/豁免（own_mod_files 過濾、純-commit `committed_this_turn`、dismiss 逃生門）全建於既有 session-filter 之上不覆蓋。**severity**（Python `aec_severity`／Node `aecSeverity` same-rule）：(b) 真偷埋→real-evasion／(a) 有修補→notable／全「無」→routine；notable/real chip 頂層一次 + HUD 紅橫幅，routine 就地覆蓋不洗 chat。**HUD**（Node `lib/anti-evasion.js` 唯讀 API+heartbeat + `lib/aec-hud-html.js` 頁）：輪詢 1.5s（非 SSE，直讀 disk report 檔，港口持有者供頁跨 instance 安全）、最新卡 (a)(b)(c)(d)+近 N 回合 severity 歷史格（dataviz 驗證 palette，CVD ΔE 44.6、皆 ≥3:1、色+文字雙編碼）、格子 click 展開。**可觀測性**：HUD 不可達+notable → Stop 大聲 fallback 回 chat（不 fail-silent）；Edge --app spawn config-gate（`aec.hud_autospawn` 預設關）+ no-shell（AV 安全）。v1＝核心閉環+唯讀歷史；Sync/role/(d) 互動鈕 defer v2。`run_verify` **791 綠**（+27 AEC 回歸：emit 閘/sibling 隔離/純-commit 豁免/severity/one-writer 落檔）+ 隔離埠 E2E（真 MCP stdio tools/call chip、/aec/hud 200、/api/aec/* 服 disk、path-traversal 拒）+ Playwright 肉眼（卡+紅格+drill）。 | `hooks/wg_evasion.py`(detect_missing_aec_emission/aec_severity/_completion_gate_applies factor), `hooks/handlers/post_tool_use.py`(MCP emit 分支+落檔+HUD spawn), `hooks/handlers/stop.py`(emit 閘雙鍵+fallback), `settings.json`(PostToolUse matcher), `tools/workflow-guardian-mcp/lib/{mcp,anti-evasion,aec-hud-html}.js`, `tools/workflow-guardian-mcp/server.js`(5 路由), `tools/workflow-guardian-mcp/lib/_MAP.md`, `hooks/verify/verify_aec_emission_gate.py`(新), `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **feat(guardian): ScanReport 收尾檢核閘新增「純 VCS commit turn 豁免」** — 承接 per-session 過濾：本 turn 有跑 git/svn commit 時豁免收尾檢核（工作已寫進 VCS 歷史＝可稽核、與「藏」相反，anti-evasion 目的在 commit 那刻消解）。`post_tool_use` Bash 分支以 `_VCS_COMMIT_RE`（限首個管線段前，`git log｜grep commit` 不誤中）偵測 commit → 記 `last_commit_turn_seq=turn_seq`；`stop.py` ScanReport 閘 `committed_this_turn`（`last_commit_turn_seq==turn_seq`）即 skip。**不開後門**：豁免綁「真的 commit 了」而非「本 turn 沒 Edit」——光宣告完成不 commit 仍被擋；未提交檔仍由 SyncReminder / 一般 block 兜底。`run_verify` **764 綠**（+13：豁免 3、commit regex 8、post_tool_use wiring 2）。 | `hooks/handlers/post_tool_use.py`(_VCS_COMMIT_RE + last_commit_turn_seq), `hooks/handlers/stop.py`(committed_this_turn 豁免), `hooks/verify/verify_scan_report_session_filter.py`(+13), `_AIDocs/Architecture.md`(ScanReport 列同步), `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **fix(guardian): ScanReport 收尾檢核閘按 per-session 過濾 modified_files** — 共用工作樹/merged state 下（`_ensure_state` 回傳兄弟或 `merged_into` 的混池 state），他 session 改的 core 檔（如隔壁拆檔 session 的 `server.js`）會混進本 session 讀到的 `modified_files`，令只改 docs 的協調 session 被誤觸發收尾檢核。**閘從來沒讀 git status**（一直讀 per-session `state.modified_files`）——真根因是 entry 未記作者 session。修法：`post_tool_use` 每筆 entry 標 `session_id`（實際 Edit/Write 的 session）；`stop.py` ScanReport 閘先算 `own_mod_files`（`session_id==本` 或未標記的 legacy＝fail-open 保守視為本 session），只用它做 core-touch／`min_files_to_block` 門檻判定。Sync Reminder 閘與底部一般 block 仍用全量（提醒 commit 性質，刻意不動）。`run_verify` **751 綠**（含新增 `verify_scan_report_session_filter` 4 條端到端：他 session core 不觸／本 session core 觸／legacy fail-open／協調-docs-only 實例不觸）。 | `hooks/handlers/post_tool_use.py`(entry 標 session_id), `hooks/handlers/stop.py`(own_mod_files 過濾), `hooks/verify/verify_scan_report_session_filter.py`(新), `_AIDocs/Architecture.md`(ScanReport 列同步), `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **refactor(guardian): server.js 4394→401 行純機械拆分為 進入點 + 11 lib 模組** — 巨檔超「AI 安全編輯不失真」線，按功能拆為 CommonJS 模組（`lib/{paths,log,state,realm,atom-render,atom-access,funnel,atom-tools,mcp,http-api,dashboard-html}.js` + `_MAP.md`），**行為保留、零新依賴、無 router 框架**。手法：讀完整 4394 行 → 機械 depgraph 算跨模組相依 → carve 腳本逐段 **verbatim** 抽原檔行拼模組 + accounting 不變式（每非空行覆蓋剛好一次）→ 波次冒煙。守地雷 **C1**（埠自癒 `__filename`/`SELF_MTIME` 鎖 server.js）· **C2**（mcp `buffer` 私有）· **C4**（`require.main` guard + `buildAtomContent/renderKnowledgeLines/isBlockKnowledge` re-export）· **C7**（dashboard 內層瀏覽器端 const 勿 hoist）；**mcp↔atom-tools 循環**由 `handleToolCall` lazy require 化解；api/* 4 域 + http-util helpers 併單一 `http-api.js`。parity 隨碼重指：`verify_promotion_gate_phase0`→`atom-access.js`+`atom-tools.js`、`verify_atom_io_equivalence` test_14/17/22→`realm.js`（test_17 端標 `const TOOLS_DIR`→`function slugify`）·test_25→`atom-tools.js`。6 驗證全綠：require 不綁埠+3 export · handshake 4 tool · **run_verify 747** · GET /+全 /api/* 200 · 埠自癒雙實例 relinquish/rebind · live MCP 4 atom tool。 | `tools/workflow-guardian-mcp/server.js`(4394→401), `tools/workflow-guardian-mcp/lib/*.js`(11 新模組+`_MAP.md`), `hooks/verify/verify_promotion_gate_phase0.py`, `lib/verify/verify_atom_io_equivalence.py`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **feat(guardian): server.js :3848 埠孤兒「預防」根治 — stdin-EOF 自行退出（Option A）** — 補齊 `d070472` 協作式交棒（只「回收」孤兒）的缺口：server.js 由唯一一個 CC client 經 stdio spawn，該 client 退出（session / VS Code 關閉）時 OS 關 pipe 寫端 → 我方 stdin 收 EOF → 本進程 `process.exit(0)` 自行退出，孤兒根本不產生、:3848 自然釋放，無需回收任何其他進程。**只在「自己的父程序」退出時退出 → 不觸碰別實例**，活躍 session 的 MCP 安全（其 stdin 仍連活父、不會 EOF）。`end`+`close` 雙保險 + `_parentGone` 防重入，置於 `require.main === module` 守門（parity 測試 `require()` 不受影響）。既有交棒保留為 abrupt-kill / 新舊碼升級兜底（防禦縱深）。`run_verify` 747 綠 + 隔離埠（`WG_DASHBOARD_PORT=38481`）E2E：關 stdin 後子進程 2s 內 code 0 退出 + orphan-prevention log PASS。 | `tools/workflow-guardian-mcp/server.js`(`require.main` 塊 stdin end/close 自退), `_AIDocs/Architecture.md`(重啟 SOP 註), `_AIDocs/_atoms/MemDev/guardian-dashboard-孤兒佔埠與新碼重啟.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **refactor(memory): MemDev「對談結束自動記憶」atom dev-history 化 + P1 write-gate 偽衝突結案** — **Task1**：編年/開發敘事（session_end flush 落點路由 gap-fix、DPM 的 edit-count proxy 拔除、獨立預算演化、verify wiring）遷 `DevHistory/auto-memory-writeback.md`（新 #19）；atom 走 funnel 瘦身成 timeless 機制圖 + 維護同步 gotcha（frontmatter byte-stable、零 index drift），修 L15 過時 claim（同檔 `edit_counts>=3` 已自 `_should_deep_postmortem` 拔）+ L18 變更叙事收斂為現況。〔P7 sweep 曾標「對談結束md 瘦身」實為部分、本次補完〕**Task2**：實測 `check_memory_path_block` [P1] 是 PreToolUse guard、只攔 Claude Write/Edit 工具呼叫，harness 原生 auto-memory 走內部 I/O、不觸 hook → **偽衝突·非工具層·guard 無需改**，於撞名辨識 atom 標記結案。〔發現既有 drift：funnel guard 未覆蓋 `_AIDocs/_atoms/` local-realm atom，待裁決〕純 docs、零 code 改。 | `_AIDocs/DevHistory/auto-memory-writeback.md`(新), `_AIDocs/DevHistory/_INDEX.md`(#19), `_AIDocs/_atoms/MemDev/{對談結束自動記憶與錯誤加權深記,harness原生memory與atom索引marker撞名辨識}.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-02 | **feat(guardian): server.js :3848 埠自癒 — 純 Node HTTP 協作式交棒，佔埠舊碼自我退出（不 spawn shell、不殺別進程）** — 舊 session 的 server.js 孤兒不隨 VS Code 關閉退出、續佔 3848 服務舊碼（改過的路由不上線 → 新路由 404）。`tryBindDashboard` 探到埠被佔時呼 `reclaimStaleOrphan()`：`POST /api/relinquish{requesterMtime,requesterFile}` 請佔埠者交棒——holder 只在「同 server.js 檔 ∧ 對方 mtime > 自己 boot 時 mtime（＝我是舊碼）」時 ACK 並 `process.exit(0)` **自我退出**，新實例再 rebind；peer 跑當前碼回 `relinquishing:false`、非 guardian 無此路由 → 一律讓步。**「只殺自己人」由構造保證：從不殺別進程，舊碼 holder 自己退，且只有我方碼有 relinquish 合約**。另加 `GET /api/whoami`（pid/檔路徑/boot-mtime，供判定 + 「新碼是否上線」驗證）+ `WG_DASHBOARD_PORT` env override（隔離測試）+ `require.main === module` 守門（bare require 不綁埠）。**設計轉折（見上一 commit）**：初版走 JS `execFile powershell`（Get-CimInstance/Stop-Process）查殺，實測 detached 情境 `spawn EPERM` 且觸發卡巴斯基封鎖（node→powershell→殺進程＝AV 紅旗）→ 全面改純 http 協作、跨平台、零外部 spawn。`run_verify` 747 綠 + node --check + 雙 E2E（交棒 PASS／同碼 peer 讓步不驅逐 PASS，全程自我退出零 kill）。 | `tools/workflow-guardian-mcp/server.js`(relinquish/whoami 路由 + reclaimStaleOrphan + env/require.main 守門), `_AIDocs/_atoms/MemDev/guardian-dashboard-孤兒佔埠與新碼重啟.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **docs(audit): P6 文件收尾 — TECH/Architecture/SPEC/Install 逐值對齊審查後實碼** — 22 子系統多鏡審後的封頂文件校準（只碰 docs、不改 code）。**TECH.md**：檔頭標 post-audit；§2 樹補 `lang_guard.py`·標 quick-extract 孤兒·skill 21+`_archived`·settings 8→9 事件；§7 token budget 1500/3000/5000→**1000/2000/3000**·always-load 3,200-5,400→實務 **~2-2.5k**（誠實揭兩估算器分歧：flat 1,387／CJK-aware 3,739）·加 dispatcher 惰性 import ~639→~120ms；§9/§5.2/§8.1 BM25 min_score 1.0→**3.5**·ACT-R 補核心豁免 distraction penalty；§11 三層管線標 per_turn/session_end_flush 停產·quick-extract 孤兒；§13 逐列（Hot Cache/Response Capture 停產·Self-Iter α/β 豁免·FixEsc error-based〔**保 6-agent**〕·Realm llm_fallback off·Codex 拔 subprocess_timeout·新增 lang_guard/治理原則/DPM 獨立預算/atom-heal L2 列）·§14/§15 單人化+global 66；版本歷史加 audit 列。**Architecture**：hook 10→9 事件·Stop 3 支 standalone·quick-extract/DPM 獨立預算修正·skill archive·realm off·治理原則段。**SPEC/Install**：bm25 3.5·17→30 core·subprocess_timeout 標拔除·llm_fallback off·init/conflict archive。〔**校驗發現**：prompt 稱「非 6-agent」與「§5.3 刪 subprocess_timeout 列」經實碼查證為誤——fix-escalation SKILL 仍 6-agent、TECH §5.3 無此列，未照錯改；另 wg_core.py:53 註解「1500/3000/5000」為 stale drift，因「不改 code」約束留報告不動〕`run_verify` 710 綠。 | `TECH.md`, `_AIDocs/Architecture.md`, `_AIDocs/SPEC_ATOM_V5.md`, `Install-forAI.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **chore(sweep): P8 收尾封頂 — 多人團隊層降 dormant + lang_guard 英文漂移攔截** — **P8a**（`17f7b0d`+`a598d92`+`e6e1262`）：單人環境把多人團隊層降 dormant——`init-roles`/`conflict-review` skill archive 至 `skills/_archived/`（tools/ 版保留）、`is_management` 硬 `return True` 假閘誠實化、IDENTITY/USER 拔團隊措辭改單人、heal-review 修 conflict-review archived 後 dangling `[[ref]]`。**P8b**（`f12e0c8`）：`lang_guard.py` 英文回應漂移**程式化攔截**——獨立 Stop hook（仿 codex_companion），量測終版訊息英文佔比 >0.5（≥40 語言字元）→ `systemMessage` 注入繁中提醒（stateless 每輪自校正、無 flag、規則化不靠 instruction salience）；config `lang_guard{enabled,english_ratio_threshold:0.5,min_lang_chars:40}`。 | `hooks/lang_guard.py`(新), `settings.json`(Stop hook), `workflow/config.json`(lang_guard), `skills/_archived/{init-roles,conflict-review}/`, `hooks/wg_roles.py`(is_management), `IDENTITY.md`/`USER.md`, `skills/heal-review/`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **fix(memory): P5 深化 — DPM 獨立預算 + atom-heal L2 抽出 + World 正名 + 治理原則入 rules** — （`2a617a4`+`cb0810c`）**DPM 獨立預算**：實測 DPM gate 被 Sync+TestFail 吃光 `stop_gate_max_blocks(2)` 餓死 → 拔 `_should_deep_postmortem` 共用預算檢查、改純 one-shot `deep_postmortem_done`（獨立預算 1），補回歸 `test_dpm_not_starved`。**atom-heal L2 抽出**：server.js `apiHealAll` 去 `missing_reverse_refs`（L1 已由 SessionEnd `--fix-refs` 補）＝只掃 `broken_refs`、與腦內世界解耦；`/memory health` 接 L2 死連結自癒 OFFER。**World 正名**：world.html/server.js 頂註標 L2 娛樂外掛、非 guardian 常駐、按需通道非死路（隔離不刪）。**治理原則**：`rules/core.md` 加段 Native-first（原生優先·自製只做結構化跨-session 高價值）+ 可觀測性鐵律（fail-open 必告知·vector 靜默死 27d 反例）。 | `hooks/handlers/stop.py`(DPM 獨立預算), `hooks/verify/verify_deep_postmortem_gate.py`, `tools/workflow-guardian-mcp/server.js`(apiHealAll+world 註), `tools/world.html`, `skills/memory/`(L2 OFFER), `rules/core.md`(治理原則), `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **fix(rules+identity): P4 契約鬆綁 — 條件觸發 + 單人化 + 假閘誠實化** — （`a2798ac`+`2424e66`+`a4d5ca5`+`a598d92`）反退避契約由「每 session 硬觸發」鬆綁為條件觸發：ScanReport gate (a)(d) 降條件觸發、`claims_completion` 收窄（TestFailGate 留原樣）；rules/core.md 強制並行 agent 硬規則→**按需評估**；USER template/實例拔多使用者段+去 V4 版本標記→單人化（留「伺服器級多使用者總決策」未來提醒）；IDENTITY 微削重複 bullet + `is_management` 假閘誠實化。 | `hooks/wg_evasion.py`(ScanReport 條件觸發), `hooks/handlers/stop.py`(claims_completion), `rules/core.md`(並行按需), `USER.template.md`/`USER.md`, `IDENTITY.md`, `hooks/wg_roles.py`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **fix(memory): P3 校準 — α/β 核心豁免 + BM25 min_score 1.0→3.5 + Realm 外科停 LLM + atom-funnel 5→1** — （`4e8674a`+`fff80eb`）**α/β 反噬止血**：核心保護清單 atom（decisions/preferences/workflow）豁免「高曝光低效用」distraction penalty，止 Self-Iteration 對核心 atom 降注入序。**BM25**：`bm25_min_score` 1.0→**3.5**（濾雜訊召回）。**Realm**：`realm.llm_fallback.enabled=false` 外科停 LLM，只跑 deterministic 詞庫（含 learned）保確定性 sweep。**atom-funnel（皇冠明珠強化）**：server.js `atom_write` create 路徑 5→**1** spawn（build→write_raw→access init→write_index 單一 subprocess），byte-parity test_27 續守；`atom_promote` description + reject-msg 假 ReadHits 閘改 **Confirmations OR Wilson** 誠實化。CLI smoke + verify 全綠。 | `hooks/wg_atoms.py`(α/β 豁免), `workflow/config.json`(bm25_min_score/realm.llm_fallback), `tools/workflow-guardian-mcp/server.js`(create 5→1·atom_promote desc), `lib/verify/verify_atom_io_equivalence.py`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **verify(memory): P5 收尾 — Vector daemon @ 3849 去留複測（判定 KEEP）+ episodic 注入端 read_hits/24d purge 驗證（前 session 已落地、無改碼）** — 續接 P5 兩子項。**A**：實證 `ae7461f` 已落地 episodic 注入端 `increment_read_hits`（`ups_context._record_episodic_exposure`，只計實際注入、`is_file()` 防污染）+ SessionEnd 依 `Expires-at` 24d TTL purge（`wg_episodic._purge_expired_episodic` 走 `move_atom_pair` funnel、md+sidecar 原子搬可逆）；28 顆 episodic 已 6 顆 read_hits>0（原長期全 0）→ 機制活、非死碼。**無需改碼**。**B**：複測 `Logs/vector-observation.log`（1118 筆）→ ready 命中率全體 87.4%(534/611)、P1 後真實 session 88.9%(16/18)、P1 後 error 歸零（P1 前 127）；遠超 REVIVE≥50% 門檻 → **daemon KEEP**（無效果才砍，本例高效不砍）；`no_flag` 42% 為 fire-and-forget 冷啟動假象非命中品質問題。續接 prompt「歷史 51%」查無來源、codebase 實測歷史與當前皆 ~87% 已校正。**doc heal**：聚合工具 `vector-observation-summary.py`/`vector-probe-burst.py` 已於 `d76bba1`（Wave 5 汰舊）刻意移除 → 不復活（Native-first），修 `bm25-global-layer.md` L134 現在式 dangling ref + vectordb doc 補 §2026-07-01 複測（含 ad-hoc 可複現法 + snapshot/非一次性註記）。`run_verify` 710 綠。 | `_AIDocs/Failures/vectordb-silent-failure-2026-04.md`(§2026-07-01 複測), `_AIDocs/Tools/bm25-global-layer.md`(L134 dangling ref), `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **chore(sweep): P7 全庫版本操作脈絡殘留掃除（Zone 1-7 + REVIEW + 政策批）** — 依 [[feedback-live-檔與記憶不留版本操作脈絡歷史歸專門檔]]，剝 live 檔與 atom 的 milestone（V5 Pxx / Sprint N / Wave N / 非檔名耦合 Phase N / 方案甲 / 當里程碑用的 V2.xx）、dated 戳、commit-hash、spec 錨（[Fxx] / Sx.x）、stderr `[vN]`/`[phaseN]` 前綴、debug 事件 key 前綴、自我編輯鏈、新舊 diff 敘事 → timeless 功能敘述。**Zone 4** 全 verify 目錄陳舊 `test_→verify_` docstring 自名 + authoring 戳；**Zone 5** skills dev-phase banner；**Zone 3** hooks core 18 檔~194 處（3 並行 agent + wisdom_engine 親改·stderr 前綴 42 處）；**Zone 1/7 漏網補**（atom_io/atom_index_json/atom_spec/ollama_extract_core 的 V5 Pxx·spec 錨）；**Zone 2/6** atom body（decisions-arch/atom-元資料/internal-pipeline/realm-v5 自編輯鏈收斂/對談結束md 瘦身+修過時觸發 claim/腦內世界）+ DESIGN.md 砍變更記錄 + config 方案甲。**REVIEW 逐檔 user 裁決**：B1 dated-incident 統一 trim、B2 世代標籤全剝（含 user-facing 印出值）、spec 錨/plan-doc 指標剝、SGI 污染脈絡剝（r10854 SVN rev KEEP）。**KEEP**：SCHEMA_VERSION/_migrate_vXXX/schema 遷移、裸 V4/V5/V6 能力 scope、測試 fixture 值、功能 literal（migrated-v2.21/v41_extraction/w155007·e155007 SVN 碼）、檔名耦合 Phase 2(#2)、DevHistory 導覽錨、功能專案錨（atom_classify/unity-yaml SGI）。**教訓**：staging 逐檔清單不可信賴為完整，須 pattern-first 全掃再動（首批 Zone 4 漏系統性 stale docstring、Zone 3 partition 漏 stderr 前綴檔）。**驗證**：run_verify 696 綠全程 + server.js node --check + 全 hooks AST OK + 收尾 pattern-first grep 確認 live 零 REMOVE 殘留；行為零改（純註解/docstring/標籤/atom body，atom 走 byte-exact 不動 frontmatter→零 index/mirror drift）。 | `hooks/`(core+handlers+verify 40+ 檔), `lib/`(atom_io/atom_locations/atom_spec/atom_index_json/ollama_extract_core/taxonomy_*/verify), `tools/`(codex-companion/memory-*/sync-*/init-roles/cleanup/check-bypass/atom-health-check/indexer/verify), `skills/`(codex-companion/memory/conflict-review/init-roles/heal-review SKILL.md), `memory/`(decisions-architecture/atom-元資料/_reference/internal-pipeline/wisdom/DESIGN), `_AIDocs/_atoms/`(MemDev/realm-v5·對談結束, World/腦內世界), `workflow/config.json`, `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **fix(memory): FixEscalation/DeepPostMortem retry 訊號 error-based 化 + 拔 same_file_3x edit-count proxy**（P3 尾·審查續；設計決策 user 拍板 error-based）。**根因**：`track_retry` 用「同檔 edit≥threshold」當 retry proxy，52/52 false-fire——零測試失敗的正常反覆迭代被誤判為「反覆修不好」，污染 FixEscalation 注入 / α-β outcome / reflect accuracy / episodic rut-signal 四個下游。**修**：①`wisdom_engine.track_retry` 加 error gate——僅 `state["failing_tests"]` 非空（detect_test_failure 偵到、尚未清的真測試/lint 失敗）期間、同檔反覆編輯才計 retry；plan-path 豁免與 threshold(plan 4/direct 2) 語意不變。②`stop._should_deep_postmortem` 的 effort OR 拔掉 `same_file_3x`（唯一還能獨立驅動 gate 的純 edit-count proxy、未 failure-gate）；現 effort=retry≥2 ∨ fix_escalation_triggered，外層 AND real_failure 不變。**測試連同重設計**（非只讓過）：`verify_wisdom_retry_calibration`（+ error-gate 核心測）、`verify_fix_escalation_plan_retry`（「真碼 edit 應觸發」意圖誠實化為「失敗未解時的真碼 edit 才觸發」）、`verify_deep_postmortem_gate`（same_file_3x 單獨不再觸發）。**清理**：移除本次觸及 code/test/config 的版本操作脈絡（V2.x/方案A/日期），改 timeless。**atom 同步**：修 `escalation-hook…false-fire` 過時 proxy claim 為現況；新 `feedback-live 檔與記憶不留版本操作脈絡`。`run_verify` 全綠。 | `hooks/wisdom_engine.py`(track_retry error-gate), `hooks/handlers/stop.py`(拔 same_file_3x), `hooks/verify/verify_wisdom_retry_calibration.py`, `hooks/verify/verify_fix_escalation_plan_retry.py`, `hooks/verify/verify_deep_postmortem_gate.py`, `workflow/config.json`(deep_postmortem._doc), `memory/escalation-hook…false-fire…`, `_AIDocs/Failures/feedback-live-檔與記憶不留版本操作脈絡…`(新), `_AIDocs/_CHANGELOG.md` |
| 2026-07-01 | **refactor(memory): 自動萃取層淨值審查 → 調整式拔除（auto-capture/DedupStage 停產、注入縮量、遙測誠實化）**（8-agent 對抗審查＋codex 第二意見＋逐檔實證；user 批准「調整式拔除」+ 縮量選項 A）。**證據**：auto-capture 34.3 草稿/天、7% 存活、**0 下游消費**（write-only 死路）；現存 draft 池 81% 近重複，DedupStage 實跑截斷 0/16、子字串去重 0/16、真近重複 0/3、`restore_from_trash` 可執行碼 0 呼叫點；晉升遙測 prior-subtracted 成功率 0.373、全系統 max Wilson 下界 0.264 < promote_lb 0.6（實務永不晉升，保守不誤晉升）。**拔**：①`response_capture.per_turn.enabled`+`session_end_flush.enabled`→false（保留 `failure_extraction`＋episodic）②settings.json 移除 quick-extract Stop hook ③DedupStage 全套（`lib/dedup_stage.py`+2 verify+`_dedup_sweep_core` def/call）。**縮量(A)**：`TURN_BUDGET_LIMIT` 800→500、`compute_token_budget` 1500/3000/5000→1000/2000/3000（-40%）。**誠實化**：turn-global outcome 頻率偏差 + confirmations 休眠記入新 MemDev 決策 atom + 連結既有 [[confirmations-已退役…]]（未直改 [固] decisions.md：funnel guard 擋直 Edit + 避 bloat，改由 trigger 命中兩 atom 更正）。**駁回的審查主張（實證為非 bug，不動）**：「37 顆假 episodic 種子」＝Beta(1,1) Laplace prior（`atom_access` succ=α−1 已減 prior、n=0 排除於晉升/降級），純審查聚合口徑誤；「死的確認碼」＝2026-06-12 蓄意退役的休眠 OR 軌，非疏於維護，不強拆。**保留**：62 人工 atom+failure atom、手動 /extract+atom_write、/refile、failure→Failures、episodic 生成、read_hits 曝光計數。回滾：全 git-tracked，`git revert` 復原。 | `workflow/config.json`(per_turn/session_end_flush disabled), `settings.json`(移 quick-extract hook), `lib/dedup_stage.py`(刪), `hooks/verify/verify_dedup_stage.py`(刪), `hooks/verify/verify_session_end_dedup_wiring.py`(刪), `hooks/handlers/session_end.py`(移 dedup 接線), `hooks/wg_core.py`(budget 縮量), `_AIDocs/_atoms/MemDev/自動萃取層淨值審查-調整式拔除-2026-07.md`(決策 atom·新·含遙測誠實化), `_AIDocs/_CHANGELOG.md` |
| 2026-06-30 | **feat(dedup): _drafts 牢籠去蕪 DedupStage 引擎 + 接線 core SessionEnd（SoT §3 落地）**（接 `8701aa5` 乾淨基線；user 眼驗 dry-run 批准後接線）。**引擎** `lib/dedup_stage.py`：與確定性 classify 主路**物理隔離**（信任模型不同：classify=不可逆寫索引/詞庫、dedup=可逆 soft-delete），只在 `_drafts/` 牢籠運作（`cage_assert` fail-closed + CI grep 鎖死禁索引/詞庫寫入符號、連 docstring 字面都不出現；牢籠原語複用 `taxonomy_jury` 不重造）。**截斷三訊號全中才判** `is_truncated_fragment`（①行內未閉合 ②連接符收尾 ③`## 行動` 佔位）——**80 份真實 draft 實證**：訊號③ 80/80（builder 統一附加→單獨無鑑別力、真正用途是排除有真實行動項的 curated atom）、全中 1/80（真截斷）；**禁全文末字**＝主記憶 26% 假陽性（slug 恆截 60 字但 body 完整）。**per-env**：project=截斷清除+叢集去重 / core=碎片吸收不去重；cluster 首版只收 **literal substring 冗餘（零知識損失保證）**，paraphrase 級語意去重刻意不做（禁 LLM、違 dedup 確定性信任模型→留人工 /refile）→ §0「41% 可省」首版未達成（安全優先、誠實標記）。**soft-delete** → `_drafts/_trash/`(+`.trashmeta.json` sidecar 記原位/時戳/理由) 14 天閘 `purge_expired_trash`（唯一硬刪）+ `restore_from_trash` 救回；`sweep_drafts` 持非阻塞 file-lock 拿不到即 skip。**接線**：`session_end.py::_dedup_sweep_core()`（env=core 只清截斷不去重、fail-soft 吞例外絕不弄垮 SessionEnd），實測真 SessionEnd 已 soft-delete 1 真截斷 draft → `_drafts/_trash`（可逆）。project 端接線＝Phase B 另議。**§9-vs-§1 釐清**：`taxonomy_jury`/`game_taxonomy`=Phase 0 牢籠地基**保留複用**；「已取代」指舊 atom 分類路（game_taxonomy 為主+三路 jury）→已退 `atom_classify.score_by_lexicon`（確定性），三者正交。**驗證**：`verify_dedup_stage.py`(26)+`verify_session_end_dedup_wiring.py`(5)，`run_verify` **651→688 全綠**；知識落 MemDev `[臨]` atom。commit `ed51b48`+`20dd4d2`+`604e898`。 | `lib/dedup_stage.py`(新), `hooks/handlers/session_end.py`(`_dedup_sweep_core` 接線), `hooks/verify/verify_dedup_stage.py`(新·26), `hooks/verify/verify_session_end_dedup_wiring.py`(新·5), `_AIDocs/_atoms/MemDev/dedup-stage-牢籠去蕪設計…`(atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-30 | **fix(memory): index 表 parser 空行容忍 + SessionStart 0-atom fail-loud（silent-failure 真因 direction 1+2 治本）＋核實 atom_promote regex 已修**（從 c:\Projects /memory 流程派來核實 regex bug；`CrossRealmWriteBlock` 正確擋下從專案改核心碼，故移至 ~/.claude session）。**①核實 atom_promote regex**：已於 `6b8caa7`（今 16:38）修畢、HEAD 已含、live MCP 本 session 已載入（dry-run 正常回應）。位元組精確 node 對照證實舊形 `` `- \\${conf.replace(...)}` `` 拋 `Unterminated character class`、新形正常且能 bump `- [觀]`→`- [固]`；grep server.js 無殘留壞 pattern。**無需再改**。附帶實證：全域 146 sidecar 無一達 usefulness `lb≥0.6 & n≥3` 閾（top=`feedback-memory-system-doc-sync` lb=0.328；confirmations 已退役恆 0），故 `execute=true` gold-standard 無合格對象——不偽造資格（會污染 earned-confidence 語意）；regex 路徑僅在 execute=true 對合格 atom 觸發，現無合格者故 prod 暫不會踩，修已就位待 atom 達閾。**②同族治本（Failures doc 明列「待 ~/.claude session 一併治本」）**：`_parse_trigger_table` 與 `parse_aidocs_index` 表內遇空行即 `in_table=False` → silent 掉後續 entry（**實證**：`| Atom |` 表中插一空行，3→1 atom）。`continue`(非 break) 只讓它「下個表頭可重入」、未容忍**單表內**空行。改：空行 `skip` 不結束表 + skip 重複表頭（多區塊表不誤收表頭為假 atom）+ 僅「非空且非 `|`」真內容才結束表。**③SessionStart fail-loud**：全域 index 檔存在但 `parse_memory_index` 回 0 atom → `_atom_debug_error` log + `[Guardian:IndexZero]` 顯著 advisory（全域恆有 atom，0=解析壞；專案層可合法空故僅檢全域，零 false-fire）。**同族非問題（核實後不動）**：`memory-audit` metadata parser 遇空行 break 為**正確**（乾淨 metadata 區無真空行；雙 CR 假空行已由 Fix B/`d3afda7` 正規化），audit 0 error。**驗證**：新增 `verify_trigger_table_blank_tolerance.py`（6 測：空行/多空行/真內容結束/重複表頭/真 index 非零）；`run_verify` **683 全綠**（677+6）；真 `_ATOM_INDEX.md` 仍 61 atom、`_AIDocs/_INDEX.md` 仍 14 entry、`memory-audit` 仍 0 error。**py hook 即時生效（下個 session start 套用）**。 | `hooks/wg_atoms.py`(`_parse_trigger_table`+`parse_aidocs_index` 空行容忍), `hooks/handlers/session_start.py`(`global_index_zero` fail-loud), `hooks/verify/verify_trigger_table_blank_tolerance.py`(新), `_AIDocs/Failures/memory-pipeline-silent-failure-2026-05.md`(③更新), `_AIDocs/_CHANGELOG.md` |
| 2026-06-30 | **fix(memory-tools): atom_promote regex crash + 兩支健檢工具子目錄/跨層感知補齊（碼補齊文件既述意圖）**（專案層 `/memory health` 報根層問題，user 要求核實後才修；CrossRealmWriteBlock 已正確擋下從專案改根碼）。**核實**：三項皆實證為真且非專案層/skill 過時——skill 正確傳 `--project-dir`/`--memory-root`，bug 純在工具；`_atom_index.json`（SoT）本身把 26 atom 記為 `memory/shared/<domain>/…`，SPEC/DocIndex 早述「多根/project→global up-ref」為意圖，本次＝碼補齊文件既述行為（非改意圖）。**①server.js:1848 regex crash**：模板 `` `- \\${conf.replace(...)}` `` 多餘 `\\` 前綴，`.replace` 已跳脫 `[]` → 產出 `- \\[觀\]` 字元類未閉合，**每次 `execute=true` 晉升必崩 Unterminated character class**（node before/after 實證）。移除 `\\` 前綴。**②memory-audit `validate_index`**：扁平 `iterdir()`+flat 存在檢 → V5 `shared/<domain>/` 子目錄 atom 誤報「索引指向不存在」（專案 26 個全誤）。補遞迴 stem 集 fallback（委派既有 `iter_atom_files`：global=memory+Failures+_atoms、非 global=rglob 單根）；**僅用 index→file 方向**，file→index 仍扁平避免子目錄 auto-capture atom 誤報「未在索引」洪水。**③atom-health-check `EXTRA_SCAN_ROOTS`**：只搜 `~/.claude/memory` → 對 `_AIDocs/Failures/`(feedback-*/cognitive-patterns) 與 `_AIDocs/_atoms/`(local realm 深層) 的 up-ref 全誤報 broken；改用 `atom_search_roots()` 三根（與 `find_atoms` 全域掃描對齊）。**驗證**：`py_compile` 雙過；專案 index error **26→0**、broken_refs **13→1**（殘留 1＝真缺 ref `本機-mysqlsh-…`，正確保留不遮）；global 層零回歸（index err 0 / broken_refs 0）；專案 file→index 警告無洪水(0)。**doc-sync**：依 [[feedback-memory-system-doc-sync]] 清單逐項過，無架構/規則/計數/檔案結構變動 → 僅 `_CHANGELOG` 一條（SPEC/DocIndex 已述意圖，本修使其為真，不改）。**py 即時生效；server.js → MCP 重啟（下個 session）生效**。 | `tools/workflow-guardian-mcp/server.js`(L1848 regex), `tools/memory-audit.py`(`validate_index` tree_stems fallback), `tools/atom-health-check.py`(`EXTRA_SCAN_ROOTS`=atom_search_roots), `_AIDocs/_CHANGELOG.md` |
| 2026-06-26 | **docs(vision): 新增 `_AIDocs/Vision/` 發想分類 + JARVIS 企業 AI 平台主題（README + 9 子檔 + 指標 atom）**（user 要求把「原子記憶系統還缺什麼才能當核心」gap 分析 + 延伸知識存入 _AIDocs，立場「說不定某天會做成成品」）。**新分類**：`_AIDocs/Vision/` 為「發想/前瞻設計」read-on-demand 參考層（非 atom、非 `_atoms/`、零 session 注入；與 `_staging`(進行中/易變) 區別＝有深度值得長期保留的發想）。**主題 jarvis-enterprise-ai-platform**：主題檔 README（定調「記憶＝海馬迴非大腦本身、核心是編排者」+ 願景16條覆蓋度對照 + 缺口分級 + P0-P2 路線 + 子檔導讀）+ 9 深掘子檔（01 記憶共享皮層/02 編排核心/03 模型路由/04 工具註冊協定/05 知識攝取/06 多模態/07 作業紀錄/08 安全治理/09 演進路線），每檔統一骨架（願景對應→現有方案比對表附 URL→推薦取捨→★現有原子記憶系統落地切入點→風險紅線）。**研究**：3 Explore + 自查 WebSearch，比對 Mem0/Zep/Cognee·LangGraph/CrewAI/MS Agent Framework Magentic/Glean/Devin·Portkey/OpenRouter/RouteLLM·MCP Registry/A2A/AGNTCY·WhisperX/Fireflies·Recall/Rewind·多租戶 RAG/EU AI Act（子 agent 並修正 Zep 數據/Whisper 99 語言/EU AI Act 條號/Rewind 已被 Meta 收購下架 等）。**索引**：_INDEX.md +1 行(#13)。**指標 atom**：local-realm [臨]，trigger 命中時喚起。**純文件新增，未動記憶系統碼**。 | `_AIDocs/Vision/_INDEX.md`(新), `_AIDocs/Vision/jarvis-enterprise-ai-platform/`(README+01~09 共 10 檔·新), `_AIDocs/_INDEX.md`(#13), `memory/_atom_index.json`(指標 atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-26 | **fix(atom-move): V5 SoT-correct 重寫 — 修 V4 殘留靜默損壞單中央索引**（專案層 CC 用 MCP `atom_move` 搬 atom 資料夾時觸發 SoT 損壞，user 要求查核＋修）。**查核實證**（三方佐證）：MCP `atom_move` 確 spawn `tools/atom-move.py`（server.js:2053）；該 V4 工具只 `import lib.atom_io` 改 deprecated `_ATOM_INDEX.md` 鏡像、**全程不碰 `_atom_index.json`（V5 SoT）**→ 鏡像被 `regenerate_atom_index_md` 從舊 JSON 重生覆蓋、JSON path 停在舊處→注入讀 JSON path 找不到檔＝**半孤兒**；`cmd_move` 只 rename `.md` **不搬 `.access.json` sidecar**（計數歸零）；子夾被誤當 memory root 建孤兒 per-folder 索引。`atom-set-realm.py:7-12` 註解早已獨立記載此壞。**修**：①`lib/atom_access.py` 抽 public `access_sidecar_path`/`move_atom_pair`/`prune_empty_parents`（sidecar-aware 原子搬，單一來源，atom-set-realm 改用之消重）；②重寫 `atom-move.py` 走 JSON SoT（`upsert_atom`/`delete_atom`+audit）、`find_index_dir` 上溯偵測 index-root、同根搬移保留 scope、`special_realm_reason` 守門拒 `_AIDocs/_atoms`·`Failures`、搬後 `validate_index` 自驗；③server.js `atom_move` description 改述 JSON SoT。**驗證**：`verify_atom_move.py` 24 檢全 PASS（同根分類·sidecar 計數保全·JSON/鏡像一致·守門·跨根 delete+upsert），atom-set-realm dry-run 回歸正常。**py 即時生效；server.js → MCP 重啟生效**。 | `lib/atom_access.py`(3 helper), `tools/atom-move.py`(重寫), `tools/atom-set-realm.py`(改用共用 helper+註解), `tools/workflow-guardian-mcp/server.js`(description), `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/_atoms/MemDev/atom-move-v5-sot-correct…`(新·atom_write), `_AIDocs/_CHANGELOG.md` |
| 2026-06-25 | **docs(governance): 「記憶治理（Memory Governance）」統合命名 + 文件同步 + 知識庫補完**（user 三問：①整串叫什麼 ②該不該更 TECH/Architecture/Memory 文件 ③研究知識是否全入庫）。**①命名**：整串（Task 1 憲法＋atom／Task 2 A 分心懲罰·C relevance gate·D selective forgetting／兩 realm 修復）統合為記憶系統的「**記憶治理（Memory Governance）**」自檢層（即 L4 學門名、governance doc 標題已錨定）。**②文件同步**（補先前延後的 doc-sync）：`TECH.md` §13 加「記憶治理」子系統列；`Architecture.md` ups_search/ups_inject/session_end 三列標註 governance A/C/D 落點；`DocIndex-System.md` 同步三列。**③知識庫補完**：`context-memory-governance.md` §4 從「候選方向」更新為「**已落地實況**」（A/C/D ✅、B 冗餘、E defer，附 verify 件）＋新增 §3.5「根理論速查」（人類科學/統計/ML：CLT·Information Overload·Bounded Rationality·Shannon SNR·Catastrophic Forgetting·Concept Drift，把研究的人類科學分支完整入庫）＋§5 補 Shannon/catastrophic-forgetting/concept-drift 來源。SPEC_ATOM_V5 不動（atom schema spec，非注入管線行為）。**純文件、未動碼**。 | `_AIDocs/context-memory-governance.md`(命名+§3.5+§4已落地+§5), `TECH.md`(§13 列), `_AIDocs/Architecture.md`(3 列), `_AIDocs/DocIndex-System.md`(3 列), `_AIDocs/_CHANGELOG.md` |
| 2026-06-25 | **fix(atom_io)+atom: 修 write_raw 註解說謊 + 兩顆紀律 atom 搬遷/新建**（user 審視後裁決執行）。**①fix(atom_io.py:45)**：原註解寫「未列舉 source 會 raise ValueError」與實作矛盾（409-411 實回 `WriteResult(ok=False)`、不 raise）→ 註解誤導讀者去 try/except。選**修註解對齊實作**（非改 raise）：write_raw 的 OSError 路徑（414-415）也回 ok=False＝統一 WriteResult 契約；且 `atom_io_cli.py:68-71` 只 catch TypeError/KeyError，raise ValueError 會穿透破壞 MCP funnel 的 JSON 回報。**②atom 搬遷（SGI→global core）**：`escalation-hook…false-fire…` 由 SGI 專案層搬全域 core（FixEscalation/DeepPostMortem hook 每專案都 fire，辨識紀律屬跨專案）；非裸複製＝generalize 掉 SGI 戰報細節、修 Related（去 global→project down-ref）、保 slug 不變以續接 chardatablobtool 上連結。**③新 atom（global local/MemDev）**：`write_raw 對未列舉 source 靜默回 ok=False…` 記此坑＋防再犯（呼叫端必驗 res.ok、破壞性步驟前先讀檔）。 | `lib/atom_io.py`(L45 註解), `memory/escalation-hook…false-fire…`(搬入·atom_write), `_AIDocs/_atoms/MemDev/write-raw…okfalse…`(新·atom_write), SGI `memory/shared/MemoryMeta/`(清副本), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **fix(realm): systemic 根治 — context-engineering/governance 概念詞列入 lexicon 泛用詞庫（斷自我強化污染）**（接前一條 per-atom 保護的系統性 follow-up，user 要求直接執行）。**根因**：`_LEXICON_GENERIC_TOKENS` 已含 inject/memory/atom 等，但缺 context/rot/poisoning/engineering/forgetting/selective…→ sweep 學「context rot」等概念詞進 learned 詞庫後，**未來任何含同詞 atom 都被 deterministic 誤降 local**（自我強化迴圈，本 session 實際發生）。**修**：`_LEXICON_GENERIC_TOKENS` 增補 context-engineering/memory-governance 概念詞群（context/rot/poisoning/distraction/confusion/clash/engineering/governance/forgetting/selective/relevance/extract/retrieval/… ＋中文 萃取/上下文/汙染/分心/遺忘/腐化）。因 `is_generic_lexicon_term` **需全 token 泛用才拒**，含實例 token 的真 local 詞（auto-handoff/eaddrinuse/guardian-dashboard/wsl2）仍照收、不誤殺。讀（`load_learned_lexicon`）寫（`append_learned_terms`）兩端皆套用。learned 詞庫護欄無 js 對拍面 → **py-only**（base lexicon/classify_realm 結構未動，parity 不破）。**驗證**：新 `verify_lexicon_concept_terms.py` 4 測（概念詞拒收/中文概念詞/實例詞照收/append 寫入端拒收），`run_verify.py` **620 passed** 零回歸。比 per-atom 保護更通用（任何未來 governance/context atom 自動免疫）。**py 即時生效**。 | `lib/atom_locations.py`(`_LEXICON_GENERIC_TOKENS` 增補), `lib/verify/verify_lexicon_concept_terms.py`(新·4測), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **fix(realm): governance atom 列入核心保護硬擋 — 修本 session 內被 realm sweep 誤搬 local（覆轍第 N 次）**（收尾 git status 抓到）。Task 1 建的跨專案 core atom `記憶汙染與上下文腐化-注入萃取自檢` 在本 session 期間被 `_sweep_realm_auto_migrate` 由 `memory/`(core) 誤搬到 `_AIDocs/_atoms/MemDev/MemoryIndex/`(local)，且 lexicon **自我學習** 4 個通用詞（context rot/selective forgetting/context poisoning/context engineering → MemDev，會污染未來同詞 atom）。**嚴重性**：該 atom 設 core 是要跨專案注入（痛點在 ~/.claude 外大專案），誤搬 local 後只在 ~/.claude 注入＝Task 1 跨專案目的被靜默廢除。**修**：①`git checkout` 還原整批 sweep 側效（atom 回 core、lexicon 4 詞清零、索引/backlink 復原）＋刪 MemDev 副本；②比照 `自己flag`/`goal-driven`，slug 列入 `LOCAL_REALM_CORE_PROTECTED_EXACT`（py `lib/atom_locations.py` + js `server.js` 鏡像，protected 永不喚 LLM／永不搬／永不學詞）。**驗證**：`classify_realm` 對該 slug（帶全部誤判 trigger 詞）回 `{realm:core, protected:True}`，`node --check` 過，`run_verify.py` **616 passed** 零回歸。**系統性 follow-up（未解）**：realm sweep + lexicon 學習持續把「記憶系統術語」atom 誤判 local，per-atom 保護是 whack-a-mole；根治需擋 lexicon 學通用詞或改分類器（另案）。**server.js → MCP 重啟生效；py 即時**。 | `lib/atom_locations.py`(`LOCAL_REALM_CORE_PROTECTED_EXACT`+slug), `tools/workflow-guardian-mcp/server.js`(鏡像), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **feat(memory): Task 2 Phase D — selective forgetting（接活 archive advisory → 隔離 _distant/）＋反諷防線評估**（接續 Phase C；本 session 連續 A→D 完工）。憲法 **Forgetting** 對策：現況 `_self_iterate_atoms` 算 archive 候選只寫 `_staging/archive-candidates.md`、`_distant/` 在 skip-list 卻無人搬。新 `select_forget_candidates`（純函式：score<isolate_threshold ∧ 非 `LOCAL_REALM_CORE_PROTECTED_EXACT`）＋ `apply_selective_forget`（搬 md+access → `memory/_distant/`，被 sync-atom-index 排除→不注入、**可逆**、觸發 index 重產）。**預設 dry-run**（`forget.enabled=false`，user 裁決）→ 只寫 `_staging/forget-candidates.md`、絕不搬；真隔離需 `enabled=true ∧ dry_run=false`。整合進 `_self_iterate_atoms`（archive 寫入後）＋ session_end 日誌反映模式。**反諷防線評估**：`_emit_usefulness_hints` 由 `usefulness_hint_tier` 驅動（n<min_n/純曝光回 None）→ 本就極少觸發、非 distraction，無需精簡（誠實結論）。**驗證**：新 `verify_selective_forget.py` 6 測（閾值/保護排除/dry-run寫清單不搬/預設安全/真隔離搬_distant/缺檔skip），`run_verify.py` **616 passed** 零回歸。**hook .py＋config → 下個 session 生效；真隔離預設關**。 | `hooks/wg_atoms.py`(`select_forget_candidates`+`apply_selective_forget`+整合), `hooks/handlers/session_end.py`(日誌), `workflow/config.json`(`self_iteration.forget`), `hooks/verify/verify_selective_forget.py`(新·6測), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **feat(memory): Task 2 Phase C — 注入 relevance gate（related-spread 最小高訊號集裁切）＋ Phase B 判冗餘略過**（接續 Phase A，本 session 連續執行）。**Phase B 執行發現（grep 實證）冗餘**：`_drafts/` 全 codebase 無讀回/晉升消費端、`verify_flush_routing` 已斷言所有 auto-capture → `_drafts/`（不入索引/注入），加上刻意 atom_write 強制 `[臨]` 起步＋confirmation 晉升閘＋write-gate，故「萃取臆測污染核心」vector **已關閉**，再加 gate ＝冗餘演戲（違反不純加法），**不寫碼**。**Phase C**（憲法 **Context Confusion** 對策）：新 `ups_inject._filter_related_by_relevance` **只裁 related-edge spreading**（非 prompt 命中、純連結擴散＝最易成 distractor），主迴圈 trigger/bm25/vector 命中者完全不碰 → **不誤殺 prompt 相關或新 atom**。規則：①skip_demoted 剔除「已證明低效用」(demote_candidate, n≥min_n；只動證明過的) ②依 `compute_injection_rank` 降序保留前 max_related(6)。**減少**注入＝淨負 token、降 distraction。config kill-switch `injection.related_gate.{enabled,skip_demoted,max_related}`（預設 ON-保守）。**驗證**：新 `verify_related_relevance_gate.py` 5 測（停用passthrough/空/剔demoted/新atom不誤殺/cap保留top-rank），`run_verify.py` **610 passed** 零回歸。**hook .py → 下個 session 生效**。 | `hooks/handlers/ups_inject.py`(`_filter_related_by_relevance`+整合), `workflow/config.json`(`injection.related_gate`), `hooks/verify/verify_related_relevance_gate.py`(新·5測), `plans/cozy-sauteeing-jellyfish.md`(B 冗餘留證), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **feat(memory): Task 2 Phase A — 注入分心懲罰（context governance 落地之一）**（接續憲法 doc；本 session 連續執行 A→D）。憲法 **Context Distraction** 對策：新 `wg_atoms.compute_injection_rank()` = ACT-R activation − `w·log10(read_hits+1)·(1−wilson_lb)`，對「高曝光(read_hits 純曝光)但已證明低效用(n≥min_n 且 Wilson 下界低)」的 atom 降注入優先序；`ups_search` ACT-R 排序鍵改用之。**寧漏勿誤殺**：n<min_n（新 atom/樣本不足）一律不罰、read_hits=0 不罰、無 config/資料缺失 fail-open 回純 activation。純重排不丟 atom、**不增注入 token**（反諷防線天然滿足）。config kill-switch `usefulness.distraction_{enabled:true,weight:0.5}`（預設 ON-保守，user 裁決）。**驗證**：新 `verify_distraction_penalty.py` 7 測（停用/無config/無access/低效用受罰/樣本不足不罰/零曝光不罰/高效用小罰），`run_verify.py` **605 passed** 零回歸。**hook .py → 下個 session 生效**。 | `hooks/wg_atoms.py`(`compute_injection_rank`), `hooks/handlers/ups_search.py`(`_activation_key` 改用), `workflow/config.json`(`usefulness.distraction_*`), `hooks/verify/verify_distraction_penalty.py`(新·7測), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **docs+atom(governance): 新增「上下文與記憶治理」設計憲法 doc + 跨專案自檢 atom**（user 探問「累積知識造成壓力/錯亂/誤判/汙染」有無統合術語/專門學問，要落到注入·萃取自檢 hook）。研究（CC Opus ×2 web agent + Codex 第二意見交叉驗證）定位四層術語：**L1 病徵分類法** Drew Breunig《How Long Contexts Fail》4 模式（poisoning/distraction/confusion/clash，與症狀近一對一）／**L2** Context Rot（Chroma 2025 實測 18 模型）／**L3** Context Engineering（Karpathy·Anthropic·LangChain）／**L4** Agent Memory Management·Memory Governance（本系統所屬）＋人類根 Cognitive Load Theory（Sweller）。定性：**避免汙染 > 省 token**；核心能力＝selective forgetting + relevance gating + 最小高訊號集。新增根層 doc（術語地圖＋病徵對應表＋對到 `ups_inject`/`memory-write-gate`/SessionEnd 衰減的 hook 落點，作 Task 2 設計憲法）＋ core atom（[臨]，跨專案注入，18 trigger，4 問自檢行動，cross-link cognitive-patterns/decisions）。**純知識新增、未動碼**；Task 2（注入·萃取自檢 hook 實作）另起 Plan-Mode 分階段 session。 | `_AIDocs/context-memory-governance.md`(新), `memory/記憶汙染與上下文腐化-注入萃取自檢.md`(新·atom_write), `memory/_atom_index.json`+`MEMORY.md`(自動同步), `_AIDocs/DocIndex-System.md`(登錄新 doc), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **fix(handoff): Layer 1 token 預警改讀 `message.usage` 真實 token + 自我校準 200k/1M 分母（預設 1M）— 修 1M 視窗下 proxy 誤鳴**（user 報「目前 token 警示抓多少 bytes、是否該預設 1M」）。**根因**：`estimate_context_usage` 用 char-proxy（字數×係數）當分子、`context_window_tokens` 寫死 **200000** 當分母；user 常跑 `claude-opus-4-8[1m]`（1M 視窗），同一份 transcript 真實佔用 9% 卻被算成 47%，逼近 0.85 門檻誤鳴。實測三信號源（transcript `message.model` 恆 `claude-opus-4-8` 不含 `[1m]`／頂層無 beta 欄／settings env 空）→ **「按 model id 動態判斷視窗」不可行**（[1m] 是 context beta、不在任何 hook 可見處）。**修（user 裁決 C 案＋預設 1M）**：①分子改讀每輪 `message.usage`（input+cache_creation+cache_read＝API 真實佔用，已含常駐開銷故不再加 `base_overhead`），取最近一輪＝現在實塞量（新 `_usage_totals_from_text`）；②分母 `_calibrated_window` 自我校準——物理事實「200k 視窗逼近前 harness 先 auto-compact、usage 摸不到 200k」→ **曾觀測 >200k 數學上必為 1M**、否則用 `default_window`（`max` 確保 default 調低仍會真突破後自動升頂）；③`context_window_tokens` 200000→**1000000**、`token_warn_payload` fallback 預設同步；④無 usage（全新 session／讀取失敗／舊格式）→ degrade 回原 char-proxy，既有測試零回歸。**驗證**：`verify_auto_handoff.py` **29 passed**（原 25＋新 4：usage 主路徑／自我校準 1M／未破 200k 用 default／usage 優先於 char-proxy）；真實當前 transcript 實跑 @1M=9%（不鳴）vs @200k=47%（舊行為），自我校準正確。**hook .py＋config → 下個 session 生效**。 | `hooks/wg_handoff.py`(`_usage_totals_from_text`+`_calibrated_window`+`estimate_context_usage` 重寫+`import json`+`token_warn_payload` 預設+預警句措辭+docstring), `workflow/config.json`(`context_window_tokens` 1M+`_doc`), `hooks/verify/verify_auto_handoff.py`(+4 usage 測), `_AIDocs/Architecture.md`(Layer 1 描述), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **fix(memory): auto-capture [臨] 草稿隔離到 `_drafts/` — 根治 content-as-filename 碎片污染 memory/ 根**（user 重啟電腦後見一堆 q1/q2/sgi 碎片冒出 memory/ 根，要求「即時寫入的也該分類，避免大量污染 memory」）。**根因**：`response_capture` 的 session_end flush（per_turn 只入 queue、session_end 才 materialize）走 `_flush_item_to_atom`→`write_atom` 把萃取知識寫成 `[臨]`／`Author=auto-captured`／content-as-filename atom **扁平落 memory/ 根（global core，scope=global→注入每個專案）**；既有 content-type gate 只擋 plan 型、放行 knowledge 型。本 session 修的 P2 只擋 sweep 搬移、沒擋這個 flush 寫入。**修（user 裁決：隔離，不走「寫入時跑 realm 分類器」以免重蹈 lexicon 污染）**：`_flush_route` 回的 dedup_dir 改為 `_drafts/auto-capture/` 子層（global→`memory/_drafts/…`、project→`{shared}/_drafts/…`）；`_flush_item_to_atom` 從 `write_atom`（入索引/注入）改 `build_atom_content`+`write_raw` **直寫 dedup_dir**——`sync-atom-index` EXCLUDED_DIR_PARTS 已排除 `_drafts` → **不入索引、不注入、不計數**；`.gitignore` 加 `memory/_drafts/`＋`**/memory/shared/_drafts/`（暫態不入版控）。草稿待人工檢視/晉升，真有值知識在工作中已正規記錄。**不影響 `memory-peek.py`**（它掃 `personal/auto/`／author=auto-extracted-v4.1，與本 flush 的 auto-captured 不同機制）。**驗證**：功能性（寫 _drafts／frontmatter 正確／不入主 index／dedup 有效），`verify_flush_routing`＋`verify_session_end_flush`＋failure/handoff/postmortem 共 **62 passed** 零回歸（含斷言更新）。順手清掉本 session flush 已污染的 8 顆碎片（重複，知識已存）。**hook .py → 下個 session 生效**。 | `hooks/extract-worker.py`(`_flush_route`+`_flush_item_to_atom` write_raw 隔離), `hooks/verify/verify_flush_routing.py`(斷言更新), `.gitignore`(`_drafts/`), `memory/_atom_index.json`+mirrors(清 8 碎片重生), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **fix(realm): `自己flag…` 列入核心保護硬擋 — 同 session 第三度被 LLM sweep 誤搬根治**（接續同日 realm 修復，收尾時 git status 抓到再犯）。本 session 期間舊 sweep（修復下個 session 才生效）又跑一次：P0 清乾淨詞庫後 deterministic 不再命中 `自己flag的維護動作直接做完不要反問`，但它 **Author=holylight／[臨]** → P2 `_is_unconfirmed_autocapture` 不護（非 auto-captured），LLM fallback 遂把它判 local→`Else`（de5fa9f 歸 core→被搬回、本日歸 core→又搬 Else，第三度）。**根治**：比照 goal-driven，slug 列入 `LOCAL_REALM_CORE_PROTECTED_EXACT`（py `lib/atom_locations.py` + js `server.js` 鏡像，protected 永不喚 LLM／永不搬）；atom 移回 `memory/`（core）。**驗證**：`classify_realm` 回 protected=True/realm=core，`verify_atom_io_equivalence` **27 passed** 零回歸，js `node --check` 過，index 53 零 Else 殘留。**server.js → MCP 重啟生效；py 即時**。附帶澄清：前一 commit abbc94f 夾帶的 4 顆 MemDev atom 1/1 改動是建新 atom 時的 `Related` 雙向反向連結回填（良性自維護，非誤改）。 | `lib/atom_locations.py`(`LOCAL_REALM_CORE_PROTECTED_EXACT`+自己flag), `tools/workflow-guardian-mcp/server.js`(鏡像), `memory/自己flag…md`(Else→core), `memory/_atom_index.json`+mirrors(重生), `_AIDocs/_CHANGELOG.md` |
| 2026-06-24 | **feat(codex): 跨 session handoff 對抗式自檢 — 把作者「自評」升級為獨立「他評」**（user 要求 Q2 自檢機制；接續同日 realm 修復）。**問題**：`/handoff` 既有 Step 3.5 對抗式複審是**同一個 Claude 自評**，抓不到自身盲點（SKILL 自引 2026-06 dogfood：CC 自寫 next-phase 缺口直到被 user 提醒才發現）；codex-companion 雖預設開、基礎設施齊（短命子程序／sandbox／JSON schema／severity 路由／UPS 注入），但只做 plan/turn/architecture 三類審計，grep handoff 零命中、完全沒接交接流程。**做**：擴 codex-companion 加第四類審計 `handoff_review`（中等改造、複用全套基礎設施，非新做）——①`_detect_checkpoint` 加 `_NEXT_PHASE_RE`，PostToolUse 偵測 `_staging/next-phase*.md`／handoff 檔 Write/Edit（持久化 artifact＝/continue 接手端實讀者）→ 觸發；②`handle_post_tool_use` 讀 handoff 全文（≤6000 字）塞進 `context.handoff_content`，經 `audit.py` 流到 assessor `extra_context`；③`prompts.py` 新 `HANDOFF_REVIEW` 模板＋`build_handoff_review_prompt`，把 Step 3.5 的 **8 問**（why／決策理由／未解問題獨立區／只寫 delta／load-bearing 逐字／假設vs已驗證／矛盾顯式裁決／約束放首尾）當對抗 checklist 餵 codex，OUTPUT_SCHEMA 加 `handoff_gap` category；④`assessor.run_assessment` 加 dispatch 分支；⑤`handle_user_prompt_submit` 對 handoff_review **降注入門檻至 medium**（user 主動要求的交接自檢，不被預設 `max_inject_severity=high` 靜默吞，中度缺口即以 `[Codex Companion: Handoff 自檢]` 浮上）＋label。`soft_gate.handoff_review`（預設開）可控。**不取代**自審（codex 可能離線）——自審仍是硬閘，本機制為後盾。**驗證**：新 `verify_handoff_review.py` 12 測（觸發/不誤觸/Read 不觸/toggle/8問齊/turn_index 回抄/handoff_gap/空文件缺口），codex verify 全套 **46 passed**（34→46）零回歸；prompts/assessor/codex_companion/config 語法皆過。**hook .py＋config → 下個 session 生效**。 | `tools/codex-companion/prompts.py`(`HANDOFF_REVIEW`+builder+schema category), `tools/codex-companion/assessor.py`(dispatch 分支), `hooks/codex_companion.py`(`_NEXT_PHASE_RE`+`_detect_checkpoint`+`_read_handoff_content`+UPS 降門檻+label), `tools/codex-companion/verify/verify_handoff_review.py`(新·12 測), `workflow/config.json`(`soft_gate.handoff_review`), `skills/{codex-companion,handoff}/SKILL.md`(說明同步), `_AIDocs/_CHANGELOG.md` |
| 2026-06-18 | **feat(realm+tooling): 解綁「儲存位置 vs 注入範圍」＋ 人讀文件計數 SoT 自動同步**（接續同日前一條，user 裁決三項）。**①解綁 cross-project-local**：user 心智模型「memory/＝索引、_AIDocs/_atoms＝知識儲存」要求知識住 _atoms 但仍跨專案注入。新增 `CROSS_PROJECT_LOCAL_DOMAINS`（`lib/atom_locations.py` frozenset＝{Continuity}）＋ `is_cross_project_local()`；`session_start.py` 注入閘門對清單內 Lv1 根 local atom 例外放行（外部專案仍注入），storage／write 路由／realm／catalog 全不變、**僅注入範圍**跨專案，對偶 feedback-*。py-only（注入無 js 對拍面）。守門 `verify_realm_injection_gate` ＋1 case ＋ predicate test。**②計數自動化（根治 2026-06-17 chronic drift）**：新 `tools/sync_doc_counts.py`——讀 `_atom_index.json` SoT、只改 `<!-- atom-total／breakdown／core／feedback／failmode／local -->` marker 夾心（不碰歷史敘述如 SPEC「V4 ~30 atoms」、零誤改史料）；`--check`／`--write`、`newline=""` 保留各檔行尾（修首版 write_text 把 LF 檔整檔 CRLF 化的假 drift）。**靜默自動推進**：`sync-memory-index.py`（atom_write 經 server.js 背景觸發）末尾 piggyback `sync_doc_counts.sync()`，故 atom 增刪／搬移後計數自動跟上、不再進對話雜訊。TECH／_INDEX／DocIndex 共 6 處 live-count 埋 marker，48→54 實算（core14＋feedback8＋失敗模式2＋local30〔World4／Tools11／MemDev10／OS3／Continuity2〕）。**③解綁 doc-sync**：realm atom（直接 Edit 補 bullet——append funnel 對 local atom 報 not-found、find-fallback 未覆蓋 append）＋ rules/core.md ＋ SPEC §2.2 注入閘門 ＋ Architecture ＋ DocIndex ＋ TECH 各加例外註記。**驗證**：`run_verify.py` **576 passed** 零回歸。**hook .py 改動 → 下個 session 生效**。 | `lib/atom_locations.py`, `hooks/wg_core.py`, `hooks/handlers/session_start.py`, `lib/verify/verify_realm_injection_gate.py`, `tools/sync_doc_counts.py`(新), `tools/sync-memory-index.py`(piggyback), `tools/verify/verify_doc_counts.py`(新·4測), `TECH.md`, `_AIDocs/{_INDEX,DocIndex-System,SPEC_ATOM_V5,Architecture}.md`, `rules/core.md`, `_AIDocs/_atoms/MemDev/MemoryIndex/realm-範疇分區機制-v5.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-18 | **feat: 跨 session 接續安全 — handoff 品質／抗失真知識 atom ＋ skill 流程硬閘**。**①新增 2 顆知識 atom**（`handoff-綜觀品質與抗失真寫法`、`跨session資訊失真機制與對策`），由 3 路並行 web 研究綜整（Anthropic context-engineering／long-running-agents 工程文 ＋ arXiv 2307.03172 lost-in-middle／2505.06120 LLMs-get-lost-multi-turn ＋ 醫療 I-PASS·SBAR／ADR 交接實務），定位為**語意品質層**（補既有「結構完整＝6 區塊」之不足）：why／未解問題獨立區／只寫 delta 不複述通則／verified-vs-assumption／首尾放約束／10 失真機制×對策表。**②依 user 心智模型搬範疇**（memory/＝索引角色、_AIDocs/_atoms＝知識儲存）：以 `atom-set-realm` 移入**新建 local 範疇 `Continuity`**（scope 仍 global、sidecar 原子同搬、`sync-memory-index --check` exit 0）。**③skill 流程硬閘**：`/handoff` 加 **Step 3.5 對抗式自我複審 gate**（dogfood 實證：作者寫完不會自動發現自身缺口，2026-06 截圖事件）；`/continue` Step 3 加 **read-back 開場**（動工前覆述現狀／下一步／約束）。**④新 feedback atom** `feedback-complexity-origin-trace`（core）：user 反饋「回答超預期複雜＝設計矛盾症狀，先追蹤起源、勿堆疊解釋」。**⑤揭露待裁決設計矛盾**：realm 把「儲存角色」綁死「注入範圍」（core＝全專案／local＝僅 ~/.claude），`feedback-*` 為唯一解綁特例；故移入 Continuity 後兩 atom 僅在 ~/.claude 注入——是否解綁待 user 決。 | `skills/handoff/SKILL.md`(Step 3.5), `skills/continue/SKILL.md`(read-back), `_AIDocs/_atoms/Continuity/`(2 atom 新建＋`_INDEX.md`), `_AIDocs/Failures/feedback-complexity-origin-trace.md`(新), `memory/{MEMORY.md,_local_catalog.md,_ATOM_INDEX.md,_atom_index.json}`(sync), `_AIDocs/_CHANGELOG.md` |
| 2026-06-17 | **docs: 人讀文件 atom 計數 doc-sync — 校正至 `_atom_index.json` SoT（48 atoms）**。新增 1 顆 local atom 後稽核發現人讀文件 atom 計數**早已 chronic drift**（總數 32/38、core 13/15、local 10/14 三方互相矛盾且全落後磁碟），非僅本次 +1。**以 JSON SoT 實算為唯一真相校正**：**48 atoms = core 14 + feedback 7 + 失敗模式 2 + local 25〔World4/Tools10/MemDev8/OS3〕**（script 直讀 `_atom_index.json` 驗證、partition 自洽，且與 SessionStart「Global 47」+1 吻合）。修 TECH.md（3 處：JSON SoT 計數 + 機器源 32→48 + BM25 規模 ~32→~48）、_AIDocs/_INDEX.md（2 處）、DocIndex-System.md（2 處：計數 + core 名單成員校正——`realm-範疇分區機制-v5` 早已搬 local 卻仍列 core、補漏列的 goal-driven/自己flag 兩顆）。Install-forAI.md 補 Stage 1/3 兩個 config 關閉項（`response_capture.session_end_flush.enabled` / `deep_postmortem.enabled`，正確 key）。索引類（_ATOM_INDEX.md/MemDev_INDEX/_local_catalog）由 atom_write 已自動同步；MEMORY.md 正確未列 local atom。README.md 確認無需動（對外、機制透明）。**未解 flag**：skill 計數跨檔不一致（Install-forAI 內 99 行「23」vs 258/277 行「22」、changelog 史載「24」）與本次變更無關，需另次專門稽核 skills/ 實數定義（含外部 karpathy/unity 與內建 CC skill 是否計入）後統一，本次不擅改以免引入新錯數。 | `TECH.md`, `_AIDocs/_INDEX.md`, `_AIDocs/DocIndex-System.md`, `Install-forAI.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-17 | **fix(memory): Deep Post-Mortem Gate 收斂觸發條件（方案 A）— 修 dogfood 誤觸**。前一 commit 的 gate 在「本機制自身開發 session」即誤觸（零失敗卻因 `same_file_3x`/`wisdom_retry` 觸發）——純 effort 三 OR 把「成功的多次迭代開發」與「反覆修不好的失敗」混為一談。**修**：`_should_deep_postmortem` 改為 **(effort) AND (real_failure)**，effort＝retry≥2 ∨ fix_escalation_triggered ∨ 同檔 edit≥3，real_failure＝failing_tests 非空 ∨ evasion_flag ∨ 未宣告完成；簽名加 `claims_done` 參數由 caller 算 `bool(last_text and claims_completion(last_text))` 傳入（保持純函式、不在 helper 讀 transcript）。**驗證**：verify_deep_postmortem_gate.py 9→13 測（加 `test_effort_but_success_not_triggered` 鎖成功 session 不誤觸 + effort×failing/evasion 縱宣告完成仍觸 + real_failure 無 effort 不觸），run_verify.py **563 passed** 零回歸。user 考量「核心功能不靠轉述」選同 session 修正而非 handoff。 | `hooks/handlers/stop.py`(`_should_deep_postmortem` + caller claims_done), `hooks/verify/verify_deep_postmortem_gate.py`(9→13 測), `_AIDocs/Architecture.md`(stop.py 列), `_AIDocs/_atoms/MemDev/對談結束自動記憶與錯誤加權深記.md`(append 方案 A), `_AIDocs/_CHANGELOG.md` |
| 2026-06-17 | **feat(memory): 對談結束自動記憶 + 錯誤加權深記 — Stage 2-4**（Stage 1 已於 8fffa8c 落地：session_end flush → global [臨] atom）。**Stage 2 失敗多區塊骨架**：`extract-worker._failure_writeback` 由「`- [臨]` 一行」改寫成五區塊骨架（始末/根因/設計原理/運作邏輯/防再犯）——`_build_failure_skeleton` 把 LLM 敘事填始末、`_split_root_cause` 從「（根因: …）」拆根因，餘三段留 `待補` 標記給 Claude；`_failure_dedup_hit` 新格式始末行 + 舊單行格式皆去重；`_create_failure_atom` 首寫即骨架。**Stage 3 Deep Post-Mortem Gate**：`handlers/stop.py` 新 `_should_deep_postmortem`（純判定）+ gate——高 effort 失敗訊號（`wisdom_retry_count≥2` ∨ `fix_escalation_triggered` ∨ 同檔 edit≥3）且本 session 未深寫過 → `output_block` 指示 Claude 結束前用 atom_write 補完整 post-mortem（始末＋設計原理＋運作邏輯）；`deep_postmortem_done` 一次性防重複、共用 `stop_gate_max_blocks` 預算、config `deep_postmortem.enabled` 可關。**設計**：hook 骨架永遠自動寫（小模型填能填的），高 effort 時才加碼喚 Claude 深寫。**驗證**：新 `verify_failure_skeleton.py`（11 測）+ `verify_deep_postmortem_gate.py`（9 測，含 handle_stop 端到端首次擋/設旗標放行），全套 **412 passed** 零回歸。**hook .py + config → 下個 session 生效**。 | `hooks/extract-worker.py`(失敗骨架×4 helper), `hooks/handlers/stop.py`(Deep Post-Mortem Gate), `workflow/config.json`(`deep_postmortem`), `hooks/verify/verify_failure_skeleton.py`(新·11 測), `hooks/verify/verify_deep_postmortem_gate.py`(新·9 測), `_AIDocs/Architecture.md`(Stop/extract-worker doc-sync), `_AIDocs/_atoms/MemDev/`(機制 atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **refactor(lib/mcp)+fix(realm): parity 方案 B 落地 + realm 分類雙 guard**（user 裁決方案 B，熱點重構收尾遺留兩案）。**① py↔js parity 方案 B**：`lib/atom_io_cli.py` 加 `build`（`build_atom_content`+validate 回 content 不落檔）/ `append`（新 `atom_io.append_atom_file`：read→`_build_append_content` 共用拼接→validate→write_raw）兩 action；server.js toolAtomWrite **create/replace 的內容構造與 append 拼接 3 處改 spawn py CLI**（先例 spawnAtomCli/spawnAtomAccess），js `buildAtomContent`/`renderKnowledgeLines`/自拼 splice 退役為 test_13 parity fixture——~160 行鏡像邏輯單一來源化，js append `readFileSync`+`\n` 自拼的 CRLF 混寫面根除（`atom_io._atomic_write` L135-138 風險註解對應）。**② realm 分類雙 guard**：(a) **字元集 guard**（韓文「자동화」亂碼 domain 實案根因修）——domain 段含非 CJK/ASCII 字元即整段非法降 `Else`：py `_clean_segment`(`_SEG_ALLOWED_RE`) 蓋 `normalize_domain_path`/`local_write_target`/`set_realm`，js `cleanRealmSegment` + `classifyRealm` 出口鏡像（eval-block 自足內聯）；(b) **詞庫泛用詞防護**（goal-driven-verify-loop 誤降 local 實案防再犯）——`append_learned_terms` sink 端雙護欄：`is_generic_lexicon_term`（token 全落 `_LEXICON_GENERIC_TOKENS` 黑名單即拒，「fix bug/verify loop/寫程式」拒、「gpo/bom/auto-handoff」不誤殺）+ 亂碼 domain 整條拒收；`classify_realm` 對已污染 learned 出口再降 `Else`。**驗證**：新 test_24（append CRLF byte-stability）/ test_25（CLI build/append 跨語言對拍 + server.js delegation source-guard）/ test_26（詞庫雙護欄）+ test_18/22 fixtures 擴 Hangul/Cyrillic；test_17 prelude 補 `LOCAL_REALM_DEFAULT_DOMAIN`（eval block 外常數）。全套 **532 passed**（529+3）零回歸。**③ 收尾時當場抓到二度再犯並根治**：git status 發現 core atom `goal-driven-verify-loopkarpathy-吸收` 本 session 期間**又被 LLM sweep 誤搬 local**（dc43019 矯正後，sweep 重新學進「karpathy/verify loop/可驗證目標/成功標準/imperative→declarative」5 污染 term → name 命中再搬；新黑名單只擋得住其中 "verify loop"）→ ① `atom-set-realm --to-core` 搬回（含 sidecar）+ `sync-memory-index --write` 清 stale `_INDEX` 列；② 詞庫清除 5 污染 term；③ **根治 = slug 列入 `LOCAL_REALM_CORE_PROTECTED_EXACT`（py+js 鏡像，protected 永不喚 LLM 永不搬）** + 黑名單補「可驗證目標/成功標準/驗證目標」；test_16/17 fixtures 鎖保護，實庫 lexicon 驗證 protected=True。**server.js 改動 → 重啟 MCP 生效；lib py 端即時生效**。 | `lib/atom_io.py`(`append_atom_file`+`_build_append_content`), `lib/atom_io_cli.py`(build/append action), `tools/workflow-guardian-mcp/server.js`(3 處 spawn+雙 guard 鏡像+protected), `lib/atom_locations.py`(字元集+詞庫雙護欄+protected), `lib/verify/verify_atom_io_equivalence.py`(+3 測+fixtures), `memory/goal-driven-…md`(搬回 core)+`memory/_meta/realm-lexicon-learned.json`(清污染), `_AIDocs/{SPEC_ATOM_V5,Architecture}.md`+`TECH.md`(doc-sync), `_AIDocs/_atoms/MemDev/realm-遷移-…md`(裁決落地 append), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **fix(hooks): CC harness 原生 memory dir 誤納防護 — atom 索引 marker 撞名辨識**。新版 CC harness 內建 file-based memory（`projects/<slug>/memory/`）自建 MEMORY.md（`- [Title](file.md) — hook` 清單格式），與 atom 索引 marker 撞名。**實測確認**：`discover_all_project_memory_dirs()` 已回傳 `c--users-holylight--claude` 的 harness 預建空 dir——洩漏點不是 Phase-0 fallback（它至少要求 MEMORY.md/_atom_index.json 存在），而是 **registry old-path 分支（無任何 marker 檢查，`is_dir()` 即納入）**；一旦 harness 寫入記憶檔，`discover_v4_sublayers` flat-legacy 路徑會把 harness 自寫 .md 當 shared atom 注入。**修**：新 `_has_atom_index_marker` helper（_atom_index.json / _ATOM_INDEX.md 存在，或 MEMORY.md 含「\| Atom」trigger 表頭 / migrated-v2.21 slug-pointer stub 才算 atom 索引），registry old-path + Phase-0 兩分支皆套用；同日 cbf7c15 `_is_global_mem` 護欄保留未動。**驗證**：discover 實跑 harness dir 消失、9 個合法專案 dir 不變；新 `verify_native_memory_dir_guard.py` 6 測（兩分支排除 / 空 dir 排除 / 三型合法 marker 照常納入 / 全域護欄回歸 / 靜態守門），全套 **529 passed**（523+6）零回歸。**hook .py 改動 → 下個 session 生效**。**後續同步**：踩坑上船 atom `harness原生memory與atom索引marker撞名辨識`（local/MemDev，含「P1 gate 把此路徑一律當殘骸 deny 的假設已過時、待裁決」）+ Architecture.md 強制門禁 (a) 認知更新註記。 | `hooks/wg_core.py`(`_has_atom_index_marker`+兩分支), `hooks/verify/verify_native_memory_dir_guard.py`(新·6 測), `_AIDocs/Architecture.md`(門禁 (a) 註記), `_AIDocs/_atoms/MemDev/harness原生memory…md`(新 atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **fix(hooks/lib): atom 雙注入修復 + 頂層 import 修復 — cross-project 掃描把全域 memory 當「專案」回灌**（C:\Projects session 實測 `[Atom:decisions]` 同 turn 注入兩次，user 回報）。**① 根因**：registry 有 `c--users-holylight: root=C:\Users\holylight`（家目錄曾開過 session）→ `discover_all_project_memory_dirs()` 解析 `root/.claude/memory` == **全域 MEMORY_DIR 本身**，cross-project 掃描（trigger 雙命中 ≥2）把全域 atom 再補進 `all_atoms` 一次；`ups_search` 主 trigger 迴圈無同名去重 → 同 atom 雙注入（每次觸發多耗一份 atom 本體 token，decisions ~300 tok）。**修**：discover 加 `_is_global_mem` 護欄（registry new_mem/old_mem + projects/ fallback 三路皆擋，resolve() 比對）；ups_search trigger 迴圈補 `name in atom_source` 同名去重（縱深防禦，未來多層混同名也只注首見）。**② 順手修（log 刷版 ERROR）**：`lib/atom_locations.py` line 117/240 lazy 相對 import 在頂層載入模式（wg_core `sys.path.insert` 後 `import atom_locations`）炸 `ImportError: no known parent package`，致 `resolve_atom_path` 的 multi 路徑全失敗（usefulness 查找靜默退化到 MEMORY_DIR fallback）→ 補 dual-safe try/except（同檔 line 211/383 既有 pattern）。**驗證**：discover 不再回傳全域目錄；本輪原始 prompt 端到端重跑 `collect_matched_atoms` → matched 零重複；頂層 `iter_atom_files_multi()` 41 atoms 零 ImportError。**hook .py 改動 → 下個 session 生效**。**Guardian 註**：cross-realm guard 攔截後依其指引走 allowlist 通道（user 明確要求修），修畢已復原 `allowlist: []`。 | `hooks/wg_core.py`(discover 護欄), `hooks/handlers/ups_search.py`(trigger 同名去重), `lib/atom_locations.py`(dual-safe import ×2), `workflow/config.json`(allowlist 加→復原·淨零), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **refactor(hooks): 複雜度熱點重構（Fable 總檢視遺留大工程，staging 接續）— UPS 790→195 行拆四段 + wg_atoms 抽共用 + budget 集中 + 修 realm 遷移韓文亂碼 domain**。**① UPS 拆分**（風險最低先做，一段一 verify）：`handlers/user_prompt_submit.py` 790 行大函式拆四子模組——`ups_gates.py`（detect 段：evasion 追蹤/V4.1/long_die/hot cache/atom-write guard）、`ups_context.py`（session context/wisdom/parallel/AIDocs/JIT）、`ups_search.py`（index 組裝/跨專案 alias/trigger→BM25→vector/supersedes/ACT-R）、`ups_inject.py`（hot/cold/budget/related spread/效用晉升提示），orchestrator 收斂為串聯呼叫 + 收尾職責（195 行）；**純搬移零行為變更**，等價性=523 tests + git-stash 新舊對拍同輸入同輸出；`verify_usefulness_loop_phase2.test_ups_hint` 改掃 ups_inject+orchestrator 雙檔。**② wg_atoms 抽共用**（Explore agent 備料後逐項驗證，報告兩處言過其實已實證駁回）：(a) **budget 集中 wg_core**——實況=三個不同概念（compute_token_budget 1500/3000/5000、CONTEXT_BUDGET_DEFAULT 3000、TURN_BUDGET_LIMIT 800）非三重計算，集中宣告+文件化**兩個同名 `_estimate_tokens` 口徑不同勿合併**（wg_core CJK-aware 量 transcript vs wg_atoms len//4 注入預算·verify 鎖定），wg_atoms re-export 舊名零 caller 變更；(b) trigger/keyword 比對抽 `any_trigger_hit`/`count_trigger_hits` 原語（match_triggers/ups_search×2/ups_context AIDocs 共用）；(c) ups_search BM25/vector 兩段重複合併迴圈抽 `_merge_hit`；(d) **BM25↔vector「top-k 排序 70% 重複」不成立**——一個 in-memory 一個 server-side HTTP，可共用僅 ~8 行，強行抽象=反向工程，**判定不做**（證據留檔）。**③ 順手修（發現即處理）**：本 session realm 自動遷移把 `專案等級-mcpskillhookslog-不放全域根層` 搬進**韓文亂碼 domain**「자동화流程與協議」（本地 LLM 分類器吐韓文「자동화」=「自動化」；`normalize_domain_path` snap 防線只擋大小寫/空白變體，字元級穿透）→ 檔案搬回「自動化流程與協議/SGI-ProjectStructure」+ 5 處同步（_atom_index.json/_ATOM_INDEX.md/詞庫 learned.json/Tools/_INDEX.md×2）；**根因 guard（domain 段非中英文字元→低信心降級）涉 py+js 鏡像，超順手修門檻，待裁決**。**④ py↔js parity 評估**（staging 步驟 4，僅評估不動手）：Explore agent 全景報告→產三方案分析表給 user 裁決（見 session 報告；推薦方案 B=js buildAtomContent/append 改 spawn py CLI，~160 行鏡像邏輯消重 + append CRLF 混寫風險根治）。**驗證**：每段拆完跑 `run_verify.py`，全程 **523 passed** 零回歸 ×6 輪；dispatcher 端到端 smoke（SessionStart→UPS 真跑零錯誤）。**hook .py 改動 → 下個 session 生效**。 | `hooks/handlers/user_prompt_submit.py`(790→195), `hooks/handlers/ups_{gates,context,search,inject}.py`(新×4), `hooks/wg_core.py`(budget 單一來源), `hooks/wg_atoms.py`(原語+re-export), `hooks/verify/verify_usefulness_loop_phase2.py`(test_ups_hint 雙檔掃), `_AIDocs/_atoms/Tools/自動化流程與協議/`(亂碼修復), `memory/{_atom_index.json,_ATOM_INDEX.md,_meta/realm-lexicon-learned.json}`(同步), `_AIDocs/{Architecture,DocIndex-System}.md`+`TECH.md`(doc-sync), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **feat(guard): v1.1 擴充 — 根層敏感檔 + 全域 MCP 變更閘**（user 核准兩項）。**①根層敏感檔**：守門清單加 `~/.claude` 根層 settings.json/CLAUDE.md/IDENTITY*.md/USER*.md（settings.json 可注入任意 hook 指令、比 skills 更敏感；根層其餘檔如 README/TECH 不擋）。**②Bash 全域 MCP 閘**：新 `check_cross_realm_mcp_cmd`——外部專案 session 的 `claude mcp add -s user`（寫全域 ~/.claude.json）或 `claude mcp remove` 未限定 project\|local scope → deny 指路 `-s project`；預設 local/顯式 project 放行（效果限本專案）。共用 `_is_core_session` helper + 同一 config 開關。**驗證**：guard 測試 14→27（+13），全套 **523 passed**（510+13）零回歸。 | `hooks/wg_core.py`(v1.1 兩閘+helper), `hooks/handlers/pre_tool_use.py`(Phase 3b 串接), `hooks/verify/verify_cross_realm_guard.py`(+13 測), `_AIDocs/{Architecture,DocIndex-System}.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **fix(settings)🔥: hooks 全滅 3 天根治 — 18 條 hook 指令空格丟失（黑窗根治 v2 轉換事故）** — user 問「VS Code 變更清單這些是什麼」時從 settings.json diff 揪出：6/9 commit `ce6ca7c` 把 hook 指令轉 GUI pythonw 全路徑時**丟失執行檔與參數間空格**（16×`pythonw.exe-c` + 2×`pythonw.exe"$HOME`），cmd/bash 皆找不到執行檔 → **18 hook 全部靜默死亡**（hook 崩潰=被忽略、零報錯）。**實證**：workflow/state-*.json / atom-debug log / probe log 全停在 6/9 19:25——整條記憶管線（注入/萃取/episodic/Stop gate）死 3 天，前報「episodic 停 6/9 再觀察」實為此症狀。**當時驗證缺口**＝只驗「JSON 合法 18/18」沒驗指令可執行。**修**：18 條補空格 + 2 條 `$HOME` 改顯式路徑（消 shell 展開不確定）。**驗證（新鐵律=端到端）**：擷取實際 command 字串餵 SessionStart JSON stdin 真跑 → `hookSpecificOutput` 完整回應（Guardian Active·23 atoms·realm 過濾正確）+ JSON 合法 + 18 條 grep 零黏連。**生效=重啟 session**（settings 啟動時讀）。教訓 append 入 atom [[windows-cc-hook-閃-console-pythonw-修-layer-1勿只補巢狀-creationflags]]；事後偵測信號=state/log mtime 斷流。順帶：補上船 dotnet-run atom（前 session 漏 commit，索引已引用造成 remote dangling）；unity-mcp-skill 由 user 決定搬遷專案層、不上船。 | `settings.json`(18 hook 指令), `memory/dotnet-run-…demo….md`(補上船), `_AIDocs/_atoms/OS/Windows/windows-cc-hook-…md`(+教訓), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **feat(guard)+fix(memory): Fable 總檢視修補批次 — Cross-Realm Write Guard（方案甲）+ 靜默吞錯 43 站接 log + Failures append 路由缺口 + 詞庫鎖 + 索引校驗 + confirmations 退役定調**（user 全單核准「高/中高/中全修」）。**① Guard 甲**：新 `wg_core.check_cross_realm_write`——外部專案 session（cwd∉~/.claude）寫核心層 `~/.claude/{skills,tools,hooks,lib,rules}/` → PreToolUse deny + 指路專案層（SGI 跨層污染教訓；判別子=cwd，deterministic 零誤判核心開發；config `guard.cross_realm_write` enabled/allowlist；NotebookEdit 一併涵蓋；cwd 缺失 fail-open）。新 `verify_cross_realm_guard.py` 14 測。**② [高] 靜默吞錯**：~38 處 `except: pass` 體檢點全清——helper 既有 `_atom_debug_error`（ERROR tag always-on 不受 debug 開關 gate）接線 **43 站**（我方 13：wg_atoms 6/wg_core 4/session_start 2/session_end 1；agent 30：wg_extraction 4/post_tool_use 3/codex_companion 14/ensure-mcp 5/extract-worker 4，獨立腳本用 stderr `_log_err` fallback）；控制流零變更、fail-open 語意保留；純清理/已有 log/embedded 字串內者按規則跳過。**③ funnel 缺口（修 ② 時挖出）**：非 feedback- 前綴但物理在 Failures 的 atom（cognitive-patterns / memory-pipeline-*）**append/replace 在 py/js 雙端都找不到檔**——`is_failures_routed_title` 補「已註冊 Failures stem」判定（lib/atom_locations.py + server.js `isRegisteredFailuresStem` 鏡像），新 `verify_failures_routing.py` 5 測。**④ [中高] 詞庫 lost-update**：`append_learned_terms` 讀-改-寫加 msvcrt advisory lock（仿 write_state；LK_LOCK 阻塞等待——學詞丟失不可恢復，異於 state 的 NBLCK）。**⑤ [中] 索引校驗**：SessionStart 載入後廉價雙向核對（index→disk 存在性 + memory/ 頂層→index 漏登），失配→always-on log + 可見 advisory 指路 sync-atom-index（不自動重建避免與 funnel 互搶；2026-05 靜默失效前科同型防範）。**⑥ confirmations 定調**：徹查判定**已退役**——晉升=OR（confirmations≥4 OR Wilson 下界≥0.6）、Phase 2 usefulness 活躍接管、py/js 鏡像一致；恆 0 屬正常非失效。寫 atom `confirmations-已退役-phase2-usefulness-接管晉升`（local/MemDev）+ memory-pipeline-silent-failure-2026-05 簽章更新行（勿再誤報）。**⑦ 雜項**：github MCP 移除（user 指示，剩 4 server 全 Connected）；Failures/_INDEX.md feedback 6→7 補 2 列（前一 commit）。**驗證**：`run_verify.py` **510 passed**（基線 491 +14 guard +5 routing）零回歸；js `node --check` 綠；config 合法 JSON。**settings 未動；hook .py + config → 下個 session 生效；server.js 改動 → 重啟 MCP 生效**。**遺留**：複雜度熱點（user_prompt_submit 790 行 / wg_atoms 1898 行 / py↔js parity 脆弱）→ `_staging/next-phase-hotspot-refactor.md` 分 session 處理；sgi-*.cs + sgi-play-log 等活 session 結束後搬遷。 | `hooks/wg_core.py`(guard 函式+DEFAULTS+4 log), `hooks/handlers/pre_tool_use.py`(Phase 3b), `hooks/verify/verify_cross_realm_guard.py`(新·14 測), `lib/atom_locations.py`(路由補判+詞庫鎖+import sys), `tools/workflow-guardian-mcp/server.js`(isRegisteredFailuresStem), `lib/verify/verify_failures_routing.py`(新·5 測), `hooks/wg_atoms.py`(6 log), `hooks/handlers/session_start.py`(索引校驗+2 log), `hooks/handlers/session_end.py`(1 log), `hooks/wg_extraction.py`(4), `hooks/handlers/post_tool_use.py`(3), `hooks/codex_companion.py`(14), `hooks/ensure-mcp.py`(5), `hooks/extract-worker.py`(4), `workflow/config.json`(guard 區), `memory/`(confirmations atom+簽章更新+索引), `_AIDocs/{Architecture,DocIndex-System}.md`+`TECH.md`(doc-sync), `_AIDocs/_CHANGELOG.md` |
| 2026-06-12 | **feat(skills): 吸收外部 karpathy-guidelines（混合載體）+ 全 doc-sync 數字校正** — user 要求裝 `multica-ai/andrej-karpathy-skills` 並評估與 IDENTITY 異同。**裝法（user 拍板「混合」）**：① `skills/karpathy-guidelines/SKILL.md` 全文（外部 skill·MIT·on-demand 被動觸發，非 always-on）；② 四原則前三條（Think Before/Simplicity/Surgical）IDENTITY+USER 已涵蓋，**唯一加值** = Goal-Driven 的「成功標準 + 逐步 `→verify` loop」→ 萃成 core atom `goal-driven-verify-loop`，內含**衝突調和**：karpathy「Surgical Changes 別動相鄰碼」與本系統 [[feedback-workflow-discipline]]「順手修補/drift 修補」反向 → 裁決**以後者門檻為準**，故 karpathy 僅以 skill (on-demand) 存在、不全域 always-on 避免覆蓋既有契約。**doc-sync 稽核**（user 追加「README/TECH/Arch/_ATOM_INDEX/_INDEX/Install-forAI 這一切」）：發現文件**彼此早已矛盾且全落後磁碟**——skill 數 Arch 20 / DocIndex 22 / TECH 22 / _INDEX 20；atom 數 TECH 32〔core13/local10〕 vs _INDEX 31〔core14/local8〕。**以 `_atom_index.json` SoT 為唯一真相校正**：skill 全部 → **24**（22 記憶系統 + 外部 unity-mcp-skill/karpathy-guidelines）；atom → **36**（core 14 + feedback 7 + 失敗 2 + local 13〔World3/Tools5/MemDev3/OS2〕）；順手收 `MCP tool 3→4`（Arch:245 早已 4）與 Arch/DocIndex 漏列的 heal-review/refile/unity-mcp-skill。MEMORY.md/_ATOM_INDEX.md/_atom_index.json 由 atom_write 自動更新。**GIT 教訓**：首推後為修 commit 訊息小錯字而 `--amend`，撞 gitlab `main` protected-branch 禁 force-push → 兩鏡像分岔 → 退回 `9d7c1b3` 收斂；結論「推完別為 cosmetic 改寫已發布歷史」。**待辦**：`unity-mcp-skill` 仍 git untracked，文件已列入 24 但 repo 未含 → 待 user 決定是否另 commit。 | `skills/karpathy-guidelines/SKILL.md`(新·外部), `memory/goal-driven-verify-loop…吸收.md`(新 core atom), `_AIDocs/DocIndex-System.md`(§5 skill 22→24+2 row), `_AIDocs/Architecture.md`(skill 20→24+4 row), `TECH.md`(skill+atom 計數×4), `_AIDocs/_INDEX.md`(計數×2), `Install-forAI.md`(skill 22→24), `memory/{MEMORY,_ATOM_INDEX}.md`+`_atom_index.json`(atom_write 自動), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **fix(hooks/mcp): Windows 黑窗根治 v2 — 真兇＝`WindowsTerminal -Embedding` host 窗（非 hook）；推翻「pythonw=GUI」舊認知** — user 截圖持續黑窗 `AppData\Local\…python.exe`、「修過很多次」。**系統層 trace 釘死**（新 `tools/console-window-trace.ps1` 抓視窗隱→顯翻轉+父鏈）：黑窗＝`WindowsTerminal.exe -Embedding`（Win11 預設終端機），`claude.exe`(GUI) spawn 任何 console-subsystem 子行程無 `CREATE_NO_WINDOW` 即彈，標題=被托管 exe 路徑 → **跟 hook 無關**（故改 N 次 hook 沒用）。**兩大翻案**：① 持續窗＝`git` MCP（`uvx mcp-server-git`→uv-cache python.exe）→**移除**（user 授權·Bash git 冗餘）；② **bare `pythonw` 竟是 console(subsystem=3)**——hermes uv-venv pythonw 是 trampoline re-exec 成 base `python.exe`，**早年 commit `1d73e55`/「python→pythonw」兩次『真修』其實都沒中**。**真修**：(a) 4 個 `cmd /c node` MCP server → `<GUI-pythonw> run-hidden.py node <js>`（新 `hooks/run-hidden.py`：pythonw `sys.std*`=None、裸繼承斷 stdio → 改 ctypes `GetStdHandle`+`STARTUPINFO`/`STARTF_USESTDHANDLES` 直傳+`CREATE_NO_WINDOW`）；(b) `settings.json` 18 hook + `wg_extraction._spawn_extract_worker` 的 bare pythonw/sys.executable → 穩定 GUI pythonw 完整路徑 `AppData\Local\Python\bin\pythonw.exe`（**subsystem=2 永不配窗·路徑無版本號→uv 升級不破**）。**驗證**：PE subsystem=2、GUI pythonw 重導向 stdin/stdout 通、import 全 hook 模組 OK（純 stdlib）、settings.json 合法 JSON 18/18 轉換、py_compile 綠、`claude mcp list` 4 server √Connected + trace 零 MCP 彈窗。**settings/MCP 改動 → 下個 session 全生效**。**鐵律**：選 pythonw 前驗 PE subsystem(PE+0x5C)，勿信 bare pythonw。 | `hooks/run-hidden.py`(新·STARTUPINFO 隱窗 stdio launcher), `tools/console-window-trace.ps1`(新·forensic 診斷), `settings.json`(18 hook→GUI pythonw 完整路徑), `hooks/wg_extraction.py`(`_gui_python` + worker spawn), `.claude.json`(移除 git MCP·4 node server 重包 GUI pythonw+run-hidden), `_AIDocs/_atoms/OS/Windows/windows-cc-hook-…md`(推翻舊認知+擴充), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **docs: doc-sync 稽核補漏（user 觸發「檢察相關文件同步」）** — 逐項過 [[feedback-memory-system-doc-sync]] 清單，揪出 2 處非 Phase 4 造成的歷史 drift 並當場修（≤5 行門檻）：①**`IDENTITY.md`(c) + `IDENTITY-holylight.md`(c) Phase 2 漏改**：Phase 2 _CHANGELOG 宣稱「IDENTITY (c) + stop.py (c) 同步降級」，實際**只 stop.py:289 落地**、IDENTITY (c) 仍是舊「純 AI 自估 token」文字 → 補成「見 hook `[Auto-Handoff]` 程式化預警則判斷是否已處理失真（壓縮真發生自動備 stub 保底）」與 stop.py ScanReport gate (c) 同步、保留語意判斷層（plan line 136 本就要求）。〔IDENTITY*.md gitignored·本地生效·`IDENTITY-holylight.md` 同 drift 待確認是否同步〕②**`Install-forAI.md`:282 MCP tool 計數 stale**：realm-V6 加 `atom_edit_meta` 後該檢核列仍寫「3 tool」→ 改「4 tool（atom_write/move/promote/edit_meta）」，與 `DocIndex-System.md:138` / `Architecture.md:148` 對齊。**核對無需改**：root `README.md`（不列 tools/hooks）、`DocIndex-System.md`§6（只列 MCP/Vector 主子系統·非窮舉 tool·auto-continue PoC 不入）、`SPEC_ATOM_V5.md`（未動 atom schema）。 | `IDENTITY.md`(c·gitignored), `Install-forAI.md`(MCP tool 3→4), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **feat(tools): Auto-Handoff Phase 4 — `claude -p /continue` headless watcher PoC（外部編排·獨立於 hook·實驗性·非正式上線）**（plan `wise-wobbling-gem.md` line 50-58/81-82）。Auto-Handoff 收官最後一哩：hook 無 spawn API、無法主動起新 session → 獨立常駐腳本補足。**Step 1 實證查證**（依 atom `cc-能力查證反編譯實跑-binary`，不憑記憶）：環境 VSCode 擴充套件 `claude.exe` **2.1.169**（native install 停 2.1.37 過舊·版本分裂）。① binary 字串表 `rg -a`：`local-command-stdout`×51 / `slashCommand`×18 / `disable-slash-commands`×8（=slash 即 skills·預設開）。② `--help`：`-p/--print` 存在、`--bare` 註「Skills still resolve via /skill-name」。③ **實跑**（隔離空目錄）`claude -p "/continue" --output-format json --dangerously-skip-permissions` → `is_error:false`/`terminal_reason:completed`/exit 0/`num_turns:3`，`result` 為 /continue skill 在 0-stub 時原文且回報掃了 skill 文件兩條路徑 → **skill 邏輯實際執行**（非 prompt 透傳）；`total_cost_usd≈0.276`；觀察 `no stdin data received in 3s` → spawn 接 `stdin=DEVNULL`。**結論：可行**。**Step 2 watcher**（`tools/auto-continue/auto_continue.py`）：監看 `resolve_staging_dir(cwd)`（複用 wg_core 同源）的 `next-phase*.md` → 穩定即起 headless `/continue` → 同步阻塞至子 session 結束（序列化＝天然「當前 session 結束才接棒」）→ 下輪偵測新 stub 再 spawn → 遞迴；無新 stub 逾 `idle_timeout` 正常退出。**四道 guard**：`max_consecutive_spawns`(5) / `budget_usd`(累計子 session `total_cost_usd`·$5) / `confirm_every_n`(每 N 次人工確認·TTY input ∨ `confirm.ok` flag) / `kill_switch`(watch dir 下 `STOP` flag·每輪+spawn 前檢查)；附 **single-stub 不變式**（多 stub→headless `/continue` 選單卡死→停手交人工）。spawn/sleep/now/confirm 全可注入。**驗證**：新 `verify_auto_continue.py` **20 測**（四 guard×含絕對路徑 kill switch + 不變式 + dry-run + idle + 出錯停鏈 + 穩定門檻 + JSON 解析跳 warning 行 + 組態優先序 + binary 偵測 + 版本解析）；`run_verify.py` 注冊 `tools/auto-continue/verify`；**491 passed**（基線 471 +20）零回歸；端到端 `--dry-run` 三情境真跑（空→idle 退出 / 1 stub→偵測不 spawn / 2 stub→不變式擋下）綠、binary 自動偵測命中 2.1.169。**文件同步**：`Architecture.md`（Phase 4 未做→PoC 完成）、`TECH.md`（tools 樹 +auto-continue/、verify 26→28、446→491 回補 Phase 1-3 drift）。**純 tools/ 新檔 + 文件 → 免重啟**。**衍生暫存**：probe 暫存 `C:\Temp\cc-skill-probe`/`cc-watch-test` + session residue 已刪。 | `tools/auto-continue/auto_continue.py`(新·watcher), `tools/auto-continue/config.example.json`(新), `tools/auto-continue/README.md`(新), `tools/auto-continue/verify/verify_auto_continue.py`(新·20 測), `run_verify.py`(注冊 verify 路徑), `_AIDocs/Architecture.md`(Phase 4 PoC), `TECH.md`(tools 樹+verify 計數), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **feat(hooks): Auto-Handoff Phase 3 — Layer 4 SessionEnd 兜底 + 全文件同步（補 Phase 2 漏記）+ A執P / feedback atom（機制收官）**（plan `wise-wobbling-gem.md`）。承 Phase 1（壓縮自動備 stub）+ Phase 2（token 預警）。**Layer 4**（`session_end.py`，既有 staging reminder 旁、`config.auto_handoff.sessionend_fallback` 守門、fail-open）：session **直接結束（非壓縮）**、有未完成工作且無既有手寫 `next-phase*.md` → `build_handoff_stub` 寫客觀 stub 到 `resolve_staging_dir`，**不設** `pending_handoff_emit`（session 已結束、無 PostToolBatch 可消費），補「PreCompact 沒觸發但 session 直接結束」缺口。**閘判定**＝`should_write_stub`（`modified_files`）；查證 `sync_pending` 與 `modified_files` 同源（`post_tool_use.py:168` 一併設、session 內不清，且 commit 後 stop.py sync gate 走實際 git status 不碰此旗標）→ 二者耦合，故未冗加 `sync_pending` 檢查（plan line 79「modified_files + sync_pending」已被涵蓋）。**全文件同步**：`Architecture.md`（Hooks 表補 Layer 1/4 + `handlers`/`wg_handoff` row + **新增「Auto-Handoff 四層自動交接」機制段**）、`TECH.md`（架構樹 `wg_handoff.py`）、本 `_CHANGELOG` 回補 Phase 2 漏記列。**新 atom**：`A執P / 自執驗上P` 定義（plan line 9）+ `feedback-workflow-discipline` 補 auto-handoff↔手動 `/handoff` 關係。**驗證**：`run_verify.py` **471 passed**（+4 Layer 4 verify：寫 stub/disabled 不寫/尊重手寫/無工作不寫；neutralize 重型協作者整合測）零回歸。**純 hook .py + doc → 下個 session 生效、免重啟**。**Phase 4**（`claude -p "/continue"` headless watcher 外部編排）獨立、未做。**衍生暫存**：無。 | `hooks/handlers/session_end.py`(Layer 4 + import wg_handoff), `hooks/verify/verify_auto_handoff.py`(+4 Layer 4), `_AIDocs/Architecture.md`(Layer 1/4 + 機制段), `TECH.md`(架構樹), `memory/`(A執P atom + feedback-workflow-discipline), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **feat(hooks): Auto-Handoff Phase 2 — Stop token 預警（Layer 1 piggyback）+ IDENTITY (c) 程式化串接**（plan `wise-wobbling-gem.md`，commit `6f738fe`，本列為 Phase 3 回補漏記）。承 Phase 1。**`wg_handoff.py` 加**：`estimate_context_usage`（proxy context 佔用＝transcript `_estimate_tokens` + `base_overhead` 補償常駐開銷〔system/tools/CLAUDE.md/atom ~15k〕／`window`；**僅觸發信號、傾向低估**，無法量測回 0.0 自然落門檻下）+ `token_warn_payload`（**純函式無副作用**決策預警句：`enabled`/`token_warn`/一次性/門檻全在此判，**不自設旗標**）。**Layer 1**（`stop.py`）：在既有 4 個 gate `output_block` 前算 proxy ratio，≥`token_warn_ratio`(預設 0.85) ∧ 未提醒過 → piggyback 附 token 預警句到該 reason 末尾、設 `token_warn_emitted`（旗標由 stop.py **實際 append 時**才標，非 payload 自設，防 warning 未顯示就誤記）。**IDENTITY (c) 降級**：`IDENTITY.md` (c) + `stop.py` ScanReport gate (c) 文字從「純 AI 自估 token」改為「見 hook `[Auto-Handoff]` 程式化預警則判斷是否已處理失真」（語意判斷層保留、非移除）。state flag `token_warn_emitted` 遵 runtime-flag 慣例。**驗證**：`run_verify.py` **467 passed**（+8 verify）零回歸；push gitlab+github main。 | `hooks/wg_handoff.py`(estimate_context_usage + token_warn_payload), `hooks/handlers/stop.py`(Layer 1 piggyback + (c) 文字), `IDENTITY.md`(c 降級·gitignored), `hooks/verify/verify_auto_handoff.py`(+8), `workflow/config.json`(token_warn 旗標) |
| 2026-06-09 | **fix(hooks): 收尾 2 條 `bash` hook 閃 console — `run-bash-hidden.py` 中介啟動器（pythonw → bash NO_WINDOW，I/O 全中介）** — 承上「python→pythonw」條的**殘留**（user 指定必修）。**問題**：`bash.exe` 是 console-subsystem 且無 `bashw` 變體；console-less 的 `claude.exe` 直接 spawn → 配可見 console。**踩坑（實測 4 組合）**：bash 帶 `CREATE_NO_WINDOW` + **繼承**原生 pipe stdin → MSYS2 `cat` 讀不到（exit 1、無 I/O）；`bin\bash.exe` 啟動器連 echo 都失敗。**可行解**＝stdio 走 temp file（C）或 **python 全中介 input=/PIPE（D）**，且須用 `usr\bin\bash.exe`。**修**：新 `hooks/run-bash-hidden.py`——`pythonw`（GUI-subsystem 無窗）跑啟動器→自己讀 stdin（python 處理原生 pipe 無虞）→以 managed pipe 餵 `usr\bin\bash.exe`（`CREATE_NO_WINDOW`）→擷取 stdout/stderr 回吐→透傳 exit code；basename 防穿越、bash 缺失/infra 錯 fail-open(exit0)。`settings.json` 2 條改 `pythonw "$HOME/…/run-bash-hidden.py" <script.sh>`（user-init timeout 3→5 吸收雙啟動冷啟）。**驗證**：py_compile 綠；端到端真跑 user-init.sh(exit0·純副作用 cp 無 stdout 屬正常)/webfetch-guard.sh(exit0·讀 stdin·curl 探測)；**關鍵 console 探針**＝bash 子孫 `python -c GetConsoleWindow()` 回 **0**（無 console＝無閃）、`curl_ran=0`——證實 MSYS bash 無窗時其 python/curl 子行程亦不配 console。**settings 改動 → 下個 session 生效**。至此 SessionStart/WebFetch 兩 bash hook 閃窗全清。 | `hooks/run-bash-hidden.py`(新,中介啟動器), `settings.json`(2 bash hook → pythonw 啟動器), `_AIDocs/_atoms/OS/Windows/windows-cc-hook-…md`(+知識), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **feat(hooks): Auto-Handoff 自動無損交接 Phase 1 — 壓縮前自動備六區塊 stub + 壓縮後注入補全提示**（plan `wise-wobbling-gem.md`，PlanMode 拍板）。**問題**：大型工項跨 session 靠手動 `/handoff` 才有六區塊交接；context auto-compact / token 將盡而沒先 handoff → 下個 session 裸奔失真。既有只到「壓縮後 atom 內文還原」（pending_reinjection 鏈），無任何自動 handoff 保底。**做法（分層、全包 `config.auto_handoff.*` 可一鍵關回現狀）**：新模組 `wg_handoff.py`——`build_handoff_stub` 生六區塊（客觀區塊自動填 git branch/commit/modified_files/injected_atoms ＋ 主觀區塊 why/做法/決策依據留 `TODO(模型補全)` 佔位；第一行＝/continue 選單摘要）、`should_write_stub`（有手寫 next-phase*.md 不覆蓋·無 modified_files 不寫；自身 auto stub 可更新）、git subprocess 帶 `_NO_WINDOW` fail-open。**Layer 2**（pre_compact.py）壓縮真發生＝最可靠信號（**不依賴 token 量測**）→ `resolve_staging_dir(cwd)/next-phase-auto.md` 寫 stub ＋ 設 `pending_handoff_emit`；**Layer 3**（post_tool_batch.py）壓縮後首批工具呼叫→與既有 `pending_reinjection` blob **合流**同一 additionalContext（不互搶）注入補全提示→清旗標一次性。state flag 遵既有 runtime-flag 動態慣例（不入 new_state，`.get` default；偏離 plan「顯式加 new_state」字面、與 pending_reinjection* 等既有 flag 一致）。**驗證**：`run_verify.py` **459 passed**（+13 `verify_auto_handoff.py`）零回歸；端到端冒煙真實 git 回 branch=main。**純 hook .py + config → 下個 session 生效、免重啟 MCP**。**Phase 2-4 待續**：Stop token 預警(85%)＋IDENTITY (c) 改寫／SessionEnd 兜底＋全文件同步＋A執P atom／`claude -p` 外部編排 PoC。 | `hooks/wg_handoff.py`(新), `hooks/handlers/pre_compact.py`(Layer 2), `hooks/handlers/post_tool_batch.py`(Layer 3 合流), `workflow/config.json`(auto_handoff 區), `hooks/verify/verify_auto_handoff.py`(新,13 測試), `plans/wise-wobbling-gem.md`(plan 設計), `_AIDocs/_CHANGELOG.md` |
| 2026-06-09 | **fix(hooks): 真·根治 Windows hook 閃 console — `python` → `pythonw`（前兩次修錯層）** — 承前條（commit `1d73e55`）。**前次誤診**：把 `creationflags=CREATE_NO_WINDOW` 補在 hook **內部**的 `subprocess`（git/svn status＝**layer 2** 巢狀子行程），但閃窗視窗標題＝`C:\Users\…\AppData\Local\hermes\…\python.exe`＝**hook 解譯器自身（layer 1）**。該行程由 `claude.exe` spawn，console 在我方任何程式碼執行**前**就被 Windows 配給；script 內的 flag 無法抑制自己父行程的視窗 → 兩次修都沒中。**確證**：grep 全 hook 巢狀 spawn 皆已帶 flag，仍閃 ⇒ 只可能是 layer 1。**真修**：`settings.json` 16 條 hook 指令 `python -c "…"` → `pythonw -c "…"`（GUI-subsystem 解譯器，Windows 永不配 console；stdio pipe 仍通＝實測 stdin→stdout JSON round-trip exit0）。`pythonw.exe` 與作用中 venv `python.exe` 同層、PATH 先命中。**layer-2 的 `creationflags` 保留**（互補非取代、Unix no-op）。**settings 改動 → 下個 session 生效**。**殘留**：2 條 `bash …user-init.sh`(SessionStart)/`webfetch-guard.sh`(WebFetch) 仍 console-subsystem，只在 session 起始/WebFetch 時閃（非每輪），無 `bashw` 變體、視需要再 pythonw 包裹。**覆轍教訓**：Windows hook 閃窗＝先認 layer（視窗標題＝執行檔路徑），interpreter 級用 `pythonw`，勿只補巢狀 `creationflags`。 | `settings.json`(16 hook `python`→`pythonw`), `_AIDocs/_CHANGELOG.md` |
| 2026-06-08 | **fix(hooks): 根治 Windows「每次對話結束閃 console 視窗」— 4 處 subprocess 補 `CREATE_NO_WINDOW`** ⚠️**（事後證實只治 layer 2、未根治；見上條）** — 症狀：每次對話結束彈一個一閃即逝的主控台視窗。**根因**：無主控台的 hook 父行程外呼 console 子系統程式（`git`/`svn`/`python`）時未帶 `CREATE_NO_WINDOW`，Windows 替子行程另配一個可見 console（`capture_output` 只重導 stdio、**不**抑制視窗）。**真兇**＝Stop hook 的 `git/svn status`（每對話結束逐變更檔外呼）；同類缺陷另 3 處分別在 SessionStart（project hook）/ PostToolUse（docdrift）/ SessionEnd（sync-memory-index）各自閃窗。**修**：4 處 `subprocess.run`/`Popen` 補 `creationflags=(subprocess.CREATE_NO_WINDOW if win32 else 0)`（沿用 `ensure-mcp._NPM_FLAGS` 既有慣例，非 win32 為 0＝no-op）。**純 hook .py 改動 → 下個 session 自動生效、免重啟 MCP**。`py_compile` 四檔全綠。 | `hooks/handlers/stop.py`(git+svn status), `hooks/handlers/_shared.py`(_call_project_hook), `hooks/wg_docdrift.py`(_git_dirty_files), `hooks/wg_atoms.py`(_trigger_sync_memory_index), `_AIDocs/_CHANGELOG.md` |
| 2026-06-04 | **feat(realm): Phase G — `/refile` 手動歸檔 skill + 剩餘 doc-sync（realm V6 收官）** — 承下條 V6「未竟」。**`/refile` skill**（`skills/refile/`，用 skill-creator 建、pipeline、audit 0 fail）：拖入非 `_AIDocs/_atoms/` 的 `.md` → deterministic 三段護欄全在 `scripts/refile_classify.py`：① 已歸檔拒搬 ② **核心/設定檔辨識**＝bootstrap/設定集精確·樣式檔名 ∨ `rules/*.md` ∨ `_meta/*.json` ∨ protected slug ∨ memory/ 內被 bootstrap 鏈引用 → 不搬+回報角色/關聯+提供中斷或升級 EnterPlanMode ③ 分類提議＝復用 `classify_realm`→miss→`llm_classify_realm` 同引擎（Fail-safe 四態 defer/core/local/else）。SKILL.md 只編排互動：確認 → 移檔（既有 atom 走 `atom-set-realm`、loose `.md` 走 `atom_write`、非 atom 型建議 `_staging/`）→ `docrefs` 子命令掃移檔後 doc-ref。引擎手測 7 case 全綠（核心檔/已歸檔/protected/not_found/詞庫命中/LLM 分類/docrefs）。**剩餘 doc-sync**：`SPEC_ATOM_V5 §2.2` 補 V6 塊 + 規則來源 + 守門 test_18–22；`Architecture.md` Realm 子節補 V6 bullet + 守門 test_14–22；`DocIndex-System.md` skill 20→22(+/refile)、atom 計數校正＝32（core 13 / local 10）、`_atoms/`·catalog·sync-memory-index 階層化描述；`TECH.md`（skill 樹 20→22 +heal-review/refile、atom 17→32、`_atoms` 多段階層、verify 14→26、遷移段/能力矩陣計數）；`Install-forAI.md`（skill 20→22、MCP 3→4 tool +edit_meta）〔TECH/Install-forAI 經 user 點出補，教訓寫進 feedback-memory-system-doc-sync〕。**守門列補完**：SPEC/Architecture/atom 補列 V6 守門全集＝`lib/verify/verify_atom_io_equivalence.py` test_18–22 + `tools/verify/verify_realm_llm_classify.py`（LLM 分類器 9 test）+ `hooks/verify/verify_realm_sweep.py`（SessionEnd sweep Fail-safe 四態 10 test）+ `verify_realm_injection_gate` + `verify_local_catalog_split`。**並行事件（機制如設計運作）**：本 session V6 sweep 自動把 `memory-index-caption-regen` 由 core 經 LLM fallback 判 local 搬到 `_AIDocs/_atoms/MemDev/MemoryIndex/`（handoff §5 預告、user 放行；marker `realm_automove_pending.json` via=LLM；`--to-core` 可逆），catalog/index 已自我 regen、doc 計數已反映。**驗證**：`run_verify.py` **446 passed** 零回歸、`sync-memory-index --check` exit0、skill audit 0 fail。**未改 server.js → 免重啟**。realm 原子 append 2 知識行。**衍生暫存**：無。 | `skills/refile/SKILL.md`(新), `skills/refile/scripts/refile_classify.py`(新), `skills/refile/evals/triggers.json`(新), `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md`, `memory/realm-範疇分區機制-v5.md`(+知識), `TECH.md`, `Install-forAI.md`, `_AIDocs/Failures/feedback-memory-system-doc-sync.md`(+覆轍知識), `_AIDocs/_CHANGELOG.md`；〔並行 sweep〕`_AIDocs/_atoms/MemDev/MemoryIndex/memory-index-caption-regen.md`(+access,搬入), `memory/memory-index-caption-regen.md`(刪,搬出), `_AIDocs/_atoms/MemDev/_INDEX.md`(新), `memory/MEMORY.md`+`memory/_local_catalog.md`+`memory/_atom_index.json`+`memory/_ATOM_INDEX.md`(regen), `memory/_meta/realm-lexicon-learned.json`, `workflow/realm_automove_pending.json`(marker) |
| 2026-06-04 | **feat(realm): V6 LLM-assisted realm 分類 + 詞庫自學 + 關聯式分級階層 domain**（plan `wobbly-conjuring-aho.md`，PlanMode 拍板 OPEN 1-4 + user 4 點回饋）。**根因**：`classify_realm` 詞庫=封閉 allow-list、「無命中→安全預設 core」→ wsl2（WSL2/vhdx/GPO 排錯）漏進 core 全專案 always-load。**修**：①**LLM fallback**（`tools/realm_llm_classify.py` 複用 atom-heal Ollama 樣板）掛 SessionEnd sweep 對「unknown core」喚 LLM 判 realm+階層 domain；**四態 Fail-safe**（`error`基礎設施失敗→defer 留原地〔防 Ollama 離線把全部掃進 Else〕／`core`留／`local`≥min_conf→搬／`unsure`·低信心→`Else`〔取代 Misc〕）；protected 永不喚 LLM；`max_per_session` 限額；server.js 寫入熱路徑**不**掛 LLM。②**關聯式分級階層 domain**：多段 `_AIDocs/_atoms/<L1>/…/`；`normalize_domain_path` canon（同層 snap 精確/前綴 len≥3 治 Win→Windows/difflib≥.85）+ `_clean_segment` 拒 path-traversal；**增量深度閘**（新分支封頂 Lv3、只能比既有最深已積 atom 前綴深 1 層→depth=volume，MAX_DEPTH=7 絕對天花板；deterministic 不靠 LLM 自律）。③**詞庫自學**：LLM 判 local 後 validated terms→domain_path append `memory/_meta/realm-lexicon-learned.json`（py-only，`classify_realm(extra_lexicon=)`；js 維 base-only 保 test_17 parity）→下次免 LLM。④**catalog 階層化**：`_local_catalog.md` 只 Lv1 根+遞迴計數+drill（O(根數)）；每層 `_INDEX.md` 按需（有子層∨atom≥2；`_` 前綴非 atom）；caption preserve 擴及 _INDEX；`--check` drift/stale→exit1。⑤**移檔後 doc-sync**（user 補充）：`_scan_doc_refs` 掃人面向說明文件舊引用→sweep marker/refile 提示。**dogfood**：wsl2 詞庫 miss→LLM local OS/Windows/WSL conf≥.9→搬+學 5 詞→退 MEMORY.md/進 catalog(Lv1=OS)/生 _INDEX→自學閉環 deterministic 再命中；`--to-core` 可逆。**驗證**：`run_verify.py` **446 passed**（+24）；`node --check` 綠；`--check` exit0。**⚠ server.js 改動需重啟 MCP 生效**。**Phase G 收官見上條**（`/refile` skill + SPEC/Architecture/DocIndex doc-sync 已補完）。 | `lib/atom_locations.py`, `tools/realm_llm_classify.py`(新), `tools/atom-set-realm.py`, `tools/workflow-guardian-mcp/server.js`, `hooks/wg_atoms.py`, `hooks/handlers/session_start.py`, `tools/sync-memory-index.py`, `workflow/config.json`, `memory/_meta/realm-lexicon-learned.json`(新), `_AIDocs/_atoms/OS/**`, `lib/verify/`+`tools/verify/`+`hooks/verify/`(新測), `memory/realm-範疇分區機制-v5.md`(+7 知識), `TECH.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-04 | **perf(memory): always-load token 收斂 — 本地範疇 catalog 跨錯界修復（realm 貫徹到 index 層）** — plan `misty-nibbling-raven.md`，PlanMode 拍板選項 A。**問題**：realm gate 只擋 atom **body** 注入，但 catalog（`MEMORY.md` 全文，含本地範疇段 ~722 字元/8 顆 local atom）走 `CLAUDE.md` 靜態 `@import` **無條件漏進每個外部專案** always-load → realm 原則沒貫徹到 index 層。**修法（架構級、fail-safe）**：`sync-memory-index.py` render 拆雙輸出——`render_core_section`→`MEMORY.md`（core-only，仍 `@import`，**fail-safe 退路**+免 compact 失憶）；`render_local_catalog`→側檔 `memory/_local_catalog.md`（自含 H1+domain 子表）；共用 `_classify_rows`（DRY）。`main()` 雙檔 `--write`/`--check`（任一 drift→exit1，無 local→移除殘留側檔）；caption preserve 跨兩檔合併（migration 首跑本地描述仍在舊 MEMORY.md→自動保留）。`session_start.py` 共同尾段（startup/resume 皆生效）依 `_is_under_claude_dir(cwd)` gate 注入側檔，外部專案不注入→**省 ~450 tok/session**；fail-safe：缺檔/讀錯/非核心→靜默略過。`CLAUDE.md` 與 `server.js` 零改（`syncMemoryIndex` 只 spawn 該 .py；`_` 前綴側檔不被任何 scanner 當 atom）。**驗證**：`run_verify.py` **417 passed**（+5 新範疇閘測試 `verify_local_catalog_split.py`：core 不含 local 8 顆/含 core+feedback、側檔 domain 分組、雙檔 round-trip `--check`）；`node --check server.js` exit0；真實 repo `--write`→`--check` exit0；人工驗證 MEMORY.md 無 `## 本地範疇`/無 8 顆本地名·含 decisions-architecture+feedback-*，`_local_catalog.md` 含 8 顆分組，`_is_under_claude_dir` external→False/core→True。**衍生暫存**：規格檔 `next-phase-alwaysload-token.md` + plan 檔已刪。 | `tools/sync-memory-index.py`(render 拆 _classify_rows+core+local、main 雙檔), `hooks/handlers/session_start.py`(本地 catalog 注入), `tools/verify/verify_memory_index_caption_preserve.py`(改 core/local render), `tools/verify/verify_local_catalog_split.py`(新), `memory/MEMORY.md`(regen core-only), `memory/_local_catalog.md`(新,側檔), `_AIDocs/Architecture.md`, `_AIDocs/SPEC_ATOM_V5.md`, `TECH.md`, `_AIDocs/DocIndex-System.md`, `memory/realm-範疇分區機制-v5.md`(meta+知識), `memory/memory-index-caption-regen.md`(知識), `_AIDocs/_CHANGELOG.md` |
| 2026-06-03 | **docs(memory): Realm 維度 S3 — Phase 4 文件/Rules 全同步 ∥ Phase 5 雞肋稽核（驗證收斂）** — 承 S1（路徑單一源+寫入路由）/S2（注入閘門+分類器+8 顆搬遷）。**Phase 4 文件同步**：`rules/core.md`「記憶」加 Realm 小節（核心判定三問：可重用≥2 專案／系統規則 vs 特定範疇／月級 vs 週級）；`SPEC_ATOM_V5` 新增 **§2.2 Realm 章**（core vs local 路由表 + 分類器守則 + atom-set-realm + 守門測試）+ §13 紀錄列；`Architecture.md`「記憶系統」加 Realm 子節（path 推導／注入閘門／find-fallback／守門）；`DocIndex-System.md` 修正 atom 計數 16→31（core 14 + feedback 7 + 失敗 2 + local 8）+ 補 `_atoms/` 層；種子 atom `realm-範疇分區機制-v5` append 3 知識行（Phase4 完工／MEMORY 本地範疇段／本顆自身=core 判定）+ 雙向反向連結（↔ decisions-architecture／memory-index-caption-regen）；`extract/SKILL` 補萃取期 realm 判定提示；server.js schema realm/domain description（S1 已備、確認無缺）。**MEMORY.md「本地範疇」段**：`sync-memory-index` render 把 path 落 `_AIDocs/_atoms/` 的 atom 抽出主表、依 domain（MemDev/Tools/World）分組進尾段（`lib.atom_locations` 加 `local_realm` row_kind + `local_realm_domain()`）；round-trip 穩定（`--write`→`--check` exit 0 idempotent）；caption preserve 沿用一般規則；+3 回歸測試。**Phase 5 雞肋稽核（零硬刪、紀律收斂）**：read-only agent 查證**推翻計畫兩假設**——`templates/icld-sprint-template.md` 非「零引用 v4 遺物」（被核心 atom `workflow-icld` 活引用）、`_distant/` 非「散落待整」（已是 code-blocklist 隔離的 dated 歸檔）；`known-regressions.md` 經查仍準確（REG-006 三萃取管線確仍並存、非 stale）；6 顆「過時」DevHistory 根筆記（v4-conflict/v4-layers/version-migrations/ab-test-gemma4/ab-test-ollama/vector-threshold-calibration）查出**全被活引用為證物**（核心 atom toolchain-ollama + TECH.md + 活躍 atom-injection-refactor + Architecture）且 `_INDEX` 已標「不自動注入」→注入成本＝0 → **判定不下沉**（歸檔區內再歸檔零收益＋斷同層相對連結），改在 `DevHistory/_INDEX.md` 標記「已結案歷史證物·保留原位」。**真正動的高價值項**＝`Project_File_Tree.md` 就地更新成 V5 現況（commands→skills、補 lib//workflow//plugins//_atoms/）。**驗收**：pytest lib/verify/ + tools/verify/ 全綠、`node --check` server.js、MEMORY.md round-trip 穩定。 | `rules/core.md`, `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/Project_File_Tree.md`, `_AIDocs/DevHistory/_INDEX.md`, `_AIDocs/_INDEX.md`, `lib/atom_locations.py`, `tools/sync-memory-index.py`, `tools/verify/verify_memory_index_caption_preserve.py`, `memory/realm-範疇分區機制-v5.md`(+access), `memory/decisions-architecture.md`(meta), `memory/memory-index-caption-regen.md`(meta), `skills/extract/SKILL.md`, `memory/MEMORY.md`(regen), `_AIDocs/_CHANGELOG.md` |
| 2026-06-03 | **feat(world): 腦內世界 · 區域自主環境演化（放置式，Phase 1-5 完工）** — plan `sleepy-stirring-crystal.md`，user 三決策：①引擎=瀏覽器驅動（world.html 開著就跑/關了暫停/狀態存 server 重開續長）②時間=雙軌（slow 放置 + fast demo/驗證）③完工門檻=每區首風格 dev≥80。**★硬約束＝零影響原子記憶**：只讀生物個性、發展狀態只寫 gitignore 的 `workflow/world-dev.json`，絕不碰 `memory/`/`_atom_index.json`/`*.access.json`/funnel——結構性隔離（獨立 API+獨立檔，server.js 既有碰記憶路徑一律不呼）。**Phase 1 後端+目錄**：server.js `readWorldDev/writeWorldDev`(原子 .tmp→rename 仿 writeState)+`worldDev` 單例+`GET/POST /api/world-dev`(深合併+debounce)；新 `environment-catalog.json`(8 家族×6 tier 累加目錄+種子池+fits_personality)。**Phase 2 前端狀態+渲染**：`ENV_CATALOG` fetch(相對路徑→須 :8899 同層伺服)、`regionDev:Map`(模組級持久鏡像，★絕不存每5s重建的 model.c)、`reconcileDev/renderEnv/placeEnv`(seeded LCG hash32 禁 Math.random→deterministic 不位移、env-layer SVG `<text>`emoji/`<use>`地標、`el._envSig` 髒檢查 reconcile 友善)、招牌 dev% 進度條+env symbols defs。**Phase 3 免LLM 引擎**：proposals/配對擴散/共識/`devStep=max(.3,step×(1−dev/140))` diminishing 夾頂單調/tier 解鎖去重(unique 限一)/鎖定/`next_style_eligible`(60-80 開第二)/完工+`el._carry` 攜帶想法+雙軌時間(config `world_dev` 旋鈕/`worlddev` 指令/`?fast=1`)。**Phase 4 LLM+跨區**：`envBrainstorm`(#1 種子)/`envDirection`(#2 定案)複用 `/api/creature-chat`+`chatBusy` 序列鎖+`ENV_LLM_MIN_GAP`≈4s+8 家族白名單+`cleanLine` 剝 crack 模型洩漏 token→`JSON.parse`(失敗抓 `/\{[\s\S]*\}/` 重試)；**fallback 鐵則**＝斷網/逾時/解析失敗→純前端投票選 family+目錄種子+罐頭台詞仍建/仍鎖定永不阻塞；跨區串門子 `_visiting` lerp 走他區（只動 el._x/_y 不改 c.region＝守 reconcile-render 位移軸鐵律）作客跨區擴散+歸位；對話記憶只記 LLM。**Phase 5 /loop 運作+驗收**：起 `:8899` 靜態伺服器(cwd=world.html 同層,供相對 catalog/CORS→:3848 API)→Playwright `:8899/world.html?fast=1`→fast 模式單次推進即收斂（4 活躍區 core/c--tslg/c--tmp/c--tmp-docs-progg 全 rank1 dev=100 completed，且各開出 rank2 第二風格 100；3 個 1-生物區 list<2 正確不發展只留 🔥 種子）。**E2E ①種子→②擴散→③dev升→④元素渲染(DOM 82 `<text>`+4 `<use>` 地標/8 env-layer)→⑤鎖定(c--tmp 醞釀 proposal→playground 即時鎖)→⑥60-80 開第二→⑦80 完工 全鏈見證**。**★隔離證明**：整段 fast 驅動(70→100+第二風格+LLM 呼叫)後 `git status` 全空、`memory/**`+`_atom_index.json`+`*.access.json` 零 diff、`world-dev.json` 確 gitignore——零影響達成。`run_verify.py` **402 passed** 零回歸。收尾 reset slow + 刪 Playwright 截圖 + 停 :8899。新 atom `腦內世界-環境演化-放置式架構`（envLlm fallback 鐵則/`_visiting` 接位移軸/crack JSON 解析/零影響隔離設計）。 | `tools/workflow-guardian-mcp/world.html`(引擎主體 reconcileDev/renderEnv/placeEnv/envSeed/diffuse/envBrainstorm/envDirection/_visiting/worlddev), `tools/workflow-guardian-mcp/server.js`(readWorldDev/writeWorldDev+worldDev 單例+GET/POST /api/world-dev), `tools/workflow-guardian-mcp/environment-catalog.json`(新,8×6 風格目錄), `workflow/config.json`(world_dev 旋鈕), `workflow/world-dev.json`(唯一存檔,gitignore), `_AIDocs/Architecture.md`(環境演化章節), `memory/腦內世界-環境演化-放置式架構.md`(新 atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **fix(world): 腦內世界生物每 5s 彈回原位根治 — 漫步狀態歸屬從 ephemeral model 搬到持久 DOM 元素** — user 回報「除拖過的外，所有生物/整個畫面每隔一段時間跳刷新回原位，很不流暢」。**根因（非表面）**：`render()` 是 reconcile（`crEls` 以穩定 uid 為 key 持久存活、跨 refresh 重用 DOM），但 `refresh()` 每 `POLL_MS=5000ms` 跑 `model = buildModel()` **整批重建** creature 物件 `c` 再 `el._c = c` 重綁；而漫步暫態 `_wox/_woy/_wtx/_wty/_wnext` 卻存在被丟棄重建的 `c` 上 → 每 5s 歸零 → 下一幀 `el._x = c.slotX + 0 + sin()` → 全體彈回 slot。拖過的不彈＝其 `el._pinned`/`el._x` 存在持久 `el` 上躲過重建（吻合 user 描述）。**修法**：`wander(c,el,t)` 把這 5 個暫態欄位全搬到持久的 `el`（與 `el._x`/`el._pinned`/`el._tier` 同類），`c` 只留 model 資料（slotX/roomMinX/active）；tick line 764 同步改讀 `el._wox`。**非**把 POLL_MS 調大去遮症狀。**驗證**（Playwright :8899 載 world.html→fetch live :3848，跨 2 個 5s 輪詢邊界每 200ms 取樣 `el._x`）：結構面 `el_has_wox:true / c_has_wox:false`（暫態確在 el、c 上恆 null）；行為面 refreshCount=2 兩次換身邊界 prevX≈newX（483.29→483.99、485.70→486.06）**無跳**、`el._wox` 帶過去（18→19）、maxSingleStep **僅 4px**（lerp 漂移）、offsetRange [16,40]px（確有漫步且未歸零）。**純 world.html 靜態檔 → 免重啟 guardian，Ctrl+F5 即生效**。新 atom `reconcile-render-動畫狀態歸屬陷阱`（guardian 覆轍 same_file_3x:world.html 要求記錄防再犯）。**衍生暫存**：驗證用 :8899 靜態伺服器、Playwright 12:08 scratch 已清。 | `tools/workflow-guardian-mcp/world.html`(wander 簽名+5 暫態欄位 c→el、tick line 764 讀 el._wox), `memory/reconcile-render-動畫狀態歸屬陷阱.md`(新 atom), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(world): 腦內世界 v3 — 個性 / 自主行為 / Command Bus / 真·記憶自癒**（commit 55be297；plan `clever-sparking-castle.md`，user 拍板「一次做完 P1+P2+P3、自主只跑免費 L1 L2 手動、執行器可配置混合預設本地免費、建置期多 agent 並行」）。**硬約束**＝單張 3090 序列 → 行為分免費層(移動/罐頭/機械修)與昂貴層(LLM 節流/可配置並行)。**P1 個性**：`personaOf()` 依類別(name/type)×年資(confidence)×狀態(sick/lonely/elder) 注入 `creatureChat` 的 sys prompt，**不增 LLM 呼叫數**。**P1/P2 自主**：`wander` 房內漫步(房框夾限)、`sickWalk` 生病生物自走診所觸發自動 L1、`dialogueDirector` 每 18–30s 在 `chatBusy` 空閒時挑一對聊一次 → **LLM 速率封頂、與生物數無關**；`autoOn` 總開關。**P2 Command Bus**：單一 `WORLD_COMMANDS` registry **衍生**選項式指令台 UI(指令下拉+行內說明+依參數型別渲染生物/房間下拉)+executor+`/api/world-command|world-commands|world-result|world-snapshot` 輪詢；加指令＝改 registry 一處；Claude 走同套 API(curl POST + GET snapshot)驅動/觀測。**P3 記憶自癒**（`tools/atom-heal.py` 單一來源，importlib 重用 `atom-health-check.py` 偵測/`single_atom_report`/`--atom` + `lib.atom_io.edit_metadata`(source=`tool:atom-heal`)+`lib.atom_spec.validate_atom_content`+`tools/ollama_client.get_client`）：**L1** missing_reverse_refs 機械補反向連結(免 LLM)／**L2** broken_refs 呼本地 Ollama 出結構化提案 repoint/remove/needs_human → 腳本經 funnel 套用 → 驗證(重掃+atom_spec)／**L3** stale 喚醒。**後端可插拔** config `heal.backend` 預設 ollama(免費序列 max_concurrent=1)、cloud 選配(並行 cap=N，adapter 待接)。**安全**：禁盲刪、repoint 限真實候選、LLM 失敗/不確定一律 needs_human、local crack 模型 format=json 仍加 ``` 圍欄 → `_extract_json` 剝除。**修不好退人工**：`memory/_heal_review/<atom>.json` 診斷卡+`_merge_history.log`+`tools/heal-review.py`+`/heal-review` skill(resolve 重掃確認/dismiss，需 management)。**server.js**：抽 `makeJobRunner`+`execJson` 重構 testJobs(DRY)，新增 heal 路由(`/api/heal/:atom?auto=1`、`heal-job/:id`、`heal-all`、`heal-review`)；spawn 前 `ATOM_NAME_RE` 擋 shell 注入。**誠實痊癒**：前端只有 server 回 `fixed` 才移 `.sick`(收緊 healed Set)；修不好貼 🩹「轉診人工」不假裝。**⚠ 改 server.js → 走重啟 SOP**(殺舊碼孤兒 5352、spawn 新碼 42324 重綁 :3848；見 atom `guardian-dashboard-孤兒佔埠與新碼重啟`)。**驗證**：`run_verify` **402 passed** 零回歸；L1/L2 真壞 atom 端到端(本地 LLM 正確 repoint `decisionz`→`decisions`、無對應的 fake→needs_human 退人工)；重啟後 live :3848 command bus(snapshot 93 生物)+heal pipeline HTTP 全綠；Playwright `:8899` 載入 world.html 無 JS error、93 生物/8 房渲染、persona「碎念的檔案管理員…毛躁新人」、指令台 say/spotlight/auto 全正確。**衍生暫存**：`_syntax_check.js`/`_heal_test_fixtures.py`/fixtures/playwright 截圖/jpeg 全清；`workflow/_heal_restart_std*.log` 因 live 42324 開著留待下 session 清。**未提交**(非本次)：`memory/toolchain.md`(1 行對話前已 M)、`state-*.json.corrupt`。 | `tools/workflow-guardian-mcp/world.html`(personaOf/wander/sickWalk/dialogueDirector/WORLD_COMMANDS registry+選項式指令台+poll/healReal+輪詢+bandaged/healed Set 收緊), `tools/workflow-guardian-mcp/server.js`(makeJobRunner+execJson 重構 testJobs、command bus+heal 路由群), `tools/atom-heal.py`(新,自癒單一來源), `tools/heal-review.py`(新,人工裁決), `skills/heal-review/SKILL.md`(新), `tools/atom-health-check.py`(--atom 單顆過濾), `lib/atom_io.py`(VALID_SOURCES+tool:atom-heal), `workflow/config.json`(heal 區塊), `_AIDocs/Architecture.md`(腦內世界 v3 章節), `memory/腦內世界-v3-自癒與-command-bus-架構.md`(新 atom), `memory/guardian-dashboard-孤兒佔埠與新碼重啟.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(world): 腦內世界生物 LLM 對話 — 拖近觸發 rdchat-direct 31B 即時對話（async + fallback）** — user 要求把拖兩隻靠近的罐頭台詞換成真 LLM 生成、更像真實對話。**新增 `world-chat.js`**（被 server.js `require` 的**同進程模組**，user 提議拆獨立 js + 隨 server 啟停；選 require 模組而非另起 process，薄代理免 port/IPC）：代理 `POST /api/creature-chat` → config `vector_search.ollama_backends['rdchat-direct'].base_url`（`http://192.168.199.130:11434` 3090）原生 `/api/chat`；**base_url/認證留 server 端（單一來源、不外洩 token）、model/prompt/options 前端帶（免重啟調風格）**；30s upstream 逾時、100KB body 上限、缺 messages→400。server.js 僅 2 行（require + 路由分派）。**world.html**：拖近先冒「💭…」→async `creatureChat()` 問 31B（`hf.co/edwardwallst/Gemma-4-31B-JANG_4M-CRACK-GGUF:Q4_K_M`，think:false/num_predict 64/temp 0.9）→回來 `updateBubble` 替換；**9s 逾時/錯誤/忙線 → fallback 既有罐頭**；**in-flight 鎖**（`chatBusy`）同時只一請求不灌爆 3090；**`cleanLine` 剝除 crack 模型洩漏的 `<\|channel>thought<channel\|>` 控制 token**（去引號、>40 字截斷）。prompt 用兩隻 atom 身份（name/階段/類型/腦區/related）建人設。**驗證**（temp harness :8077 require 模組打真 3090，零干擾 live；Playwright :8090）：standalone POST→3090 ~6s in-character（workflow-rules↔svn「版本紀錄對上了嗎？」）+ 缺 messages→400；瀏覽器 e2e（CORS harness→3090→cleanLine）decisions↔preferences「照你的喜好來決定吧！」；cleanLine 單元 5 例（含真實洩漏樣本）；async 泡泡 💭…→替換 / 失敗 fallback / in-flight 鎖 全綠。**⚠ 改 server.js → live :3848 需重啟 guardian（開新 Claude session）才有 `/api/creature-chat`；未重啟前拖近 → 404 → 自動 fallback 罐頭（不破）**。**衍生暫存**：harness `_wc_test.js`/:8077/:8090、截圖/log 全清；`workflow/state-*.json.corrupt` 為 daemon 執行期產物非本次（未動）。 | `tools/workflow-guardian-mcp/world-chat.js`(新,代理模組), `tools/workflow-guardian-mcp/server.js`(require + /api/creature-chat 路由), `tools/workflow-guardian-mcp/world.html`(async creatureChat/cleanLine/updateBubble/in-flight 鎖/sayPair 改 LLM+fallback), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(memory): 選配收尾 — edit_metadata 進 run_verify baseline + AtomFunnelBlock 補齊 `_AIDocs/Failures/` 覆蓋缺口** — 承 d3afda7/e126641（edit_metadata 合法元資料外科編輯）與 b3b5196 附帶發現的待補項。**(1) edit_metadata 單元測試進基線**：新增 `lib/verify/verify_atom_io_edit_metadata.py`（10 test），守 byte-stable（只改目標行、非目標欄位/知識區/EOL/BOM 原樣）、triggers 變更 **SoT 先行**（先 `_atom_index.json` 再 frontmatter）、SoT 失敗不續寫 frontmatter、找不到欄位非靜默 no-op、invalid source/read 失敗/檔不在 CLAUDE_DIR 各自 error；先前僅 dogfood + 對抗式審查、未進 baseline。**(2) funnel guard 覆蓋缺口補齊**：`hooks/wg_core.py:check_memory_path_block` 原只攔 `.claude/memory/` 樹下 atom（上游 `_path_under_memory_dir` 對 `_AIDocs/Failures/` 早 return None），失敗 atom（feedback-*/cognitive-patterns/memory-pipeline-*）物理居 Failures、直 Write/Edit 繞過 funnel+audit。新增 `_is_failures_atom_path()`：以 `atom_locations.failures_atom_stems()` 比對 `_atom_index.json` **精準**鎖定 9 個註冊 atom，**不誤擋**同目錄混居的 7 個 legacy 失敗筆記（env-traps/silent-failures…未進 index）與 `_INDEX.md`。**(3) import dual-safe**：`failures_atom_stems()` 內相對 import 改 try-相對/fallback-絕對，解 wg_core 以頂層模組載入 atom_locations 時 ImportError（原本根本呼不到此函式，覆蓋缺口無法啟用的隱性根因）。**(4) 白名單除雷**：從 `atom_writable_dir_segments()` 移除 dormant 的 `Failures` 條目（parts_lower vs 'Failures' 大小寫不 match、本就無效）+ 改寫 docstring——避免未來把 caller intersect 改 case-insensitive 反而豁免整個 Failures 目錄、廢掉 (2)（覆蓋缺口復發 footgun）；wg_core fallback set 同步移除、清 unused `FAILURES_DIR_NAME`。新增 guard 測試 `hooks/verify/verify_failures_atom_funnel_guard.py`（20 test，含「白名單不得含 Failures」結構不變式 + stems=None 退化不攔 + memory/ 不回歸 + bypass env）。**驗證**：`python run_verify.py` **402 passed**（372 baseline +30）零回歸；真 index smoke 確認註冊 atom 擋／legacy 放行／memory 不回歸。**未改 server.js → 免重啟 MCP**（純 Python hook + lib）。atom `atom-元資料編輯與晉升閘真相` 補 1 知識行（走 funnel append、原樣帶回 12 trigger 防截斷覆轍）。**衍生暫存**：無。 | `hooks/wg_core.py`(import+`_is_failures_atom_path`+guard 接線+fallback+訊息補 atom_edit_meta), `lib/atom_locations.py`(`failures_atom_stems` dual-safe import + `atom_writable_dir_segments` 移 Failures + 移 unused `FAILURES_DIR_NAME`), `lib/verify/verify_atom_io_edit_metadata.py`(新,10 test), `hooks/verify/verify_failures_atom_funnel_guard.py`(新,20 test), `_AIDocs/Architecture.md`(guard 段補 (d)+白名單註+修 `wg_pretool_guards`→`wg_core` stale 路徑), `memory/atom-元資料編輯與晉升閘真相.md`(funnel guard append), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(world): 腦內世界 v2 Phase C — 房間網格改進 + 戰力星條/排行榜（v2 全節完成）** — plan `concurrent-frolicking-honey.md` §v2 Phase C（user 拍板「改進房間網格 + 戰力星條/排行榜」）。**(1) 房間網格**：`layout()` 重寫——核心房放大(targetW≈60%視寬)置中置頂獨佔一列、其餘 shelf-pack 且**每列收尾統一列高 → 同列 floor 對齊**、生物改 **cell 網格(76×92)置中**（cell 寬>最大體型 62 → 防重疊）+ 由房底往上排、走動幅度受 cell 餘裕夾限；scene 高度依內容增長 → 超量自動捲動。**(2) 戰力星條**：每隻生物腳下 ⭐×N（`powerStars`＝round(power×5) 0..5；power 缺則隱藏），詳情卡顯示「戰力 ★★☆☆☆（power x.xx／樣本數 n）」。**(3) TOP3 排行**：topbar「🏆 最強記憶」按 `power` 降序前 3（附腦區色點），無遙測 → 顯「待戰力遙測…」。**(4) 人氣皇冠**：`elder` 改接真實**人氣＝read_hits+confirmations** 前 max(3,5%) 名（取代 v1 入度近似；全 0 時 fallback 入度長老），詳情卡文案改「被讀取 R 次／跨 session 確認 C 次」。**(5) 精準沉睡**：改用真實 `days_since_used`(Phase A server 算)／否則正則抽 `last_used` 前導日期（容忍髒尾註）判 **>60d 才睡**，取代 episodic 文字近似。**零 server.js 改動**（純消費 Phase A 既有欄位）。**驗證**（temp `http.server :8090`＋Playwright，擋 file:// 走 plan fallback；不干擾 live）：daemon 已重啟 → live :3848 **89 atom 全帶 power**（前置警告「需重啟」自解）；5 api 全 200、無 JS error（僅 favicon 404）；**核心置中**(x+w/2=720=視寬/2)、**同列等高**(3 列 allSame)、**重疊 0**、scene 1386>視窗 900 可捲；**沉睡 7** 顆 daysUsed 61~70 全≥60；**皇冠 5**＝read_hits 前 5（decisions258/toolchain230/workflow-rules223/workflow-svn200/preferences191）；**TOP3** 按 power 正確；真實 power 偏低(Wilson 下界×小樣本)→星條多 0 星（邏輯正確），**mock power 注入驗 round(power×5) 渲染 mismatch=0**（74 隻顯星、TOP3 重排 1.00）。**衍生暫存**：phaseC 截圖 3 張＋playwright log 已刪、temp :8090 已殺——全清。 | `tools/workflow-guardian-mcp/world.html`(buildModel 遙測/沉睡/皇冠/top3、layout 重寫、render 星條+TOP3、showDetail 文案、CSS .stars/#top3+topbar TOP3), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(memory): 原子記憶系統工具強化 — 合法元資料外科編輯入口 + 晉升閘對齊 + sidecar 計數修正 + funnel 紀律延伸** — 五點：**(1)** 新增 `lib/atom_io.edit_metadata()`——atom 元資料（Trigger/Related/Tags）「外科編輯」入口，只替換 frontmatter 對應行、byte-stable（不重建知識區）；triggers 變更時 **先寫 `_atom_index.json`(SoT) 再寫 frontmatter**（衍生），走既有 funnel（write_index + write_raw + audit）；取代被 Guardian guard 擋的「直 Edit atom .md」與會重建整檔的「整檔 atom_write replace」。**(2)** MCP 工具 `atom_edit_meta`（server.js）暴露此能力給 AI（**改全域 server 需重啟生效**）。**(3)** `memory-audit.py` 修正：晉升建議由退役的 ReadHits 閾值 → 改對齊線上 `atom_promote` 的 **usefulness Wilson 下界 + confirmations** 閘（復用 `lib/atom_access.usefulness_promote_eligible`，ReadHits 純曝光不再參與晉升）；parser 容忍 `\r\r\n` 雙 CR 並對壞檔 emit「行尾損壞」warning。**(4)** `atom-health-check.py` 修正：計數欄位改讀 `<atom>.access.json` sidecar（Wave 2 後 inline 已移除），消除「全 atom 誤報 missing」。**(5)** funnel 寫入紀律延伸：`atom-health-check.py` / `sync-atom-index.py` 的裸 `write_text` → 改走 `lib.atom_io.write_raw`（EOL-preserving + audit）。文件同步 `Architecture.md`（funnel 入口 + caller 表 + MCP 4 tool + atom_edit_meta 節）、`SPEC_ATOM_V5.md`（§3.4 契約 + §9 tool 數 + §13）。 | `lib/atom_io.py`(`edit_metadata`), `tools/workflow-guardian-mcp/server.js`(`atom_edit_meta`/`toolAtomEditMeta`), `tools/memory-audit.py`(晉升閘對齊+雙CR 容忍), `tools/atom-health-check.py`(sidecar 計數+write_raw funnel), `tools/sync-atom-index.py`(write_raw funnel), `_AIDocs/Architecture.md`, `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **feat(world): 腦內世界 v2 Phase B — SVG 手繪生物造型（emoji 臉+漸層圓 → 4 階體型 `<symbol>`+`<use>`）** — plan `concurrent-frolicking-honey.md` §v2 Phase B（user 拍板「SVG 手繪生物」）。把 `.cr .body` radial-gradient 圓 + `faceOf()` emoji 換成 **5 個可復用 `<symbol>`**（egg🥚→slime史萊姆→monster小怪→dragon龍 + ghost幽靈），各生物 `<use>` 復用、零素材。**體色＝腦區 hue**：主體 `fill=currentColor`、`.cr{color:hsl(var(--h)…)}` 注入；明暗用白/黑 opacity 疊層（與 hue 無關 → 任何腦區色都立體）。`tierOf()` **沿用 faceOf mass 門檻 3/6/10/16**（≥10 龍、≥16 加 `.mega` 威風呼吸縮放）。**表情純 CSS class 驅動**：健康(睜眼+微笑)/病(`.sick`→病綠 currentColor+反胃 X 眼+波浪嘴+綠煙)/睡(`.sleep`→閉眼+傾身 14°+變淡+💤)/進化(`.evolving`→紫光脈衝)——眼/嘴 6 組畫在各生物 light-DOM `<svg>`（symbol 內無法被外層 class 選取，故體型走 use、表情走 light-DOM）。**保留**：金[固]/銀[觀]進化環（改 drop-shadow 光暈貼合輪廓，非方框 box-shadow）、💤🆙👑🌱🔨 角標、拖曳/對話/診所互動、reconcile（`el._tier` 變了才換 symbol）；ghost(episodic) 一併 SVG 化。**零 server.js 改動**（仍靠既有 CORS `*`）。**驗證**（temp `http.server :8090`＋Playwright，Playwright 擋 `file://` 故走 plan fallback；不干擾 live :3848）：render 無 JS/CSS/SVG error（僅 favicon 404）；四階全present（dragon 33/slime 33/monster 16/**egg 6**）；per-region 變色目視；sick 切換程式化確認（`eyes-sick=inline/eyes-open=none/m-sick=inline/smoke=block`＋病綠）；sleep 傾身+💤、金銀環、皇冠、幽靈 48 目視。**⚠ Phase A 的 `power` 欄位 live :3848 尚未生效**（常駐 daemon 未重啟，本 Phase 用不到；Phase C 戰力星條前需重啟 guardian MCP）。**衍生暫存**：temp http server(8089/8090) 已殺、`.playwright-mcp/` 截圖已刪、`_wb_check.js` 已刪——全清。 | `tools/workflow-guardian-mcp/world.html`(symbol defs+CSS SVG 化+tierOf/crSvg/render/ghost/heal/legend), `_AIDocs/_CHANGELOG.md` |
| 2026-06-02 | **fix(memory): atom .md funnel-bypass 根治（EOL+audit）+ 復原 trigger 12-截斷流失（覆轍同源）** — Phase A commit 後查 git churn 揪出的雙重問題。**(1) EOL churn＝良性**：工作區 100% 乾淨、HEAD 殘留 double-CR 僅 3 檔（修復前舊汙染），本 session 寫入時被 healed，無 active 汙染源——`_atomic_write` EOL 修復正常運作。**(2) Bug-funnel-bypass**：`tools/atom-health-check.py:fix_reverse_refs`（反向參照補全）與 `tools/sync-atom-index.py:fix_frontmatter_from_index`（`--fix` index→.md Trigger 對齊）皆用裸 `Path.write_text` → 繞過 `lib.atom_io` EOL-preserving funnel + audit log（上次 EOL 修復 `f013ae5` 只收束 `atom_io._atomic_write`/`atom_index_json`，漏這兩個 CLI 工具）→ Windows 翻整檔 EOL + 寫入無 audit（隱形）。**根治**：兩處改走 `write_raw(path, content, source=…, op=…)`（VALID_SOURCES 既有 `tool:sync-atom-index`/`tool:atom-health-audit`）→ EOL byte-stable + audit 留痕。**(3) trigger 流失**：本 session 一個「語義去重/重生」寫入路徑把 `_atom_index.json`（SoT）+ 4 個 atom 的 frontmatter Trigger 砍到 12（`feedback-completion-gates` 19→12、`-workflow-discipline` 14→12、`-tooling-reliability` 14→12、`workflow-parallel-agents` 13→12），丟失 `上 SVN`/`pytest`/`測試上傳`/`研究先行`/`並行加速` 等人工策展 trigger，**無 atom_io audit**（與 caption-regen 沖人工策展同源覆轍 [[memory-index-caption-regen]]）。`TRIGGER_MAX=12` 經查只是 `memory-audit` 建議值、非截斷器；**確切重生器未定位**（無 audit 痕跡＝疑另一 bypass writer 或長 session 較早 LLM trigger-refine pass）。**復原**：index `git checkout HEAD`（僅該 4 處 trigger 差異、無 EOL 風險）、3 個 Failures atom 直接復原 Trigger 行、`workflow-parallel-agents.md` 經修好的 `sync-atom-index.py --fix`（dogfood funnel：補回 `並行加速`、double-CR=0、留 audit `op:trigger-align`）→ **`--check` has_drift:false（index↔frontmatter 全對齊）**。**附帶發現**：AtomFunnelBlock guard 只攔 `memory/*.md`、**不攔 `_AIDocs/Failures/*.md`**（故 Failures atom 直接 Edit 未被擋——guard 覆蓋缺口，待補）。**驗證**：`run_verify.py` **372 passed**（零回歸）+ 4 atom EOL 全 0 double-CR + funnel audit 留痕。**衍生暫存**：無（forensic 全用唯讀分析）。 | `tools/sync-atom-index.py`(write_raw funnel), `tools/atom-health-check.py`(write_raw funnel), `_AIDocs/Failures/feedback-completion-gates.md`+`feedback-workflow-discipline.md`+`feedback-tooling-reliability.md`(復原 Trigger), `memory/workflow-parallel-agents.md`(funnel 復原), `memory/_atom_index.json`(復原), `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **feat(world): 腦內世界 v2 Phase A — `/api/atoms` 加戰力遙測欄位（解鎖星級/皇冠/精準沉睡）** — world.html v2 三方向（SVG 生物 + 房間網格 + 戰力星條/排行榜，user 拍板；plan `concurrent-frolicking-honey.md` §v2）的資料地基。`server.js:apiAtoms()` 兩處組 atom（主 `scanDirs` 迴圈 + `pushAtomFromFile`）新增 `enrichAtomWithAccess()`：併入**現成** `readAtomAccess()` 的 `<atom>.access.json` 遙測（`useful_hits`(α)/`used_fail`(β)/`confirmations`/`read_hits`/`last_used` 權威覆寫 .md）+ `usefulnessStats(acc,1.96)` 算 `power`(Wilson 下界 0..1＝戰力)/`power_mean`/`power_n`(樣本數)。**不新增路由、純加欄位、向後相容**。同份資料一次解鎖「人氣皇冠(read_hits/confirmations)」+「精準沉睡(真實 last_used 天數，取代 episodic 文字近似)」。**端到端驗證**（臨時 server :3899，零干擾 live :3848，驗畢即刪）：88 atom 全返回不掉球、全帶 power、18 顆 n>0；Wilson 數值與手算逐位吻合（decisions 0.118/n5、atom-table-support 0.022/n8、cc-能力 0.095/n2）；TOP5 可算（decisions-architecture 0.121 領先）。**⚠ 改 server.js → live dashboard 需重啟 guardian MCP 才吃到新欄位**（world.html 已能 fetch，待 B/C 消費）。**衍生暫存**：`_world_verify_tmp.js`/`_wv.log`/`_wv_atoms.json` 已刪。 | `tools/workflow-guardian-mcp/server.js`(`enrichAtomWithAccess`+apiAtoms 兩處接線), `plans/concurrent-frolicking-honey.md`(§v2), `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **feat(atom): 選配 #4 — 壓縮後 atom 內文重注入（PostCompact + PostToolBatch；plan `typed-purring-stearns.md §78-79` 評估後實作）** — **評估先行非盲實作**：反編譯實跑 binary 查證三事件（`SessionStart(compact)`/`PostCompact`/`PostToolBatch`），**證偽 docs agent 幻覺**（其伪造 WebFetch 不可能有的行號引用），並揭兩個翻轉 plan 假設的硬事實：(1) **版本分裂**——三事件僅在實跑環境 **VSCode 擴充套件 v2.1.159** 存在，終端 native **v2.1.37 grep 0 次**；(2) **`PostCompact` 不能注入 additionalContext**（Zod schema 實證，只收 `trigger`+`compact_summary`），plan 把它當「重注入索引」載體**是錯的**。再經分析確認**失憶真實缺口比 plan 假設小**——atom 索引(MEMORY.md) 經 CLAUDE.md @import 常駐系統提示 + `InstructionsLoaded(compact)` 重載 → 壓縮不丟索引；唯一缺口 = **mid-turn auto-compact** 丟失的完整 atom 內文。**設計（Option B，user 拍板；#5 PostToolBatch 萃取 DEFER）**：`injected_atoms`（全程在 state 累積）→ **PreCompact 快照**(`pre_compact_injected_atoms`，免受 SessionStart(compact) 清空順序影響) → **PostCompact** 依名單複用 `wg_atoms.load_atoms_within_budget`(印象式 strip+budget 800+緊湊) stash 緊湊內文+`pending_reinjection` flag（**不注入**）→ 下個 **PostToolBatch** idle 極輕 early-exit、見 flag 時**一次性** `additionalContext` 注入並清 flag、名單 merge 回 `injected_atoms`（維持 Phase 2 效用歸因）。dispatcher.HANDLERS + settings.json 加 PostCompact/PostToolBatch 接線。**四紅線**：低 token/印象式 ✓、不失憶（閉 mid-turn 缺口）✓、注入→使用更有效 ✓、單一來源(state/access funnel) ✓。**Phase-0 實測閘（已收尾）**：user 跑 `/compact` 後讀 `Logs/compact-probe.log` 揭示 2.1.159 觸發序 = `PreCompact`→`SessionStart(source=compact)`→`PostCompact`（full compact），**證偽「死碼」假設**——`session_start.py:230-249` compact 分支確實觸發、**保留並加註**（僅列名稱 ~30 tok，與 #4 內文復原互補；且 SessionStart(compact) 不保證觸發＝另見一次 1 s no-op compact 僅 Pre+Post，故內文復原不可依賴它）。已移除暫時 probe `_phase0_compact_probe` + 刪 `Logs/compact-probe.log`。**E2E 實證**：`/compact` 後 PostToolBatch 成功復原 5 atom 內文。**驗證**：`run_verify.py` **372 passed**（362 baseline +10 新 `verify_post_compact_reinjection_phase4.py`）+ 真實生產路徑 smoke（PostCompact stash→PostToolBatch 注入 `[Atom:workflow-rules]`+`[Atom:preferences]` ~370tok→二次 idle 空）。**未改 server.js → 免重啟 MCP**。**衍生暫存**：無（smoke 用 throwaway state 已清）。**全節完成**（Phase-0 實測閘已收尾，見上）。**結構文件同步補正**（原 #4 commit 僅更 _CHANGELOG + cc-hook-system，漏 5 份結構文件，doc-sync 紀律 [[feedback-memory-system-doc-sync]]）：`Architecture.md`（hook 事件數 8→10 + hook 表 + handler 模組表加 PostCompact/PostToolBatch）、`SPEC_ATOM_V5.md`（handler 清單）、`DocIndex-System.md`（流程圖 + handler 表）、`TECH.md`（檔案樹 + 2 處計數 8→10 + handler 列表）、`Install-forAI.md`（**settings.json 安裝範本補 PostCompact/PostToolBatch hook——否則新裝者拿不到 #4** + 檢核清單計數）。 | `hooks/handlers/post_compact.py`(新), `hooks/handlers/post_tool_batch.py`(新), `hooks/handlers/pre_compact.py`(快照 injected_atoms), `hooks/handlers/__init__.py`, `hooks/dispatcher.py`(HANDLERS 接線；Phase-0 probe 已移除), `hooks/handlers/session_start.py`(compact 分支非死碼加註), `settings.json`(PostCompact/PostToolBatch hook), `hooks/verify/verify_post_compact_reinjection_phase4.py`(新,10 tests), `_AIDocs/ClaudeCodeInternals/cc-hook-system.md`(2.1.159 實測 roster/版本分裂/additionalContext 能力/Phase-0 觸發序結論), `_AIDocs/Architecture.md`(hook 事件 8→10 + 2 handler), `_AIDocs/SPEC_ATOM_V5.md`(handler 清單), `_AIDocs/DocIndex-System.md`(流程圖 + handler 表), `TECH.md`(檔案樹 + 計數 + handler 列表), `Install-forAI.md`(安裝範本 + 檢核清單), `plans/deep-wobbling-bentley.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **fix(memory): Phase 2 後收斂 — UPS 注入晉升提示去 stale-ReadHits、改效用 Wilson 驅動 + embedding tiebreak live 煙測** — Phase 2(`6b1435b`) 已讓 ReadHits 退出晉升路徑、降純曝光，但 `user_prompt_submit.py:614-628` 的注入提示仍按 `READHIT_THRESHOLDS`(臨≥20/觀≥50) 喊「ReadHits 已達輔助門檻請確認晉升」——**與新晉升閘矛盾的死提示語**（純曝光不再能晉升）。**修**：(1) `lib/atom_access.py` 新增純函式 `usefulness_hint_tier(access)` → `'eligible'`(Wilson lb≥promote_lb) / `'near'`(promote_lb−0.1≤lb<promote_lb) / `None`（n<min_n 或離升門遠，**純 prior 一律 None 杜絕曝光雜訊**），SYNC `usefulness_promote_eligible`；(2) `user_prompt_submit.py` 注入區改：ReadHits 仍 `increment`（純曝光遙測保留）但提示改由 `usefulness_hint_tier` 驅動、訊息顯示 `效用 lb=… (n=…) 目前X→Y：已達/接近效用升門`、audit 帶 `method=usefulness/tier/lb/n`、[固] 與 n<min_n 不提示；刪 `READHIT_THRESHOLDS`。**embedding tiebreak live 煙測**（`make_embed_tiebreak_fn` 對真實 Ollama `qwen3-embedding` 4096-dim）：cosine 語義正確（相似 0.85 / 無關 0.29、similar≫unrelated）、fail-safe 穩（逾時/服務掛→None 不污染主判）；**發現**：暖機短文 ~0.6s/embed 但 CJK 長句單次可 >prod `embed_timeout_s=1.5` → tiebreak 常 fail-safe None、冷載 ~24s → 模型未保溫時近惰性（功能正確、屬 best-effort by-design；若要 CJK 實效可酌升 timeout≈2.5，記入 [`atom-usefulness-loop`](../memory/atom-usefulness-loop.md)）。**驗證**：`run_verify.py` **362 passed**（+6：5 `usefulness_hint_tier` 純函式 + 1 UPS 源碼守門 stale-ReadHits 退場）。**未改 server.js → 免重啟 MCP**（Python hook 下次呼叫即生效）。順帶（DocDrift 信號揪出）：補正 `Architecture.md` 既有 drift——atom_promote 門檻去舊 ReadHits Auxiliary(≥20/≥50)+7 天 fallback、改「Confirmations 主軌 OR 效用 Wilson 下界」；atom_access API 補 Phase 2 效用 7 函式；schema v2→v3(α/β)（Phase 0/2 已 ship 但 Architecture.md 漏同步）。**衍生暫存**：無。 | `lib/atom_access.py`(`usefulness_hint_tier`+常數), `hooks/handlers/user_prompt_submit.py`(注入提示效用化), `hooks/verify/verify_usefulness_loop_phase2.py`(+6 tests), `memory/atom-usefulness-loop.md`(embedding 煙測發現), `_AIDocs/Architecture.md`(atom_promote 門檻/atom_access API/schema v3 補正), `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **fix(memory): atom_write reformat blast radius 根治 — funnel `_atomic_write` EOL-preserving** — bug-3（承 fork/replace-upsert 兩缺口 `f013ae5`）：append 2 行卻整檔 48 行 diff。**根因不在 `buildAtomContent`（產純 LF、無辜）**，而在 funnel `lib/atom_io.py:_atomic_write` 用 `Path.write_text(newline=None)`——Windows 把每個 `\n` 翻成 `os.linesep`；`server.js` append 用 Node `fs.readFileSync` 原樣讀既有 CRLF 再拼 `\n` → 混合 EOL → 既有 `\r\n` 被二次翻成 `\r\r\n`(double-CR)、整檔行尾全變 → git 視為全行更動。**修**：新增 `_detect_eol`，`_atomic_write` 先正規化純 LF → 偵測既有檔行尾原樣套回 → `open(newline="")` 關平台轉譯（既有 CRLF byte-stable、僅新增行進 diff）；同源修 `lib/atom_index_json.py` 兩處索引 regen（新增 `_write_text_preserving_eol`，回應 Guardian「1-3 行能修當場修」）。Python 端改、下次 CLI 呼叫即生效（免重啟 MCP）。**驗證三層**：byte-level 單元（修後 CR_CR_LF=0、舊寫法同輸入 28 處 double-CR 反證）+ `atom_io_cli` stdin bridge 端到端 + **dogfood**（真 MCP `atom_write` append 進當初 blast 的 `feedback-tooling-reliability.md` → `git diff` = 1 insertion、既有行 byte-stable）。**衍生暫存**：`_tmp_eol_test.py` 已刪、無保留。 | `lib/atom_io.py`(`_detect_eol`+`_atomic_write` EOL-preserving), `lib/atom_index_json.py`(`_write_text_preserving_eol`×2 索引 regen), `_AIDocs/Failures/feedback-tooling-reliability.md`(bug-3 記錄+dogfood), `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **feat(atom): Phase 2 — 注入→使用→結果 閉環效用歸因（#2）** — 用「真實效用」(α,β) 取代「曝光次數」校準 atom 信心（Beta-Bernoulli + 詞彙重疊 use-gate + Wilson 下界 + 慢衰減 λ=0.97）。承 Phase 0(`63ae3d2`)+Phase 1(`ed1de67`)。**八步**（plan `plans/typed-purring-stearns.md` §52-65）：(1) `lib/atom_access.py` 遙測 schema v2→v3 加 `useful_hits`(α)/`used_fail`(β)（prior 1、冪等可重入 migration）+ `record_usefulness`（三值 used×success，**unknown=no-op 防雜訊**）+ `decay_usefulness` + Wilson 下界/升降資格（單一來源）；(2) `user_prompt_submit.py` `state["turn_injected"]` per-turn 注入記錄 + `turn_seq`（補 `injected_atoms` 合併後 per-turn delta 遺失）；(3) `wg_atoms.detect_atom_use` 零成本詞彙重疊（稀有 code token + CJK bigram、去停用詞、containment/Jaccard；embedding cosine 邊界 tiebreak fail-safe）；(4) `wg_evasion.get_current_turn_text`（turn 邊界擷取 assistant 活動）；(5) `stop.py` 3 值 success 偵測（複用 `failing_tests`/`claims_completion`/`evasion`/`retry`）+ 注入→使用→結果歸因（`turn_injected`+Phase 1 `subagent_injections`，per-turn 一次性走 funnel）；(6) `_self_iterate_atoms` SessionEnd 慢衰減 + 晉升閘改「Confirmations 主軌 OR 效用 Wilson 下界（升≥0.6,n≥3）」、**ReadHits 退出晉升降純曝光**、降級候選（≤0.35,n≥3）列報告；(7) `server.js` 晉升閘鏡像（`wilsonLowerBound`/`usefulnessStats`，SYNC: atom_access）+ `readAtomAccess` 讀 α/β + `memory/decisions.md` 門檻對齊；(8) `config.json` `usefulness.{...}` 旋鈕。**四紅線**：低 token（α/β 兩 scalar 不寫 .md）/不失憶/注入→使用更有效/協作升級。**驗證**：`run_verify.py` **356 passed**（baseline+39 新測試：18 access+21 loop，Phase 0 晉升閘測試更新為效用驅動）；py↔js Wilson 逐位 parity；端到端 used×success/fail/unused → (α,β) 正確、晉升 crossover α=7/β=1(lb=0.610)。文件同步 SPEC_ATOM_V5 §12 + atom [`atom-usefulness-loop`](../memory/atom-usefulness-loop.md)。**⚠ 改 server.js 後須重啟 MCP server 才生效新晉升閘。衍生暫存**：無。 | `lib/atom_access.py`, `hooks/handlers/user_prompt_submit.py`, `hooks/handlers/stop.py`, `hooks/wg_atoms.py`, `hooks/wg_evasion.py`, `tools/workflow-guardian-mcp/server.js`, `workflow/config.json`, `memory/decisions.md`, `hooks/verify/verify_promotion_gate_phase0.py`, `hooks/verify/verify_usefulness_loop_phase2.py`(新), `lib/verify/verify_usefulness_access_phase2.py`(新), `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **fix(memory): MEMORY.md 描述欄 regen 保留人工策展 — 杜絕 atom_write 靜默沖掉手寫描述（覆轍根治）** — 跨多 session 反覆出現的覆轍：手改 MEMORY.md 描述列 → 下次任何全域 `atom_write` 又被沖回裸名。**根因鏈鎖定**（前一份自檢只標「根因未明、grep 無命中」，本次完整定位）：(1) `server.js:toolAtomWrite` 在 `mode=create`(L1320)/`mode=replace`(L1438) 且 `scopeLabel==="global"` 時 fire-and-forget spawn `sync-memory-index.py --write`（[`syncMemoryIndex` L1005](tools/workflow-guardian-mcp/server.js#L1005)，detached/stdio ignore → **靜默**）；(2) `sync-memory-index.py:extract_atom_caption` 讀每個 atom 檔 **H1** 當「說明」欄；(3) 走 funnel 的 atom `H1=裸 kebab-name`（`build_atom_content` title=slug）→ regen 把描述降級成裸名。**衝突本質**：人把 MEMORY.md 當「手工策展 caption」、機器卻每次全域 atom_write 自動 H1-regen，兩套心智模型互斥。**修法（Option B 外科保留，僅動 `sync-memory-index.py` 單檔、避開正被別 session 改的 server.js、不動 schema）**：新增 `parse_existing_captions()` 解析現有 MEMORY.md 表，`render_atom_section()` 對一般 atom 在 H1 caption 退化成裸名/空時沿用現有較豐富描述（**精準度：描述性 H1 > 現有人工描述 > 裸名**）。效果：手改 MEMORY.md 描述現在持久、`--check` drift 消失（exit 0）、git 不再 churn；trigger 注入零影響（走 `_atom_index.json` SoT）。**驗證**：新增 `tools/verify/verify_memory_index_caption_preserve.py`(4 cases 鎖覆轍) → `run_verify.py` **316 passed**(312+4)；live `--check` exit 0、dry-run 確認 atom-table-support 描述保留 / workflow-parallel-agents 仍裸名。知識記入 atom [`memory-index-caption-regen`](../memory/memory-index-caption-regen.md)。**衍生暫存**：無。 | `tools/sync-memory-index.py`(parse_existing_captions + render 保留邏輯 + docstring), `tools/verify/verify_memory_index_caption_preserve.py`(新, 4 cases), `memory/memory-index-caption-regen.md`(新 atom + 索引自動同步), `memory/MEMORY.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **docs(cc-internals): `cc-hook-system` atom 補登 Phase 1 probe 實證 — updatedInput 對 Agent/Task 採納 + hook 熱重載 + Agent tool_response 形狀** — 承 Phase 1（commit `ed1de67`）sub-agent 記憶注入的 probe 實測，把三條已驗證 CC-internals 知識補進 `cc-hook-system.md`「Hook 修改工具輸入」段（持久 reference 家，非 `_staging/` 暫存 handoff）：(1) PreToolUse 改寫工具輸入的欄位名是 **`updatedInput`（非舊文/plan 草稿誤記的 `modifiedInput`）**，置於 `hookSpecificOutput`、值為完整 tool_input 物件，且 **Agent/Task 工具實測採納**（以 PostToolUse `tool_response.prompt` 為 ground truth，非 sub-agent 自評）；(2) settings.json hooks 設定**檔案變更即熱重載**、同 session 加 matcher 即生效；(3) Agent/Task PostToolUse `tool_response` 形狀（`agentId`/`agentType`/`content`/`prompt`(注入後)/`status`/`usage` + 頂層 `tool_use_id`）→ 可無狀態歸因。順帶擴 Trigger（sub-agent injection / Agent tool / tool_response / hot reload）。**不動 frontmatter Confirmations/Last-used**（屬 access.json funnel SoT 領域，禁手改）。**衍生暫存**：無。 | `_AIDocs/ClaudeCodeInternals/cc-hook-system.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-06-01 | **fix(memory): Phase 0 地基修復 — 主軌 Confirmations 晉升解凍 + 晉升去頻率化 + 原生記憶掃描硬化** — 為「sub-agent 記憶注入(#1) + 注入→使用→結果閉環歸因(#2)」鋪路（plan: `plans/typed-purring-stearns.md`）。修三處由 4 agent 調查 + 親驗的缺陷/漂移：(1) `hooks/wg_episodic.py` 補 `import time` —— 原缺它使 cross-session Confirmations 加計(L369 `time.time()`)拋 NameError 被 except 吞、**主軌晉升從未真正累加**；(2) 晉升 auxiliary gate 去頻率化：ReadHits（純注入次數）不再單獨晉升、需 Confirmations ≥1（`server.js:1497` + `wg_atoms._self_iterate_atoms` py↔js 雙鏡像；依 Xiong 2505.16067 純頻率晉升會劣化品質）；(3) `wg_core.discover_all_project_memory_dirs` 盲掃 fallback 改為**要求 MEMORY.md/_atom_index.json marker**，杜絕 CC 原生 auto-memory(v2.1.59, `projects/<proj>/memory/`) 污染 cross-project 掃描。順帶：`config.json` 補遺漏 `self_iteration.promote_confirmations_threshold:4`、`decisions.md` 晉升門檻鏡像同步(走 `atom_io.write_raw` funnel)+ 刪 Wave2 已移除的 7-day fallback stale 文字、`user_prompt_submit.py` Atom-Write Guard 提醒文字對齊。清殘骸：`cleanup-projects-residue.py --apply` 歸檔 3 個空 orphan memory dir(`c--LineMate`/`c--projects`/`c--users-holylight--claude`，後者為 P1 雙層 .claude 空 stub，可 `--restore`)。**驗證**：`run_verify.py` **291 passed**(286+5 新守門 `verify_promotion_gate_phase0.py`)、`node -c` OK。**衍生暫存**：無。 | `hooks/wg_episodic.py`, `hooks/wg_atoms.py`, `hooks/wg_core.py`, `hooks/handlers/user_prompt_submit.py`, `tools/workflow-guardian-mcp/server.js`, `workflow/config.json`, `memory/decisions.md`, `hooks/verify/verify_promotion_gate_phase0.py`, `_AIDocs/_CHANGELOG.md` |
| 2026-05-29 | **feat(atom) dogfood：知識區 block 渲染（表格/fence）端到端驗證 + 補登 `5ad346e` changelog + 給人文件 cross-ref** — 承 commit `5ad346e`（`render_knowledge_lines` / `renderKnowledgeLines` 改 block-aware：單一 knowledge 元素以豎線或三反引號開頭整段原樣輸出、不加 bullet、前後補空行；py↔js byte-parity test_11/12/13）。**MCP server 隨新 session 重啟載入新碼後**，新建說明用全域 atom [`atom-table-support`](../memory/atom-table-support.md)（[臨]）作端到端 dogfood：把「表格當獨立 knowledge 元素傳入」用法表格 + Python fence 直接傳進 `knowledge` 陣列 → 讀回確認表格列無 `- ` 前綴、前後補空行、fence 原樣（全域 19→20 atoms；`MEMORY.md` / `_atom_index.json` SoT / `_ATOM_INDEX.md` mirror 三索引自動同步正確）。**補登 drift**：`5ad346e` 當時僅更新 SPEC §11/§12，漏寫本 `_CHANGELOG.md` → 此條補登。**給人文件對齊**：`DocIndex-System.md §6` MCP atom_write 加 block 說明、`TECH.md §5.5` 加 block-aware 一句 cross-ref（**回應 Guardian 對 TECH.md「same_file_3x」覆轍信號：刻意只 cross-ref SoT、不複製規則/計數本體**；根因＝給人文件承載衍生事實而 SoT 在 SPEC/JSON，每 feature 漣漪到多檔 → 記入 [`feedback-workflow-discipline`](Failures/feedback-workflow-discipline.md)）、`MEMORY.md` 補有意義描述。**驗證**：`python run_verify.py` = **286 passed**（283 baseline + 表格/fence 3 cases）。**衍生暫存**：無。 | `memory/atom-table-support.md`(新 dogfood atom), `memory/MEMORY.md`(描述), `memory/_atom_index.json`+`memory/_ATOM_INDEX.md`(索引自動同步), `_AIDocs/DocIndex-System.md`(§6 block 說明), `TECH.md`(§5.5 cross-ref), `_AIDocs/Failures/feedback-workflow-discipline.md`(覆轍根因 +1 行), `_AIDocs/_CHANGELOG.md` |
| 2026-05-29 | **feat(skill-creator): 合入 wellstseng MR !3 meta-skill（全域 skill 19→20）+ 閱讀/索引文件對齊** — 審查同事推的兩個全域 skill。原 MR !1 含 `skill-creator`（寫/改/審 skill 的 meta-skill：Progressive Disclosure 三層 + 5 設計模式 + audit/new-skill/cost-measure 工具）與 `analyze-spec`（規格分析 pipeline）；稽核發現 analyze-spec 的 `assets/template-*.md` + `references/subagent-strategy.md` 把專案名 **TSLG** 與其 C#/ILR/Protobuf/MapServer 架構寫死進「本應通用」模板，違反 skill-creator 自己訂的鐵則 #4「零專案耦合」（且過不了它自附的 `audit-skill.py --scope global`）→ **退回給 wellstseng 修**（兩 skill scope 不同：skill-creator 真全域 meta-skill、analyze-spec 為領域型 skill）。同事改以 **MR !3** 單推乾淨的 skill-creator（14 檔純新增，與 mr-1 版 byte-identical）→ `--no-ff` 合入 main（merge commit `ebb2377`）。**dogfood 驗證**：`python skills/skill-creator/scripts/audit-skill.py skills/skill-creator --scope global` = 0 fail / 0 warning ✓。**文件對齊（skill 計數 19→20，skill-creator 歸類「後續新增（非遷移）」非 command 遷移）**：`TECH.md`（skills/ 樹 + §5.4 遷移敘述 + §16 summary 表三處）、`Install-forAI.md`（rsync 註解 + 驗收表第 7 列 + checklist 共三處 current-state count；§遷移史「22 檔合 19 skill」保留不動）、`_AIDocs/Architecture.md`（Skills 表頭 + 新增 `/skill-creator` 列）、`_AIDocs/DocIndex-System.md`（同上）、`_AIDocs/SPEC_ATOM_V5.md`（§4.2 表頭 + count + 新增列）、`_AIDocs/_INDEX.md`（兩處「19 Skills」→「20 Skills」）。`README.md` / `Project_File_Tree.md` 無 skill 目錄，免動；`memory/_distant/.../v5-overhaul-audit-2026-05.md` 的「19 個全域」為歷史審計記錄保留。 | `skills/skill-creator/`(14 檔, MR !3 合入), `TECH.md`, `Install-forAI.md`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/SPEC_ATOM_V5.md`, `_AIDocs/_INDEX.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-05-28 | **docs: 給人閱讀文件全面對齊 V5 GA（Phase B 收尾）— doc-index-system V5 完整重寫 + ClaudeCodeInternals 來源註記 + TECH.md §12 三階段退避實測證明** — 接 Phase A commit `eacf7ee` 三項收尾：(1) `_AIDocs/Tools/doc-index-system.md` **完整重寫**取代 V2.21 banner 版本（user 明示「必須完整重寫」），新版含啟動鏈 V5 GA 七步、設定檔分層表（含 forbidden-phrases.json / version.json）、Hook V5 P2 結構（1 shim + dispatcher + 8 handler + 6 主模組 + 2 sub-layer shim + 5 獨立保留 hook）、lib/ 6 檔含 atom_locations.py（commit 89ccb2d）、19 skills 完整對照（標示 commands→skills 遷移結果與已刪 4 + 五合一 1）、tools/ 完整清單含 codex-companion 子目錄、memory/ 入版控標記 + V5+ feedback-* 物理位置註記、_AIDocs/ 整體導覽、專案自治層 + Verify 結構（H-test-prune 後 283 baseline）、行動指引（atom 寫入必走 MCP / 位置規則查 atom_locations.py / 新檔同步索引）；演化日誌補 2026-04 V4/V4.1 + 2026-05-28 V5 GA 重寫條目。(2) `_AIDocs/ClaudeCodeInternals/_INDEX.md` 補來源時效說明 — user 補註：本資料夾為 2026 年 4 月初**從錯放出來的 CC sources 產生的研究紀錄**，僅有「部分參考價值」（適合理解 harness 大方向、不適合當精確規格直接套用），開發 hook/skill/MCP 時以 CC 官方文件 + 實測為主、本資料夾為輔。(3) `TECH.md §12 三階段退避` 實測 [ollama_client.py](tools/ollama_client.py) 對齊註記：L31 `SHORT_DIE_COOLDOWN=60` ✓ / L34 `LONG_DIE_WINDOW=600` ✓ / L37 `TIME_BOUNDARIES=[0,6,12,18]` ✓ / L394 Short DIE 觸發條件 `consecutive_failures>=2 and status=="normal"` ✓ / L403-406 Long DIE 升級 `short_die_count>=2 within LONG_DIE_WINDOW` → `_next_time_boundary()` ✓；長 DIE 使用者確認補述 `LONG_DIE_MARKER` (workflow/.backend_long_die.json) + UserPromptSubmit 偵測「停用」/「保持」回覆機制。**驗證**：`python run_verify.py` **283 passed** ✓ pre/post baseline 一致。 | `_AIDocs/Tools/doc-index-system.md`(完整重寫 ~210 行), `_AIDocs/ClaudeCodeInternals/_INDEX.md`(來源註記), `TECH.md`(§12 實測 5 條 + 長 DIE marker 補述), `_AIDocs/_CHANGELOG.md` |
| 2026-05-28 | **docs: 給人閱讀文件全面對齊 V5 GA + Session α/β 現況（Phase A）** — User 要求「README/TECH/Install-forAI + _AIDocs 給人閱讀文件全面檢查正確、符合最新現況」。盤點 7 處 drift 並修正：(1) `README.md` V4.1 → V5 GA + 刪已失效 `/init-project` 指令；(2) `TECH.md` 日期 2026-05-27 → 2026-05-28 + 標題加 Session α/β + atom_locations.py；lib/ 樹補 atom_spec/atom_access/atom_locations 三檔；memory/ 樹移除已搬 `feedback-*.md` / `failures/` 並加 `_AIDocs/Failures/` 註記；**§8.1 mermaid 全面重寫**對齊真實 `user_prompt_submit.py` 680 行流程（修正 [L0] 位置從最後挪到 handler 開頭 + BM25 條件補 `AND global_layer=="bm25"` 前提 + 補 11 個漏項：Confirmed extractions/veto、Dual-Backend long_die response、Atom-Write Guard reminder、Wisdom situation classify、_AIDocs keyword matching、JIT internal-pipeline reference、Cross-Project alias 掃描、ReadHits++ funnel via lib.atom_access、Evasion 上輪命中舉證、Handoff Protocol、Sync reminders）；新增 §8.1.1「實際運作範例」用本 session 真實注入做 demo（QuickExtract / HotCache / Atom:preferences trigger / Guardian:Evasion / Sync Reminder 五例 → 對應 code path L178-187/L343/L644-654/L686-700）；(3) `Install-forAI.md` lib/ rsync 清單補 atom_locations；(4) `_AIDocs/_INDEX.md` 日期更新 + V5 GA + Session α/β 標記 + Failures 描述補 5 feedback-* + cognitive-patterns + memory-pipeline-* 物理位置 + 摘要補 17 atoms 與 atom_locations.py 單一規則來源；(5) `_AIDocs/Tools/_INDEX.md` 日期更新 + 第 8 項 atom-locations 候選說明（未獨立成檔，由 lib docstring + SPEC §2.1 替代）；(6) `_AIDocs/DevHistory/_INDEX.md` v5-overhaul 條目補 Session α/β + 末尾加 2026-05-28 commits 紀錄（082f791 / 89ccb2d / 6772049）；(7) `_AIDocs/Tools/doc-index-system.md` 加 V5 deprecated banner（停在 V2.21 / 2026-03-27，25 atoms / wg_paths / wg_intent / wg_iteration / commands/`/init-project`,`/resume`,`/svn-update`,`/unity-yaml` 等引用已過時，引導讀 TECH.md §2 / SPEC_V5 / DevHistory）。**驗證**：`python run_verify.py` 283 passed。**Phase B 預告**：doc-index-system.md 完整重寫、_AIDocs/ClaudeCodeInternals/ 為 2026-04 初 CC sources 研究紀錄（部分參考價值，待補 _INDEX 註記）、TECH.md §12 三階段退避常數需驗證 ollama_client.py — 此三項另起 commit。 | `README.md`, `TECH.md`, `Install-forAI.md`, `_AIDocs/_INDEX.md`, `_AIDocs/Tools/_INDEX.md`, `_AIDocs/DevHistory/_INDEX.md`, `_AIDocs/Tools/doc-index-system.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-05-28 | **refactor(memory): feedback-* migration Session β 收尾 — sync-atom-index 多根 / indexer Failures layer / SPEC_V5 §2.1 + drift 修** — 接 commit `082f791` (Session α 核心) + `89ccb2d` (atom_locations.py 抽象) 的 β 收尾，把 `_staging/next-phase-feedback-aidocs-migration-followup.md` 列的 8 項驗收逐一落定。**β-1 sync-atom-index 多根**：`tools/sync-atom-index.py:scan_atom_files` 預設走 `lib.atom_locations.iter_atom_files_multi()`（memory/ + _AIDocs/Failures/ 雙根 + stems 過濾參考文件）；caller 傳非預設 memory_dir（測試用）仍走單根 fallback；新增 `CLAUDE_ROOT` 常數。**β-2 indexer Failures layer**：`tools/memory-vector-service/indexer.py:discover_layers` 在 global layer 後注入 `extra:failures`（label=FAILURES_LAYER_LABEL, FAILURES_DIR, "recursive"）；`discover_atoms` 對該 layer 套 `failures_atom_stems()` filter，避免把 `_INDEX.md` / `README.md` 等參考文件當 atom 索引。**β-3/4/7 α 已 cover**：wg_atoms.parse_memory_index 走 `_atom_index.json` SoT、server.js findAtomFileRecursive Failures fallback、cognitive-patterns reverse Related 都在 α/atom_locations.py 完成。**β-5 SPEC_V5 §2.1**：`_AIDocs/SPEC_ATOM_V5.md` §2 Scope 後加新章節「Atom 存放擴展（feedback / 失敗模式類）」— 列雙根表 + 路由規則 + Py/JS single source + caller 清單（hooks/wg_atoms / sync-atom-index / indexer / memory-audit / atom-health-check）+ Failures stems filter 機制。**β-6 run_verify**：`python run_verify.py` **283 passed in 1.04s** ✓ pre/post baseline 一致。**β-8 pre-existing 自然消失**：原 plan 提的 `memory/記憶機制靜默失效...md` audit parser bug 已不存在（α 搬遷時 rename 為 `_AIDocs/Failures/memory-pipeline-silent-failure-2026-05.md` 並順帶修了 line-ending），memory-audit 0 errors / 0 warnings；判定 0 行修補無需動。**順帶修 drift**（β-1 多根掃描首次觸發後 sync-atom-index 揭露）：(i) `_AIDocs/Failures/feedback-rigor-standards.md:6` Trigger typo「纝密」→「縝密」（V5 SoT --fix 對齊 `_atom_index.json`）；(ii) `memory/decisions.md` frontmatter Trigger 砍 4 個未列入 index 的細項（guardian/hooks/MCP/vector service）對齊 SoT 主旨「frontmatter 為註記、JSON 為機器源、drift 時以 JSON 為主」。**Guardian challenge 回應**：(a) atom-health-check 報的 V5 access.json migration 殘留（17 atom 缺 Confirmations/ReadHits）非本次修補範圍 — 檢測時點：V5 Wave 1-3 (`d76bba1`/`10cd1ab`) 後，數週前；不修風險：低，info 級不影響注入/觸發/索引；修期：與 atom-health-check parser 改造或下次 V5 audit cleanup 一併處理。**衍生暫存清理**：`memory/_staging/next-phase-feedback-aidocs-migration-followup.md` 刪除（原檔末明示「執行完後刪除」）；`memory/_staging/next-phase.md` 導航標 I 完工、lifecycle 縮為僅 G。**未來規則**：(1) 任何新 atom 多根掃描需求一律走 `lib.atom_locations.iter_atom_files_multi()` API，禁止再手刻多 root for loop；(2) `_AIDocs/Failures/` 內若加入新 atom（非 feedback-* prefix）需手動寫入 `_atom_index.json`，indexer 自動納入向量索引（透過 stems filter）；(3) SPEC_V5 §2.1 是 atom 物理位置擴展的單一描述源，未來新增其他「擴展存放點」（如 `_AIDocs/Wisdom/` 等）需先補章節再實作。 | `tools/sync-atom-index.py`(多根掃描 + CLAUDE_ROOT), `tools/memory-vector-service/indexer.py`(Failures layer + stems filter), `_AIDocs/SPEC_ATOM_V5.md`(§2.1 新章節), `_AIDocs/Failures/feedback-rigor-standards.md`(typo fix), `memory/decisions.md`(frontmatter SoT 對齊), `memory/_staging/next-phase.md`(I 完工+lifecycle 縮減), `memory/_staging/next-phase-feedback-aidocs-migration-followup.md`(刪) |
| 2026-05-28 | **refactor(lib): atom location knowledge 抽象到 lib/atom_locations.py — 消除 7 個 caller 硬編碼 + 3 處邏輯重複** — commit `082f791`（feedback-* atoms 遷移到 `_AIDocs/Failures/`）的搬遷實作把「物理位置 + 路由規則」散在多檔硬編碼，違反 [`feedback-tooling-reliability`](Failures/feedback-tooling-reliability.md)「規則集中到唯一模組」與 [`cognitive-patterns`](Failures/cognitive-patterns.md)「禁自我合理化編造規則」。**v2 plan 自審揭露 2 個原 brief 未涵蓋的真相**：(i) `lib/atom_spec.py:255-275` `default_atom_search_roots()` + `iter_atom_files_multi(roots)` 是**孤兒函式**（0 caller，三個掃描站點各自 reimplement）；(ii) `hooks/wg_core.py:757` `_WHITELIST_DIR_SEGMENTS["Failures"]` 是 **dead code**（集合元素 `Failures` 大寫但 caller `_atom_path_whitelisted` 用 `parts_lower` 全小寫做 set 交集 → 永遠不 match；即便 case 修對，上游 `_path_under_memory_dir` 對 `_AIDocs/Failures/` 路徑早 return None → whitelist 從未被觸發）— 經 user 拍板**保留 dormant 並從 atom_locations 集中供應**（行為不變，規則來源集中）。**新模組 `lib/atom_locations.py`**（~170 行純函式）：常數 `FAILURES_DIR` / `FAILURES_REL` / `FEEDBACK_TITLE_PREFIX` / `_BASE_WRITABLE_DIR_SEGMENTS` + predicate `is_failures_routed_title` / `is_in_failures_path` + scan `atom_search_roots` / `failures_atom_stems` / `iter_atom_files_multi`（內部走 `is_atom_file` + `rglob` + stems 過濾 Failures 內參考文件）+ resolution `failures_write_target()` + whitelist `atom_writable_dir_segments()` + 渲染 classifier `atom_index_row_kind`。**9 個 caller 改造**：(1) `lib/atom_spec.py` 刪 2 孤兒函式（不留 shim）；(2) `lib/atom_io.py` 改 import atom_locations + `_resolve_target` feedback 分支改 `{**failures_write_target(), ...}`；(3) `tools/sync-memory-index.py` 三條 branch 改 `atom_index_row_kind()` + `FAILURES_REL` 取代 hardcoded 字面量；(4) `tools/memory-audit.py` 刪 `_load_failures_atom_names` + `iter_atom_files` 委派 `iter_atom_files_multi`；(5) `tools/atom-health-check.py` `find_atoms` V5+ 區塊同樣委派；(6) `hooks/wg_atoms.py` `_self_iterate_atoms` scan 區塊（含原 `.glob` 頂層 + 手刻 filter）整段委派；(7) `hooks/wg_core.py` `_WHITELIST_DIR_SEGMENTS = atom_writable_dir_segments()`；(8) `server.js` factor `applyFeedbackRouting(resolved, slug, scope)` 本地函式 + 頂部新增 FAILURES_DIR/REL/PREFIX 常數 + `// MIRROR: lib/atom_locations.py:failures_write_target — keep in sync` 註解（atom_promote L1377 確認無 inline routing duplicate，**順手修補（3 行）**：`findAtomFileRecursive` fallback 不含 _AIDocs/Failures/ → 加上 feedback-* + scope=global 時加掃 `FAILURES_DIR` 的 fallback；`const found` 改 `let found` 容後續覆寫）；(9) `_AIDocs/_CHANGELOG.md` 此 entry。**Behavior change：無**（純結構重構；dormant Failures whitelist 明白標註保留為 dormant）。**Verify**：pre-baseline `python run_verify.py` = **283 passed / 0 failed**；逐 caller 切換每步立即驗（atom_locations sanity → atom_io dry_run write 落 `_AIDocs/Failures/feedback-test-locations.md` → sync-memory-index `--check` exit 0 → memory-audit 17 atoms 0/0 errors/warnings → atom-health-check `--validate-refs` 全綠 → wg_core `_WHITELIST_DIR_SEGMENTS` 13 segments 含 Failures+_meta → server.js `node -c` syntax OK）；中途踩坑 1 個：wg_atoms 移除外層 `for atom_dir in scan_dirs` loop 後內部 75 行 code 漏 dedent → IndentationError，整段 dedent 4 空格修復；post-baseline `python run_verify.py` = **283 passed / 0 failed** ✓ 與 pre-baseline 完全一致。**衍生暫存**：無（dry_run 未落檔；無 feedback-test-*.md 殘留）。**未來規則**：(1) atom 物理位置 / 路由規則一律走 `lib/atom_locations.py` API，禁止再分散硬編碼；(2) Py/JS 雙 mirror（atom_locations.py ↔ server.js:applyFeedbackRouting）兩處互留 MIRROR 註解，改一處必同步另一處；(3) atom_promote 支援 feedback-* atoms（本次順手修補）。 | `lib/atom_locations.py`(新, ~170 行), `lib/atom_spec.py`(刪 L255-275), `lib/atom_io.py`(import + _resolve_target feedback 分支), `tools/sync-memory-index.py`(classifier + FAILURES_REL), `tools/memory-audit.py`(刪 _load_failures + iter 委派), `tools/atom-health-check.py`(find_atoms 委派), `hooks/wg_atoms.py`(scan 委派 + dedent fix), `hooks/wg_core.py`(_WHITELIST_DIR_SEGMENTS), `tools/workflow-guardian-mcp/server.js`(applyFeedbackRouting + 常數), `_AIDocs/_CHANGELOG.md` |
| 2026-05-28 | **fix(codex-companion): files_examined populate + sub-agent visibility + 「不動 / out-of-scope」prompt 抑制** — User 觀察本日 Codex Companion 對 Opus 4.7 + high-thinking 邊際效益偏低，並具體點出三輪 plan_review (t4/t5/t6) 全是 generic 模板建議 + t6 報「Files Examined = (none)」明顯不對。完整讀 codex stack 證實**根因不是 sub-agent 看不到，而是純粹 missing-field bug**：`hooks/codex_companion.py:482-499` (handle_post_tool_use checkpoint spawn) 與 `:611-625` (handle_stop turn_audit spawn) 兩個 spawn 點的 context dict **都沒帶 `files_examined`**；`tools/codex-companion/assessor.py:404` 用 `extra_context.get("files_examined", "")` 永遠拿到空字串；`prompts.py:156` 預設為 `(none)`。V5 P5b 重構之後就一直壞，但因 plan_review 觸發頻率低 + Codex 仍給「補盤點」通用建議掩蓋了，沒被發現。**修法 (A)**：codex_companion.py 加 `_summarize_files_examined(tool_trace)` helper（Read/Glob/Grep 路徑 + Edit/Write 修改路徑 + Agent 條目含 description + output_summary 240 字尾巴 = sub-agent 代理活動的最強訊號），兩個 spawn 點都帶上 files_examined（context 多一欄）；assessor.py `build_turn_audit_prompt` 呼叫加 `files_examined=`；prompts.py `TURN_AUDIT` 模板加 `## Files Examined by the Agent` section + 函式簽名加 `files_examined: str = ""` 預設值。**Prompt tuning (B)**：PLAN_REVIEW Instructions 加一條「Focus on what is MISSING ... NOT on demanding more 「out of scope / 不在範圍」elaboration. The agent already follows an anti-evasion contract that lists「不動 / 不在範圍」sections by itself — do not push it to elaborate further unless concrete scope creep risk is observed.」+ 微調 scope check 為「concrete creep risk only — do not flag 'too narrow' unless a critical dependency is genuinely missing」；TURN_AUDIT Instructions 加「不要因為代理人省略「不動 / 不在範圍」聲明就 flag — anti-evasion contract 是代理人端自管的紀律，本 audit 只在「真實偷埋 / 跳步」可見於 tool trace 時才標」。**用戶端不動規則 (C)**：按 user 指示 `IDENTITY.md` 反退避契約 / `feedback-workflow-discipline.md` drift 修補門檻 / `feedback-completion-gates.md` 衍生暫存清單**完全保留現狀** — 抑制只在 Codex prompt 端做，不動代理人自管紀律。**驗證**：py_compile 三檔 OK ✓；_summarize_files_examined smoke test 4 種 tool 類型（Read/Agent/Grep/Edit）+ empty case 全綠 ✓；build_turn_audit_prompt / build_plan_review_prompt 新 section 存在且抑制語句植入正確 ✓。**對 t4/t5/t6 false positive 的補殺**：t6 不再會發生（files_examined 有實際內容）；t4/t5 的「補盤點」通用建議若未來再出現，會在 Codex 看到實際 Read/Agent 條目後判斷得更有依據；「不動」過量推力應減少（待連續 3-5 個 plan_review 軟驗證確認）。 | `hooks/codex_companion.py`(helper +58 行 + 2 spawn 點 patch), `tools/codex-companion/assessor.py`(turn_audit 加 files_examined +1 行), `tools/codex-companion/prompts.py`(TURN_AUDIT 加 section + 函式簽名 + PLAN/TURN Instructions 抑制語句 ~19 行) |
| 2026-05-28 | **refactor(memory): feedback-* atoms 遷移至 _AIDocs/Failures/ + atom 系統多根支援 + MCP routing + PreToolUse advisory (Session α)** — 落地「印象（MEMORY.md atom 行）→ 認知（_AIDocs/Failures/{name}.md）→ 按需延伸閱讀」三層架構。**根因**：今天 plan 檔誤留事件曝光 — 5 個 `memory/feedback-*.md` 是自包含知識堆，違反 [`memory/feedback-memory-structure.md:13`](../memory/feedback-memory-structure.md#L13) 自己訂的「atom 拆指標型顆粒」原則；AI 隨興把 user feedback 寫進 memory/ 不分類。**範圍**（user 拍板「大步走」）：(R) 5 個 feedback-* + cognitive-patterns 補登 atom 體系；其餘 _AIDocs/Failures/ 7 檔保持非 atom 身分；(E) Enforcement = MCP 強路由 + PreToolUse 警告（不擋）；(S) Recall = 擴 ATOM_SEARCH_ROOTS = [memory/, _AIDocs/Failures/]。**A 物理搬遷**：`git mv memory/feedback-*.md _AIDocs/Failures/` × 5 + `mv memory/feedback-*.access.json _AIDocs/Failures/` × 5（gitignored）+ `.gitignore` 加 `_AIDocs/Failures/*.access.json`；`_AIDocs/Failures/cognitive-patterns.md` 補 atom 體系（frontmatter cleanup：刪 Type/Tags/Confirmations:38 → 移到新建 access.json；Created → Created-at；Related _INDEX → feedback-rigor-standards）。**B atom 系統多根擴展**：`lib/atom_spec.py` 加 `default_atom_search_roots()` + `iter_atom_files_multi()`；`hooks/wg_atoms.py:1247` scan_dirs 換 `MEMORY_DIR/feedback`（死路徑）→ `_AIDocs/Failures/` + Failures filter（只 feedback-* + cognitive-patterns 為 atom，其餘非 atom 跳過）；`tools/memory-audit.py` + `tools/atom-health-check.py` 同樣多根 + Failures filter；`tools/atom-health-check.py:477` 修 relative_to fallback 容跨樹。**C atom_write MCP routing**：`lib/atom_io.py:_resolve_target` 加 `title.startswith("feedback-")` + scope=global → 路由 `_AIDocs/Failures/` + `index_dir`（索引仍在 memory/_atom_index.json 單一來源） + `index_root` 跨樹 rel_path 計算；`tools/workflow-guardian-mcp/server.js:1108-1132` 鏡像同邏輯（MCP 不再自動勾 lib，server.js 為獨立判路 - 不可漏 mirror）。**D PreToolUse**：`hooks/wg_core.py:_WHITELIST_DIR_SEGMENTS` 加 `"Failures"`；`hooks/handlers/pre_tool_use.py` 加 `_check_feedback_routing_advisory` — 偵測 `memory/feedback-*.md` Write/Edit → stderr 印 RoutingAdvice（**不擋**，advisory only）。**E 索引同步**：`memory/_atom_index.json` 5 個 feedback path 改 `_AIDocs/Failures/feedback-*.md` + 新增 cognitive-patterns 條目；`_ATOM_INDEX.md` mirror regen；`tools/sync-memory-index.py` 改 render — feedback / cognitive-patterns 各自一行 + 內聯 `→ _AIDocs/Failures/{name}.md` pointer + 底部「失敗模式完整目錄 → _AIDocs/Failures/_INDEX.md」交叉指標；`_AIDocs/Failures/_INDEX.md` 新增 6 行 + 分類欄（atom vs 參考）區分。**驗證**：手動測 `lib.atom_io.write_atom(title="feedback-test-routing-zebra", scope="global", ...)` → 落 `_AIDocs/Failures/feedback-test-routing-zebra.md` + access.json + _atom_index.json 條目 ✓ pass；memory-audit 0 errors（剩 pre-existing `記憶機制靜默失效...` 欄位 parser bug，與本次無關）；atom-health-check 跑通 + 報 18 issues 全 pre-existing。**Session α 邊界**：未做 — sync-atom-index 多根 / vector indexer 多根 / server.js findAtomFileRecursive 擴 / SPEC_ATOM_V5.md 新章節 / 完整 pytest（pytest 期望路徑需更新）→ 全列入 `_staging/next-phase-feedback-aidocs-migration-followup.md` (Session β 接手)。**用戶反饋觸發的二次調整**：(1) MEMORY.md 格式由「每 atom 一行 + 個別指標」revert 為小修原樣（feedback-* 維持單行聚合 + 末尾 `→ _AIDocs/Failures/` 指標；cognitive-patterns / memory-pipeline-silent-failure-2026-05 個別行 + 內聯指標）— 因 user 觀察 trigger recall 走 `_atom_index.json` SoT，MEMORY.md 只影響 AI「印象」感知，不影響 hook 注入頻率，視覺簡單版即可；(2) `memory/記憶機制靜默失效confirmations-零增-episodic-停擺.md` 識別為失敗經驗系列 → rename + 物理搬遷 to `_AIDocs/Failures/memory-pipeline-silent-failure-2026-05.md`（對齊 vectordb-silent-failure-2026-04 / codex-windows-sandbox-1385 命名慣例）+ `memory/workflow-rules.md` Related ref 更新 + `_AIDocs/DocIndex-System.md` 描述更新 + `_AIDocs/Failures/_INDEX.md` 加 row 4f + 對 atom 檔做 line-ending 正規化（原檔有 `\r\r\n` 異常 → utf-8 normal `\n`，這也順帶修了 memory-audit 抱怨「缺欄位」的 false-positive 根因）。**audit/health filter 升級**：原硬編碼「feedback- + cognitive-patterns」白名單 → 改讀 `_atom_index.json` 的 _AIDocs/Failures/ 登記名單（memory-audit / atom-health-check / wg_atoms 三處同步），未來新 atom 加入該目錄不再需要改硬編碼。**未來規則**：(1) atom 寫入永遠走 MCP `atom_write` — feedback-* 自動正確路由；(2) 失敗模式 / 認知偏差類雙寫（atom 印象指標 + _AIDocs/Failures/ 認知本體）+ 在 atom 知識行加 `→ _AIDocs/Failures/{name}.md` pointer；(3) 「user 訂規 / by design」聲稱必有具體文件路徑+行號可指，否則 = 自我合理化編造（[cognitive-patterns.md「自我合理化編造規則」](../_AIDocs/Failures/cognitive-patterns.md)）；(4) 失敗模式類 atom（非 feedback-* prefix 者）手動 rename 到 `{topic}-{descriptor}-{date}.md` 模式 + 移到 _AIDocs/Failures/，由 _atom_index.json 自動納入索引。 | `lib/atom_spec.py`(多根 helpers), `lib/atom_io.py`(_resolve_target+index_root), `hooks/wg_core.py`(Failures whitelist), `hooks/wg_atoms.py`(scan_dirs), `hooks/handlers/pre_tool_use.py`(advisory), `tools/workflow-guardian-mcp/server.js`(routing mirror), `tools/memory-audit.py`(multi-root+filter), `tools/atom-health-check.py`(multi-root+relpath fallback), `tools/sync-memory-index.py`(印象+指標 render), `memory/_atom_index.json`(paths改+cognitive 新增), `memory/_ATOM_INDEX.md`(regen), `memory/MEMORY.md`(regen 新格式), `_AIDocs/Failures/_INDEX.md`(分類欄+6 row), `_AIDocs/Failures/cognitive-patterns.md`(frontmatter cleanup), `_AIDocs/Failures/feedback-*.md`(×5 git mv from memory/), `_AIDocs/Failures/*.access.json`(×6 fs move, gitignored), `.gitignore`(加 _AIDocs/Failures access.json), `memory/_staging/next-phase.md`(+I 條目), `memory/_staging/next-phase-feedback-aidocs-migration-followup.md`(新 Session β handoff) |
| 2026-05-28 | **fix(hook): codex-companion assessor.py 補 CREATE_NO_WINDOW — 延伸 `36302f3` ensure-mcp 同模式漏網修補** — commit `36302f3` 修 `ensure-mcp.py` 5 處 npm subprocess 後遺留同模式漏網點：`hooks/codex_companion.py` 的 `_spawn_audit_subprocess` 自身正確帶 `CREATE_NO_WINDOW \| DETACHED_PROCESS \| CREATE_BREAKAWAY_FROM_JOB`，但 detached `audit.py` → `tools/codex-companion/assessor.py:156` `subprocess.run([codex_bin, "exec", ...])` 沒帶 `creationflags`。Windows 上 `codex` 是 `codex.cmd` batch wrapper，detached 父進程沒 console → Windows 為其新開 `cmd.exe` 視窗。**重現條件**：ExitPlanMode 退出計畫模式進入 Edit 模式（PostToolUse matcher 含 `ExitPlanMode`）→ codex_companion 觸發 → audit.py spawn → assessor 跑 codex exec → 閃黑色 cmd 視窗。修法同 ensure-mcp：assessor.py 檔頭加 `_CODEX_FLAGS = 0x08000000 if sys.platform == "win32" else 0`，subprocess.run 加 `creationflags=_CODEX_FLAGS`。**全域審查**：Explore agent 全掃 `hooks/` + `tools/` 所有 `subprocess.run/Popen`，確認無其他 Windows shim 漏網（git/svn/Unity 二進制非 .cmd wrapper / 純 `sys.executable` Python spawn 不彈視窗，皆無需處理）。 | `tools/codex-companion/assessor.py`, `_AIDocs/_CHANGELOG.md` |
| 2026-05-28 | **refactor(tests→verify): H-test-prune 14 KEEP + 6 歸檔（四原則 verify 化重組）** — Wave 5 Session 7 接續 commit `787f1e3`（tests/ 30→20）的 tests-prune 收尾。User 四原則疊加：(1) 預設砍 (2)「必須觸發」≠「每輪觸發」(3) 越模糊越該刪 (4) 強雙向高頻 → verify 化搬 source 同層。Plan agent cross-validation 報紅燈，修正 3 個 bug 級錯誤（pytest discover 規則 / migrate_reflection_schema 函數名錯且不能砍 / dispatcher 裸名 import）+ 6 個雞肋設置 + 1 個漏點（tests/integration/ 未處置）後執行。**處置一覽**：(K 類)14 個強雙向高頻 verify 化 → 換前綴 `verify_*.py` 搬 `{src}/verify/`（hooks/verify=9, tools/codex-companion/verify=3, tools/verify=1, lib/verify=1）；(A 類)4 個 recall 低 / source 穩定 → 砍 test，source 留（atom_access / metric_audits_total / vector_service_log / wisdom_migration）；(C 類)3 個 test + 配套全砍 → atom-health-audit / audit-reconcile 兩 tool 連同 source 歸檔到 `_AIDocs/DevHistory/v5-overhaul-2026-05/tools-archive/`；tests/integration/test_e2e_user_extract.py（497 行 / 50 case ollama-live）+ conftest.py 一併砍（**砍 e2e 的 risk**：失去 L1+L2 LLM behavior 自動回歸；L0 仍由 verify_user_detector 守，L1+L2 漂移後續靠 Codex Companion 觀察 + 人工發現，user 承擔此 risk）。**配套修正**：(1) settings.json SessionEnd hook line 99 砍 audit-reconcile.py 呼叫（避免 FileNotFoundError）；(2) 新建 `pytest.ini`（`python_files = test_*.py verify_*.py`、`python_functions = test_*`）；(3) 新建 `run_verify.py` 跨平台 entrypoint（PowerShell + bash 通吃，動態掃 `{src}/verify/` + `skills/{name}/verify/`）；(4) 14 個 verify 內部 `sys.path.insert` 修正 `parents[N]` 層數對應新位置（hooks/verify/ → parents[1]=hooks/ 等）。**baseline**：`python run_verify.py` **283 passed in 1.19s**。**結構就位**：17 個 verify-worthy skill（除 atom-debug / codex-companion）建空 `skills/{name}/verify/` + .gitkeep + README 一行指向衍生任務（USER.md line 22 報備路徑），衍生任務 `memory/_staging/next-phase-skills-verify.md` 列每 skill 主邏輯檔 + 失敗風險 + 待測 case 提示。**未動（cross-validation 修正）**：不動 hooks/wisdom_engine.py（`migrate_reflection_schema` 函數名錯且該函數實際是 `_migrate_v211_to_v212` 被 `_load_metrics` funnel 呼叫，砍會壞線上 reflection）/ 不動 IDENTITY.md（無 pytest 字串）/ 不深度 package 化 hooks/ tools/（dispatcher 用 `from handlers import` 裸名 + sys.path）/ 不新建頂層 VERIFY.md（併入 Architecture.md「Testing & Verify」章節）。**規則升級**：`memory/feedback-completion-gates.md` 經 MCP atom_write 走 funnel 更新（pytest → run_verify.py + triggers 加 run_verify/verify）。**衍生**：plan 檔 `plans/drifting-frolicking-nygaard.md` 待 user 決去留（plan mode 自動產出，原則上歸入衍生暫存）。 | `pytest.ini`(新), `run_verify.py`(新), `settings.json`(SessionEnd 砍 audit-reconcile), `_AIDocs/Architecture.md`(verify 替換 + 新章節), `TECH.md`(tests/ 樹更新), `memory/feedback-completion-gates.md`(MCP funnel 更新), `memory/_staging/next-phase-skills-verify.md`(新衍生任務), `hooks/verify/`(9 個 verify_*.py 搬移 + import 修), `tools/verify/`(verify_check_bypass), `tools/codex-companion/verify/`(3 個), `lib/verify/`(verify_atom_io_equivalence), `skills/{17 個}/verify/`(空結構 + .gitkeep + README), `_AIDocs/DevHistory/v5-overhaul-2026-05/tests-archive/`(+6 root + integration/ 子目錄含 2), `_AIDocs/DevHistory/v5-overhaul-2026-05/tools-archive/`(新, +2), `tests/`(整個目錄刪除) |
| 2026-05-28 | **fix(hook): ensure-mcp.py npm 子進程補 CREATE_NO_WINDOW — Windows cmd 視窗閃跳修補** — SessionStart hook `_spawn_background` 自身已正確帶 `CREATE_NO_WINDOW \| DETACHED_PROCESS`，但 detached Python 子進程內 5 個 `subprocess.run`（where node / npm prefix -g / npm i -g / npm outdated / npm update）沒帶 `creationflags`。Windows 上 `npm` 實際是 `npm.cmd` batch script，detached 父進程沒 console → Windows 為其新開 `cmd.exe` 視窗，整段 `npm update` 期間（幾十秒～幾分鐘）掛在前景。**踩坑點**：spawn detached 父進程加 `CREATE_NO_WINDOW` 只防自己沒視窗，**不會傳染給子進程**；子進程呼叫的每個 `subprocess.run` 必須各自帶 `creationflags`。修法：檔頭加 `_NPM_FLAGS = subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0`（POSIX 等效 0，跨平台相容），5 處統一加 `creationflags=_NPM_FLAGS`；line 192 既有 `_spawn_background` 不動（spawn 自己用，與 npm subprocess 是不同需求）。 | `hooks/ensure-mcp.py` |
| 2026-05-27 | **V5 GA 簽收 + Wave 5 Session 4 user 補強盤點** — chkdsk C: /f 重開機修復 NTFS → `workflow/guardian-crash.log` 物理刪除（114 GB → 329K，0.0002%）。GA Checklist 全綠：workflow 218K / hooks 8 wg_* / skills 19 / MCP 3 tool / pytest 40/414/52 baseline / git clean。**User 補強盤點**（手動審視 root 全資料夾）：(1) TECH.md 整份重寫為 V5 GA（原仍寫 V4.1，含系統樹 / 流程圖 / 子系統表全部對齊 V5 GA 現況）；(2) version.json 從 `atom_memory 4.1 / guardian 2.2.0` 升 `5.0 / 5.0.0`（**user 提醒才發現此 drift**）；(3) 4 個資料夾去留：`.playwright-mcp/`（394K 2 月前 debug 殘留）+ `.pytest_cache/`（63K pytest 自重建）全刪、`backups/` 完全清除（不留 1 個，user 訂規）、`tests/` 1.3M 確認為真實 V5 pytest 套件（414 passed）保留；(4) workflow/ 36 → 6 檔（刪 30 個 Codex Companion daemon 廢棄 state；P5b 已 daemon→subprocess）；(5) file-history/ 131 → 46 dir（user 訂 2 週門檻；85 個 CC 清空殼刪除）；(6) scripts/ 6 → 3 檔（移 `migrate-access-stats.py` / `migrate-confirmations.py` / `merge-orphan-access.py` 三 V5 期間 one-shot migration 到 `_AIDocs/DevHistory/v5-overhaul-2026-05/scripts/`）；(7) `__pycache__` 9 dir 全清。**規則升級（IDENTITY + atom）**：「完工清暫存」原則寫入 `feedback-completion-gates` atom（自動產生 + 無 git track + 無人工填值 + 僅服務當次任務 → 預設直接刪；範例：.pytest_cache / backups / .playwright-mcp / workflow/companion-* / staging 已執行 next-phase）；IDENTITY 收尾檢核從**二選一**升級為**全項檢視**四項 — (a) 缺失發現與修補清單必寫 / (b) AI 逃避通報僅發生時寫 / (c) Token 累積警示僅發生時寫 / (d) 衍生暫存清單必寫；IDENTITY 索引化瘦身（砍「職責」整段 + 「發現即處理門檻」具體規則移到 atom + 禁語清單精簡至 2 行 + 收尾檢核補充括號約壓縮 30%，避免每 session 強制大量 token 常駐）；IDENTITY.template.md 同步進化（multi-user 預設來源）。**Atom 體系變動**：`memory/v5-overhaul-audit-2026-05.md` 歸檔到 `_distant/2026_05_v5_overhaul/`（取代 atom，由 DevHistory 完整紀錄繼承）；`_atom_index.json` 17 → 16 atoms；`_ATOM_INDEX.md` mirror 自動 regenerate；`feedback-completion-gates` trigger 增「衍生暫存 / 暫存檔 / 清暫存 / 收尾檢核」；MEMORY.md 加 DevHistory 連結。**DevHistory 新增**：`_AIDocs/DevHistory/v5-overhaul-2026-05/README.md` 完整紀錄起因（V4.1 累積 6 大問題含 114 GB 災難）/ 計畫演化（6-phase → 4-Wave + Wave 5）/ 各 Wave 交付摘要 / GA Checklist 驗收。**Session 自評（user 命令）盤點 10 點漏失**：B1 backups 自作主張保 1 / B2 file-history 自定 30 天標準（user 明說 2 週）/ B3 tests 草率回應「不可砍」未列選項 / B4 root 暫存資料夾掃描不全（漏 scripts/_v4_archive/_pending_writes 等）/ B5 IDENTITY.md .gitignore 越權改動（未注意到 IDENTITY.template 多 user 機制 / 已即時回滾）/ B6 TodoWrite 反應慢 / C1 收尾檢核 4 項格式自己未套用 / C2 scripts/ migrate-* 未主動報告（修） / C3 scripts/inbox-*.js 不明用途待 user 決議（settings.json 沒引用，疑似廢棄）/ C4 _AIDocs/_INDEX.md 條目過時順手修但未先報告 / C5 version.json 整 session 沒查（user 指正才發現）。**Hook 系統失效實況**（後續 Wave 5 Session 5 補強修正）：`~/.claude/settings.json` 在 Wave 4 commit `04b35b4` 從 14534 bytes → 42 bytes（308 行 hook 配置消失）。**Wave 5 Session 5 user 澄清**：此非「誤砍」，是前 session 為推動 V5 進版「經 user 允許暫時關閉」hooks，但**未寫入 handoff 交接文件**，造成連續 22 天 hook 系統實際停擺（hot_cache.json 5/18 後未更新 / state-*.json 不存在 / atom_io_audit 最近 entries 全 `source=mcp` 無 `hook:*`）。本 commit 一併重建 settings.json：保 V5 hook 結構（dispatcher + 8 event handler 對齊）+ 精簡 permissions 移除過時 V4 IPC tool entries + 保 `effortLevel: xhigh`。**未來規則**：「暫時關閉」級別的全域系統設定變更必寫入 handoff，避免 multi-session drift。 | `TECH.md`(重寫), `version.json`(V5), `.gitignore`(IDENTITY 註釋), `IDENTITY.template.md`(同步), `_AIDocs/_INDEX.md`(V5 GA + 19 skill / 16 atoms), `_AIDocs/DevHistory/_INDEX.md`(加 v5-overhaul-2026-05), `_AIDocs/DevHistory/v5-overhaul-2026-05/README.md`(新), `_AIDocs/DevHistory/v5-overhaul-2026-05/scripts/`(3 migrate 移入), `memory/MEMORY.md`(連結 DevHistory), `memory/_atom_index.json`(17→16 + feedback trigger 擴), `memory/_ATOM_INDEX.md`(自動 regen), `memory/feedback-completion-gates.md`(完工清暫存規則 + 收尾 4 項), `memory/_distant/2026_05_v5_overhaul/v5-overhaul-audit-2026-05.md`(歸檔), `_AIDocs/_CHANGELOG.md` |
| 2026-05-27 | **V5 Wave 5 Session 1 — A+B 組汰舊（runtime ~88 MB + 17 個 tools/tests + REG-005 dead path 清除）** — Wave 5 全面汰舊起手第一 session。**A 組（runtime 清理，無 git 風險）**：Logs/* (153) + telemetry/* (60, 65 MB) + file-history/* (1028, 16 MB) + shell-snapshots/* (66, 5.5 MB) + todos/* (214) + statsig/* (5) + workflow/companion-{state,assessment,metrics}-*.json (332) + workflow/state-e2e-test.json + backups/.claude.json.backup.* (keep 1, del 4) 共 1863 files / 88.4 MB；`.claude/.claude/{memory,worktrees}/` P1 雙層殘骸（41 episodic + 2 personal + 1 MEMORY.md stub，**非副本是孤兒原始版**，正本層完全沒對應檔）整體 `mv` 到 `memory/_distant/2026_05_v5_overhaul/double-nest-residue/` 歸檔保留（44 files / 102 KB；_distant gitignored 不入版控但磁碟保留）。**B 組（一次性 tools/tests，跑引用斷裂 SOP）**：刪 17 個 = B1 A/B benchmark 8 (ab_extract_test / ab_gemma4_test/round2/round3 / eval-ranked-search / vector-probe-burst / vector-observation-summary / vector-threshold-calibration) + B2 migration 4 (migrate-v221 / migrate-v3-to-v4 / migrate-scope-field / snapshot-v4-atoms) + B3 REG-005 4 (reg005-rollback-cleanup + atom-injection-summary + 兩個對應 tests) + B4 test-memory-v21，共 233 KB。**REG-005 dead path 修補**：B3 SOP 發現 `hooks/handlers/session_start.py:156-191` 兩個 dead 函式 (`_check_reg005_observation_status` + `_format_reg005_highlight`) 與 453-458 呼叫點 — 觀察期 flag 已早刪 → 函式必 return None；移除三段共 ~45 行 dead code。`tools/check-bypass.py` WHITELIST 移除已刪的 snapshot-v4-atoms.py + test-memory-v21.py 兩條目；`_AIDocs/DocIndex-System.md` 移除 9 條死連結（vector-observation-summary / vector-probe-burst / atom-injection-summary / reg005-rollback-cleanup / migrate-v221 / migrate-v3-to-v4 / migrate-scope-field / test-memory-v21 / eval-ranked-search），check-bypass.py 描述同步更新。**保留判定**：`hooks/wg_atom_observation.py` 為 V5 flag-gated 零開銷 shim（無 flag 即 sys.exit(0)），保留待未來重用；`tools/cleanup-old-files.py` 規則覆蓋率不足（只 3 目錄）本次未擴充，A 組用 PowerShell 一次性處理。**驗證**：跑 stash 對拍 baseline pytest — 變更前 56 failed / 424 passed / 52 skipped，變更後 40 failed / 414 passed / 52 skipped（16 fail + 10 pass 差異全來自被刪 reg005 兩個 test 共 26 案），**未引入新 regression**；剩 40 個 pre-existing fail 為 V5 Wave 重構遺留（workflow-guardian module attributes / wg_session_evaluator import / test_v41_disabled），跨 Wave 5 範圍不在本 commit 處理。**Session 2 範圍**：C 組 _AIDocs/DevHistory/ 子目錄歸檔 + D 組 plan/audit 歸檔 + E 組 V4 SPEC 評估 + runtime 誤入版控修正（見更新後 `memory/_staging/next-phase.md`）。 | `hooks/handlers/session_start.py`(去 dead code), `tools/check-bypass.py`(WHITELIST 縮減), `_AIDocs/DocIndex-System.md`(9 條死連結), `_AIDocs/_CHANGELOG.md`, `memory/_staging/next-phase.md`(更新 Session 2 範圍), tools/* (B 組 13 個刪除), tests/* (B3 reg005 2 個 test 刪除), memory/_distant/2026_05_v5_overhaul/double-nest-residue/(新 archive, gitignored) |
| 2026-05-05 | **Wave 3 收尾 — memory/ 根目錄整理：6 atom 搬入 feedback/ + 9 access.json 合併 + 1 殘骸刪除 + atom_promote 子目錄修補** — Wave 2/3 完工後檢視 `memory/` 根目錄三件殘骸：(1) 6 個 `feedback-*.md` 散在根目錄但 `memory/feedback/` 已收 16 atom（新建時漏放）；(2) 14 個錯位/孤兒 `*.access.json`（5 misplaced：atom .md 已搬到 feedback/ 但 root access 未清；4 rename：feedback-handoff/research/scope-sensitive/fix-escalation 改名後 root 舊 access 落單；1 殘骸：mail-sorting atom 已刪、access 純垃圾）；(3) `tools/node22/` 誤刪後遺。**(A) 新 `scripts/merge-orphan-access.py`**（一次性合併工具）：dry-run 預設 + `--apply` + active session guard + idempotent。9 對合併走 max(read_hits/confirmations/last_used/last_promoted_at) + min(first_seen) + timestamps 聯集去重取最新 50 + confirmation_events 時間排序聯集；走 `lib.atom_access._normalize/_write_raw` + `lib.atom_io._audit_log`（op="access_merge"/"access_delete_orphan"，source="tool:memory-cleanup"，commit 15b9de2 已加入 VALID_SOURCES）。**plan defect 順手修**：原 plan 寫 `git rm src` 但 `.gitignore:55` 早把 `**/memory/**/*.access.json` 排除 → 改 `Path.unlink()`（git rm 必失敗）。fix-escalation → feedback-fix-escalation 保留 35 條 timestamps 歷史紀錄。執行紀錄寫 `migration.json[merge-orphan-access]`。**(B) 6 個 feedback atom 搬遷**：`git mv` × 6 (.md 檔，git tracked) + `Path.rename` × 6 (.access.json 檔，gitignored 無法 git mv)。`memory/_ATOM_INDEX.md` 6 行 Path 欄手動改正（plan 寫 `sync-atom-index.py --write`，但實際 flag 是 `--check/--fix/--add-from-frontmatter`，皆不適用「修改既有 row 路徑」場景；直接 Edit 6 行最簡）。`tools/sync-atom-index.py --check` 跑後 0 drift。**(C) atom_promote MCP 子目錄查找補修（plan 預測風險落地）**：`tools/workflow-guardian-mcp/server.js:1453` `toolAtomPromote` 寫死 `path.join(memDir, atom_name + ".md")`，從不掃子目錄，所以 ALL feedback/ atom（含搬遷前已在 feedback/ 的 16 個）都找不到。新增 `findAtomFileRecursive(memDir, atomName)`（mirrors `lib/atom_spec.SKIP_DIRS` 排除 `_reference/_archived/_pending_review/_staging/templates/wisdom/_drafts/episodic/_meta`）作 fallback：直接路徑不存在時掃子目錄；既有 root atom 走原路徑（向後相容）。standalone node 驗證：feedback-clean-before-build / feedback-pointer-atom / decisions / nonexistent 四場景全部正確。**注意**：MCP server 為長期 process，新代碼需 session restart 才生效；本次 commit 落 source code，下個 session 自動受惠。**(D) `tools/node22/` 確認忽略**：grep 全 codebase 0 caller，`server.js` 的 `child_process.spawn("node",...)` 走 PATH 不依賴 tools/node22/。本回合不復原（節省 ~50MB）；若未來需要 → plan 文件附下載步驟。**驗證**：全 pytest 460 passed + 52 skipped（含新增 20 案 `test_merge_orphan_access.py`）；SessionStart smoke EXIT=0；`ls memory/feedback-*.md` 0 結果；`ls memory/feedback/feedback-*.md` 22（16 + 6）；`ls memory/*.access.json` 從 26 降到 10（純 root atom），無 plan 預期的 16 中間態（plan 階段切分為 A→C，實際分兩 commit 跑完降到終態）；audit log 出現 9 筆 `op="access_merge"` + 1 筆 `op="access_delete_orphan"` 條目；server.js node syntax check OK。**Plan 對照**：plan 寫 24 預期 feedback atom 數，實際 22（plan 把 16 寫成 18 minor 失準）；plan 寫 16 中間態，實際 A1 commit 後 16、C1 commit 後 10，亦合計符合預期路徑。 | `scripts/merge-orphan-access.py`(新), `tests/test_merge_orphan_access.py`(新), `tools/workflow-guardian-mcp/server.js`(findAtomFileRecursive), `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_meta/migration.json`, `memory/_meta/atom_io_audit.jsonl`, `memory/_ATOM_INDEX.md`(6 行 Path 欄), `memory/feedback/`(+6 .md +6 .access.json) — 14 個 root .access.json 刪除（5 misplaced merge + 4 rename merge + 1 mail-sorting 殘骸 + 6 隨 .md 搬走，後者透過 fs rename 不顯示在 git diff 因 gitignored） |
| 2026-05-05 | **Wave 2 — Atom 遙測解耦：旁路檔 access.json 取代 .md 檔頭計數欄** — REG-005 收尾後第二波，修補 Wave 1 遺留的 funnel 違規（`workflow-guardian.py:1340-1375` atom 注入直接 `.write_text()` 改 atom .md 繞過稽核）並停止 atom .md 因「讀」而髒。**(1) 新模組 `lib/atom_access.py`**：`<atom>.access.json` 旁路檔（schema `atom-access-v2`）讀寫單一通道。對外 API：`read_access` / `init_access` / `increment_read_hits`（atomic write + 50ms backoff 三次重試，耗盡寫稽核 `op="access_increment_dropped"` best-effort）/ `increment_confirmation` / `record_promotion` / `write_access_field` / `bulk_read`。CLI 入口 `python -m lib.atom_access` 給 server.js spawn。舊 schema 自動正規化（confirmations 陣列 → confirmation_events、補 schema key、計數轉整數）。**(2) 8 處寫入 + 3 處讀取 reroute**：`hooks/workflow-guardian.py:1340-1375` atom 注入計數改走 `increment_read_hits(source="hook:atom-inject")`；`hooks/wg_episodic.py:361-378` cross-session confirm 改 `increment_confirmation`、舊雙寫路徑（update_atom_field + 直接寫 access.json）合併；`hooks/wg_episodic.py:776-800` episodic create 後呼叫 `init_access`；`lib/atom_io.py:write_atom` create/append/replace 在 _atomic_write 後同步維護 access；`lib/atom_spec.py:build_atom_content` 移除 Last-used / Confirmations:0 / ReadHits:0 三行（atom .md 不再寫計數欄）；`tools/workflow-guardian-mcp/server.js` buildAtomContent / append / replace / atom_promote 四處 + 新增 `readAtomAccess()` 同步 fs 讀 + `spawnAtomAccess()` 子程序寫；`tools/memory-audit.py:restore_atom` demote 計數歸零 + `parse_atom_metadata` 計數讀走 access.json；`hooks/wg_iteration.py:_self_iterate_atoms` 衰退分數 bulk_read。**(3) 殭屍碼清理**：`lib/atom_io.py:update_atom_field` 唯一 caller reroute 後 0 callers → 函式刪除；`lib/atom_io_cli.py` 移除 `update_atom_field` action；`lib/atom_spec.py:REQUIRED_METADATA` 移除 Last-used（移到 OPTIONAL）；server.js parseAtomMeta 移除 confirmations / readhits / last-used 三 case；`hooks/wg_pretool_guards.py` funnel 違規錯誤訊息更新；server.js 升級 gate 移除 7-day migration.json fallback（dual-field-v1 遷移於 2026-04-24 完成、超過 7 天）。**(4) 一次性遷移 `scripts/migrate-access-stats.py`**：掃 `memory/**/*.md`（排除 _meta/_staging/personal/wisdom/templates 等），每 atom 把 .md 檔頭計數欄抽到 access.json（取 max 避免回退）+ 從 .md 剝除四行 + 缺 Confidence 欄補 [臨]。冪等、預設 dry-run、`--apply` 才動、跑前偵測 active session 拒跑。執行：37 atoms 全部 migrated 0 errors，再跑 dry-run 顯示 37/37 skipped(already-v2)。記錄寫 `memory/_meta/migration.json` 加 `access-stats-v2` 區塊。**(5) DocDrift**：`Architecture.md`「Atom 寫入單點收束」section 加 `lib/atom_access.py` 條目 + caller 接線表更新（hook:atom-inject / hook:episodic-confirm 改走 atom_access）+ 新增「Atom 知識／遙測切分」段；`DocIndex-System.md` `lib/` section 補 atom_access.py + 遷移節補 migrate-access-stats.py。**驗證**：全 pytest 425 passed + 52 skipped（含新增 17 案 `test_atom_access.py` + 17 案 `test_migrate_access_stats.py`）；SessionStart smoke EXIT=0；UserPromptSubmit smoke 觸發 atom 注入後 `git status memory/*.md` diff 只剩遷移時剝除的計數行（無新 write_text）；audit log 出現新 `op="access_increment"` `source="hook:atom-inject"` 條目。**核心成功訊號達成**：atom .md 從此不因運行期計數變動而髒。 | `lib/atom_access.py`(新), `lib/atom_io.py`, `lib/atom_io_cli.py`, `lib/atom_spec.py`, `hooks/workflow-guardian.py`, `hooks/wg_episodic.py`, `hooks/wg_iteration.py`, `hooks/wg_pretool_guards.py`, `tools/memory-audit.py`, `tools/workflow-guardian-mcp/server.js`, `scripts/migrate-access-stats.py`(新), `tests/test_atom_access.py`(新), `tests/test_migrate_access_stats.py`(新), `tests/test_atom_io_equivalence.py`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_meta/migration.json`, 37 個 atom .md（剝除計數行）, 38 個 *.access.json（v2 schema 升級） |
| 2026-05-04 | **[S4-FINAL] Opus Melodic Comet — 收尾「刪 & 存」+ verify single-source-of-truth（4-Session 重構閉合）** — Opus Melodic Comet 重構（2026-05-04 一日內 4 session 完成）落地 atomic memory single-source-of-truth：S1 基礎建設（dd1be59）/ S2 入口收束 + P5+P7（58d032b）/ S3 強制收束 + 反向證明（daedad3）/ S4 收尾（本次）+ 中間有 cca5883 docdrift advisory SessionEnd-only fix。**S4 工作：(1) 自檢偏離當場修**：rmdir `~/.claude/projects/c--users-holylight--claude/memory/` 空殘骸（S1.1.2 歸檔後遺留空 dir）→ Layers 2→1；audit-reconcile.py 加 `classify_diff` 三分類（counter_only / knowledge / unknown，用 git diff 對 frontmatter COUNTER_FIELDS + [臨]/[觀]/[固] tag promotion 判定）→ 53 unmatched 自動拆 50 counter-only / 0 knowledge / 3 unknown，default 模式 exit 0；--since parser 加 "Xh ago" 容錯。**(2) 殘骸清理**：staging 任務檔 `opus-melodic-comet-progress.md` 刪除（git log 保留）；plan `opus-melodic-comet.md` 移到 `plans/_archive/`。**(3) 知識 atom 入庫（走 funnel source=mcp）**：新 `feedback-clean-before-build`（[臨]，源自 S1 殘骸先清後建教訓）+ `feedback-checker-rule-consolidation`（[臨]，源自 S1.2 4 個檢查器各自 SKIP_DIRS 漂移教訓）+ `decisions-architecture` append 印象 bullet「Atomic Memory Single Funnel → _AIDocs/Architecture.md」（write_raw 走 funnel，因 atom 用 ## 印象 而非 ## 知識，append validate 不過）。**(4) _AIDocs 更新**：Architecture.md「Atom 寫入單點收束」section 升級 S1–S4 + 補 S4 audit-reconcile classifier 三分類描述 + 加 tests/test_audit_reconcile.py / tests/test_check_bypass.py（S4.D.2 unit test）+ 加 S4 收尾段；DocIndex-System.md 同步補新檔；本 _CHANGELOG entry。**(5) S3 殘留補完**：tests/test_audit_reconcile.py + tests/test_check_bypass.py（驗 --since parser、classify_diff、bypass scan）；electron-uia-automation.md trigger 13→12（合併近義詞）；SessionEnd hook 跑 audit-reconcile（建議寫進 settings.json 由用戶 confirm）。**(6) 索引重建**：sync-atom-index + sync-memory-index 跑過。**8-matrix 驗證**（S4 完工 hard 條件）：(1) memory-audit 0 errors / 0 warnings / 30 atoms；(2) pytest 全綠；(3) check-bypass 0 violations；(4) audit-reconcile --since 1d 0 knowledge bypass；(5) audit log 含 mcp / hook:* / tool:* 多 source；(6) Architecture.md「Atomic Memory Single Funnel」section 完整；(7) staging/plan 殘骸 0；(8) /memory-health 全綠。**最終達成**：funnel 4 caller wired（mcp / hook:* / tool:* 6 條）+ PreToolUse 強制門禁（直 Edit/Write atom 一律 deny，escape hatch=`WG_DISABLE_ATOM_GUARD=1`）+ 反向證明（check-bypass 靜態 + audit-reconcile 動態）+ atom 規則單一來源（lib/atom_spec.py）。 | `tools/audit-reconcile.py`, `_AIDocs/Architecture.md`, `_AIDocs/_CHANGELOG.md`, `_AIDocs/DocIndex-System.md`, `memory/feedback-clean-before-build.md`(新), `memory/feedback-checker-rule-consolidation.md`(新), `memory/decisions-architecture.md`, `memory/electron-uia-automation.md`, `memory/_meta/atom_io_audit.jsonl`, `memory/_ATOM_INDEX.md`, `memory/MEMORY.md`, `tests/test_audit_reconcile.py`(新), `tests/test_check_bypass.py`(新), `plans/_archive/opus-melodic-comet.md`(歸檔), `memory/_staging/opus-melodic-comet-progress.md`(刪除) |
| 2026-05-04 | **[S3] Opus Melodic Comet — 強制收束 + 反向證明（funnel 完整 + PreToolUse 門禁 + 9 source contract）** — S2 接 4 caller 後，S3 補 C 類 4 入口 + MCP server.js 切 spawn CLI + PreToolUse 強制門禁 + 反向證明工具。**(1) S3.0 補 S2 漏網**：extract-worker L558/L583 (failures atom append/create) + wg_episodic L367 (Confirmations +1) 切 funnel；lib/atom_io 加 write_raw / update_atom_field + hook:episodic-confirm 入 VALID_SOURCES。**(2) S3.1 C 類 3 tool 切 funnel**：migrate-v3-to-v4 / memory-undo / atom-move（7 處 = 4 index + 3 atom）；changelog-roll 經驗證確認非 atom write 跳過。**(3) S3.2 server.js MCP 切 spawn CLI**：toolAtomWrite/Promote 5 處 atomic write + appendToIndex 切 spawn `lib.atom_io_cli`；resolveMemDir 加 P3+P4 cwd-scope 防護（force_global escape hatch）；toolAtomMove 已 spawn atom-move.py 自動受惠。**(4) S3.3 PreToolUse 強制門禁**：wg_pretool_guards 加 `_DOUBLE_CLAUDE_RE`（P6）+ AtomFunnelBlock（env `WG_DISABLE_ATOM_GUARD=1` bypass）；新增 tools/check-bypass.py（0 violations）；切 memory-audit 4 處 + wg_episodic L815 episodic create。**(5) S3.4 反向證明**：新增 tools/audit-reconcile.py（mtime × audit log 對拍）；isolated e2e 11 source 全通；終極攔截測試人為 Write memory atom 被 deny。`_AIDocs/Architecture.md` 加「Atom 寫入單點收束」章節（架構/contract 表/門禁/反向工具/測試）；DocIndex-System.md 補 atom-move/migrate/undo/memory-audit S3 標註 + check-bypass / audit-reconcile 新工具條目；test_guardian_atom_write_gate.py 兩 fixture 更新反映 S3.3「直 Write atom 一律 deny」新規則；wg_pretool_guards 加 `_` 前綴豁免（修 _INDEX.md 假陽性）。**Verification 7-matrix**：memory-audit 0/0、atom_io 11/11、guardian gate 9/9、check-bypass 0、audit-reconcile clean、9 source contract 全通、server.js syntax OK。 | `hooks/wg_pretool_guards.py`, `hooks/wg_episodic.py`, `hooks/extract-worker.py`, `lib/atom_io.py`, `tools/migrate-v3-to-v4.py`, `tools/memory-undo.py`, `tools/atom-move.py`, `tools/memory-audit.py`, `tools/check-bypass.py`(新), `tools/audit-reconcile.py`(新), `tools/workflow-guardian-mcp/server.js`, `tests/test_guardian_atom_write_gate.py`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-05-04 | **[S2] Opus Melodic Comet — 入口收束 + 漏洞修補（B/C 類入口切 funnel + P5 Scope migration + P7 dedup fix）** — S1 funnel 鋪好但未接 caller，S2 把可程式化 caller 接上、補 atom Scope 欄位、修 dedup 假陽性。**(1) S2.1 P5 Scope migration**：新 `tools/migrate-scope-field.py`（~75 行 + dry-run 預設）— 掃 `iter_atom_files`，缺 `Scope:` 由路徑推斷（`shared/` → shared / `roles/<role>/` → role / `personal/<user>/` → personal / 其他 → global），插在第一個 `- Key:` 之前（SPEC §4 Scope 為第一行）。**踩坑教訓**：首版用 `Path.write_text` 在 Windows 把整檔 LF 改 CRLF（`os.linesep`）— 修為 byte-level I/O `read_bytes()/.decode("utf-8")` + `write_bytes(...encode("utf-8"))` 保留原 line endings。實證：electron-uia-automation.md +1 line clean diff（單行 Scope: global 插入），dry-run 驗證 0 atoms 缺 Scope。**(2) S2.2 funnel 接線（4 檔，原計畫 5 檔）**：`hooks/user-extract-worker.py:317-407` `_write_atom_via_mcp` 改走 `write_atom(scope="personal", user=..., source="hook:user-extract")`，保留 dedup 預檢、slug counter、author 標記等原語意（~80 行重寫）；`tools/sync-atom-index.py:21-29,219-263` 整檔覆寫改 `write_index_full(source="tool:sync-atom-index")`，保留 4 欄（Atom/Path/Trigger/Scope）schema；`tools/sync-memory-index.py:24-28,156-164` `--write` 分支同走 `write_index_full(source="tool:sync-memory-index")`。**`lib/atom_io.py` minimal 擴充**：新增 `write_index_full(index_path, content, *, source)` helper（~35 行：atomic write + audit log，op="index_full"）— 給 batch index rewriters 用，因現役 `_ATOM_INDEX.md` 為 4 欄 schema，funnel 原 `write_index` 是 3 欄 row-by-row 會抹掉 Scope 欄；`VALID_SOURCES` 加 `"tool:sync-atom-index"` / `"tool:sync-memory-index"` 兩項。**未動 hooks/extract-worker.py + hooks/wg_episodic.py**（原計畫列為改動目標）：完整 grep + 行區精讀後確認 (a) extract-worker.py:427-584 區段只有 `_write_state_atomic`（state JSON）與 `_failure_writeback`/`_create_failure_atom`（`memory/failures/` log 條目，非 atom）— 無 atom 直寫；(b) `wg_episodic.py:577 _update_memory_index` 是 dead code（全 codebase grep 0 call site）+ episodic atom 走 `episodic/` 在 SKIP_DIRS 不視為 atom — 無 funnel 對應點。**(3) S2.3 P7 audit dedup fix**：`tools/memory-audit.py:90` `AtomMetadata` 加 `scope_label: str = ""` 欄位；line 218 `parse_atom_file` 補 `atom.scope_label = atom.scope or _infer_scope_from_path(path)`（frontmatter 優先 / 路徑推斷 fallback，對齊 `lib/atom_spec.VALID_SCOPES = {global, shared, role, personal}` 不細分）；line 1273-1290 新增 `_infer_scope_from_path` helper；line 535-538 `detect_duplicates` dedup key 從 `_normalize(title)` 改 `(scope_label, _normalize(title))` — 跨 scope 同名 atom（global vs shared 各有 `decisions`）不再誤判為重複。**驗證矩陣**（7 檢查）：(1) memory-audit: errors=0/duplicates=0/Layers=1 ✅；(2) pytest 11/11 PASS（funnel 行為未變動）✅；(3) atom-health-check 30 atoms healthy ✅；(4) migrate-scope-field --dry-run 0 missing ✅；(5) audit log `_meta/atom_io_audit.jsonl` 含 `tool:sync-memory-index` + `tool:sync-atom-index` 兩 source ✅；(6) (7) hook:* source 因無 funnel 對應點不適用（已驗證 extract-worker / wg_episodic 無真實 atom 直寫）N/A。**順手修補**：S1 後 `~/.claude/projects/c--Users-holylight--claude/memory/` 空目錄（從 9f979bc auto-promote hook 殘留）→ rmdir 清除（≤5 行門檻）；electron-uia-automation.md 補 Confirmations: 0 / ReadHits: 0（migrate-scope-field 只補 Scope，這兩個是 atom-health-check baseline）。S3 接續 C 類 4 條入口（migrate-v3-to-v4 / memory-undo / atom-move / changelog-roll）+ server.js MCP 切 spawn CLI + PreToolUse 強制門禁；plan + 進度詳見 `~/.claude/plans/opus-melodic-comet.md` 與 `~/.claude/memory/_staging/opus-melodic-comet-progress.md`。 | `tools/migrate-scope-field.py`(新), `lib/atom_io.py`, `hooks/user-extract-worker.py`, `tools/sync-atom-index.py`, `tools/sync-memory-index.py`, `tools/memory-audit.py`, `memory/electron-uia-automation.md`, `memory/_meta/atom_io_audit.jsonl`(新, runtime), `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_staging/opus-melodic-comet-progress.md` |
| 2026-05-04 | **[S1] Opus Melodic Comet — 單點收束 Foundation（基礎建設，先清→後建）** — 「7 個 /memory-health 告警」深挖根因為「規則散在 4 處 / 寫入 13 條入口 / 跨層路徑 5 漏洞」。S1 落地三件事，**不接 caller**（避免測試結果被現役寫入污染，那是 S2/S3 工作）。**(1) S1.1 殘骸清理 + P1 雙層補丁**：`hooks/wg_paths.py:88+ get_project_memory_dir` 加 6 行 CLAUDE_DIR resolve 防護，cwd=~/.claude 時短路返回 global MEMORY_DIR 而非拼出 `~/.claude/.claude/memory/`；新 `tools/cleanup-projects-residue.py`（dry-run + `--apply` + `--restore`，歸檔不刪除），歸檔 7 個 `~/.claude/projects/{slug}/memory/` 殘骸到 `_archive/projects-residue/2026-05-04/`（含 `c--users-holylight--claude/memory/` 57 個 P1 雙層 bug 副本 + 6 個 V2.21 migration stub）。Audit Layers 從 8 降到 1（global），Errors 從 3 降到 1（剩 1 個 `electron-uia-automation.md` 缺 Scope 是真實 atom 缺欄位，留 S2.1 P5 migration 系統處理）。**(2) S1.2 規則收束**：新 `lib/atom_spec.py`（~230 行，純資料 + 純函式）—— SKIP_DIRS / REQUIRED_METADATA / VALID_CONFIDENCE / VALID_SCOPES / TRIGGER_MIN/MAX 全系統唯一規則來源，含 `is_atom_file / parse_frontmatter / validate_atom_content / build_atom_content / slugify / resolve_scope_dir / iter_atom_files`，行為對拍 server.js:655-742。`tools/memory-audit.py:53-69` 刪掉本地常數改 `from lib.atom_spec import ...`；`tools/atom-health-check.py:30-31` 改 import + `find_atoms` 委派 `is_atom_file` 判斷 + REQUIRED_METADATA 完整檢查。Health-check issues 從 3 → 1：DESIGN.md（wisdom/）/ role.md（personal/）兩個假陽性歸零。**(3) S1.3 寫入 funnel**：新 `lib/atom_io.py`（~330 行）`write_atom(*, source: REQUIRED, ...)`/`write_index(...)` 對拍 server.js:1065 toolAtomWrite byte-identical 行為，含 ULID-ish audit_id + `_meta/atom_io_audit.jsonl` 反向證明日誌；新 `lib/atom_io_cli.py`（~30 行）thin CLI bridge，stdin JSON → write_atom → stdout WriteResult（供 S3.2 server.js spawn 用）；新 `tests/test_atom_io_equivalence.py`（11 cases，含 plan 要求的 10 情境 + 1 audit log bonus）global/shared/role/personal create + 全 optional 欄位 + sensitive audience auto-pending + append/replace 保留 Confirmations/ReadHits/Author/Created-at + dry-run + 5 個 error path：**11/11 PASS（W::DeprecationWarning 嚴格模式）**。**S1 邊界**：unconnected funnel — 任何 caller 切換都是 S2.2 / S3.1 / S3.2 工作。S2 接續 P5 migration + 6 條 B 類 hook/tool 入口切 funnel + P7 audit dedup；plan 與進度詳見 `~/.claude/plans/opus-melodic-comet.md` 與 `~/.claude/memory/_staging/opus-melodic-comet-progress.md`。 | `hooks/wg_paths.py`, `tools/cleanup-projects-residue.py`(新), `lib/atom_spec.py`(新), `lib/atom_io.py`(新), `lib/atom_io_cli.py`(新), `tools/memory-audit.py`, `tools/atom-health-check.py`, `tests/test_atom_io_equivalence.py`(新), `_AIDocs/_CHANGELOG.md`, `memory/_staging/opus-melodic-comet-progress.md` |
| 2026-04-29 | **REG-005 Session 1/3 — Atom 注入機制重構 A+B 兩層 + 觀察儀表骨架（commits 540cc91, 3d03ba1, 0309c89）** — 4 層設計（A 摘要優先 / B per-turn budget / C hot/cold 分級 / D Related 過濾）首階段落地，C+D 與啟動觀察期留 Session 2/3。**A 層**（`540cc91`）：`hooks/wg_atoms.py:_strip_atom_for_injection` 由 V2.14 整檔剝離改為按 atom 類型路由 — `impression_action`（如 `decisions-architecture.md`、多數 `feedback-*`）注入 frontmatter（白名單 Confidence/Trigger/Last-used）+ 印象 + 行動全段；`knowledge_mixed`（如 `decisions.md`、`toolchain.md`）注入 frontmatter + 印象（若有）+ 知識（cap 200 tokens 截斷標記）+ 行動；`fallback`（無印象無知識的舊 atom）沿用 V2.14 legacy strip。新 helpers `_detect_atom_type` / `_extract_named_section(max_tokens)` / `_extract_title_and_frontmatter` / `_strip_atom_for_injection_impression_only`（B 層 fallback 預備）。**關鍵行為改變**：注入 prompt 開始帶 `## 行動` 段（V2.14 _STRIP_SECTION_RE 之前剝除），LLM 終於看得到「該怎麼做」指引。實測縮減：toolchain 62.5% / feedback-pointer-atom 37% / decisions 14% / decisions-architecture 12%。**B 層**（`3d03ba1`）：新 `decide_atom_injection(raw, full, used_tokens)` 對每 atom 回 ok/fallback/skip 三態，hard cap `_TURN_BUDGET_LIMIT=800`；超 budget → fallback 到 impression-only，仍超則 skip 加 summary 後 break。`workflow-guardian.py:~1106` atom 注入點（trigger-matched + spread_related 兩段共享 used_tokens）重構成決策驅動，每 atom 寫 `[BUDGET]` debug log。`SECTION_INJECT_THRESHOLD` 300→200（更多中型 atom 走 vector section 提取，與 budget cap 互補）。**設計簡化**：原計畫的 module-level `_TURN_TOKEN_BUDGET_USED` 不需要，因 `used_tokens` 已是 handle_user_prompt_submit 的 local var（每 entry 自然歸 0）。**觀察儀表骨架**（`0309c89`）：先寫不啟動，flag-gated（`memory/_staging/reg-005-observation-start.flag`）。新 `hooks/wg_atom_observation.py`（~165 行）獨立 hook 不走 dispatcher，flag 不存在 → `sys.exit(0)` 零開銷返回；UserPromptSubmit 累 sessions / PostToolUse(Read) 累 reads_in_memory；log JSON line schema 寫 `Logs/atom-injection-observation-YYYY-MM-DD.log`，metric 寫 `reflection_metrics.json` 子鍵 `reg005_observation`（atomic write via .tmp rename）；路徑正規化兼容 Windows / MSYS2 / 混合格式。新 `tools/atom-injection-summary.py`（~190 行）依「事件驅動四重標準」（A 注入 ≥150 + B session ≥6 + 2 ≤ wall_clock ≤ 7 天）+ 自動止血條件（hot 命中 / cold 主動讀 / 命中錯失 / 零覆蓋，皆有最低樣本門檻）輸出 KEEP / ROLLBACK / GRAY / INCOMPLETE / NOT_STARTED。`settings.json` 新增 PostToolUse "Read" matcher + UserPromptSubmit 第三 hook group 指向 wg_atom_observation.py（既有兩 PostToolUse matchers 不動）。**驗證**：全 pytest 298 passed + 52 skipped（新 31 tests）；SessionStart smoke EXIT=0；觀察 hook flag-on/off 手動 smoke 通過（flag 缺席 → log/metric 完全不寫；flag 存在 → 正確寫入；Win 路徑與 MSYS2 路徑都命中；非 memory 路徑被忽略）；`atom-injection-summary.py --report` 對 INCOMPLETE / NOT_STARTED / 期滿三情境輸出正確判定。**設計檔**：`memory/_staging/reg-005-atom-injection-refactor.md`（含完整 4 層設計 + 觀察期事件驅動四重標準 + 不要動清單 + Session 進度表 + Session 2/3 啟動 prompt 草稿）。**Session 2/3** 範圍：commit 4 C 層 hot/cold 分級 + workflow-guardian instrument 補上 reg005_observation.{hot,cold}_injections / commit 5 D 層 Related 擴散 hot/cold 過濾 + depth=1 / `touch` flag 啟動觀察期 / 4-commit 前後 baseline 對比。 | `hooks/wg_atoms.py`, `hooks/workflow-guardian.py`, `hooks/wg_atom_observation.py`(新), `tools/atom-injection-summary.py`(新), `tests/test_atom_strip_for_injection.py`(新, 21 cases), `tests/test_atom_injection_budget.py`(新, 10 cases), `settings.json`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-28 | **[固] 邏輯程式碼化首批落地（議題 #10 強候選 2 條）** — 使用者 Stage F 重審紅色指標 #3「非常固定的邏輯應 hook 化」首批執行。新檔 `hooks/wg_pretool_guards.py`（純函式風格鏡射 wg_evasion）含 `check_memory_path_block`（PreToolUse Write/Edit 阻擋寫入 `~/.claude/projects/{slug}/memory/`）+ `check_svn_test_block`（PreToolUse Bash 阻擋 `svn commit/ci` 含 `tests?/` `__tests__/` 或 `*Test.<ext>` 檔，r10854 教訓）。`workflow-guardian.py` import 採 try/except 自動降級 no-op 防 typo 拖垮 Write/Bash 路徑；`handle_pre_tool_use` 在既有 AtomFormat/Confidence Gate 後串接 2 個 deny 檢查；`settings.json` PreToolUse 新增 Bash matcher（既有 Write matcher 不變）。對應 atom `feedback-memory-path` / `feedback-no-test-to-svn` 末尾補「## 已 hook 化」段（hook 不取代 atom，hook 訊息指回 atom 補語境）；`feedback-pointer-atom`「代 hook atom」失敗模式段補實踐回路證明。**驗證**：新測試 `test_pretool_path_block.py`（11 cases）+ `test_pretool_bash_block.py`（14 cases）共 25/25 PASS；全 pytest 267 PASS / 10 fail（10 fail 對齊 [REG-002 議題 #8 Silent Advisory Mode pre-existing](DevHistory/memory-cleanup-2026-04/follow-up-issues.md#議題-8wave-4a-衍生codex_companion-silent-advisory-mode-重構後-10-測試需對齊) commit 7794f65，與本次零相關）；SessionStart smoke exit=0；真實 stdin JSON 命中 Step 4/5 兩條 deny 訊息正確（Step 5 Bash matcher 直接由 harness 端攔截，證明 settings.json 接線正確）。**Token 影響**：atom 注入成本不變；每次規則違反不再消耗使用者糾正 turn + LLM 重寫成本（歷史節奏每 2-3 週一次，每次省 2000-4000 tokens）。`follow-up-issues.md` 議題 #10 表兩條強候選欄位改「已」+ 加 2026-04-28 處理紀錄段（**其餘 5 條由議題 #10 後續評估**，不限制下個 session）。 | `hooks/wg_pretool_guards.py`(新), `hooks/workflow-guardian.py`, `settings.json`, `memory/feedback/feedback-memory-path.md`, `memory/feedback/feedback-no-test-to-svn.md`, `memory/feedback/feedback-pointer-atom.md`, `tests/test_pretool_path_block.py`(新), `tests/test_pretool_bash_block.py`(新), `_AIDocs/Architecture.md`, `_AIDocs/DevHistory/memory-cleanup-2026-04/follow-up-issues.md`, `plans/stage-f-transient-bachman.md`(新) |
| 2026-04-28 | **Memory Cleanup Wave 6 收尾 — 計畫歸檔 + plans 目錄刪除** — 多大師協作（5 分析席 + 3 稽核席 + 4 wave-specific 紅隊）+ 6 wave 執行計畫進入沉澱階段。**(1) DevHistory 歸檔**：新建 `_AIDocs/DevHistory/memory-cleanup-2026-04/`，搬入 `synthesis.md`（plan v2.1 主控計畫）、`follow-up-issues.md`（議題 #1-#9 含 #5 Wave 3 已處理 / #8 已掛 known-regressions REG-002 / #9 Stage F BLOCK 細節）、`master-review-digest.md`（8 大師原始報告濃縮：CC 考古 + CC 架構 + Codex 保守/極簡/激進 + Codex Mechanic/Redteam/Skeptic + §9 Phase 4-5 wave-specific 紅隊摘要 + §10 跨大師共識決策黃金交集）。**(2) 通用版協作 protocol**：`multi_agent_protocol.md` → `_AIDocs/DevHistory/multi-agent-cleanup-protocol.md`（CC ≥ Codex 席次設計、防 Codex 幻覺、Windows sandbox unelevated 修復、Phase 4 audit 重派、§十一 收尾 8 項 checklist）。**(3) 失敗教訓沉澱**：新增 `_AIDocs/Failures/codex-windows-sandbox-1385.md`（CreateProcessWithLogonW failed: 1385 根因 = `[windows] sandbox = "elevated"`，修補 = `-c 'windows.sandbox="unelevated"'`，5 位 Codex 分析席第一輪全受影響、報告退化為「立場推論」非「事實核驗」、Phase 4 audit 重派才產出實證稽核）；`vectordb-silent-failure-2026-04.md` 已於 Wave 3a 寫入。**(4) follow-up 升級為 known-regressions**：REG-003 Vector DB 拆庫精密化（議題 #1，效能優化非 broken）、REG-004 Vector 路徑寫死根因 commit 追溯（議題 #2 殘留，主修補 Wave 3a 已完成）、REG-005 Atom 注入機制重構（議題 #3，前置條件 = atom 注入 3 源真相統一）、REG-006 Hook 萃取管線重複（議題 #4，3 條 worker 並存疑似職責重複）。議題 #5（自動觀察儀表）Wave 3 已處理 ✅；議題 #6 #7（vector ranked min_score / 路徑 commit 追溯）/ #9（Stage F BLOCK）保留 follow-up。**(5) 索引同步**：`_AIDocs/DevHistory/_INDEX.md` 加 `multi-agent-cleanup-protocol.md` + `memory-cleanup-2026-04/`；`_AIDocs/Failures/_INDEX.md` 加 `codex-windows-sandbox-1385.md`、最近更新改 2026-04-28。**(6) plans 目錄整體刪除**：`~/.claude/plans/memory-cleanup-2026-04-27/`（含 outputs/ 12 份原始大師報告 + outputs/wave3b/ + 4 份 dryrun/briefing/wave_3_to_6_prompts/synthesis_draft 等暫存規劃文件）— 已全數沉澱於 DevHistory + known-regressions + Failures，原檔不再需要。**收尾總成果**：Wave 1（A0+A+G1）✅ Wave 2（B+C 並行+merge）✅ Wave 3a（vector 儀表+路徑修+flag-gate）✅ Wave 3b（probe burst→REVIVE 87.6%）✅ Wave 4a（Stage E phase6 dead code 精簡）✅ Wave 4b（Stage F 5 子 stage 4 BLOCK + F4-drift 晉升門檻字串一致化）✅ Wave 5a（Stage H 3 條規則 atom）✅ Wave 5b（Phase 5 驗證未發現失憶）✅ Wave 6（本 commit）。 | `_AIDocs/DevHistory/memory-cleanup-2026-04/{synthesis.md, follow-up-issues.md, master-review-digest.md}`(新), `_AIDocs/DevHistory/multi-agent-cleanup-protocol.md`(新), `_AIDocs/DevHistory/_INDEX.md`, `_AIDocs/Failures/codex-windows-sandbox-1385.md`(新), `_AIDocs/Failures/_INDEX.md`, `_AIDocs/known-regressions.md`(REG-003/004/005/006), `plans/memory-cleanup-2026-04-27/`(整目錄刪除) |
| 2026-04-28 | **Memory Cleanup Wave 3a — Vector Observation Instrumentation + workflow-guardian Path Fix** — 鋪設 vector subsystem 4 天觀察儀表（不執行 Stage D 本身）。**(1) wg_intent.py vector 觀察 log**：`_search_episodic_context` / `_semantic_search` 的 disabled / no_flag / ready / error 出口都呼叫新 `_log_vector_obs(session_id, fn, flag_state, result_count, fallback_used)`，寫入 `~/.claude/Logs/vector-observation.log`（RotatingFileHandler 2MB×3, delay=True，含 `_vector_obs_logger_failed` sentinel 短路重試）；caller 簽名加 `session_id` 從 SessionStart state 透傳。**(2) workflow-guardian.py 路徑修正（vector silent fail 真根因）**：`vs_script` 從不存在的 `tools/vector-service.py` 改為真檔 `tools/memory-vector-service/service.py`（12 天 subprocess.Popen silent fail 根因）。**(3) flag write gate**：bg subprocess `vector_ready.flag` 改為「health 200 才寫」，poll 30×0.5s 任一輪成功才 ready=True，否則跳過 flag write（杜絕 12 天假陽性）。**(4) SessionStart known-query probe**：bg subprocess 跑 `workflow guardian SessionStart 機制` query 透過 vector + keyword grep（`memory/**/*.md`）兩路並比，寫獨立檔 `vector-observation-probe.log`（CC 稽核 F6：避開與 RotatingFileHandler 同檔的 rollover race）。**(5) tools/vector-observation-summary.py**：4 天後讀兩 log 自動判定 REVIVE（命中率 ≥ 50%）/ RETIRE（≤ 5% 且 fallback ≥ 80%）/ GRAY（5–15% 詢使用者）。**(6) plan + protocol 重寫**：plan v2.1 §修訂 #4 D4-D5 移除「使用者主訴」改「log 採樣自動判定」；multi_agent_protocol §六 新增規則「silent-failure 風險的 stage 必須加 log 採樣（hook 鏈 / always-loaded 入口 / 觀察期 ≥ 4 天 / bg subprocess）」。**多大師協作**：CC 寫手 + 1 CC 稽核（Plan agent，驗 graceful no-op；F2 sentinel + F6 分檔採納）+ 1 Codex 稽核（驗 SessionStart probe 不污染 context；C1=YES，bg DEVNULL 已切斷 stdout/stderr，rglob 實測 ~40 md 4.9ms）。**Smoke**：`import wg_intent` OK；`workflow-guardian.py` 完整 import OK；`ast.parse(_bg_code)` OK；end-to-end exec bg_code 寫 probe log（含 spawn vector service + poll ready）；`tools/vector-observation-summary.py --days 1` 讀 records 不報錯。觀察期 2026-04-28 → 2026-05-02。Stage D 本身（修活 vs 淘汰）4 天後讀 log 決定。 | `hooks/wg_intent.py`, `hooks/workflow-guardian.py`, `tools/vector-observation-summary.py`(新), `plans/memory-cleanup-2026-04-27/wave3a_dryrun.md`(新), `plans/memory-cleanup-2026-04-27/synthesis_and_plan_v2.md`, `plans/memory-cleanup-2026-04-27/multi_agent_protocol.md`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-28 | **Memory Cleanup Wave 2 收尾 + Project_File_Tree 引用同步** — 並行清理 Wave 2 (Stage B plans + Stage C projects) 收尾。Stage B (session 2a) 清掉 `memory/_distant/`、`memory/_vectordb/service.pid`、`shared/_pending_review/` 殘檔，handoff 歸檔。Stage C (session 2b) 把 `projects/c--TSG/` 移到 `projects/_archived/`、同步 `memory/project-registry.json`。本 session (Wave 2 收尾) 補 G1 連帶：Project_File_Tree.md 已於 session 1 由 218 行完整檔樹瘦身為 30 行頂層目錄角色說明、改用 `~/.claude/` 不含帳號名，故同步引用描述於 [Architecture.md:4](Architecture.md)（完整檔樹 → 頂層目錄角色說明）、[init-project.md:53,70](../commands/init-project.md)（專案結構摘要 → 頂層目錄角色說明，30 行內，完整檔樹用 `tree -L 3`）、[upgrade.md:132](../commands/upgrade.md)（2.6 _AIDocs 加註：Project_File_Tree.md 為 30 行角色說明、不含帳號名、不再是完整檔樹快照）、[upgrade.md:279](../commands/upgrade.md)（Phase 3 替換註記同步：其他檔案的帳號名需替換、Project_File_Tree.md 不需）。**驗證**：13 個 hook 模組 import 全 OK；SessionStart hook stdin smoke `exit=0` 無 ImportError；commit `59c7598` 含 27 檔變動。settings.json 兩 session 並行加項無衝突（permissions + additionalDirectories 各加各的）。 | `_AIDocs/Architecture.md`, `commands/init-project.md`, `commands/upgrade.md`, `_AIDocs/Project_File_Tree.md`(session 1), `memory/project-registry.json`, `settings.json` |
| 2026-04-27 | **Guardian Hook 兩漏洞修補** — (A) Write tool 繞過 `atom_write` confidence gate：`hooks/workflow-guardian.py::_check_memory_atom_format` 在格式 gate 通過後對「新建檔」（`Path.exists()==False`）解析 frontmatter `Confidence:` 與內文 `- [固]/- [觀]` 標籤，任一非 `[臨]` 即 deny，鏡射 [server.js:1109-1117](../tools/workflow-guardian-mcp/server.js) 的 MCP 端硬擋規則。existing file 整檔覆寫對應 MCP `mode=replace` 路徑（保留 confirmations/readhits 晉升狀態）→ 不擋；`_staging/`、`_pending_review/`、`_*` 索引、`role.md` 維持既有豁免。**動機**：Claude 用 Write tool 直接寫 memory/feedback/*.md 完全繞過 server.js gate（實證：feedback-codex-collaboration.md 初稿 16 條全 `[固]` 走 Write 通過 hook）。(B) FixEscalation 對「計畫迭代」誤判為錯誤修復重試：`hooks/wisdom_engine.py::track_retry` 新增 `_is_plan_iteration_path` 前置 gate，路徑命中 `/plans/`、`/_staging/` 或檔名命中 `is_plan_filename` 規劃詞 → 直接 return 不累計 `wisdom_retry_count`。真錯誤修復（hooks/*.py、tests/*.py 反覆 edit）仍正常觸發 `[Guardian:FixEscalation]`。**動機**：plan v3→v4→v5 多次 Edit 同一份 plans/*.md 累積 retry → 下一輪誤注入 FixEscalation 警告。新增 `tests/test_guardian_atom_write_gate.py`（9 cases，含正向擋 [固]/[觀]/內文標籤、反向放行 [臨]/existing/_staging/索引/非 memory）+ `tests/test_fix_escalation_plan_retry.py`（7 cases，含 plan 豁免、檔名關鍵字豁免、真錯誤仍累計、首編不算）。123 passed（既有 107 迴歸 test_dual_field_e2e + test_codex_companion_stop_e2e + test_evasion_guard + test_phase6_trigger 全綠）/ 0 failed。 | `hooks/workflow-guardian.py`, `hooks/wisdom_engine.py`, `tests/test_guardian_atom_write_gate.py`(新), `tests/test_fix_escalation_plan_retry.py`(新), `_AIDocs/Architecture.md` |
| 2026-04-27 | **Codex Companion Sprint 5.5** — Phase 6 自觸發 hook + metric 第六鍵。**B1**：`tools/codex-companion/state.py` `_METRIC_KEYS` 白名單新增 `audits_total_attempted`（Phase 6 §四 C3 ratio 分母）；`hooks/codex_companion.py` `handle_stop` 在 score gate 通過、所有 dedup/cap 也過、即將送 `/trigger` 前 +1，語意為「實際送出 codex audit 次數」。**B2**：新檔 `hooks/wg_codex_companion_phase6.py`（讀 reflection_metrics.json + glob real assessments → §四 C1-C5 if-check → dedup 用 `last_fired` 集合差集、樣本累積驅動不依賴時間 → 命中返回 inject 字串）；`hooks/workflow-guardian.py` SessionStart MCP 健康檢查後整合 try block，命中 inject 加入 additionalContext。**設計用意**：user 不必主動記「一週後回來看」；條件到自然會在開機 SessionStart 被推醒。新增 `tests/test_metric_audits_total.py`（5 cases）+ `tests/test_phase6_trigger.py`（15 cases，含 C1-C5 邊界 / dedup / drop-then-remeet refire / multi-condition）。251 passed (Sprint 4 從 231 +20) / 0 failed。對應 plan v5 Phase 6 自適應 Gate 推遲評估之自觸發機制。 | `tools/codex-companion/state.py`, `hooks/codex_companion.py`, `hooks/wg_codex_companion_phase6.py`(新), `hooks/workflow-guardian.py`, `tests/test_metric_audits_total.py`(新), `tests/test_phase6_trigger.py`(新), `_AIDocs/DocIndex-System.md`, `_AIDocs/Architecture.md` |
| 2026-04-27 | **Codex Companion Sprint 4** — 空回非靜默 + sandbox 失敗 escalate + 觀測落盤。assessor.py `_run_codex` 改回 `(stdout, stderr)`、新增 `_run_codex_with_retry`（空字串/非 JSON 退 0.4s 重試 1 次）+ `_classify_failure` 用 `CreateProcessWithLogon\|sandbox` regex 識別 R2-5 級錯誤（命中→`category: system, summary: "Codex sandbox 失敗"`）；其他失敗→`status: warning, summary: "退回 heuristics-only", delivery: inject, notify_next_turn: True`。hook drain 偵測 `notify_next_turn` 在 inject blocks 前插入 `[Codex Companion 提醒]` 短訊。`block_template` 從 `config.codex_companion.block_template` 讀（`{detail}` placeholder）。新增 `companion-metrics-{sid}.json` + `increment_metric` API；五鍵觀測（`audits_skipped_by_score / empty_returns / sandbox_failures / behavior_gap_blocks / quality_gap_advises`）由 hook + service 累加，SessionEnd 附到 `memory/wisdom/reflection_metrics.json` 的 `codex_companion.sessions[]`（最多 100 筆，全 zero 跳過，與 wisdom_engine 結構共存）。新增 `tests/test_assessor_retry.py`（8 cases）+ drain e2e 擴 2 case。231 passed / 0 failed。 | `tools/codex-companion/assessor.py`, `tools/codex-companion/state.py`, `tools/codex-companion/service.py`, `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_assessor_retry.py`(新), `tests/test_codex_companion_drain_e2e.py`, `_AIDocs/DocIndex-System.md` |
| 2026-04-27 | **Codex Companion Sprint 3** — Score gate + Advisory schema。新增 `scorer.py` 五因子加權（write_footprint / verification_gap / structural_risk / completion_claim / analysis_loop, 0-10）作為 Stop hook 觸發 codex turn_audit 前的最小 token gate（`score_threshold=4` / `max_audits_per_session=30` / 同 turn_index dedup）。`prompts.py` OUTPUT_SCHEMA 砍 `recommended_action`、改 `delivery: ignore\|inject` + `confidence` + `evidence` + `applies_until` + `turn_index`，徹底剝奪 codex 的 BLOCK 權（advisory only）。`TURN_AUDIT` 模板新增 Last Assistant Reply Tail / Verification Evidence Found / Heuristic Triggered (Reference Only) 三段，assessor 即時抽 verify cmd 事證避免 stale state。drain handler 依 `delivery` 路由：ignore 標 injected 不打擾、inject 注入帶信心標籤；同步退役 REG-001（V4.1 atom snapshot 漂移測試）。221 passed / 0 failed / 0 xfailed。 | `tools/codex-companion/scorer.py`(新), `tools/codex-companion/prompts.py`, `tools/codex-companion/assessor.py`, `tools/codex-companion/service.py`, `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_scorer.py`(新), `tests/test_codex_companion_drain_e2e.py`(新), `tests/regression/test_v4_atoms_unchanged.py`(刪), `tests/fixtures/v4_atoms_baseline.jsonl`(刪), `_AIDocs/known-regressions.md`, `_AIDocs/DocIndex-System.md`, `memory/preferences.md` |
| 2026-04-27 | **Codex Companion Sprint 2** — heuristic 規則重構與 BLOCK 權收斂。BLOCK 權限縮到單一 `confident_completion_without_evidence`（high）；其餘規則一律降為 `low` advisory。三條件齊備才 BLOCK：`has_claim` + `state_change` + `no_verify_evidence`。新增 Sprint 1 教訓 fallback：`last_assistant_tail` 含驗證敘述（pytest 通過 / X/Y PASS / build 成功）視為弱證據放行，避免 companion-state 被擾動誤觸 BLOCK。新增 `severity_at_or_above` API + config `block_severity_threshold`。21 unit + 3 e2e 測試全綠。 | `tools/codex-companion/heuristics.py`(rewrite), `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_heuristics.py`(rewrite), `tests/test_codex_companion_stop_e2e.py`(新), `_AIDocs/DocIndex-System.md` |
| 2026-04-26 | **Codex Companion Phase 1** — Codex (GPT) 驅動的第二觀點監督系統。確定性 hook 觸發 → HTTP service 累積事件 → `codex exec` 非同步審閱 → 下一輪 additionalContext 注入。含 4 條 heuristic 軟閘（缺驗證/完成缺證據/架構變更/空轉）、plan review / turn audit / architecture review 三種 Codex 評估、`/codex-companion` 開關 skill。 | `tools/codex-companion/`(新: service.py, assessor.py, prompts.py, heuristics.py, state.py), `hooks/codex_companion.py`(新), `commands/codex-companion.md`(新), `workflow/config.json`, `settings.json`, `_AIDocs/DocIndex-System.md` |
| 2026-04-24 | **v3 雙欄位拆分 (ReadHits + Confirmations)** — Confirmations 混合訊號拆為 ReadHits（注入讀取）+ Confirmations（跨 session 萃取），晉升門檻重校 4/10+20/50 雙軌，correlation_id 追蹤，16 檔 + migration script，24 E2E 測試全綠。 | `hooks/workflow-guardian.py`, `hooks/wg_episodic.py`, `hooks/wg_atoms.py`, `hooks/user-extract-worker.py`, `hooks/wg_iteration.py`, `tools/workflow-guardian-mcp/server.js`, `tools/memory-audit.py`, `tools/atom-health-check.py`, `tools/memory-vector-service/indexer.py`, `scripts/migrate-confirmations.py`(新), `tests/test_dual_field_e2e.py`(新), `memory/decisions.md` |
| 2026-04-23 | **ScanReport Gate + 反退避契約** — 針對 Opus 4.7 Effort=High 偷懶傾向：IDENTITY.md 加可觀測條款（禁語/成本門檻/收尾格式），Stop hook 新增「缺掃描報告 = 違約」硬阻擋。 | `IDENTITY.md`, `hooks/wg_evasion.py`, `hooks/workflow-guardian.py`, `_AIDocs/Architecture.md` |
| 2026-04-17 | **_CHANGELOG 短格式化 + session-logs 子目錄** — 每條 ~2KB 單行敘事拆為「標題 + log 連結」，8 條遷入 `../memory/_distant/2026_05_v5_overhaul/session-logs/`。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-changelog-short-form.md) | `_AIDocs/_CHANGELOG.md`, `_AIDocs/../memory/_distant/2026_05_v5_overhaul/session-logs/*`(8 新) |
| 2026-04-17 | **_CHANGELOG 自動滾動 + Architecture 索引化** — PostToolUse auto-roll + 8 pytest；Architecture 413→150 行（-64%），7 DevHistory 子檔。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-changelog-and-architecture.md) | `tools/changelog-roll.py`(新), `commands/changelog-roll.md`(新), `tests/test_changelog_roll.py`(新), `workflow/config.json`, `hooks/workflow-guardian.py`, `_AIDocs/Architecture.md`(rewrite), `_AIDocs/DevHistory/*`(7 新+1 擴) |
| 2026-04-17 | **Evasion Guard + Test-Fail Gate** — Bash 測試失敗偵測 + Stop 完成宣告攔截 + UPS 退避舉證要求，51 pytest。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-evasion-guard.md) | `hooks/wg_evasion.py`(新), `hooks/workflow-guardian.py`, `settings.json`, `tests/test_evasion_guard.py`(新) |
| 2026-04-17 | **Atom 寫入防呆 + feedback 目錄整理 + atom_promote 合併** — AUTO-DRAFT tag / Atom-Write Guard / feedback/ 子資料夾 / merge_to_preferences。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-atom-write-guards.md) | `hooks/wg_hot_cache.py`, `hooks/workflow-guardian.py`, `hooks/wg_iteration.py`, `tools/workflow-guardian-mcp/server.js`, `memory/feedback/` |
| 2026-04-16 | **V4.1 GA** `v4.1.0` — 清除 rc2 blocker（ollama_client / user-extract-worker / prompt regex 三處根因）P=1.000 R=0.480。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-ga.md) | `tools/ollama_client.py`, `hooks/user-extract-worker.py`, `tests/integration/test_e2e_user_extract.py`, `workflow/config.json` |
| 2026-04-16 | **V4.1 P4 Session 評價機制** `v4.1.0-rc2` — `wg_session_evaluator.py` 5 維度評分 + Agent 多 Role 模擬 + `/memory-session-score`。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p4.md) | `hooks/wg_session_evaluator.py`(新), `hooks/user-extract-worker.py`, `commands/memory-session-score.md`(新), `tools/memory-session-score.py`(新) |
| 2026-04-16 | **V4.1 P3 UX Commands** `v4.1.0-rc1` — `/memory-peek` + `/memory-undo` + 每日推送 [F18] + 隱私體檢 [F21]。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p3.md) | `commands/memory-peek.md`(新), `commands/memory-undo.md`(新), `tools/memory-peek.py`(新), `tools/memory-undo.py`(新), `tools/init-roles.py` |
| 2026-04-16 | **V4.1 P2 整合** `v4.1.0-beta1` — `user-extract-worker.py` L0→L1→L2 管線 + 整合測 50 條。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p2-integration.md) | `hooks/user-extract-worker.py`(新), `tests/integration/test_e2e_user_extract.py`(新), `tests/regression/test_v4_atoms_unchanged.py`(新) |
| 2026-04-16 | **V4.1 P2 前置 Prompts + lib refactor** — 新 `prompts/user-decision-{l1,l2}.md` + `lib/ollama_extract_core.py` 抽出 7 函式。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p2-prep.md) | `prompts/user-decision-l1.md`(新), `prompts/user-decision-l2.md`(新), `lib/ollama_extract_core.py`(新), `hooks/extract-worker.py` |
| 2026-04-16 | **V4.1 Phase 1 — L0 Detector + Feature Flag + V4 Baseline** `v4.1.0-alpha1`：新建 `wg_user_extract.py`（純規則 L0 detector，信號詞中+英強/中/負三權重 + 句法 pattern [F27] + 排除規則，≤5ms）。新建 `snapshot-v4-atoms.py` [F13] 產出 `v4_atoms_baseline.jsonl`（63 atoms SHA256 + metadata）。`workflow/config.json` 新增 `userExtraction{enabled:false, mode:shadow, tokenBudget:240}`。`workflow-guardian.py` UserPromptSubmit gate（flag=true 時 detect_signal → pending_user_extract GC cap 10 [F11]）+ SessionEnd drain [F26]。pytest 81 cases 全過（74 detector + 7 disabled-flag），P≥0.95 R≥0.55 ✓ | `hooks/wg_user_extract.py`(新), `tools/snapshot-v4-atoms.py`(新), `tests/test_user_detector.py`(新), `tests/test_v41_disabled.py`(新), `tests/fixtures/v4_atoms_baseline.jsonl`(新), `hooks/workflow-guardian.py`, `workflow/config.json` |
| 2026-04-16 | **V4 漸進遷移 docs-progg 專案啟用**：migrate-v3-to-v4.py 補 12 atom metadata（Scope/Author/Created-at）、init-roles bootstrap（personal/holylight/role.md + _roles.md + .gitignore + post-merge hook）、CONTRADICT 模擬驗證成功（gemma4:e4b 正確分類 sim=0.787）、SessionStart [Role] + vector layer 欄位 + memory-health 全通過。Project_File_Tree.md 更新 V4 三層 scope 目錄結構 + 新增 commands 21 個 + tools + hooks | `_AIDocs/Project_File_Tree.md`, `_AIDocs/_CHANGELOG.md`, docs-progg `.claude/memory/*.md`(12), docs-progg `.claude/memory/personal/`, docs-progg `.claude/memory/_roles.md` |
| 2026-04-16 | **原子記憶 V4.1 設計圓桌（Plan v2 approved）**：8 大師 (人文/UX/程式/AI/原子記憶/實作/精省 token/語意) × drafting + validation 雙 round + 2 資訊整合 (Prior Art/NLP Benchmark)。核心設計：三層 gating (L0 規則 → L1 qwen3:1.7b 二元 → L2 gemma4:e4b 結構化) + Stop hook async + Hybrid threshold (0.92 直寫 / 0.70-0.92 review / 丟棄) + 28 項 validation fix。NFR：P≥0.92, R≥0.30, ≤240 tok/session。4 Phase 共 8.5 dev day。V4 SPEC/server.js 零修改（Source-turn-id 走 footer comment；metadata 走 description 自由欄）。新增 personal/auto/{user}/ 物理隔離 | `_AIDocs/V4.1-design-roundtable.md`(新), `plans/purring-percolating-glacier.md`(新), `_AIDocs/_INDEX.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-04-15 | **原子記憶 V4 Phase 6 — Migration 工具 + /init-roles + E2E 驗證收尾**：新建 `tools/migrate-v3-to-v4.py`（V3 atom 補 Scope/Author/Created-at 三 metadata，從 path 推 scope，dry-run 預設 + `--apply` 才真寫，掃 global + project-registry 全專案）。新建 `commands/init-roles.md` + `tools/init-roles.py`（互動引導：bootstrap personal/{user}/role.md、scaffold shared `_roles.md` 樣板（注意：路徑為 `memory/_roles.md` 與 `wg_roles.load_management_roster` 一致，非 `memory/shared/_roles.md`）、append `.gitignore`、可選詢問裝 `post-merge` hook，全冪等）。Guardian AtomFormat hook 新增 `_pending_review/` + `role.md` 兩道豁免（避免 backend 草稿與角色宣告檔誤觸 atom 格式檢查）。`post-git-pull.sh` 修 race condition：merge 後先 `POST /index/incremental` + 輪詢 indexing=False 最多 12s 再跑 pull-audit，避免新進 atom 還沒索引就跑分類。`USER.template.md` / `IDENTITY.template.md` 補 V4 多職務段。Architecture.md 補完整三時段衝突流程 ASCII 圖（Write-time / Pull-time / Review-time + JIT 保護說明）。E2E 三模擬 user (programmer/art/management) × 兩專案：A1-A5 MCP atom_write 分流+CONTRADICT→pending+skip_check / B1-B3 post-merge hook→pull-conflict+merge_history / C1-C4 /conflict-review approve+reject+`.resolved` 支線 / D1-D2 _pending_review 不入 JIT + 管理職 [Pending Review] 計數，全綠 | `tools/migrate-v3-to-v4.py`(新), `tools/init-roles.py`(新), `commands/init-roles.md`(新), `hooks/workflow-guardian.py`, `hooks/post-git-pull.sh`, `USER.template.md`, `IDENTITY.template.md`, `_AIDocs/Architecture.md`, `_AIDocs/_INDEX.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-15 | **原子記憶 V4 Phase 5 — 三時段衝突偵測 + Pending Review Queue**：SPEC §7 實作完成。`memory-conflict-detector.py` 新增 `--mode=write-check` 與 `--mode=pull-audit`（復用既有 vector_search + ollama_classify，新增 fail-open 機制、`Merge-strategy: git-only` 跳過、TSV `_merge_history.log` append、`.last_pull_audit_ts` 增量）。`server.js` 新增 `execConflictDetector` / `appendMergeHistory` / `buildConflictReport` helper；`toolAtomWrite` create 分支對 scope=shared 插入 write-time 語意衝突掃描 → CONTRADICT 寫 `_pending_review/{slug}.conflict.md` 報告（非 atom、阻擋寫入）、EXTEND-overlap (sim≥0.85) reroute 為 `_pending_review/{slug}.md` 草稿並標 `Pending-review-by: management`；`skip_conflict_check` 旗標供受控遷移用。新增 `commands/conflict-review.md` + `tools/conflict-review.py` 後端（list/approve/reject 三動作、is_management 雙向認證 guard、approve 時搬檔+寫 Decided-by+觸發 `/index/incremental` reindex+清理同名 .conflict.md）。新增 `hooks/post-git-pull.sh` 樣板（手動 copy 到 `{proj}/.git/hooks/post-merge`）。單元驗證：is_management (holylight=True, alice=False, nobody=False)、mgmt approve 搬檔成功、非 mgmt approve/reject 被拒、pull-audit smoke 更新 .last_pull_audit_ts、write-check 對跨專案相似 atom 正確判 CONTRADICT | `tools/memory-conflict-detector.py`, `tools/workflow-guardian-mcp/server.js`, `tools/conflict-review.py`(新), `commands/conflict-review.md`(新), `hooks/post-git-pull.sh`(新), `_AIDocs/Architecture.md`, `plans/hazy-coalescing-pudding.md`(新) |
| 2026-04-15 | **原子記憶 V4 Phase 4 — Role-filtered JIT injection + Vector layer 擴充**：SessionStart 讀 `wg_roles` 取 user/roles/management 並寫入 `state["user_identity"]`，bootstrap_personal_dir 冪等執行；`get_project_memory_dir` V4 fallback（純 V4 layout 也視為合法 mem dir）；project atom_index 在 V4 layout 下完全用 `_collect_v4_role_atoms` 結果（避免 MEMORY.md 自我循環）。動態生成 role-filtered `MEMORY.md`（auto-gen header 護寫保護 V3 人手版本）+ context line `[Role] user=… roles=… mgmt=…` + 管理職 `[Pending Review] N 件`。Vector indexer 改用新 `discover_v4_sublayers`，layer label 改為 `shared:{slug}` / `role:{slug}:{r}` / `personal:{slug}:{user}`（淘汰 `project:{slug}`），chunk metadata 新增 `Scope`/`Audience`/`Author` 三欄。Searcher 新增 `_build_v4_layer_clause(user, roles)` SQL composer + sanitize 防注入，三 search 入口 + service 三 endpoint 全部接 user/roles 參數。`_semantic_search` 同步擴 user/roles signature；UPS 自動帶入 state 身份（管理職傳 None 不過濾）。`_ATOM_INDEX.md` 加 Scope 欄。end-to-end 通過：bob (programmer) 看不到 art atom；alice (art) 收到 photoshop-naming；管理職 mgmt=True 顯示 pending review 計數；vector 同 query 不同 user 結果差異符合 SPEC §8.1 | `hooks/wg_paths.py`, `hooks/wg_intent.py`, `hooks/workflow-guardian.py`, `tools/memory-vector-service/{indexer,searcher,service}.py`, `memory/_ATOM_INDEX.md`, `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 Phase 3 — atom_write schema + 寫入路徑切換**：MCP `atom_write` schema 擴 V4 三層 scope（`shared`/`role`/`personal`，含 `role`/`user`/`audience`/`pending_review_by`/`merge_strategy` 欄位），`scope=project` 透明遷移到 `shared` + stderr deprecation hint，舊 caller 不傳 scope 預設 shared 不報錯。新 metadata 自動帶入 `Author`（server 端 env/OS user）+ `Created-at`，順序依 SPEC §4。SPEC §7.4 第一版實作：`scope=shared` 且 audience 含 `architecture`/`decision` → 自動改寫到 `shared/_pending_review/` + `Pending-review-by: management`。`buildAtomContent` 與 `resolveMemDir` 重構（新增 JS 端 `findProjectRoot` / `getCurrentUser` / `isSensitiveAudience` helper，不 spawn python）。`toolAtomPromote` 同步適配新回傳格式。9 情境 16 子項 smoke test 全綠 | `tools/workflow-guardian-mcp/server.js`, `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 Phase 2 — 路徑與角色基礎建設**：`wg_paths.py` 新增 `get_scope_dir(scope, cwd, user, role)` 處理 global/shared/role/personal 四種 scope（含 `~/.claude` 自身 guard 防遞迴），`discover_memory_layers` 加 `user`/`role` 參數擴出 `shared` / `role:{r}` / `personal:{user}` 子層（V3 無參呼叫完全相容）；新建 `wg_roles.py` 五個函式（含管理職雙向認證 + bootstrap 冪等寫 .gitignore）。8 項手動驗證全綠（含雙向認證失敗與成功情境、重跑保留使用者編輯、`.gitignore` 不重複） | `hooks/wg_paths.py`, `hooks/wg_roles.py`(新), `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 SPEC freeze (Phase 1)**：定稿三層 scope (personal / shared / role) 設計、metadata schema 擴充（Audience/Author/Pending-review-by/Decided-by/Merge-strategy/Created-at）、六大分類大類（programmer/planner/art/environment/ai/other）、三時段衝突偵測（write/pull/git-conflict）、JIT 角色 filter、管理職雙向認證。實作分 6 phases，由 `plans/gentle-puzzling-kettle.md` 拆 session 執行。同時新增 feedback atom 矯正 max thinking 模式下不可外包思考的行為 | `_AIDocs/SPEC_ATOM_V4.md`(新), `_AIDocs/_INDEX.md`, `plans/gentle-puzzling-kettle.md`(新), `memory/feedback-no-outsource-rigor.md`(新) |
| 2026-04-10 | **記憶健康工具修正 + decisions-architecture 拆分**：根因為 `718504b` Token 瘦身後 MEMORY.md 改 2 欄格式，audit 工具未跟進。修 `memory-audit.py`（支援 2 欄索引 / wildcard 索引項 / Claude-native YAML frontmatter / orphan memory dir 容忍 / `TRIGGER_MAX` 8→12 / `INDEX_MAX_LINES` 30→40）+ `atom-health-check.py`（`_` 前綴豁免 / `decisions-architecture` 加入 central hub）。新建 `decisions-architecture.md`（12 條架構決策），精簡 `decisions.md` 非架構場景注入 ~950→350 tok。順帶修 `c--projects/MEMORY.md` 死連結。Settings `defaultMode` 改 `acceptEdits` + skip flags（VS Code extension 編輯彈窗 bug #43953 未修，CLI 不受影響） | `tools/memory-audit.py`, `tools/atom-health-check.py`, `memory/decisions.md`, `memory/decisions-architecture.md`(新), `memory/MEMORY.md`, `projects/c--projects/memory/MEMORY.md`, `settings.json`, `_AIDocs/DocIndex-System.md` |
| 2026-04-09 | **V3.4 rdchat LLM gemma4:e4b**：三輪 A/B 測試（14 題型 × 5 模型）驗證後，rdchat LLM 從 qwen3.5→gemma4:e4b。extract-worker think 改 "auto" + temp 0.0。num_predict 8192→4096。local/embedding 不動。 | `workflow/config.json`, `hooks/extract-worker.py`, `hooks/wg_episodic.py`, `hooks/quick-extract.py`, `tools/memory-conflict-detector.py`, `tools/memory-vector-service/config.py`, `_AIDocs/Architecture.md`, `_AIDocs/DevHistory/ab-test-gemma4.md`(新), `memory/toolchain-ollama.md` |
| 2026-04-08 | **V3.3 DocDrift Detection**：新建 `wg_docdrift.py` 模組 — src Edit/Write 自動偵測對應 _AIDocs 是否需要更新，Hybrid 映射（config 顯式 + keyword fallback），PostToolUse advisory 提醒 + Stop gate 提示。Inspired by PR #1 (@wellstseng) | `hooks/wg_docdrift.py`(新), `hooks/workflow-guardian.py`, `hooks/wg_core.py`, `workflow/config.json`, `_AIDocs/Architecture.md` |
| 2026-04-02 | **V3.1 Token Diet**：4 Phase 精簡原子記憶 — 直刪 31 條 + 信號自描述化 5 個 + JIT _reference + MCP atom_write/promote。decisions+arch+memory-system 1,841→631 tok (-65.7%) | `memory/decisions.md`, `memory/decisions-architecture.md`, `rules/memory-system.md`, `hooks/wg_iteration.py`, `hooks/wisdom_engine.py`, `hooks/workflow-guardian.py`, `memory/_reference/internal-pipeline.md`(新), `tools/workflow-guardian-mcp/server.js` |
| 2026-04-02 | **V3.0 文件全面升級**：README V3.0（標題+三欄Token表+速度效率表+架構+流程圖+Skills 17個+V3子系統+版本歷史）+ Install-forAI V3升級section + Architecture/Project_File_Tree/DocIndex/INDEX 更新 + 7 commands 移除版號標記 | `README.md`, `Install-forAI.md`, `_AIDocs/*.md`, `commands/*.md`, `CLAUDE.md` |
| 2026-04-02 | **V3 Phase1 實作：Hot Cache + Quick Extract**：新建 wg_hot_cache.py（lock+原子寫入 3 API）、quick-extract.py（Stop async hook, qwen3:1.7b 快篩→hot cache）、decisions-architecture.md Hot Cache 段落 [臨]→[觀] | `hooks/wg_hot_cache.py`(新), `hooks/quick-extract.py`(新), `memory/decisions-architecture.md` |
| 2026-04-02 | **V3 三層即時管線升級**：decisions.md 新增 V3 管線+SessionStart 風暴修復決策、decisions-architecture.md 新增 Hot Cache/Async Hook/去重技術細節、cc-hook-system.md 補充 additionalContext 可用性差異 | `memory/decisions.md`, `memory/decisions-architecture.md`, `_AIDocs/ClaudeCodeInternals/cc-hook-system.md` |
| 2026-04-01 | **atom 全面精簡 — 經歷敘述型內容歸檔**：10 個 atom 移除演化日誌段落、decisions.md 移除 5 條版本遷移敘述（V2.18~V2.21）改為 2 條現狀事實、toolchain-ollama.md A/B 完整表格改為 3 條結論、移除各處版本標籤前綴。歸檔至 `_AIDocs/DevHistory/`（3 檔）。估計省 ~1100 tok/session | `memory/*.md`（10 atoms）, `_AIDocs/DevHistory/*`（3 新檔） |
| 2026-04-01 | **原子記憶系統全面驗證 + 修正**：decisions.md 修正 4 處 + architecture 計數器歸零 + Vector DB full reindex | `memory/decisions.md`, `memory/wisdom/reflection_metrics.json` |
| 2026-03-30 | **_AIDocs V2.21 全面同步**：Architecture.md V2.17→V2.21 + Project_File_Tree 重寫 + DocIndex 更新 | `_AIDocs/Architecture.md`, `_AIDocs/Project_File_Tree.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/_INDEX.md` |
| 2026-03-30 | **全面清理 + V2.21 自治修正**：修復 2 處硬編碼 + 刪除 backups/Logs/debug + 清理孤兒 slugs | `hooks/workflow-guardian.py`, `settings.json`, `_AIDocs/`, `memory/_reference/` |
| 2026-03-30 | **Dashboard 4 修復 + 版本集中化** | `tools/workflow-guardian-mcp/server.js`, `version.json` |
| 2026-03-27 | **V2.21 Phase 7 Skills/Tools 專案層支援** | `tools/memory-audit.py`, `tools/memory-conflict-detector.py`, `commands/*.md`, `server.js` |
| 2026-03-27 | **文件版本同步 V2.21** | `README.md`, `Install-forAI.md`, `memory/_reference/*`, `memory/MEMORY.md` |
| 2026-03-27 | **V2.21 Phase 3 專案自治層建置**：`init-project` skill 新增 Step 6 建立 `.claude/` 自治層。`workflow-guardian.py` 新增 `_call_project_hook()` subprocess 隔離呼叫 | `commands/init-project.md`, `hooks/workflow-guardian.py` |
| 2026-03-27 | **V2.21 Phase 2 Project Registry + 路徑切換**：`register_project()` + registry 管理。`find_project_root()` 加 `.claude/memory/MEMORY.md` 辨識。`get_project_memory_dir()` 新路徑優先 | `hooks/wg_paths.py`, `hooks/workflow-guardian.py`, `hooks/wg_atoms.py`, `tools/workflow-guardian-mcp/server.js` |
| 2026-03-27 | **V2.20 Phase 0.3 C2 修復**：`_truncate_context_by_activation` 加 `source_dirs` 參數修正專案層 ACT-R score 永遠 -10.0 問題 | `hooks/wg_atoms.py`, `hooks/workflow-guardian.py` |
| 2026-03-24 | **V2.18 Phase 2 Section-Level 注入**：向量服務新增 `ranked_search_sections()` + `/search/ranked-sections` endpoint。`_semantic_search()` 回傳帶 sections。注入迴圈新增 section 提取（`_extract_sections()`），大 atom 省 69-87% tokens。安全防護：0 匹配/70% 閾值/服務不可用皆 fallback 全量注入 | `searcher.py`, `service.py`, `wg_intent.py`, `wg_atoms.py`, `workflow-guardian.py` |
| 2026-03-23 | **V2.18 Phase 0+1**：環境清理（LanceDB 289→25MB、刪 7 死檔）+ 9 atom Trigger 精準化 + misdiagnosis/harvester 內容精簡 | `memory/*.md`, `MEMORY.md`, `workflow/config.json` |
| 2026-03-23 | **V2.17 合併升級**：V2.16 自我迭代自動化 + V2.17 覆轍偵測 + AIDocs 內容閘門 + WebFetch Guard | `hooks/workflow-guardian.py`, `settings.json`, `rules/aidocs.md`, `memory/decisions*.md`, `CLAUDE.md`, `README.md`, `_AIDocs/*.md` |
| 2026-03-19 | **V2.15 定義版本**：全文件版本號 V2.12→V2.15 統一。README 版本歷史補 V2.13/V2.14/V2.15 | `CLAUDE.md`, `README.md`, `Install-forAI.md`, `_AIDocs/*.md`, `memory/decisions*.md`, `rules/session-management.md` |
| 2026-03-19 | **V2.14 Token Diet**：`_strip_atom_for_injection()` 注入前 strip 9 種 metadata + 行動/演化日誌。SessionEnd 從 byte_offset 跳已萃取段。cross-session lazy search 預篩。省 ~1550 tok/session | `hooks/workflow-guardian.py`, `hooks/extract-worker.py`, `memory/MEMORY.md`, `workflow/config.json` |
| 2026-03-19 | **atom 精準拆分+設定精修**：toolchain-ollama 獨立拆分 + workflow-icld 拆分 + trigger 瘦身 + failures 拆分子 atoms + GIT 流程去重 + 設定檔去重/瘦身/統一管理 | `memory/*.md`, `workflow/config.json` |
| 2026-03-19 | **V2.13 Failures 自動化系統**：Guardian 偵測失敗關鍵字 → detached extract-worker 萃取失敗模式 → 三維路由自動寫入對應 failure atom | `hooks/extract-worker.py`, `hooks/workflow-guardian.py`, `workflow/config.json`, `memory/failures/` |
| 2026-03-19 | **vector service timeout 修正**：冷啟動 7.5s 但 caller timeout 2-5s，調整 timeout + 預熱 | `hooks/workflow-guardian.py`, `tools/memory-vector-service/` |
| 2026-03-18 | **V2.12 逐輪增量萃取**：Stop hook per-turn extraction（byte_offset + cooldown 120s + PID guard）+ 萃取 dedup 統一 0.65 + intent 選取 bug 修正 | `hooks/extract-worker.py`, `hooks/workflow-guardian.py`, `workflow/config.json` |
| 2026-03-18 | **注入精準化**：AIDocs keyword 重新設計 + IDE 標籤過濾 + keyword boundary 防誤匹配 + `/atom-debug` skill | `_AIDocs/_INDEX.md`, `hooks/workflow-guardian.py`, `commands/atom-debug.md` |
| 2026-03-13 | **選擇性 cherry-pick**：從來源 V2.10 合併 `/continue` skill + `/resume` staging 安全網 + `BOOTSTRAP.md` + `workflow-rules.md` 補回大型計畫/GIT/同步判斷段落 | `commands/continue.md`, `commands/resume.md`, `BOOTSTRAP.md`, `memory/workflow-rules.md` |
| 2026-03-13 | **/read-project**: 新增 DocIndex-System.md（76 檔系統索引）+ doc-index-system atom | `_AIDocs/DocIndex-System.md`, `memory/doc-index-system.md`, `memory/MEMORY.md` |
