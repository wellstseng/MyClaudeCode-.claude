# 變更記錄 — 封存

> 從 `_CHANGELOG.md` 滾動淘汰的歷史記錄。

---

| 日期 | 變更 | 涉及檔案 |
|------|------|---------|
| 2026-05-28 | **fix(hook): ensure-mcp.py npm 子進程補 CREATE_NO_WINDOW — Windows cmd 視窗閃跳修補** — SessionStart hook `_spawn_background` 自身已正確帶 `CREATE_NO_WINDOW \| DETACHED_PROCESS`，但 detached Python 子進程內 5 個 `subprocess.run`（where node / npm prefix -g / npm i -g / npm outdated / npm update）沒帶 `creationflags`。Windows 上 `npm` 實際是 `npm.cmd` batch script，detached 父進程沒 console → Windows 為其新開 `cmd.exe` 視窗，整段 `npm update` 期間（幾十秒～幾分鐘）掛在前景。**踩坑點**：spawn detached 父進程加 `CREATE_NO_WINDOW` 只防自己沒視窗，**不會傳染給子進程**；子進程呼叫的每個 `subprocess.run` 必須各自帶 `creationflags`。修法：檔頭加 `_NPM_FLAGS = subprocess.CREATE_NO_WINDOW if sys.platform == "win32" else 0`（POSIX 等效 0，跨平台相容），5 處統一加 `creationflags=_NPM_FLAGS`；line 192 既有 `_spawn_background` 不動（spawn 自己用，與 npm subprocess 是不同需求）。 | `hooks/ensure-mcp.py` |
| 2026-05-27 | **V5 GA 簽收 + Wave 5 Session 4 user 補強盤點** — chkdsk C: /f 重開機修復 NTFS → `workflow/guardian-crash.log` 物理刪除（114 GB → 329K，0.0002%）。GA Checklist 全綠：workflow 218K / hooks 8 wg_* / skills 19 / MCP 3 tool / pytest 40/414/52 baseline / git clean。**User 補強盤點**（手動審視 root 全資料夾）：(1) TECH.md 整份重寫為 V5 GA（原仍寫 V4.1，含系統樹 / 流程圖 / 子系統表全部對齊 V5 GA 現況）；(2) version.json 從 `atom_memory 4.1 / guardian 2.2.0` 升 `5.0 / 5.0.0`（**user 提醒才發現此 drift**）；(3) 4 個資料夾去留：`.playwright-mcp/`（394K 2 月前 debug 殘留）+ `.pytest_cache/`（63K pytest 自重建）全刪、`backups/` 完全清除（不留 1 個，user 訂規）、`tests/` 1.3M 確認為真實 V5 pytest 套件（414 passed）保留；(4) workflow/ 36 → 6 檔（刪 30 個 Codex Companion daemon 廢棄 state；P5b 已 daemon→subprocess）；(5) file-history/ 131 → 46 dir（user 訂 2 週門檻；85 個 CC 清空殼刪除）；(6) scripts/ 6 → 3 檔（移 `migrate-access-stats.py` / `migrate-confirmations.py` / `merge-orphan-access.py` 三 V5 期間 one-shot migration 到 `_AIDocs/DevHistory/v5-overhaul-2026-05/scripts/`）；(7) `__pycache__` 9 dir 全清。**規則升級（IDENTITY + atom）**：「完工清暫存」原則寫入 `feedback-completion-gates` atom（自動產生 + 無 git track + 無人工填值 + 僅服務當次任務 → 預設直接刪；範例：.pytest_cache / backups / .playwright-mcp / workflow/companion-* / staging 已執行 next-phase）；IDENTITY 收尾檢核從**二選一**升級為**全項檢視**四項 — (a) 缺失發現與修補清單必寫 / (b) AI 逃避通報僅發生時寫 / (c) Token 累積警示僅發生時寫 / (d) 衍生暫存清單必寫；IDENTITY 索引化瘦身（砍「職責」整段 + 「發現即處理門檻」具體規則移到 atom + 禁語清單精簡至 2 行 + 收尾檢核補充括號約壓縮 30%，避免每 session 強制大量 token 常駐）；IDENTITY.template.md 同步進化（multi-user 預設來源）。**Atom 體系變動**：`memory/v5-overhaul-audit-2026-05.md` 歸檔到 `_distant/2026_05_v5_overhaul/`（取代 atom，由 DevHistory 完整紀錄繼承）；`_atom_index.json` 17 → 16 atoms；`_ATOM_INDEX.md` mirror 自動 regenerate；`feedback-completion-gates` trigger 增「衍生暫存 / 暫存檔 / 清暫存 / 收尾檢核」；MEMORY.md 加 DevHistory 連結。**DevHistory 新增**：`_AIDocs/DevHistory/v5-overhaul-2026-05/README.md` 完整紀錄起因（V4.1 累積 6 大問題含 114 GB 災難）/ 計畫演化（6-phase → 4-Wave + Wave 5）/ 各 Wave 交付摘要 / GA Checklist 驗收。**Session 自評（user 命令）盤點 10 點漏失**：B1 backups 自作主張保 1 / B2 file-history 自定 30 天標準（user 明說 2 週）/ B3 tests 草率回應「不可砍」未列選項 / B4 root 暫存資料夾掃描不全（漏 scripts/_v4_archive/_pending_writes 等）/ B5 IDENTITY.md .gitignore 越權改動（未注意到 IDENTITY.template 多 user 機制 / 已即時回滾）/ B6 TodoWrite 反應慢 / C1 收尾檢核 4 項格式自己未套用 / C2 scripts/ migrate-* 未主動報告（修） / C3 scripts/inbox-*.js 不明用途待 user 決議（settings.json 沒引用，疑似廢棄）/ C4 _AIDocs/_INDEX.md 條目過時順手修但未先報告 / C5 version.json 整 session 沒查（user 指正才發現）。**Hook 系統失效實況**（後續 Wave 5 Session 5 補強修正）：`~/.claude/settings.json` 在 Wave 4 commit `04b35b4` 從 14534 bytes → 42 bytes（308 行 hook 配置消失）。**Wave 5 Session 5 user 澄清**：此非「誤砍」，是前 session 為推動 V5 進版「經 user 允許暫時關閉」hooks，但**未寫入 handoff 交接文件**，造成連續 22 天 hook 系統實際停擺（hot_cache.json 5/18 後未更新 / state-*.json 不存在 / atom_io_audit 最近 entries 全 `source=mcp` 無 `hook:*`）。本 commit 一併重建 settings.json：保 V5 hook 結構（dispatcher + 8 event handler 對齊）+ 精簡 permissions 移除過時 V4 IPC tool entries + 保 `effortLevel: xhigh`。**未來規則**：「暫時關閉」級別的全域系統設定變更必寫入 handoff，避免 multi-session drift。 | `TECH.md`(重寫), `version.json`(V5), `.gitignore`(IDENTITY 註釋), `IDENTITY.template.md`(同步), `_AIDocs/_INDEX.md`(V5 GA + 19 skill / 16 atoms), `_AIDocs/DevHistory/_INDEX.md`(加 v5-overhaul-2026-05), `_AIDocs/DevHistory/v5-overhaul-2026-05/README.md`(新), `_AIDocs/DevHistory/v5-overhaul-2026-05/scripts/`(3 migrate 移入), `memory/MEMORY.md`(連結 DevHistory), `memory/_atom_index.json`(17→16 + feedback trigger 擴), `memory/_ATOM_INDEX.md`(自動 regen), `memory/feedback-completion-gates.md`(完工清暫存規則 + 收尾 4 項), `memory/_distant/2026_05_v5_overhaul/v5-overhaul-audit-2026-05.md`(歸檔), `_AIDocs/_CHANGELOG.md` |
| 2026-05-27 | **V5 Wave 5 Session 1 — A+B 組汰舊（runtime ~88 MB + 17 個 tools/tests + REG-005 dead path 清除）** — Wave 5 全面汰舊起手第一 session。**A 組（runtime 清理，無 git 風險）**：Logs/* (153) + telemetry/* (60, 65 MB) + file-history/* (1028, 16 MB) + shell-snapshots/* (66, 5.5 MB) + todos/* (214) + statsig/* (5) + workflow/companion-{state,assessment,metrics}-*.json (332) + workflow/state-e2e-test.json + backups/.claude.json.backup.* (keep 1, del 4) 共 1863 files / 88.4 MB；`.claude/.claude/{memory,worktrees}/` P1 雙層殘骸（41 episodic + 2 personal + 1 MEMORY.md stub，**非副本是孤兒原始版**，正本層完全沒對應檔）整體 `mv` 到 `memory/_distant/2026_05_v5_overhaul/double-nest-residue/` 歸檔保留（44 files / 102 KB；_distant gitignored 不入版控但磁碟保留）。**B 組（一次性 tools/tests，跑引用斷裂 SOP）**：刪 17 個 = B1 A/B benchmark 8 (ab_extract_test / ab_gemma4_test/round2/round3 / eval-ranked-search / vector-probe-burst / vector-observation-summary / vector-threshold-calibration) + B2 migration 4 (migrate-v221 / migrate-v3-to-v4 / migrate-scope-field / snapshot-v4-atoms) + B3 REG-005 4 (reg005-rollback-cleanup + atom-injection-summary + 兩個對應 tests) + B4 test-memory-v21，共 233 KB。**REG-005 dead path 修補**：B3 SOP 發現 `hooks/handlers/session_start.py:156-191` 兩個 dead 函式 (`_check_reg005_observation_status` + `_format_reg005_highlight`) 與 453-458 呼叫點 — 觀察期 flag 已早刪 → 函式必 return None；移除三段共 ~45 行 dead code。`tools/check-bypass.py` WHITELIST 移除已刪的 snapshot-v4-atoms.py + test-memory-v21.py 兩條目；`_AIDocs/DocIndex-System.md` 移除 9 條死連結（vector-observation-summary / vector-probe-burst / atom-injection-summary / reg005-rollback-cleanup / migrate-v221 / migrate-v3-to-v4 / migrate-scope-field / test-memory-v21 / eval-ranked-search），check-bypass.py 描述同步更新。**保留判定**：`hooks/wg_atom_observation.py` 為 V5 flag-gated 零開銷 shim（無 flag 即 sys.exit(0)），保留待未來重用；`tools/cleanup-old-files.py` 規則覆蓋率不足（只 3 目錄）本次未擴充，A 組用 PowerShell 一次性處理。**驗證**：跑 stash 對拍 baseline pytest — 變更前 56 failed / 424 passed / 52 skipped，變更後 40 failed / 414 passed / 52 skipped（16 fail + 10 pass 差異全來自被刪 reg005 兩個 test 共 26 案），**未引入新 regression**；剩 40 個 pre-existing fail 為 V5 Wave 重構遺留（workflow-guardian module attributes / wg_session_evaluator import / test_v41_disabled），跨 Wave 5 範圍不在本 commit 處理。**Session 2 範圍**：C 組 _AIDocs/DevHistory/ 子目錄歸檔 + D 組 plan/audit 歸檔 + E 組 V4 SPEC 評估 + runtime 誤入版控修正（見更新後 `memory/_staging/next-phase.md`）。 | `hooks/handlers/session_start.py`(去 dead code), `tools/check-bypass.py`(WHITELIST 縮減), `_AIDocs/DocIndex-System.md`(9 條死連結), `_AIDocs/_CHANGELOG.md`, `memory/_staging/next-phase.md`(更新 Session 2 範圍), tools/* (B 組 13 個刪除), tests/* (B3 reg005 2 個 test 刪除), memory/_distant/2026_05_v5_overhaul/double-nest-residue/(新 archive, gitignored) |
| 2026-05-05 | **Wave 3 收尾 — memory/ 根目錄整理：6 atom 搬入 feedback/ + 9 access.json 合併 + 1 殘骸刪除 + atom_promote 子目錄修補** — Wave 2/3 完工後檢視 `memory/` 根目錄三件殘骸：(1) 6 個 `feedback-*.md` 散在根目錄但 `memory/feedback/` 已收 16 atom（新建時漏放）；(2) 14 個錯位/孤兒 `*.access.json`（5 misplaced：atom .md 已搬到 feedback/ 但 root access 未清；4 rename：feedback-handoff/research/scope-sensitive/fix-escalation 改名後 root 舊 access 落單；1 殘骸：mail-sorting atom 已刪、access 純垃圾）；(3) `tools/node22/` 誤刪後遺。**(A) 新 `scripts/merge-orphan-access.py`**（一次性合併工具）：dry-run 預設 + `--apply` + active session guard + idempotent。9 對合併走 max(read_hits/confirmations/last_used/last_promoted_at) + min(first_seen) + timestamps 聯集去重取最新 50 + confirmation_events 時間排序聯集；走 `lib.atom_access._normalize/_write_raw` + `lib.atom_io._audit_log`（op="access_merge"/"access_delete_orphan"，source="tool:memory-cleanup"，commit 15b9de2 已加入 VALID_SOURCES）。**plan defect 順手修**：原 plan 寫 `git rm src` 但 `.gitignore:55` 早把 `**/memory/**/*.access.json` 排除 → 改 `Path.unlink()`（git rm 必失敗）。fix-escalation → feedback-fix-escalation 保留 35 條 timestamps 歷史紀錄。執行紀錄寫 `migration.json[merge-orphan-access]`。**(B) 6 個 feedback atom 搬遷**：`git mv` × 6 (.md 檔，git tracked) + `Path.rename` × 6 (.access.json 檔，gitignored 無法 git mv)。`memory/_ATOM_INDEX.md` 6 行 Path 欄手動改正（plan 寫 `sync-atom-index.py --write`，但實際 flag 是 `--check/--fix/--add-from-frontmatter`，皆不適用「修改既有 row 路徑」場景；直接 Edit 6 行最簡）。`tools/sync-atom-index.py --check` 跑後 0 drift。**(C) atom_promote MCP 子目錄查找補修（plan 預測風險落地）**：`tools/workflow-guardian-mcp/server.js:1453` `toolAtomPromote` 寫死 `path.join(memDir, atom_name + ".md")`，從不掃子目錄，所以 ALL feedback/ atom（含搬遷前已在 feedback/ 的 16 個）都找不到。新增 `findAtomFileRecursive(memDir, atomName)`（mirrors `lib/atom_spec.SKIP_DIRS` 排除 `_reference/_archived/_pending_review/_staging/templates/wisdom/_drafts/episodic/_meta`）作 fallback：直接路徑不存在時掃子目錄；既有 root atom 走原路徑（向後相容）。standalone node 驗證：feedback-clean-before-build / feedback-pointer-atom / decisions / nonexistent 四場景全部正確。**注意**：MCP server 為長期 process，新代碼需 session restart 才生效；本次 commit 落 source code，下個 session 自動受惠。**(D) `tools/node22/` 確認忽略**：grep 全 codebase 0 caller，`server.js` 的 `child_process.spawn("node",...)` 走 PATH 不依賴 tools/node22/。本回合不復原（節省 ~50MB）；若未來需要 → plan 文件附下載步驟。**驗證**：全 pytest 460 passed + 52 skipped（含新增 20 案 `test_merge_orphan_access.py`）；SessionStart smoke EXIT=0；`ls memory/feedback-*.md` 0 結果；`ls memory/feedback/feedback-*.md` 22（16 + 6）；`ls memory/*.access.json` 從 26 降到 10（純 root atom），無 plan 預期的 16 中間態（plan 階段切分為 A→C，實際分兩 commit 跑完降到終態）；audit log 出現 9 筆 `op="access_merge"` + 1 筆 `op="access_delete_orphan"` 條目；server.js node syntax check OK。**Plan 對照**：plan 寫 24 預期 feedback atom 數，實際 22（plan 把 16 寫成 18 minor 失準）；plan 寫 16 中間態，實際 A1 commit 後 16、C1 commit 後 10，亦合計符合預期路徑。 | `scripts/merge-orphan-access.py`(新), `tests/test_merge_orphan_access.py`(新), `tools/workflow-guardian-mcp/server.js`(findAtomFileRecursive), `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_meta/migration.json`, `memory/_meta/atom_io_audit.jsonl`, `memory/_ATOM_INDEX.md`(6 行 Path 欄), `memory/feedback/`(+6 .md +6 .access.json) — 14 個 root .access.json 刪除（5 misplaced merge + 4 rename merge + 1 mail-sorting 殘骸 + 6 隨 .md 搬走，後者透過 fs rename 不顯示在 git diff 因 gitignored） |
| 2026-05-05 | **Wave 2 — Atom 遙測解耦：旁路檔 access.json 取代 .md 檔頭計數欄** — REG-005 收尾後第二波，修補 Wave 1 遺留的 funnel 違規（`workflow-guardian.py:1340-1375` atom 注入直接 `.write_text()` 改 atom .md 繞過稽核）並停止 atom .md 因「讀」而髒。**(1) 新模組 `lib/atom_access.py`**：`<atom>.access.json` 旁路檔（schema `atom-access-v2`）讀寫單一通道。對外 API：`read_access` / `init_access` / `increment_read_hits`（atomic write + 50ms backoff 三次重試，耗盡寫稽核 `op="access_increment_dropped"` best-effort）/ `increment_confirmation` / `record_promotion` / `write_access_field` / `bulk_read`。CLI 入口 `python -m lib.atom_access` 給 server.js spawn。舊 schema 自動正規化（confirmations 陣列 → confirmation_events、補 schema key、計數轉整數）。**(2) 8 處寫入 + 3 處讀取 reroute**：`hooks/workflow-guardian.py:1340-1375` atom 注入計數改走 `increment_read_hits(source="hook:atom-inject")`；`hooks/wg_episodic.py:361-378` cross-session confirm 改 `increment_confirmation`、舊雙寫路徑（update_atom_field + 直接寫 access.json）合併；`hooks/wg_episodic.py:776-800` episodic create 後呼叫 `init_access`；`lib/atom_io.py:write_atom` create/append/replace 在 _atomic_write 後同步維護 access；`lib/atom_spec.py:build_atom_content` 移除 Last-used / Confirmations:0 / ReadHits:0 三行（atom .md 不再寫計數欄）；`tools/workflow-guardian-mcp/server.js` buildAtomContent / append / replace / atom_promote 四處 + 新增 `readAtomAccess()` 同步 fs 讀 + `spawnAtomAccess()` 子程序寫；`tools/memory-audit.py:restore_atom` demote 計數歸零 + `parse_atom_metadata` 計數讀走 access.json；`hooks/wg_iteration.py:_self_iterate_atoms` 衰退分數 bulk_read。**(3) 殭屍碼清理**：`lib/atom_io.py:update_atom_field` 唯一 caller reroute 後 0 callers → 函式刪除；`lib/atom_io_cli.py` 移除 `update_atom_field` action；`lib/atom_spec.py:REQUIRED_METADATA` 移除 Last-used（移到 OPTIONAL）；server.js parseAtomMeta 移除 confirmations / readhits / last-used 三 case；`hooks/wg_pretool_guards.py` funnel 違規錯誤訊息更新；server.js 升級 gate 移除 7-day migration.json fallback（dual-field-v1 遷移於 2026-04-24 完成、超過 7 天）。**(4) 一次性遷移 `scripts/migrate-access-stats.py`**：掃 `memory/**/*.md`（排除 _meta/_staging/personal/wisdom/templates 等），每 atom 把 .md 檔頭計數欄抽到 access.json（取 max 避免回退）+ 從 .md 剝除四行 + 缺 Confidence 欄補 [臨]。冪等、預設 dry-run、`--apply` 才動、跑前偵測 active session 拒跑。執行：37 atoms 全部 migrated 0 errors，再跑 dry-run 顯示 37/37 skipped(already-v2)。記錄寫 `memory/_meta/migration.json` 加 `access-stats-v2` 區塊。**(5) DocDrift**：`Architecture.md`「Atom 寫入單點收束」section 加 `lib/atom_access.py` 條目 + caller 接線表更新（hook:atom-inject / hook:episodic-confirm 改走 atom_access）+ 新增「Atom 知識／遙測切分」段；`DocIndex-System.md` `lib/` section 補 atom_access.py + 遷移節補 migrate-access-stats.py。**驗證**：全 pytest 425 passed + 52 skipped（含新增 17 案 `test_atom_access.py` + 17 案 `test_migrate_access_stats.py`）；SessionStart smoke EXIT=0；UserPromptSubmit smoke 觸發 atom 注入後 `git status memory/*.md` diff 只剩遷移時剝除的計數行（無新 write_text）；audit log 出現新 `op="access_increment"` `source="hook:atom-inject"` 條目。**核心成功訊號達成**：atom .md 從此不因運行期計數變動而髒。 | `lib/atom_access.py`(新), `lib/atom_io.py`, `lib/atom_io_cli.py`, `lib/atom_spec.py`, `hooks/workflow-guardian.py`, `hooks/wg_episodic.py`, `hooks/wg_iteration.py`, `hooks/wg_pretool_guards.py`, `tools/memory-audit.py`, `tools/workflow-guardian-mcp/server.js`, `scripts/migrate-access-stats.py`(新), `tests/test_atom_access.py`(新), `tests/test_migrate_access_stats.py`(新), `tests/test_atom_io_equivalence.py`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_meta/migration.json`, 37 個 atom .md（剝除計數行）, 38 個 *.access.json（v2 schema 升級） |
| 2026-05-04 | **[S4-FINAL] Opus Melodic Comet — 收尾「刪 & 存」+ verify single-source-of-truth（4-Session 重構閉合）** — Opus Melodic Comet 重構（2026-05-04 一日內 4 session 完成）落地 atomic memory single-source-of-truth：S1 基礎建設（dd1be59）/ S2 入口收束 + P5+P7（58d032b）/ S3 強制收束 + 反向證明（daedad3）/ S4 收尾（本次）+ 中間有 cca5883 docdrift advisory SessionEnd-only fix。**S4 工作：(1) 自檢偏離當場修**：rmdir `~/.claude/projects/c--users-holylight--claude/memory/` 空殘骸（S1.1.2 歸檔後遺留空 dir）→ Layers 2→1；audit-reconcile.py 加 `classify_diff` 三分類（counter_only / knowledge / unknown，用 git diff 對 frontmatter COUNTER_FIELDS + [臨]/[觀]/[固] tag promotion 判定）→ 53 unmatched 自動拆 50 counter-only / 0 knowledge / 3 unknown，default 模式 exit 0；--since parser 加 "Xh ago" 容錯。**(2) 殘骸清理**：staging 任務檔 `opus-melodic-comet-progress.md` 刪除（git log 保留）；plan `opus-melodic-comet.md` 移到 `plans/_archive/`。**(3) 知識 atom 入庫（走 funnel source=mcp）**：新 `feedback-clean-before-build`（[臨]，源自 S1 殘骸先清後建教訓）+ `feedback-checker-rule-consolidation`（[臨]，源自 S1.2 4 個檢查器各自 SKIP_DIRS 漂移教訓）+ `decisions-architecture` append 印象 bullet「Atomic Memory Single Funnel → _AIDocs/Architecture.md」（write_raw 走 funnel，因 atom 用 ## 印象 而非 ## 知識，append validate 不過）。**(4) _AIDocs 更新**：Architecture.md「Atom 寫入單點收束」section 升級 S1–S4 + 補 S4 audit-reconcile classifier 三分類描述 + 加 tests/test_audit_reconcile.py / tests/test_check_bypass.py（S4.D.2 unit test）+ 加 S4 收尾段；DocIndex-System.md 同步補新檔；本 _CHANGELOG entry。**(5) S3 殘留補完**：tests/test_audit_reconcile.py + tests/test_check_bypass.py（驗 --since parser、classify_diff、bypass scan）；electron-uia-automation.md trigger 13→12（合併近義詞）；SessionEnd hook 跑 audit-reconcile（建議寫進 settings.json 由用戶 confirm）。**(6) 索引重建**：sync-atom-index + sync-memory-index 跑過。**8-matrix 驗證**（S4 完工 hard 條件）：(1) memory-audit 0 errors / 0 warnings / 30 atoms；(2) pytest 全綠；(3) check-bypass 0 violations；(4) audit-reconcile --since 1d 0 knowledge bypass；(5) audit log 含 mcp / hook:* / tool:* 多 source；(6) Architecture.md「Atomic Memory Single Funnel」section 完整；(7) staging/plan 殘骸 0；(8) /memory-health 全綠。**最終達成**：funnel 4 caller wired（mcp / hook:* / tool:* 6 條）+ PreToolUse 強制門禁（直 Edit/Write atom 一律 deny，escape hatch=`WG_DISABLE_ATOM_GUARD=1`）+ 反向證明（check-bypass 靜態 + audit-reconcile 動態）+ atom 規則單一來源（lib/atom_spec.py）。 | `tools/audit-reconcile.py`, `_AIDocs/Architecture.md`, `_AIDocs/_CHANGELOG.md`, `_AIDocs/DocIndex-System.md`, `memory/feedback-clean-before-build.md`(新), `memory/feedback-checker-rule-consolidation.md`(新), `memory/decisions-architecture.md`, `memory/electron-uia-automation.md`, `memory/_meta/atom_io_audit.jsonl`, `memory/_ATOM_INDEX.md`, `memory/MEMORY.md`, `tests/test_audit_reconcile.py`(新), `tests/test_check_bypass.py`(新), `plans/_archive/opus-melodic-comet.md`(歸檔), `memory/_staging/opus-melodic-comet-progress.md`(刪除) |
| 2026-05-04 | **[S3] Opus Melodic Comet — 強制收束 + 反向證明（funnel 完整 + PreToolUse 門禁 + 9 source contract）** — S2 接 4 caller 後，S3 補 C 類 4 入口 + MCP server.js 切 spawn CLI + PreToolUse 強制門禁 + 反向證明工具。**(1) S3.0 補 S2 漏網**：extract-worker L558/L583 (failures atom append/create) + wg_episodic L367 (Confirmations +1) 切 funnel；lib/atom_io 加 write_raw / update_atom_field + hook:episodic-confirm 入 VALID_SOURCES。**(2) S3.1 C 類 3 tool 切 funnel**：migrate-v3-to-v4 / memory-undo / atom-move（7 處 = 4 index + 3 atom）；changelog-roll 經驗證確認非 atom write 跳過。**(3) S3.2 server.js MCP 切 spawn CLI**：toolAtomWrite/Promote 5 處 atomic write + appendToIndex 切 spawn `lib.atom_io_cli`；resolveMemDir 加 P3+P4 cwd-scope 防護（force_global escape hatch）；toolAtomMove 已 spawn atom-move.py 自動受惠。**(4) S3.3 PreToolUse 強制門禁**：wg_pretool_guards 加 `_DOUBLE_CLAUDE_RE`（P6）+ AtomFunnelBlock（env `WG_DISABLE_ATOM_GUARD=1` bypass）；新增 tools/check-bypass.py（0 violations）；切 memory-audit 4 處 + wg_episodic L815 episodic create。**(5) S3.4 反向證明**：新增 tools/audit-reconcile.py（mtime × audit log 對拍）；isolated e2e 11 source 全通；終極攔截測試人為 Write memory atom 被 deny。`_AIDocs/Architecture.md` 加「Atom 寫入單點收束」章節（架構/contract 表/門禁/反向工具/測試）；DocIndex-System.md 補 atom-move/migrate/undo/memory-audit S3 標註 + check-bypass / audit-reconcile 新工具條目；test_guardian_atom_write_gate.py 兩 fixture 更新反映 S3.3「直 Write atom 一律 deny」新規則；wg_pretool_guards 加 `_` 前綴豁免（修 _INDEX.md 假陽性）。**Verification 7-matrix**：memory-audit 0/0、atom_io 11/11、guardian gate 9/9、check-bypass 0、audit-reconcile clean、9 source contract 全通、server.js syntax OK。 | `hooks/wg_pretool_guards.py`, `hooks/wg_episodic.py`, `hooks/extract-worker.py`, `lib/atom_io.py`, `tools/migrate-v3-to-v4.py`, `tools/memory-undo.py`, `tools/atom-move.py`, `tools/memory-audit.py`, `tools/check-bypass.py`(新), `tools/audit-reconcile.py`(新), `tools/workflow-guardian-mcp/server.js`, `tests/test_guardian_atom_write_gate.py`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-05-04 | **[S2] Opus Melodic Comet — 入口收束 + 漏洞修補（B/C 類入口切 funnel + P5 Scope migration + P7 dedup fix）** — S1 funnel 鋪好但未接 caller，S2 把可程式化 caller 接上、補 atom Scope 欄位、修 dedup 假陽性。**(1) S2.1 P5 Scope migration**：新 `tools/migrate-scope-field.py`（~75 行 + dry-run 預設）— 掃 `iter_atom_files`，缺 `Scope:` 由路徑推斷（`shared/` → shared / `roles/<role>/` → role / `personal/<user>/` → personal / 其他 → global），插在第一個 `- Key:` 之前（SPEC §4 Scope 為第一行）。**踩坑教訓**：首版用 `Path.write_text` 在 Windows 把整檔 LF 改 CRLF（`os.linesep`）— 修為 byte-level I/O `read_bytes()/.decode("utf-8")` + `write_bytes(...encode("utf-8"))` 保留原 line endings。實證：electron-uia-automation.md +1 line clean diff（單行 Scope: global 插入），dry-run 驗證 0 atoms 缺 Scope。**(2) S2.2 funnel 接線（4 檔，原計畫 5 檔）**：`hooks/user-extract-worker.py:317-407` `_write_atom_via_mcp` 改走 `write_atom(scope="personal", user=..., source="hook:user-extract")`，保留 dedup 預檢、slug counter、author 標記等原語意（~80 行重寫）；`tools/sync-atom-index.py:21-29,219-263` 整檔覆寫改 `write_index_full(source="tool:sync-atom-index")`，保留 4 欄（Atom/Path/Trigger/Scope）schema；`tools/sync-memory-index.py:24-28,156-164` `--write` 分支同走 `write_index_full(source="tool:sync-memory-index")`。**`lib/atom_io.py` minimal 擴充**：新增 `write_index_full(index_path, content, *, source)` helper（~35 行：atomic write + audit log，op="index_full"）— 給 batch index rewriters 用，因現役 `_ATOM_INDEX.md` 為 4 欄 schema，funnel 原 `write_index` 是 3 欄 row-by-row 會抹掉 Scope 欄；`VALID_SOURCES` 加 `"tool:sync-atom-index"` / `"tool:sync-memory-index"` 兩項。**未動 hooks/extract-worker.py + hooks/wg_episodic.py**（原計畫列為改動目標）：完整 grep + 行區精讀後確認 (a) extract-worker.py:427-584 區段只有 `_write_state_atomic`（state JSON）與 `_failure_writeback`/`_create_failure_atom`（`memory/failures/` log 條目，非 atom）— 無 atom 直寫；(b) `wg_episodic.py:577 _update_memory_index` 是 dead code（全 codebase grep 0 call site）+ episodic atom 走 `episodic/` 在 SKIP_DIRS 不視為 atom — 無 funnel 對應點。**(3) S2.3 P7 audit dedup fix**：`tools/memory-audit.py:90` `AtomMetadata` 加 `scope_label: str = ""` 欄位；line 218 `parse_atom_file` 補 `atom.scope_label = atom.scope or _infer_scope_from_path(path)`（frontmatter 優先 / 路徑推斷 fallback，對齊 `lib/atom_spec.VALID_SCOPES = {global, shared, role, personal}` 不細分）；line 1273-1290 新增 `_infer_scope_from_path` helper；line 535-538 `detect_duplicates` dedup key 從 `_normalize(title)` 改 `(scope_label, _normalize(title))` — 跨 scope 同名 atom（global vs shared 各有 `decisions`）不再誤判為重複。**驗證矩陣**（7 檢查）：(1) memory-audit: errors=0/duplicates=0/Layers=1 ✅；(2) pytest 11/11 PASS（funnel 行為未變動）✅；(3) atom-health-check 30 atoms healthy ✅；(4) migrate-scope-field --dry-run 0 missing ✅；(5) audit log `_meta/atom_io_audit.jsonl` 含 `tool:sync-memory-index` + `tool:sync-atom-index` 兩 source ✅；(6) (7) hook:* source 因無 funnel 對應點不適用（已驗證 extract-worker / wg_episodic 無真實 atom 直寫）N/A。**順手修補**：S1 後 `~/.claude/projects/c--Users-holylight--claude/memory/` 空目錄（從 9f979bc auto-promote hook 殘留）→ rmdir 清除（≤5 行門檻）；electron-uia-automation.md 補 Confirmations: 0 / ReadHits: 0（migrate-scope-field 只補 Scope，這兩個是 atom-health-check baseline）。S3 接續 C 類 4 條入口（migrate-v3-to-v4 / memory-undo / atom-move / changelog-roll）+ server.js MCP 切 spawn CLI + PreToolUse 強制門禁；plan + 進度詳見 `~/.claude/plans/opus-melodic-comet.md` 與 `~/.claude/memory/_staging/opus-melodic-comet-progress.md`。 | `tools/migrate-scope-field.py`(新), `lib/atom_io.py`, `hooks/user-extract-worker.py`, `tools/sync-atom-index.py`, `tools/sync-memory-index.py`, `tools/memory-audit.py`, `memory/electron-uia-automation.md`, `memory/_meta/atom_io_audit.jsonl`(新, runtime), `_AIDocs/DocIndex-System.md`, `_AIDocs/_CHANGELOG.md`, `memory/_staging/opus-melodic-comet-progress.md` |
| 2026-05-04 | **[S1] Opus Melodic Comet — 單點收束 Foundation（基礎建設，先清→後建）** — 「7 個 /memory-health 告警」深挖根因為「規則散在 4 處 / 寫入 13 條入口 / 跨層路徑 5 漏洞」。S1 落地三件事，**不接 caller**（避免測試結果被現役寫入污染，那是 S2/S3 工作）。**(1) S1.1 殘骸清理 + P1 雙層補丁**：`hooks/wg_paths.py:88+ get_project_memory_dir` 加 6 行 CLAUDE_DIR resolve 防護，cwd=~/.claude 時短路返回 global MEMORY_DIR 而非拼出 `~/.claude/.claude/memory/`；新 `tools/cleanup-projects-residue.py`（dry-run + `--apply` + `--restore`，歸檔不刪除），歸檔 7 個 `~/.claude/projects/{slug}/memory/` 殘骸到 `_archive/projects-residue/2026-05-04/`（含 `c--users-holylight--claude/memory/` 57 個 P1 雙層 bug 副本 + 6 個 V2.21 migration stub）。Audit Layers 從 8 降到 1（global），Errors 從 3 降到 1（剩 1 個 `electron-uia-automation.md` 缺 Scope 是真實 atom 缺欄位，留 S2.1 P5 migration 系統處理）。**(2) S1.2 規則收束**：新 `lib/atom_spec.py`（~230 行，純資料 + 純函式）—— SKIP_DIRS / REQUIRED_METADATA / VALID_CONFIDENCE / VALID_SCOPES / TRIGGER_MIN/MAX 全系統唯一規則來源，含 `is_atom_file / parse_frontmatter / validate_atom_content / build_atom_content / slugify / resolve_scope_dir / iter_atom_files`，行為對拍 server.js:655-742。`tools/memory-audit.py:53-69` 刪掉本地常數改 `from lib.atom_spec import ...`；`tools/atom-health-check.py:30-31` 改 import + `find_atoms` 委派 `is_atom_file` 判斷 + REQUIRED_METADATA 完整檢查。Health-check issues 從 3 → 1：DESIGN.md（wisdom/）/ role.md（personal/）兩個假陽性歸零。**(3) S1.3 寫入 funnel**：新 `lib/atom_io.py`（~330 行）`write_atom(*, source: REQUIRED, ...)`/`write_index(...)` 對拍 server.js:1065 toolAtomWrite byte-identical 行為，含 ULID-ish audit_id + `_meta/atom_io_audit.jsonl` 反向證明日誌；新 `lib/atom_io_cli.py`（~30 行）thin CLI bridge，stdin JSON → write_atom → stdout WriteResult（供 S3.2 server.js spawn 用）；新 `tests/test_atom_io_equivalence.py`（11 cases，含 plan 要求的 10 情境 + 1 audit log bonus）global/shared/role/personal create + 全 optional 欄位 + sensitive audience auto-pending + append/replace 保留 Confirmations/ReadHits/Author/Created-at + dry-run + 5 個 error path：**11/11 PASS（W::DeprecationWarning 嚴格模式）**。**S1 邊界**：unconnected funnel — 任何 caller 切換都是 S2.2 / S3.1 / S3.2 工作。S2 接續 P5 migration + 6 條 B 類 hook/tool 入口切 funnel + P7 audit dedup；plan 與進度詳見 `~/.claude/plans/opus-melodic-comet.md` 與 `~/.claude/memory/_staging/opus-melodic-comet-progress.md`。 | `hooks/wg_paths.py`, `tools/cleanup-projects-residue.py`(新), `lib/atom_spec.py`(新), `lib/atom_io.py`(新), `lib/atom_io_cli.py`(新), `tools/memory-audit.py`, `tools/atom-health-check.py`, `tests/test_atom_io_equivalence.py`(新), `_AIDocs/_CHANGELOG.md`, `memory/_staging/opus-melodic-comet-progress.md` |
| 2026-04-29 | **REG-005 Session 1/3 — Atom 注入機制重構 A+B 兩層 + 觀察儀表骨架（commits 540cc91, 3d03ba1, 0309c89）** — 4 層設計（A 摘要優先 / B per-turn budget / C hot/cold 分級 / D Related 過濾）首階段落地，C+D 與啟動觀察期留 Session 2/3。**A 層**（`540cc91`）：`hooks/wg_atoms.py:_strip_atom_for_injection` 由 V2.14 整檔剝離改為按 atom 類型路由 — `impression_action`（如 `decisions-architecture.md`、多數 `feedback-*`）注入 frontmatter（白名單 Confidence/Trigger/Last-used）+ 印象 + 行動全段；`knowledge_mixed`（如 `decisions.md`、`toolchain.md`）注入 frontmatter + 印象（若有）+ 知識（cap 200 tokens 截斷標記）+ 行動；`fallback`（無印象無知識的舊 atom）沿用 V2.14 legacy strip。新 helpers `_detect_atom_type` / `_extract_named_section(max_tokens)` / `_extract_title_and_frontmatter` / `_strip_atom_for_injection_impression_only`（B 層 fallback 預備）。**關鍵行為改變**：注入 prompt 開始帶 `## 行動` 段（V2.14 _STRIP_SECTION_RE 之前剝除），LLM 終於看得到「該怎麼做」指引。實測縮減：toolchain 62.5% / feedback-pointer-atom 37% / decisions 14% / decisions-architecture 12%。**B 層**（`3d03ba1`）：新 `decide_atom_injection(raw, full, used_tokens)` 對每 atom 回 ok/fallback/skip 三態，hard cap `_TURN_BUDGET_LIMIT=800`；超 budget → fallback 到 impression-only，仍超則 skip 加 summary 後 break。`workflow-guardian.py:~1106` atom 注入點（trigger-matched + spread_related 兩段共享 used_tokens）重構成決策驅動，每 atom 寫 `[BUDGET]` debug log。`SECTION_INJECT_THRESHOLD` 300→200（更多中型 atom 走 vector section 提取，與 budget cap 互補）。**設計簡化**：原計畫的 module-level `_TURN_TOKEN_BUDGET_USED` 不需要，因 `used_tokens` 已是 handle_user_prompt_submit 的 local var（每 entry 自然歸 0）。**觀察儀表骨架**（`0309c89`）：先寫不啟動，flag-gated（`memory/_staging/reg-005-observation-start.flag`）。新 `hooks/wg_atom_observation.py`（~165 行）獨立 hook 不走 dispatcher，flag 不存在 → `sys.exit(0)` 零開銷返回；UserPromptSubmit 累 sessions / PostToolUse(Read) 累 reads_in_memory；log JSON line schema 寫 `Logs/atom-injection-observation-YYYY-MM-DD.log`，metric 寫 `reflection_metrics.json` 子鍵 `reg005_observation`（atomic write via .tmp rename）；路徑正規化兼容 Windows / MSYS2 / 混合格式。新 `tools/atom-injection-summary.py`（~190 行）依「事件驅動四重標準」（A 注入 ≥150 + B session ≥6 + 2 ≤ wall_clock ≤ 7 天）+ 自動止血條件（hot 命中 / cold 主動讀 / 命中錯失 / 零覆蓋，皆有最低樣本門檻）輸出 KEEP / ROLLBACK / GRAY / INCOMPLETE / NOT_STARTED。`settings.json` 新增 PostToolUse "Read" matcher + UserPromptSubmit 第三 hook group 指向 wg_atom_observation.py（既有兩 PostToolUse matchers 不動）。**驗證**：全 pytest 298 passed + 52 skipped（新 31 tests）；SessionStart smoke EXIT=0；觀察 hook flag-on/off 手動 smoke 通過（flag 缺席 → log/metric 完全不寫；flag 存在 → 正確寫入；Win 路徑與 MSYS2 路徑都命中；非 memory 路徑被忽略）；`atom-injection-summary.py --report` 對 INCOMPLETE / NOT_STARTED / 期滿三情境輸出正確判定。**設計檔**：`memory/_staging/reg-005-atom-injection-refactor.md`（含完整 4 層設計 + 觀察期事件驅動四重標準 + 不要動清單 + Session 進度表 + Session 2/3 啟動 prompt 草稿）。**Session 2/3** 範圍：commit 4 C 層 hot/cold 分級 + workflow-guardian instrument 補上 reg005_observation.{hot,cold}_injections / commit 5 D 層 Related 擴散 hot/cold 過濾 + depth=1 / `touch` flag 啟動觀察期 / 4-commit 前後 baseline 對比。 | `hooks/wg_atoms.py`, `hooks/workflow-guardian.py`, `hooks/wg_atom_observation.py`(新), `tools/atom-injection-summary.py`(新), `tests/test_atom_strip_for_injection.py`(新, 21 cases), `tests/test_atom_injection_budget.py`(新, 10 cases), `settings.json`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-28 | **[固] 邏輯程式碼化首批落地（議題 #10 強候選 2 條）** — 使用者 Stage F 重審紅色指標 #3「非常固定的邏輯應 hook 化」首批執行。新檔 `hooks/wg_pretool_guards.py`（純函式風格鏡射 wg_evasion）含 `check_memory_path_block`（PreToolUse Write/Edit 阻擋寫入 `~/.claude/projects/{slug}/memory/`）+ `check_svn_test_block`（PreToolUse Bash 阻擋 `svn commit/ci` 含 `tests?/` `__tests__/` 或 `*Test.<ext>` 檔，r10854 教訓）。`workflow-guardian.py` import 採 try/except 自動降級 no-op 防 typo 拖垮 Write/Bash 路徑；`handle_pre_tool_use` 在既有 AtomFormat/Confidence Gate 後串接 2 個 deny 檢查；`settings.json` PreToolUse 新增 Bash matcher（既有 Write matcher 不變）。對應 atom `feedback-memory-path` / `feedback-no-test-to-svn` 末尾補「## 已 hook 化」段（hook 不取代 atom，hook 訊息指回 atom 補語境）；`feedback-pointer-atom`「代 hook atom」失敗模式段補實踐回路證明。**驗證**：新測試 `test_pretool_path_block.py`（11 cases）+ `test_pretool_bash_block.py`（14 cases）共 25/25 PASS；全 pytest 267 PASS / 10 fail（10 fail 對齊 [REG-002 議題 #8 Silent Advisory Mode pre-existing](DevHistory/memory-cleanup-2026-04/follow-up-issues.md#議題-8wave-4a-衍生codex_companion-silent-advisory-mode-重構後-10-測試需對齊) commit 7794f65，與本次零相關）；SessionStart smoke exit=0；真實 stdin JSON 命中 Step 4/5 兩條 deny 訊息正確（Step 5 Bash matcher 直接由 harness 端攔截，證明 settings.json 接線正確）。**Token 影響**：atom 注入成本不變；每次規則違反不再消耗使用者糾正 turn + LLM 重寫成本（歷史節奏每 2-3 週一次，每次省 2000-4000 tokens）。`follow-up-issues.md` 議題 #10 表兩條強候選欄位改「已」+ 加 2026-04-28 處理紀錄段（**其餘 5 條由議題 #10 後續評估**，不限制下個 session）。 | `hooks/wg_pretool_guards.py`(新), `hooks/workflow-guardian.py`, `settings.json`, `memory/feedback/feedback-memory-path.md`, `memory/feedback/feedback-no-test-to-svn.md`, `memory/feedback/feedback-pointer-atom.md`, `tests/test_pretool_path_block.py`(新), `tests/test_pretool_bash_block.py`(新), `_AIDocs/Architecture.md`, `_AIDocs/DevHistory/memory-cleanup-2026-04/follow-up-issues.md`, `plans/stage-f-transient-bachman.md`(新) |
| 2026-04-28 | **Memory Cleanup Wave 6 收尾 — 計畫歸檔 + plans 目錄刪除** — 多大師協作（5 分析席 + 3 稽核席 + 4 wave-specific 紅隊）+ 6 wave 執行計畫進入沉澱階段。**(1) DevHistory 歸檔**：新建 `_AIDocs/DevHistory/memory-cleanup-2026-04/`，搬入 `synthesis.md`（plan v2.1 主控計畫）、`follow-up-issues.md`（議題 #1-#9 含 #5 Wave 3 已處理 / #8 已掛 known-regressions REG-002 / #9 Stage F BLOCK 細節）、`master-review-digest.md`（8 大師原始報告濃縮：CC 考古 + CC 架構 + Codex 保守/極簡/激進 + Codex Mechanic/Redteam/Skeptic + §9 Phase 4-5 wave-specific 紅隊摘要 + §10 跨大師共識決策黃金交集）。**(2) 通用版協作 protocol**：`multi_agent_protocol.md` → `_AIDocs/DevHistory/multi-agent-cleanup-protocol.md`（CC ≥ Codex 席次設計、防 Codex 幻覺、Windows sandbox unelevated 修復、Phase 4 audit 重派、§十一 收尾 8 項 checklist）。**(3) 失敗教訓沉澱**：新增 `_AIDocs/Failures/codex-windows-sandbox-1385.md`（CreateProcessWithLogonW failed: 1385 根因 = `[windows] sandbox = "elevated"`，修補 = `-c 'windows.sandbox="unelevated"'`，5 位 Codex 分析席第一輪全受影響、報告退化為「立場推論」非「事實核驗」、Phase 4 audit 重派才產出實證稽核）；`vectordb-silent-failure-2026-04.md` 已於 Wave 3a 寫入。**(4) follow-up 升級為 known-regressions**：REG-003 Vector DB 拆庫精密化（議題 #1，效能優化非 broken）、REG-004 Vector 路徑寫死根因 commit 追溯（議題 #2 殘留，主修補 Wave 3a 已完成）、REG-005 Atom 注入機制重構（議題 #3，前置條件 = atom 注入 3 源真相統一）、REG-006 Hook 萃取管線重複（議題 #4，3 條 worker 並存疑似職責重複）。議題 #5（自動觀察儀表）Wave 3 已處理 ✅；議題 #6 #7（vector ranked min_score / 路徑 commit 追溯）/ #9（Stage F BLOCK）保留 follow-up。**(5) 索引同步**：`_AIDocs/DevHistory/_INDEX.md` 加 `multi-agent-cleanup-protocol.md` + `memory-cleanup-2026-04/`；`_AIDocs/Failures/_INDEX.md` 加 `codex-windows-sandbox-1385.md`、最近更新改 2026-04-28。**(6) plans 目錄整體刪除**：`~/.claude/plans/memory-cleanup-2026-04-27/`（含 outputs/ 12 份原始大師報告 + outputs/wave3b/ + 4 份 dryrun/briefing/wave_3_to_6_prompts/synthesis_draft 等暫存規劃文件）— 已全數沉澱於 DevHistory + known-regressions + Failures，原檔不再需要。**收尾總成果**：Wave 1（A0+A+G1）✅ Wave 2（B+C 並行+merge）✅ Wave 3a（vector 儀表+路徑修+flag-gate）✅ Wave 3b（probe burst→REVIVE 87.6%）✅ Wave 4a（Stage E phase6 dead code 精簡）✅ Wave 4b（Stage F 5 子 stage 4 BLOCK + F4-drift 晉升門檻字串一致化）✅ Wave 5a（Stage H 3 條規則 atom）✅ Wave 5b（Phase 5 驗證未發現失憶）✅ Wave 6（本 commit）。 | `_AIDocs/DevHistory/memory-cleanup-2026-04/{synthesis.md, follow-up-issues.md, master-review-digest.md}`(新), `_AIDocs/DevHistory/multi-agent-cleanup-protocol.md`(新), `_AIDocs/DevHistory/_INDEX.md`, `_AIDocs/Failures/codex-windows-sandbox-1385.md`(新), `_AIDocs/Failures/_INDEX.md`, `_AIDocs/known-regressions.md`(REG-003/004/005/006), `plans/memory-cleanup-2026-04-27/`(整目錄刪除) |
| 2026-04-28 | **Memory Cleanup Wave 3a — Vector Observation Instrumentation + workflow-guardian Path Fix** — 鋪設 vector subsystem 4 天觀察儀表（不執行 Stage D 本身）。**(1) wg_intent.py vector 觀察 log**：`_search_episodic_context` / `_semantic_search` 的 disabled / no_flag / ready / error 出口都呼叫新 `_log_vector_obs(session_id, fn, flag_state, result_count, fallback_used)`，寫入 `~/.claude/Logs/vector-observation.log`（RotatingFileHandler 2MB×3, delay=True，含 `_vector_obs_logger_failed` sentinel 短路重試）；caller 簽名加 `session_id` 從 SessionStart state 透傳。**(2) workflow-guardian.py 路徑修正（vector silent fail 真根因）**：`vs_script` 從不存在的 `tools/vector-service.py` 改為真檔 `tools/memory-vector-service/service.py`（12 天 subprocess.Popen silent fail 根因）。**(3) flag write gate**：bg subprocess `vector_ready.flag` 改為「health 200 才寫」，poll 30×0.5s 任一輪成功才 ready=True，否則跳過 flag write（杜絕 12 天假陽性）。**(4) SessionStart known-query probe**：bg subprocess 跑 `workflow guardian SessionStart 機制` query 透過 vector + keyword grep（`memory/**/*.md`）兩路並比，寫獨立檔 `vector-observation-probe.log`（CC 稽核 F6：避開與 RotatingFileHandler 同檔的 rollover race）。**(5) tools/vector-observation-summary.py**：4 天後讀兩 log 自動判定 REVIVE（命中率 ≥ 50%）/ RETIRE（≤ 5% 且 fallback ≥ 80%）/ GRAY（5–15% 詢使用者）。**(6) plan + protocol 重寫**：plan v2.1 §修訂 #4 D4-D5 移除「使用者主訴」改「log 採樣自動判定」；multi_agent_protocol §六 新增規則「silent-failure 風險的 stage 必須加 log 採樣（hook 鏈 / always-loaded 入口 / 觀察期 ≥ 4 天 / bg subprocess）」。**多大師協作**：CC 寫手 + 1 CC 稽核（Plan agent，驗 graceful no-op；F2 sentinel + F6 分檔採納）+ 1 Codex 稽核（驗 SessionStart probe 不污染 context；C1=YES，bg DEVNULL 已切斷 stdout/stderr，rglob 實測 ~40 md 4.9ms）。**Smoke**：`import wg_intent` OK；`workflow-guardian.py` 完整 import OK；`ast.parse(_bg_code)` OK；end-to-end exec bg_code 寫 probe log（含 spawn vector service + poll ready）；`tools/vector-observation-summary.py --days 1` 讀 records 不報錯。觀察期 2026-04-28 → 2026-05-02。Stage D 本身（修活 vs 淘汰）4 天後讀 log 決定。 | `hooks/wg_intent.py`, `hooks/workflow-guardian.py`, `tools/vector-observation-summary.py`(新), `plans/memory-cleanup-2026-04-27/wave3a_dryrun.md`(新), `plans/memory-cleanup-2026-04-27/synthesis_and_plan_v2.md`, `plans/memory-cleanup-2026-04-27/multi_agent_protocol.md`, `_AIDocs/Architecture.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-28 | **Memory Cleanup Wave 2 收尾 + Project_File_Tree 引用同步** — 並行清理 Wave 2 (Stage B plans + Stage C projects) 收尾。Stage B (session 2a) 清掉 `memory/_distant/`、`memory/_vectordb/service.pid`、`shared/_pending_review/` 殘檔，handoff 歸檔。Stage C (session 2b) 把 `projects/c--TSG/` 移到 `projects/_archived/`、同步 `memory/project-registry.json`。本 session (Wave 2 收尾) 補 G1 連帶：Project_File_Tree.md 已於 session 1 由 218 行完整檔樹瘦身為 30 行頂層目錄角色說明、改用 `~/.claude/` 不含帳號名，故同步引用描述於 [Architecture.md:4](Architecture.md)（完整檔樹 → 頂層目錄角色說明）、[init-project.md:53,70](../commands/init-project.md)（專案結構摘要 → 頂層目錄角色說明，30 行內，完整檔樹用 `tree -L 3`）、[upgrade.md:132](../commands/upgrade.md)（2.6 _AIDocs 加註：Project_File_Tree.md 為 30 行角色說明、不含帳號名、不再是完整檔樹快照）、[upgrade.md:279](../commands/upgrade.md)（Phase 3 替換註記同步：其他檔案的帳號名需替換、Project_File_Tree.md 不需）。**驗證**：13 個 hook 模組 import 全 OK；SessionStart hook stdin smoke `exit=0` 無 ImportError；commit `59c7598` 含 27 檔變動。settings.json 兩 session 並行加項無衝突（permissions + additionalDirectories 各加各的）。 | `_AIDocs/Architecture.md`, `commands/init-project.md`, `commands/upgrade.md`, `_AIDocs/Project_File_Tree.md`(session 1), `memory/project-registry.json`, `settings.json` |
| 2026-04-27 | **Guardian Hook 兩漏洞修補** — (A) Write tool 繞過 `atom_write` confidence gate：`hooks/workflow-guardian.py::_check_memory_atom_format` 在格式 gate 通過後對「新建檔」（`Path.exists()==False`）解析 frontmatter `Confidence:` 與內文 `- [固]/- [觀]` 標籤，任一非 `[臨]` 即 deny，鏡射 [server.js:1109-1117](../tools/workflow-guardian-mcp/server.js) 的 MCP 端硬擋規則。existing file 整檔覆寫對應 MCP `mode=replace` 路徑（保留 confirmations/readhits 晉升狀態）→ 不擋；`_staging/`、`_pending_review/`、`_*` 索引、`role.md` 維持既有豁免。**動機**：Claude 用 Write tool 直接寫 memory/feedback/*.md 完全繞過 server.js gate（實證：feedback-codex-collaboration.md 初稿 16 條全 `[固]` 走 Write 通過 hook）。(B) FixEscalation 對「計畫迭代」誤判為錯誤修復重試：`hooks/wisdom_engine.py::track_retry` 新增 `_is_plan_iteration_path` 前置 gate，路徑命中 `/plans/`、`/_staging/` 或檔名命中 `is_plan_filename` 規劃詞 → 直接 return 不累計 `wisdom_retry_count`。真錯誤修復（hooks/*.py、tests/*.py 反覆 edit）仍正常觸發 `[Guardian:FixEscalation]`。**動機**：plan v3→v4→v5 多次 Edit 同一份 plans/*.md 累積 retry → 下一輪誤注入 FixEscalation 警告。新增 `tests/test_guardian_atom_write_gate.py`（9 cases，含正向擋 [固]/[觀]/內文標籤、反向放行 [臨]/existing/_staging/索引/非 memory）+ `tests/test_fix_escalation_plan_retry.py`（7 cases，含 plan 豁免、檔名關鍵字豁免、真錯誤仍累計、首編不算）。123 passed（既有 107 迴歸 test_dual_field_e2e + test_codex_companion_stop_e2e + test_evasion_guard + test_phase6_trigger 全綠）/ 0 failed。 | `hooks/workflow-guardian.py`, `hooks/wisdom_engine.py`, `tests/test_guardian_atom_write_gate.py`(新), `tests/test_fix_escalation_plan_retry.py`(新), `_AIDocs/Architecture.md` |
| 2026-04-27 | **Codex Companion Sprint 5.5** — Phase 6 自觸發 hook + metric 第六鍵。**B1**：`tools/codex-companion/state.py` `_METRIC_KEYS` 白名單新增 `audits_total_attempted`（Phase 6 §四 C3 ratio 分母）；`hooks/codex_companion.py` `handle_stop` 在 score gate 通過、所有 dedup/cap 也過、即將送 `/trigger` 前 +1，語意為「實際送出 codex audit 次數」。**B2**：新檔 `hooks/wg_codex_companion_phase6.py`（讀 reflection_metrics.json + glob real assessments → §四 C1-C5 if-check → dedup 用 `last_fired` 集合差集、樣本累積驅動不依賴時間 → 命中返回 inject 字串）；`hooks/workflow-guardian.py` SessionStart MCP 健康檢查後整合 try block，命中 inject 加入 additionalContext。**設計用意**：user 不必主動記「一週後回來看」；條件到自然會在開機 SessionStart 被推醒。新增 `tests/test_metric_audits_total.py`（5 cases）+ `tests/test_phase6_trigger.py`（15 cases，含 C1-C5 邊界 / dedup / drop-then-remeet refire / multi-condition）。251 passed (Sprint 4 從 231 +20) / 0 failed。對應 plan v5 Phase 6 自適應 Gate 推遲評估之自觸發機制。 | `tools/codex-companion/state.py`, `hooks/codex_companion.py`, `hooks/wg_codex_companion_phase6.py`(新), `hooks/workflow-guardian.py`, `tests/test_metric_audits_total.py`(新), `tests/test_phase6_trigger.py`(新), `_AIDocs/DocIndex-System.md`, `_AIDocs/Architecture.md` |
| 2026-04-27 | **Codex Companion Sprint 4** — 空回非靜默 + sandbox 失敗 escalate + 觀測落盤。assessor.py `_run_codex` 改回 `(stdout, stderr)`、新增 `_run_codex_with_retry`（空字串/非 JSON 退 0.4s 重試 1 次）+ `_classify_failure` 用 `CreateProcessWithLogon\|sandbox` regex 識別 R2-5 級錯誤（命中→`category: system, summary: "Codex sandbox 失敗"`）；其他失敗→`status: warning, summary: "退回 heuristics-only", delivery: inject, notify_next_turn: True`。hook drain 偵測 `notify_next_turn` 在 inject blocks 前插入 `[Codex Companion 提醒]` 短訊。`block_template` 從 `config.codex_companion.block_template` 讀（`{detail}` placeholder）。新增 `companion-metrics-{sid}.json` + `increment_metric` API；五鍵觀測（`audits_skipped_by_score / empty_returns / sandbox_failures / behavior_gap_blocks / quality_gap_advises`）由 hook + service 累加，SessionEnd 附到 `memory/wisdom/reflection_metrics.json` 的 `codex_companion.sessions[]`（最多 100 筆，全 zero 跳過，與 wisdom_engine 結構共存）。新增 `tests/test_assessor_retry.py`（8 cases）+ drain e2e 擴 2 case。231 passed / 0 failed。 | `tools/codex-companion/assessor.py`, `tools/codex-companion/state.py`, `tools/codex-companion/service.py`, `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_assessor_retry.py`(新), `tests/test_codex_companion_drain_e2e.py`, `_AIDocs/DocIndex-System.md` |
| 2026-04-27 | **Codex Companion Sprint 3** — Score gate + Advisory schema。新增 `scorer.py` 五因子加權（write_footprint / verification_gap / structural_risk / completion_claim / analysis_loop, 0-10）作為 Stop hook 觸發 codex turn_audit 前的最小 token gate（`score_threshold=4` / `max_audits_per_session=30` / 同 turn_index dedup）。`prompts.py` OUTPUT_SCHEMA 砍 `recommended_action`、改 `delivery: ignore\|inject` + `confidence` + `evidence` + `applies_until` + `turn_index`，徹底剝奪 codex 的 BLOCK 權（advisory only）。`TURN_AUDIT` 模板新增 Last Assistant Reply Tail / Verification Evidence Found / Heuristic Triggered (Reference Only) 三段，assessor 即時抽 verify cmd 事證避免 stale state。drain handler 依 `delivery` 路由：ignore 標 injected 不打擾、inject 注入帶信心標籤；同步退役 REG-001（V4.1 atom snapshot 漂移測試）。221 passed / 0 failed / 0 xfailed。 | `tools/codex-companion/scorer.py`(新), `tools/codex-companion/prompts.py`, `tools/codex-companion/assessor.py`, `tools/codex-companion/service.py`, `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_scorer.py`(新), `tests/test_codex_companion_drain_e2e.py`(新), `tests/regression/test_v4_atoms_unchanged.py`(刪), `tests/fixtures/v4_atoms_baseline.jsonl`(刪), `_AIDocs/known-regressions.md`, `_AIDocs/DocIndex-System.md`, `memory/preferences.md` |
| 2026-04-27 | **Codex Companion Sprint 2** — heuristic 規則重構與 BLOCK 權收斂。BLOCK 權限縮到單一 `confident_completion_without_evidence`（high）；其餘規則一律降為 `low` advisory。三條件齊備才 BLOCK：`has_claim` + `state_change` + `no_verify_evidence`。新增 Sprint 1 教訓 fallback：`last_assistant_tail` 含驗證敘述（pytest 通過 / X/Y PASS / build 成功）視為弱證據放行，避免 companion-state 被擾動誤觸 BLOCK。新增 `severity_at_or_above` API + config `block_severity_threshold`。21 unit + 3 e2e 測試全綠。 | `tools/codex-companion/heuristics.py`(rewrite), `hooks/codex_companion.py`, `workflow/config.json`, `tests/test_heuristics.py`(rewrite), `tests/test_codex_companion_stop_e2e.py`(新), `_AIDocs/DocIndex-System.md` |
| 2026-04-26 | **Codex Companion Phase 1** — Codex (GPT) 驅動的第二觀點監督系統。確定性 hook 觸發 → HTTP service 累積事件 → `codex exec` 非同步審閱 → 下一輪 additionalContext 注入。含 4 條 heuristic 軟閘（缺驗證/完成缺證據/架構變更/空轉）、plan review / turn audit / architecture review 三種 Codex 評估、`/codex-companion` 開關 skill。 | `tools/codex-companion/`(新: service.py, assessor.py, prompts.py, heuristics.py, state.py), `hooks/codex_companion.py`(新), `commands/codex-companion.md`(新), `workflow/config.json`, `settings.json`, `_AIDocs/DocIndex-System.md` |
| 2026-04-24 | **v3 雙欄位拆分 (ReadHits + Confirmations)** — Confirmations 混合訊號拆為 ReadHits（注入讀取）+ Confirmations（跨 session 萃取），晉升門檻重校 4/10+20/50 雙軌，correlation_id 追蹤，16 檔 + migration script，24 E2E 測試全綠。 | `hooks/workflow-guardian.py`, `hooks/wg_episodic.py`, `hooks/wg_atoms.py`, `hooks/user-extract-worker.py`, `hooks/wg_iteration.py`, `tools/workflow-guardian-mcp/server.js`, `tools/memory-audit.py`, `tools/atom-health-check.py`, `tools/memory-vector-service/indexer.py`, `scripts/migrate-confirmations.py`(新), `tests/test_dual_field_e2e.py`(新), `memory/decisions.md` |
| 2026-04-23 | **ScanReport Gate + 反退避契約** — 針對 Opus 4.7 Effort=High 偷懶傾向：IDENTITY.md 加可觀測條款（禁語/成本門檻/收尾格式），Stop hook 新增「缺掃描報告 = 違約」硬阻擋。 | `IDENTITY.md`, `hooks/wg_evasion.py`, `hooks/workflow-guardian.py`, `_AIDocs/Architecture.md` |
| 2026-04-17 | **_CHANGELOG 短格式化 + session-logs 子目錄** — 每條 ~2KB 單行敘事拆為「標題 + log 連結」，8 條遷入 `../memory/_distant/2026_05_v5_overhaul/session-logs/`。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-changelog-short-form.md) | `_AIDocs/_CHANGELOG.md`, `_AIDocs/../memory/_distant/2026_05_v5_overhaul/session-logs/*`(8 新) |
| 2026-04-17 | **_CHANGELOG 自動滾動 + Architecture 索引化** — PostToolUse auto-roll + 8 pytest；Architecture 413→150 行（-64%），7 DevHistory 子檔。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-changelog-and-architecture.md) | `tools/changelog-roll.py`(新), `commands/changelog-roll.md`(新), `tests/test_changelog_roll.py`(新), `workflow/config.json`, `hooks/workflow-guardian.py`, `_AIDocs/Architecture.md`(rewrite), `_AIDocs/DevHistory/*`(7 新+1 擴) |
| 2026-04-17 | **Evasion Guard + Test-Fail Gate** — Bash 測試失敗偵測 + Stop 完成宣告攔截 + UPS 退避舉證要求，51 pytest。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-evasion-guard.md) | `hooks/wg_evasion.py`(新), `hooks/workflow-guardian.py`, `settings.json`, `tests/test_evasion_guard.py`(新) |
| 2026-04-17 | **Atom 寫入防呆 + feedback 目錄整理 + atom_promote 合併** — AUTO-DRAFT tag / Atom-Write Guard / feedback/ 子資料夾 / merge_to_preferences。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-17-atom-write-guards.md) | `hooks/wg_hot_cache.py`, `hooks/workflow-guardian.py`, `hooks/wg_iteration.py`, `tools/workflow-guardian-mcp/server.js`, `memory/feedback/` |
| 2026-04-16 | **V4.1 GA** `v4.1.0` — 清除 rc2 blocker（ollama_client / user-extract-worker / prompt regex 三處根因）P=1.000 R=0.480。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-ga.md) | `tools/ollama_client.py`, `hooks/user-extract-worker.py`, `tests/integration/test_e2e_user_extract.py`, `workflow/config.json` |
| 2026-04-16 | **V4.1 P4 Session 評價機制** `v4.1.0-rc2` — `wg_session_evaluator.py` 5 維度評分 + Agent 多 Role 模擬 + `/memory-session-score`。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p4.md) | `hooks/wg_session_evaluator.py`(新), `hooks/user-extract-worker.py`, `commands/memory-session-score.md`(新), `tools/memory-session-score.py`(新) |
| 2026-04-16 | **V4.1 P3 UX Commands** `v4.1.0-rc1` — `/memory-peek` + `/memory-undo` + 每日推送 [F18] + 隱私體檢 [F21]。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p3.md) | `commands/memory-peek.md`(新), `commands/memory-undo.md`(新), `tools/memory-peek.py`(新), `tools/memory-undo.py`(新), `tools/init-roles.py` |
| 2026-04-16 | **V4.1 P2 整合** `v4.1.0-beta1` — `user-extract-worker.py` L0→L1→L2 管線 + 整合測 50 條。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p2-integration.md) | `hooks/user-extract-worker.py`(新), `tests/integration/test_e2e_user_extract.py`(新), `tests/regression/test_v4_atoms_unchanged.py`(新) |
| 2026-04-16 | **V4.1 P2 前置 Prompts + lib refactor** — 新 `prompts/user-decision-{l1,l2}.md` + `lib/ollama_extract_core.py` 抽出 7 函式。[log](../memory/_distant/2026_05_v5_overhaul/session-logs/2026-04-16-v41-p2-prep.md) | `prompts/user-decision-l1.md`(新), `prompts/user-decision-l2.md`(新), `lib/ollama_extract_core.py`(新), `hooks/extract-worker.py` |
| 2026-04-16 | **V4.1 Phase 1 — L0 Detector + Feature Flag + V4 Baseline** `v4.1.0-alpha1`：新建 `wg_user_extract.py`（純規則 L0 detector，信號詞中+英強/中/負三權重 + 句法 pattern [F27] + 排除規則，≤5ms）。新建 `snapshot-v4-atoms.py` [F13] 產出 `v4_atoms_baseline.jsonl`（63 atoms SHA256 + metadata）。`workflow/config.json` 新增 `userExtraction{enabled:false, mode:shadow, tokenBudget:240}`。`workflow-guardian.py` UserPromptSubmit gate（flag=true 時 detect_signal → pending_user_extract GC cap 10 [F11]）+ SessionEnd drain [F26]。pytest 81 cases 全過（74 detector + 7 disabled-flag），P≥0.95 R≥0.55 ✓ | `hooks/wg_user_extract.py`(新), `tools/snapshot-v4-atoms.py`(新), `tests/test_user_detector.py`(新), `tests/test_v41_disabled.py`(新), `tests/fixtures/v4_atoms_baseline.jsonl`(新), `hooks/workflow-guardian.py`, `workflow/config.json` |
| 2026-04-16 | **V4 漸進遷移 docs-progg 專案啟用**：migrate-v3-to-v4.py 補 12 atom metadata（Scope/Author/Created-at）、init-roles bootstrap（personal/holylight/role.md + _roles.md + .gitignore + post-merge hook）、CONTRADICT 模擬驗證成功（gemma4:e4b 正確分類 sim=0.787）、SessionStart [Role] + vector layer 欄位 + memory-health 全通過。Project_File_Tree.md 更新 V4 三層 scope 目錄結構 + 新增 commands 21 個 + tools + hooks | `_AIDocs/Project_File_Tree.md`, `_AIDocs/_CHANGELOG.md`, docs-progg `.claude/memory/*.md`(12), docs-progg `.claude/memory/personal/`, docs-progg `.claude/memory/_roles.md` |
| 2026-04-16 | **原子記憶 V4.1 設計圓桌（Plan v2 approved）**：8 大師 (人文/UX/程式/AI/原子記憶/實作/精省 token/語意) × drafting + validation 雙 round + 2 資訊整合 (Prior Art/NLP Benchmark)。核心設計：三層 gating (L0 規則 → L1 qwen3:1.7b 二元 → L2 gemma4:e4b 結構化) + Stop hook async + Hybrid threshold (0.92 直寫 / 0.70-0.92 review / 丟棄) + 28 項 validation fix。NFR：P≥0.92, R≥0.30, ≤240 tok/session。4 Phase 共 8.5 dev day。V4 SPEC/server.js 零修改（Source-turn-id 走 footer comment；metadata 走 description 自由欄）。新增 personal/auto/{user}/ 物理隔離 | `_AIDocs/V4.1-design-roundtable.md`(新), `plans/purring-percolating-glacier.md`(新), `_AIDocs/_INDEX.md`, `_AIDocs/_CHANGELOG.md` |
| 2026-04-15 | **原子記憶 V4 Phase 6 — Migration 工具 + /init-roles + E2E 驗證收尾**：新建 `tools/migrate-v3-to-v4.py`（V3 atom 補 Scope/Author/Created-at 三 metadata，從 path 推 scope，dry-run 預設 + `--apply` 才真寫，掃 global + project-registry 全專案）。新建 `commands/init-roles.md` + `tools/init-roles.py`（互動引導：bootstrap personal/{user}/role.md、scaffold shared `_roles.md` 樣板（注意：路徑為 `memory/_roles.md` 與 `wg_roles.load_management_roster` 一致，非 `memory/shared/_roles.md`）、append `.gitignore`、可選詢問裝 `post-merge` hook，全冪等）。Guardian AtomFormat hook 新增 `_pending_review/` + `role.md` 兩道豁免（避免 backend 草稿與角色宣告檔誤觸 atom 格式檢查）。`post-git-pull.sh` 修 race condition：merge 後先 `POST /index/incremental` + 輪詢 indexing=False 最多 12s 再跑 pull-audit，避免新進 atom 還沒索引就跑分類。`USER.template.md` / `IDENTITY.template.md` 補 V4 多職務段。Architecture.md 補完整三時段衝突流程 ASCII 圖（Write-time / Pull-time / Review-time + JIT 保護說明）。E2E 三模擬 user (programmer/art/management) × 兩專案：A1-A5 MCP atom_write 分流+CONTRADICT→pending+skip_check / B1-B3 post-merge hook→pull-conflict+merge_history / C1-C4 /conflict-review approve+reject+`.resolved` 支線 / D1-D2 _pending_review 不入 JIT + 管理職 [Pending Review] 計數，全綠 | `tools/migrate-v3-to-v4.py`(新), `tools/init-roles.py`(新), `commands/init-roles.md`(新), `hooks/workflow-guardian.py`, `hooks/post-git-pull.sh`, `USER.template.md`, `IDENTITY.template.md`, `_AIDocs/Architecture.md`, `_AIDocs/_INDEX.md`, `_AIDocs/DocIndex-System.md` |
| 2026-04-15 | **原子記憶 V4 Phase 5 — 三時段衝突偵測 + Pending Review Queue**：SPEC §7 實作完成。`memory-conflict-detector.py` 新增 `--mode=write-check` 與 `--mode=pull-audit`（復用既有 vector_search + ollama_classify，新增 fail-open 機制、`Merge-strategy: git-only` 跳過、TSV `_merge_history.log` append、`.last_pull_audit_ts` 增量）。`server.js` 新增 `execConflictDetector` / `appendMergeHistory` / `buildConflictReport` helper；`toolAtomWrite` create 分支對 scope=shared 插入 write-time 語意衝突掃描 → CONTRADICT 寫 `_pending_review/{slug}.conflict.md` 報告（非 atom、阻擋寫入）、EXTEND-overlap (sim≥0.85) reroute 為 `_pending_review/{slug}.md` 草稿並標 `Pending-review-by: management`；`skip_conflict_check` 旗標供受控遷移用。新增 `commands/conflict-review.md` + `tools/conflict-review.py` 後端（list/approve/reject 三動作、is_management 雙向認證 guard、approve 時搬檔+寫 Decided-by+觸發 `/index/incremental` reindex+清理同名 .conflict.md）。新增 `hooks/post-git-pull.sh` 樣板（手動 copy 到 `{proj}/.git/hooks/post-merge`）。單元驗證：is_management (holylight=True, alice=False, nobody=False)、mgmt approve 搬檔成功、非 mgmt approve/reject 被拒、pull-audit smoke 更新 .last_pull_audit_ts、write-check 對跨專案相似 atom 正確判 CONTRADICT | `tools/memory-conflict-detector.py`, `tools/workflow-guardian-mcp/server.js`, `tools/conflict-review.py`(新), `commands/conflict-review.md`(新), `hooks/post-git-pull.sh`(新), `_AIDocs/Architecture.md`, `plans/hazy-coalescing-pudding.md`(新) |
| 2026-04-15 | **原子記憶 V4 Phase 4 — Role-filtered JIT injection + Vector layer 擴充**：SessionStart 讀 `wg_roles` 取 user/roles/management 並寫入 `state["user_identity"]`，bootstrap_personal_dir 冪等執行；`get_project_memory_dir` V4 fallback（純 V4 layout 也視為合法 mem dir）；project atom_index 在 V4 layout 下完全用 `_collect_v4_role_atoms` 結果（避免 MEMORY.md 自我循環）。動態生成 role-filtered `MEMORY.md`（auto-gen header 護寫保護 V3 人手版本）+ context line `[Role] user=… roles=… mgmt=…` + 管理職 `[Pending Review] N 件`。Vector indexer 改用新 `discover_v4_sublayers`，layer label 改為 `shared:{slug}` / `role:{slug}:{r}` / `personal:{slug}:{user}`（淘汰 `project:{slug}`），chunk metadata 新增 `Scope`/`Audience`/`Author` 三欄。Searcher 新增 `_build_v4_layer_clause(user, roles)` SQL composer + sanitize 防注入，三 search 入口 + service 三 endpoint 全部接 user/roles 參數。`_semantic_search` 同步擴 user/roles signature；UPS 自動帶入 state 身份（管理職傳 None 不過濾）。`_ATOM_INDEX.md` 加 Scope 欄。end-to-end 通過：bob (programmer) 看不到 art atom；alice (art) 收到 photoshop-naming；管理職 mgmt=True 顯示 pending review 計數；vector 同 query 不同 user 結果差異符合 SPEC §8.1 | `hooks/wg_paths.py`, `hooks/wg_intent.py`, `hooks/workflow-guardian.py`, `tools/memory-vector-service/{indexer,searcher,service}.py`, `memory/_ATOM_INDEX.md`, `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 Phase 3 — atom_write schema + 寫入路徑切換**：MCP `atom_write` schema 擴 V4 三層 scope（`shared`/`role`/`personal`，含 `role`/`user`/`audience`/`pending_review_by`/`merge_strategy` 欄位），`scope=project` 透明遷移到 `shared` + stderr deprecation hint，舊 caller 不傳 scope 預設 shared 不報錯。新 metadata 自動帶入 `Author`（server 端 env/OS user）+ `Created-at`，順序依 SPEC §4。SPEC §7.4 第一版實作：`scope=shared` 且 audience 含 `architecture`/`decision` → 自動改寫到 `shared/_pending_review/` + `Pending-review-by: management`。`buildAtomContent` 與 `resolveMemDir` 重構（新增 JS 端 `findProjectRoot` / `getCurrentUser` / `isSensitiveAudience` helper，不 spawn python）。`toolAtomPromote` 同步適配新回傳格式。9 情境 16 子項 smoke test 全綠 | `tools/workflow-guardian-mcp/server.js`, `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 Phase 2 — 路徑與角色基礎建設**：`wg_paths.py` 新增 `get_scope_dir(scope, cwd, user, role)` 處理 global/shared/role/personal 四種 scope（含 `~/.claude` 自身 guard 防遞迴），`discover_memory_layers` 加 `user`/`role` 參數擴出 `shared` / `role:{r}` / `personal:{user}` 子層（V3 無參呼叫完全相容）；新建 `wg_roles.py` 五個函式（含管理職雙向認證 + bootstrap 冪等寫 .gitignore）。8 項手動驗證全綠（含雙向認證失敗與成功情境、重跑保留使用者編輯、`.gitignore` 不重複） | `hooks/wg_paths.py`, `hooks/wg_roles.py`(新), `_AIDocs/Architecture.md` |
| 2026-04-15 | **原子記憶 V4 SPEC freeze (Phase 1)**：定稿三層 scope (personal / shared / role) 設計、metadata schema 擴充（Audience/Author/Pending-review-by/Decided-by/Merge-strategy/Created-at）、六大分類大類（programmer/planner/art/environment/ai/other）、三時段衝突偵測（write/pull/git-conflict）、JIT 角色 filter、管理職雙向認證。實作分 6 phases，由 `plans/gentle-puzzling-kettle.md` 拆 session 執行。同時新增 feedback atom 矯正 max thinking 模式下不可外包思考的行為 | `_AIDocs/SPEC_ATOM_V4.md`(新), `_AIDocs/_INDEX.md`, `plans/gentle-puzzling-kettle.md`(新), `memory/feedback-no-outsource-rigor.md`(新) |
| 2026-04-10 | **記憶健康工具修正 + decisions-architecture 拆分**：根因為 `718504b` Token 瘦身後 MEMORY.md 改 2 欄格式，audit 工具未跟進。修 `memory-audit.py`（支援 2 欄索引 / wildcard 索引項 / Claude-native YAML frontmatter / orphan memory dir 容忍 / `TRIGGER_MAX` 8→12 / `INDEX_MAX_LINES` 30→40）+ `atom-health-check.py`（`_` 前綴豁免 / `decisions-architecture` 加入 central hub）。新建 `decisions-architecture.md`（12 條架構決策），精簡 `decisions.md` 非架構場景注入 ~950→350 tok。順帶修 `c--projects/MEMORY.md` 死連結。Settings `defaultMode` 改 `acceptEdits` + skip flags（VS Code extension 編輯彈窗 bug #43953 未修，CLI 不受影響） | `tools/memory-audit.py`, `tools/atom-health-check.py`, `memory/decisions.md`, `memory/decisions-architecture.md`(新), `memory/MEMORY.md`, `projects/c--projects/memory/MEMORY.md`, `settings.json`, `_AIDocs/DocIndex-System.md` |
| 2026-04-09 | **V3.4 rdchat LLM gemma4:e4b**：三輪 A/B 測試（14 題型 × 5 模型）驗證後，rdchat LLM 從 qwen3.5→gemma4:e4b。extract-worker think 改 "auto" + temp 0.0。num_predict 8192→4096。local/embedding 不動。 | `workflow/config.json`, `hooks/extract-worker.py`, `hooks/wg_episodic.py`, `hooks/quick-extract.py`, `tools/memory-conflict-detector.py`, `tools/memory-vector-service/config.py`, `_AIDocs/Architecture.md`, `_AIDocs/DevHistory/ab-test-gemma4.md`(新), `memory/toolchain-ollama.md` |
| 2026-04-08 | **V3.3 DocDrift Detection**：新建 `wg_docdrift.py` 模組 — src Edit/Write 自動偵測對應 _AIDocs 是否需要更新，Hybrid 映射（config 顯式 + keyword fallback），PostToolUse advisory 提醒 + Stop gate 提示。Inspired by PR #1 (@wellstseng) | `hooks/wg_docdrift.py`(新), `hooks/workflow-guardian.py`, `hooks/wg_core.py`, `workflow/config.json`, `_AIDocs/Architecture.md` |
| 2026-04-02 | **V3.1 Token Diet**：4 Phase 精簡原子記憶 — 直刪 31 條 + 信號自描述化 5 個 + JIT _reference + MCP atom_write/promote。decisions+arch+memory-system 1,841→631 tok (-65.7%) | `memory/decisions.md`, `memory/decisions-architecture.md`, `rules/memory-system.md`, `hooks/wg_iteration.py`, `hooks/wisdom_engine.py`, `hooks/workflow-guardian.py`, `memory/_reference/internal-pipeline.md`(新), `tools/workflow-guardian-mcp/server.js` |
| 2026-04-02 | **V3.0 文件全面升級**：README V3.0（標題+三欄Token表+速度效率表+架構+流程圖+Skills 17個+V3子系統+版本歷史）+ Install-forAI V3升級section + Architecture/Project_File_Tree/DocIndex/INDEX 更新 + 7 commands 移除版號標記 | `README.md`, `Install-forAI.md`, `_AIDocs/*.md`, `commands/*.md`, `CLAUDE.md` |
| 2026-04-02 | **V3 Phase1 實作：Hot Cache + Quick Extract**：新建 wg_hot_cache.py（lock+原子寫入 3 API）、quick-extract.py（Stop async hook, qwen3:1.7b 快篩→hot cache）、decisions-architecture.md Hot Cache 段落 [臨]→[觀] | `hooks/wg_hot_cache.py`(新), `hooks/quick-extract.py`(新), `memory/decisions-architecture.md` |
| 2026-04-02 | **V3 三層即時管線升級**：decisions.md 新增 V3 管線+SessionStart 風暴修復決策、decisions-architecture.md 新增 Hot Cache/Async Hook/去重技術細節、cc-hook-system.md 補充 additionalContext 可用性差異 | `memory/decisions.md`, `memory/decisions-architecture.md`, `_AIDocs/ClaudeCodeInternals/cc-hook-system.md` |
| 2026-04-01 | **atom 全面精簡 — 經歷敘述型內容歸檔**：10 個 atom 移除演化日誌段落、decisions.md 移除 5 條版本遷移敘述（V2.18~V2.21）改為 2 條現狀事實、toolchain-ollama.md A/B 完整表格改為 3 條結論、移除各處版本標籤前綴。歸檔至 `_AIDocs/DevHistory/`（3 檔）。估計省 ~1100 tok/session | `memory/*.md`（10 atoms）, `_AIDocs/DevHistory/*`（3 新檔） |
| 2026-04-01 | **原子記憶系統全面驗證 + 修正**：decisions.md 修正 4 處 + architecture 計數器歸零 + Vector DB full reindex | `memory/decisions.md`, `memory/wisdom/reflection_metrics.json` |
| 2026-03-30 | **_AIDocs V2.21 全面同步**：Architecture.md V2.17→V2.21 + Project_File_Tree 重寫 + DocIndex 更新 | `_AIDocs/Architecture.md`, `_AIDocs/Project_File_Tree.md`, `_AIDocs/DocIndex-System.md`, `_AIDocs/_INDEX.md` |
| 2026-03-30 | **全面清理 + V2.21 自治修正**：修復 2 處硬編碼 + 刪除 backups/Logs/debug + 清理孤兒 slugs | `hooks/workflow-guardian.py`, `settings.json`, `_AIDocs/`, `memory/_reference/` |
| 2026-03-30 | **Dashboard 4 修復 + 版本集中化** | `tools/workflow-guardian-mcp/server.js`, `version.json` |
| 2026-03-27 | **V2.21 Phase 7 Skills/Tools 專案層支援** | `tools/memory-audit.py`, `tools/memory-conflict-detector.py`, `commands/*.md`, `server.js` |
| 2026-03-27 | **文件版本同步 V2.21** | `README.md`, `Install-forAI.md`, `memory/_reference/*`, `memory/MEMORY.md` |
| 2026-03-27 | **V2.21 Phase 3 專案自治層建置**：`init-project` skill 新增 Step 6 建立 `.claude/` 自治層。`workflow-guardian.py` 新增 `_call_project_hook()` subprocess 隔離呼叫 | `commands/init-project.md`, `hooks/workflow-guardian.py` |
| 2026-03-27 | **V2.21 Phase 2 Project Registry + 路徑切換**：`register_project()` + registry 管理。`find_project_root()` 加 `.claude/memory/MEMORY.md` 辨識。`get_project_memory_dir()` 新路徑優先 | `hooks/wg_paths.py`, `hooks/workflow-guardian.py`, `hooks/wg_atoms.py`, `tools/workflow-guardian-mcp/server.js` |
| 2026-03-27 | **V2.20 Phase 0.3 C2 修復**：`_truncate_context_by_activation` 加 `source_dirs` 參數修正專案層 ACT-R score 永遠 -10.0 問題 | `hooks/wg_atoms.py`, `hooks/workflow-guardian.py` |
| 2026-03-24 | **V2.18 Phase 2 Section-Level 注入**：向量服務新增 `ranked_search_sections()` + `/search/ranked-sections` endpoint。`_semantic_search()` 回傳帶 sections。注入迴圈新增 section 提取（`_extract_sections()`），大 atom 省 69-87% tokens。安全防護：0 匹配/70% 閾值/服務不可用皆 fallback 全量注入 | `searcher.py`, `service.py`, `wg_intent.py`, `wg_atoms.py`, `workflow-guardian.py` |
| 2026-03-23 | **V2.18 Phase 0+1**：環境清理（LanceDB 289→25MB、刪 7 死檔）+ 9 atom Trigger 精準化 + misdiagnosis/harvester 內容精簡 | `memory/*.md`, `MEMORY.md`, `workflow/config.json` |
| 2026-03-23 | **V2.17 合併升級**：V2.16 自我迭代自動化 + V2.17 覆轍偵測 + AIDocs 內容閘門 + WebFetch Guard | `hooks/workflow-guardian.py`, `settings.json`, `rules/aidocs.md`, `memory/decisions*.md`, `CLAUDE.md`, `README.md`, `_AIDocs/*.md` |
| 2026-03-19 | **V2.15 定義版本**：全文件版本號 V2.12→V2.15 統一。README 版本歷史補 V2.13/V2.14/V2.15 | `CLAUDE.md`, `README.md`, `Install-forAI.md`, `_AIDocs/*.md`, `memory/decisions*.md`, `rules/session-management.md` |
| 2026-03-19 | **V2.14 Token Diet**：`_strip_atom_for_injection()` 注入前 strip 9 種 metadata + 行動/演化日誌。SessionEnd 從 byte_offset 跳已萃取段。cross-session lazy search 預篩。省 ~1550 tok/session | `hooks/workflow-guardian.py`, `hooks/extract-worker.py`, `memory/MEMORY.md`, `workflow/config.json` |
| 2026-03-19 | **atom 精準拆分+設定精修**：toolchain-ollama 獨立拆分 + workflow-icld 拆分 + trigger 瘦身 + failures 拆分子 atoms + GIT 流程去重 + 設定檔去重/瘦身/統一管理 | `memory/*.md`, `workflow/config.json` |
| 2026-03-19 | **V2.13 Failures 自動化系統**：Guardian 偵測失敗關鍵字 → detached extract-worker 萃取失敗模式 → 三維路由自動寫入對應 failure atom | `hooks/extract-worker.py`, `hooks/workflow-guardian.py`, `workflow/config.json`, `memory/failures/` |
| 2026-03-19 | **vector service timeout 修正**：冷啟動 7.5s 但 caller timeout 2-5s，調整 timeout + 預熱 | `hooks/workflow-guardian.py`, `tools/memory-vector-service/` |
| 2026-03-18 | **V2.12 逐輪增量萃取**：Stop hook per-turn extraction（byte_offset + cooldown 120s + PID guard）+ 萃取 dedup 統一 0.65 + intent 選取 bug 修正 | `hooks/extract-worker.py`, `hooks/workflow-guardian.py`, `workflow/config.json` |
| 2026-03-18 | **注入精準化**：AIDocs keyword 重新設計 + IDE 標籤過濾 + keyword boundary 防誤匹配 + `/atom-debug` skill | `_AIDocs/_INDEX.md`, `hooks/workflow-guardian.py`, `commands/atom-debug.md` |
| 2026-03-13 | **選擇性 cherry-pick**：從來源 V2.10 合併 `/continue` skill + `/resume` staging 安全網 + `BOOTSTRAP.md` + `workflow-rules.md` 補回大型計畫/GIT/同步判斷段落 | `commands/continue.md`, `commands/resume.md`, `BOOTSTRAP.md`, `memory/workflow-rules.md` |
| 2026-03-13 | **/read-project**: 新增 DocIndex-System.md（76 檔系統索引）+ doc-index-system atom | `_AIDocs/DocIndex-System.md`, `memory/doc-index-system.md`, `memory/MEMORY.md` |
