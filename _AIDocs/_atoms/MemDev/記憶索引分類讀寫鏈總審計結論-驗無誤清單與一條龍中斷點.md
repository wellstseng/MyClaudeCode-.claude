# 記憶索引分類讀寫鏈總審計結論-驗無誤清單與一條龍中斷點

- Scope: global
- Author: holylight
- Confidence: [臨]
- Trigger: 總審計, 索引審計, 分類審計, 一條龍, 讀寫鏈, discover_all_project, discover_layers, project-registry, register_project, cross-project-index-cache, syncMemoryIndex, 專案層判定, 壞連結, Related 壞, 常數飄移
- Created-at: 2026-08-31
- Related: 注入預算三教訓-裁切要回填-分級看token不看字元-橋接檔須隨索引重產, realm-範疇分區機制-v5, memory-index-caption-regen, 驗證探針的副作用與假失敗-heredoc反斜線-假session登記-dry-run留目錄-fallback索引源, 健檢error與索引矛盾即解析器誤報-audit跨層全掃自08-31起-週報global-only看不到專案層

## 知識

- [臨] 審計方法：三個唯讀 Explore agent 各切一面（索引連結／atom 內容品質／讀寫分類鏈）＋機器檢查器（sync-atom-index --check、sync-memory-index --check、memory-audit、atom-health-check --validate-refs）並行；只修「≥ 2 路確認屬實」的項目，審完無誤的明列不動。agent 的主張逐條拿程式碼與指令輸出回驗：有一條（atom-move 重複實作 find_index_dir）實查已包裝 lib 版，是誤報。
- [臨] 驗無誤清單（下次不必重審）：_atom_index.json path/name/trigger/scope 全對；MEMORY.md 範疇計數與各層 _INDEX 全對；taxonomy Lv1 ↔ 資料夾一一對應；保留夾零洩漏；_ATOM_INDEX.md 唯一寫入者、JSON 為 SoT；taxonomy 單源（py fail-closed、js 只拿來組錯訊）；範疇閘裁決點唯一（atom_io._resolve_target）；projects/ 下大小寫不同的兩個 slug 目錄是同一 inode（NTFS 不分大小寫）。
- [臨] 一條龍中斷點模式：同一件事在多處各寫一份。實例：「哪些目錄算專案記憶層」執行期（wg_core）有 atom 標記檢查，健檢／衝突偵測自掃 projects/*/memory 沒有 → 把 CC 原生 auto-memory 目錄（grok/hermes/Temp 測試夾）當記憶層。正解是工具全部 import 執行期那一份，不要「對拍」。
- [臨] 專案登記表會被探針／測試 session 汙染：cwd=~/.claude 或家目錄會登記成專案（家目錄的 .claude/memory 就是全域記憶）；8.3 短檔名 cwd（C:\Users\HOLYLI~1）會多一個 slug。register_project 現已擋這兩類；探針後仍要看 memory/project-registry.json 有無新垃圾。
- [臨] 每 prompt 的 hook 都是新進程，進程內 memoize 跨 prompt 無效；跨專案索引（最多 20 專案 × MEMORY.md+_atom_index.json）原本每 prompt 全重讀約 220 KB。現由 workflow/cross-project-index-cache.json 聚合（鍵＝兩檔 mtime_ns）；同類每 prompt 固定 I/O 都該用「磁碟快取＋mtime 鍵」而非進程快取。
- [臨] 已落地：atom 落點／定位／路由（cwd-scope 防護、範疇閘、feedback 家族、local realm 自動分類、subdir 沙盒、待審路由、分隔符變體撞名、既有檔定位）**只在 py `lib/atom_io.locate_atom` 一份**；js（atom-tools.js）create/append/replace/promote/edit_meta 一律 `spawnAtomCli("locate")` 取 target_dir/index_dir/index_root/rel_path 照用，realm.js 只剩 getCurrentUser/dedupLayersFor。`verify_atom_io_equivalence.test_14` 守 js 不得再長出鏡像。改 js 需重啟 MCP。

## 行動

- 審計記憶系統先跑四個機器檢查器，再拍 agent 切面；修前至少兩路證據
- 新工具要列專案層一律 from wg_core import discover_all_project_memory_dirs，不自掃 projects/
- 改完索引／連結跑 atom-health-check --validate-refs 與兩個 --check 同步器
